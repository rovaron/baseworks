---
phase: 07-accessibility
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/src/components/form.tsx
  - packages/ui/src/components/data-table-cards.tsx
  - apps/web/app/(dashboard)/page.tsx
  - apps/web/app/(dashboard)/billing/page.tsx
  - apps/web/app/(dashboard)/settings/page.tsx
  - apps/admin/src/pages/tenants.tsx
  - apps/admin/src/pages/users.tsx
  - apps/admin/src/pages/billing.tsx
  - apps/admin/src/pages/system.tsx
autonomous: true
requirements:
  - A11Y-02
  - A11Y-04
  - A11Y-05
must_haves:
  truths:
    - "Screen reader announces form validation errors immediately when they appear"
    - "Loading states in both apps announce Loading to screen readers"
    - "All interactive elements have visible focus indicators when navigated via keyboard"
    - "Escape key closes open dialogs, sheets, and dropdowns"
  artifacts:
    - path: "packages/ui/src/components/form.tsx"
      provides: "FormMessage with role=alert"
      contains: "role=\"alert\""
    - path: "apps/web/app/(dashboard)/page.tsx"
      provides: "Customer dashboard with aria-busy loading states"
      contains: "aria-busy"
  key_links:
    - from: "packages/ui/src/components/form.tsx"
      to: "FormMessage rendered in forms"
      via: "role=alert announces errors to screen readers"
      pattern: "role=\"alert\""
---

<objective>
Add aria-live regions for dynamic content, add role="alert" to FormMessage, audit and fix focus indicator gaps, and add aria-busy to loading states across both apps.

Purpose: Screen reader users hear meaningful announcements for errors and loading states, and keyboard users see visible focus on all interactive elements (A11Y-02, A11Y-04, A11Y-05, D-05, D-07, D-08).

Output: Updated FormMessage with role="alert", loading states with aria-busy, focus gaps fixed.
</objective>

<execution_context>
@C:/Projetos/baseworks/.claude/get-shit-done/workflows/execute-plan.md
@C:/Projetos/baseworks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-accessibility/07-CONTEXT.md
@.planning/phases/07-accessibility/07-RESEARCH.md
@.planning/phases/07-accessibility/07-UI-SPEC.md

<interfaces>
From packages/ui/src/components/form.tsx (line 143-165):
```typescript
const FormMessage = React.forwardRef<
  HTMLParagraphElement,
  React.HTMLAttributes<HTMLParagraphElement>
>(({ className, children, ...props }, ref) => {
  const { error, formMessageId } = useFormField()
  const body = error ? String(error?.message ?? "") : children
  if (!body) { return null }
  return (
    <p ref={ref} id={formMessageId}
      className={cn("text-sm font-medium text-destructive", className)}
      {...props}>{body}</p>
  )
})
```
Note: FormMessage returns null when no error -- safe for role="alert" (Pitfall 4 in RESEARCH.md).

Focus ring pattern used across shadcn components:
`focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2`
Sidebar variant: `focus-visible:ring-2 focus-visible:ring-sidebar-ring`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add role="alert" to FormMessage and aria-busy to loading states</name>
  <files>
    packages/ui/src/components/form.tsx,
    packages/ui/src/components/data-table-cards.tsx,
    apps/web/app/(dashboard)/page.tsx,
    apps/web/app/(dashboard)/billing/page.tsx,
    apps/web/app/(dashboard)/settings/page.tsx,
    apps/admin/src/pages/tenants.tsx,
    apps/admin/src/pages/users.tsx,
    apps/admin/src/pages/billing.tsx,
    apps/admin/src/pages/system.tsx
  </files>
  <read_first>
    packages/ui/src/components/form.tsx,
    packages/ui/src/components/data-table-cards.tsx,
    apps/web/app/(dashboard)/page.tsx,
    apps/web/app/(dashboard)/billing/page.tsx,
    apps/web/app/(dashboard)/settings/page.tsx,
    apps/admin/src/pages/tenants.tsx,
    apps/admin/src/pages/users.tsx,
    apps/admin/src/pages/billing.tsx,
    apps/admin/src/pages/system.tsx,
    .planning/phases/07-accessibility/07-RESEARCH.md
  </read_first>
  <action>
1. **Add `role="alert"` to FormMessage** (per D-08):
   - In `packages/ui/src/components/form.tsx`, find the `<p>` element inside FormMessage (around line 155)
   - Add `role="alert"` attribute to the `<p>` element
   - Result: `<p ref={ref} id={formMessageId} role="alert" className={cn("text-sm font-medium text-destructive", className)} {...props}>`
   - FormMessage already returns `null` when no error (line 149-151), so role="alert" will not fire on page load (Pitfall 4 safe)

2. **Add `aria-busy` and sr-only loading text to loading states** (per D-07):
   For each page that has a loading/fetching state (check for `isLoading`, `isPending`, `isFetching`, or loading spinner patterns), wrap the loading container with:
   - `aria-busy={isLoading}` on the container div (where `isLoading` is the relevant boolean)
   - `aria-live="polite"` on the same container
   - Inside the loading branch, add `<span className="sr-only">Loading...</span>` after any spinner/skeleton
   - When data loads, the aria-busy becomes false and content replaces the loading text naturally

   **Customer app pages to audit:**
   - `apps/web/app/(dashboard)/page.tsx` -- dashboard overview, check for loading states
   - `apps/web/app/(dashboard)/billing/page.tsx` -- billing page, check for loading states
   - `apps/web/app/(dashboard)/settings/page.tsx` -- settings page, check for loading states

   **Admin pages to audit:**
   - `apps/admin/src/pages/tenants.tsx` -- tenant list, likely has loading
   - `apps/admin/src/pages/users.tsx` -- user list, likely has loading
   - `apps/admin/src/pages/billing.tsx` -- billing overview, likely has loading
   - `apps/admin/src/pages/system.tsx` -- system health, likely has loading

   Pattern to apply to each loading container:
   ```tsx
   <div aria-busy={isLoading} aria-live="polite">
     {isLoading ? (
       <>
         <ExistingSpinnerOrSkeleton />
         <span className="sr-only">Loading...</span>
       </>
     ) : (
       <ActualContent />
     )}
   </div>
   ```

   If a page has no loading state (static content), skip it. Document which pages were skipped and why in the SUMMARY.

3. **data-table-cards.tsx accessibility** (per D-07):
   - In `packages/ui/src/components/data-table-cards.tsx`, if there is a loading state, add `aria-busy` + sr-only loading text
   - Ensure card containers have appropriate semantic structure (use `<article>` or keep as-is if list items)
  </action>
  <verify>
    <automated>cd C:/Projetos/baseworks && grep 'role="alert"' packages/ui/src/components/form.tsx && grep -rl "aria-busy" apps/web/app/\(dashboard\)/ apps/admin/src/pages/ 2>/dev/null | head -10 && grep 'sr-only.*Loading' apps/web/app/\(dashboard\)/page.tsx apps/admin/src/pages/tenants.tsx 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - packages/ui/src/components/form.tsx contains `role="alert"` on the FormMessage p element
    - packages/ui/src/components/form.tsx still contains `if (!body) { return null }` or equivalent null guard
    - At least 3 page files contain `aria-busy` attribute
    - At least 3 page files contain `<span className="sr-only">Loading...</span>` or equivalent sr-only loading text
    - Pages with loading states have `aria-live="polite"` on the loading container
  </acceptance_criteria>
  <done>
    FormMessage announces validation errors to screen readers via role="alert". Loading states across both apps have aria-busy and sr-only loading text for screen reader users. All per D-07, D-08.
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit and fix keyboard focus indicator gaps</name>
  <files>
    packages/ui/src/components/data-table-cards.tsx
  </files>
  <read_first>
    packages/ui/src/components/button.tsx,
    packages/ui/src/components/dialog.tsx,
    packages/ui/src/components/sheet.tsx,
    packages/ui/src/components/dropdown-menu.tsx,
    packages/ui/src/components/data-table-cards.tsx,
    packages/ui/src/components/sidebar.tsx,
    apps/web/components/sidebar-nav.tsx,
    apps/admin/src/layouts/admin-layout.tsx,
    .planning/phases/07-accessibility/07-UI-SPEC.md
  </read_first>
  <action>
**Focus style audit** (per D-05):

Audit every interactive element in the following components for `focus-visible:ring-2` or equivalent focus indicator. The standard pattern is: `focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2`.

1. **Components to audit for focus gaps:**
   - `packages/ui/src/components/dialog.tsx` -- check DialogClose / close button
   - `packages/ui/src/components/sheet.tsx` -- check SheetClose / close button
   - `packages/ui/src/components/dropdown-menu.tsx` -- check DropdownMenuItem focus states
   - `packages/ui/src/components/data-table-cards.tsx` -- check any interactive elements (buttons, links) in card layout
   - `packages/ui/src/components/sidebar.tsx` -- check SidebarTrigger

2. **For each gap found:**
   - Add `focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2` (or `focus-visible:ring-sidebar-ring` for sidebar components) to the component's className
   - Do NOT restyle existing focus rings -- only add where missing (per D-05)

3. **Escape key verification** (Claude's discretion):
   - Radix Dialog, Sheet, and DropdownMenu already handle Escape key natively
   - Verify by reading component source that they use Radix primitives (`@radix-ui/react-dialog`, `@radix-ui/react-dropdown-menu`)
   - If any component does NOT use Radix and lacks Escape handling, note it in the SUMMARY for potential follow-up
   - Do NOT add custom Escape handlers where Radix already handles it

4. **Document audit results:**
   - In the SUMMARY, list each component audited, whether it had focus gaps, and what was fixed
   - If all components already have proper focus indicators (likely due to shadcn defaults), document that finding
  </action>
  <verify>
    <automated>cd C:/Projetos/baseworks && grep -c "focus-visible" packages/ui/src/components/button.tsx packages/ui/src/components/input.tsx packages/ui/src/components/dialog.tsx packages/ui/src/components/sheet.tsx packages/ui/src/components/dropdown-menu.tsx packages/ui/src/components/data-table-cards.tsx packages/ui/src/components/sidebar.tsx 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - Every interactive element (button, link, trigger) in audited components has a focus-visible class
    - No `outline: none` or `outline-none` applied globally to interactive elements (only allowed on main element with tabIndex={-1})
    - data-table-cards.tsx interactive elements have focus-visible ring if they were missing
    - Dialog, Sheet, DropdownMenu use Radix primitives (confirmed by import statements)
  </acceptance_criteria>
  <done>
    All interactive elements across shared UI components have visible keyboard focus indicators. Escape key handling confirmed via Radix primitives. Focus gaps identified and fixed. Audit documented in SUMMARY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan modifies only ARIA attributes and CSS classes. No data flow changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | I (Info Disclosure) | aria-live loading text | accept | Loading text says "Loading..." only -- no sensitive data exposed in screen reader announcements |
</threat_model>

<verification>
- FormMessage in form.tsx has role="alert"
- Loading states in pages have aria-busy and sr-only text
- Focus audit completed with results documented
- No focus-visible gaps on interactive elements
</verification>

<success_criteria>
Screen reader users hear form validation errors immediately (role="alert"). Loading states announce "Loading..." via aria-live. All interactive elements have visible focus indicators on keyboard navigation. Escape dismisses overlays via Radix primitives.
</success_criteria>

<output>
After completion, create `.planning/phases/07-accessibility/07-02-SUMMARY.md`
</output>
