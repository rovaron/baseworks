---
phase: 07-accessibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/src/components/skip-link.tsx
  - packages/ui/src/index.tsx
  - apps/web/app/(dashboard)/layout.tsx
  - apps/web/app/(auth)/layout.tsx
  - apps/web/hooks/use-focus-on-navigate.ts
  - apps/web/components/sidebar-nav.tsx
  - apps/admin/src/layouts/admin-layout.tsx
  - apps/admin/src/hooks/use-focus-on-navigate.ts
autonomous: true
requirements:
  - A11Y-01
  - A11Y-03
must_haves:
  truths:
    - "User pressing Tab on page load sees a visible skip-to-content link in both apps"
    - "Skip link click moves focus to main content area"
    - "Customer app dashboard has header, nav, and main landmarks"
    - "Admin dashboard sidebar is wrapped in nav element with aria-label"
    - "Auth layout has main landmark with skip link"
    - "Route change in customer app moves focus to main content"
    - "Route change in admin dashboard moves focus to main content"
  artifacts:
    - path: "packages/ui/src/components/skip-link.tsx"
      provides: "Shared SkipToContent component"
      exports: ["SkipToContent"]
    - path: "apps/web/hooks/use-focus-on-navigate.ts"
      provides: "Next.js route change focus hook"
      exports: ["useFocusOnNavigate"]
    - path: "apps/admin/src/hooks/use-focus-on-navigate.ts"
      provides: "React Router route change focus hook"
      exports: ["useFocusOnNavigate"]
  key_links:
    - from: "apps/web/app/(dashboard)/layout.tsx"
      to: "packages/ui/src/components/skip-link.tsx"
      via: "import SkipToContent"
      pattern: "SkipToContent"
    - from: "apps/admin/src/layouts/admin-layout.tsx"
      to: "packages/ui/src/components/skip-link.tsx"
      via: "import SkipToContent"
      pattern: "SkipToContent"
    - from: "apps/web/app/(dashboard)/layout.tsx"
      to: "apps/web/hooks/use-focus-on-navigate.ts"
      via: "useFocusOnNavigate() call"
      pattern: "useFocusOnNavigate"
---

<objective>
Add semantic HTML landmarks, skip-to-content links, and route change focus management to both the customer app and admin dashboard.

Purpose: Users relying on screen readers and keyboard navigation can identify page regions, skip navigation, and receive focus context after route changes (A11Y-01, A11Y-03, D-01 through D-04, D-06).

Output: SkipToContent shared component, useFocusOnNavigate hooks for both apps, landmark-retrofitted layouts.
</objective>

<execution_context>
@C:/Projetos/baseworks/.claude/get-shit-done/workflows/execute-plan.md
@C:/Projetos/baseworks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-accessibility/07-CONTEXT.md
@.planning/phases/07-accessibility/07-RESEARCH.md
@.planning/phases/07-accessibility/07-UI-SPEC.md

<interfaces>
<!-- Existing exports from packages/ui used by both apps -->
From packages/ui/src/index.tsx:
- All shadcn components re-exported (Sidebar*, SidebarInset, etc.)

From apps/web/components/sidebar-nav.tsx:
- export function SidebarNav() -- customer sidebar, wraps Sidebar component

From apps/admin/src/layouts/admin-layout.tsx:
- export function Component() -- admin layout with AuthGuard wrapper
- AdminLayoutContent() -- inner layout with SidebarProvider, Sidebar, header, main
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SkipToContent component and retrofit customer app layouts</name>
  <files>
    packages/ui/src/components/skip-link.tsx,
    packages/ui/src/index.tsx,
    apps/web/app/(dashboard)/layout.tsx,
    apps/web/app/(auth)/layout.tsx,
    apps/web/hooks/use-focus-on-navigate.ts,
    apps/web/components/sidebar-nav.tsx
  </files>
  <read_first>
    packages/ui/src/components/skip-link.tsx,
    packages/ui/src/components/button.tsx,
    packages/ui/src/index.tsx,
    apps/web/app/(dashboard)/layout.tsx,
    apps/web/app/(auth)/layout.tsx,
    apps/web/app/layout.tsx,
    apps/web/components/sidebar-nav.tsx,
    .planning/phases/07-accessibility/07-RESEARCH.md,
    .planning/phases/07-accessibility/07-UI-SPEC.md
  </read_first>
  <action>
1. **Create `packages/ui/src/components/skip-link.tsx`** (per D-04):
   - Export `SkipToContent` component accepting optional `targetId` prop (default: `"main-content"`)
   - Render an `<a>` element with `href={#${targetId}}`
   - Classes: `sr-only focus:not-sr-only focus:fixed focus:left-4 focus:top-4 focus:z-50 focus:rounded-md focus:bg-background focus:px-4 focus:py-2 focus:text-sm focus:text-foreground focus:shadow-md focus:ring-2 focus:ring-ring`
   - Text content: "Skip to content" (per UI-SPEC copywriting contract)

2. **Export from `packages/ui/src/index.tsx`**:
   - Add `export { SkipToContent } from "./components/skip-link";`

3. **Create `apps/web/hooks/use-focus-on-navigate.ts`** (per D-06, Claude's discretion for Next.js):
   - `"use client"` directive at top
   - Import `usePathname` from `next/navigation`, `useEffect` and `useRef` from `react`
   - Export `useFocusOnNavigate()` function
   - Use `isFirstRender` ref to skip initial mount
   - On pathname change (after first render): find `document.getElementById("main-content")`, set `tabindex="-1"` attribute, call `.focus({ preventScroll: false })`
   - Exact implementation per RESEARCH.md Pattern 2

4. **Retrofit `apps/web/app/(dashboard)/layout.tsx`** (per D-01, D-02, D-06):
   - Import `SkipToContent` from `@baseworks/ui` and `useFocusOnNavigate` from `@/hooks/use-focus-on-navigate`
   - Extract inner content into a `DashboardContent` component (to call hooks inside SidebarProvider)
   - Call `useFocusOnNavigate()` inside `DashboardContent`
   - Add `<SkipToContent />` as the first child inside `<SidebarProvider>`
   - Add `<header className="flex h-14 items-center gap-2 border-b px-4">` wrapping a `<SidebarTrigger />` inside `<SidebarInset>` before the main element
   - Import `SidebarTrigger` and `Separator` from `@baseworks/ui`
   - Change the content wrapper from `<div className="mx-auto max-w-4xl p-6">` to `<main id="main-content" tabIndex={-1} className="flex-1 p-6 focus:outline-none"><div className="mx-auto max-w-4xl">{children}</div></main>`
   - Keep `TenantProvider` wrapping everything

5. **Wrap customer sidebar in `<nav>`** (per D-02):
   - In `apps/web/components/sidebar-nav.tsx`, wrap the entire `<Sidebar collapsible="icon">` element in `<nav aria-label="Main navigation">` (per UI-SPEC copywriting contract)

6. **Retrofit `apps/web/app/(auth)/layout.tsx`** (per D-01, D-04):
   - Import `SkipToContent` from `@baseworks/ui/components/skip-link`
   - Add `<SkipToContent />` before the content wrapper
   - Change the outer `<div>` to keep it but wrap children in `<main id="main-content" tabIndex={-1} className="focus:outline-none">`
   - Final structure: `<div class="flex min-h-screen..."><SkipToContent /><main id="main-content" tabIndex={-1} className="focus:outline-none">{children}</main></div>`
  </action>
  <verify>
    <automated>cd C:/Projetos/baseworks && grep -l "SkipToContent" packages/ui/src/components/skip-link.tsx packages/ui/src/index.tsx apps/web/app/\(dashboard\)/layout.tsx apps/web/app/\(auth\)/layout.tsx && grep -l "useFocusOnNavigate" apps/web/hooks/use-focus-on-navigate.ts apps/web/app/\(dashboard\)/layout.tsx && grep -l 'aria-label="Main navigation"' apps/web/components/sidebar-nav.tsx && grep 'id="main-content"' apps/web/app/\(dashboard\)/layout.tsx apps/web/app/\(auth\)/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - packages/ui/src/components/skip-link.tsx contains `export function SkipToContent`
    - packages/ui/src/components/skip-link.tsx contains `href={` and `#${targetId}`
    - packages/ui/src/components/skip-link.tsx contains `sr-only` and `focus:not-sr-only`
    - packages/ui/src/components/skip-link.tsx contains text "Skip to content"
    - packages/ui/src/index.tsx contains `export { SkipToContent }` or re-export from skip-link
    - apps/web/hooks/use-focus-on-navigate.ts contains `export function useFocusOnNavigate`
    - apps/web/hooks/use-focus-on-navigate.ts contains `usePathname` and `isFirstRender`
    - apps/web/hooks/use-focus-on-navigate.ts contains `getElementById("main-content")`
    - apps/web/app/(dashboard)/layout.tsx contains `<SkipToContent` component
    - apps/web/app/(dashboard)/layout.tsx contains `<header` element
    - apps/web/app/(dashboard)/layout.tsx contains `<main id="main-content" tabIndex={-1}`
    - apps/web/app/(dashboard)/layout.tsx contains `focus:outline-none` on main
    - apps/web/app/(dashboard)/layout.tsx contains `useFocusOnNavigate()`
    - apps/web/components/sidebar-nav.tsx contains `<nav aria-label="Main navigation">`
    - apps/web/app/(auth)/layout.tsx contains `<SkipToContent`
    - apps/web/app/(auth)/layout.tsx contains `<main id="main-content"`
  </acceptance_criteria>
  <done>
    Customer app dashboard layout has header, nav (around sidebar), and main landmarks with id="main-content" and tabIndex={-1}. Auth layout has main landmark. SkipToContent component is first focusable element in both layouts. Route changes move focus to main via useFocusOnNavigate hook.
  </done>
</task>

<task type="auto">
  <name>Task 2: Retrofit admin dashboard layout with landmarks, skip link, and focus hook</name>
  <files>
    apps/admin/src/layouts/admin-layout.tsx,
    apps/admin/src/hooks/use-focus-on-navigate.ts
  </files>
  <read_first>
    apps/admin/src/layouts/admin-layout.tsx,
    packages/ui/src/components/skip-link.tsx,
    .planning/phases/07-accessibility/07-UI-SPEC.md
  </read_first>
  <action>
1. **Create `apps/admin/src/hooks/use-focus-on-navigate.ts`** (per D-06, Claude's discretion for React Router):
   - Import `useLocation` from `react-router`, `useEffect` and `useRef` from `react`
   - Export `useFocusOnNavigate()` function
   - Use `isFirstRender` ref to skip initial mount
   - On `location.pathname` change (after first render): find `document.getElementById("main-content")`, set `tabindex="-1"`, call `.focus({ preventScroll: false })`
   - Exact implementation per RESEARCH.md Pattern 3

2. **Modify `apps/admin/src/layouts/admin-layout.tsx`** (per D-01, D-02, D-04, D-06):
   - Import `SkipToContent` from `@baseworks/ui` (it is exported from the index)
   - Import `useFocusOnNavigate` from `@/hooks/use-focus-on-navigate`
   - Call `useFocusOnNavigate()` inside `AdminLayoutContent` function
   - Add `<SkipToContent />` as the first child inside `<SidebarProvider>`, before `<NavigationAutoClose />`
   - Wrap the `<Sidebar collapsible="icon">` element in `<nav aria-label="Main navigation">`
   - On the existing `<main className="flex-1 p-6">` element, add `id="main-content"`, `tabIndex={-1}`, and `focus:outline-none` to className
   - Result: `<main id="main-content" tabIndex={-1} className="flex-1 p-6 focus:outline-none">`
   - The existing `<header>` element stays unchanged (already has correct structure)
  </action>
  <verify>
    <automated>cd C:/Projetos/baseworks && grep -l "SkipToContent" apps/admin/src/layouts/admin-layout.tsx && grep 'aria-label="Main navigation"' apps/admin/src/layouts/admin-layout.tsx && grep 'id="main-content"' apps/admin/src/layouts/admin-layout.tsx && grep "useFocusOnNavigate" apps/admin/src/hooks/use-focus-on-navigate.ts apps/admin/src/layouts/admin-layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - apps/admin/src/hooks/use-focus-on-navigate.ts contains `export function useFocusOnNavigate`
    - apps/admin/src/hooks/use-focus-on-navigate.ts contains `useLocation` from `react-router`
    - apps/admin/src/hooks/use-focus-on-navigate.ts contains `getElementById("main-content")`
    - apps/admin/src/layouts/admin-layout.tsx contains `<SkipToContent` import and usage
    - apps/admin/src/layouts/admin-layout.tsx contains `<nav aria-label="Main navigation">`
    - apps/admin/src/layouts/admin-layout.tsx contains `id="main-content"` on the main element
    - apps/admin/src/layouts/admin-layout.tsx contains `tabIndex={-1}` on the main element
    - apps/admin/src/layouts/admin-layout.tsx contains `focus:outline-none` on the main element
    - apps/admin/src/layouts/admin-layout.tsx contains `useFocusOnNavigate()` call
  </acceptance_criteria>
  <done>
    Admin dashboard has nav landmark wrapping sidebar with aria-label="Main navigation", main element has id="main-content" with tabIndex={-1} and focus:outline-none, SkipToContent is first focusable element, and route changes move focus to main content via useFocusOnNavigate hook.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan modifies only HTML structure and CSS classes. No data flow, no input handling, no auth changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | T (Tampering) | skip-link.tsx href | accept | The href is a same-page anchor (`#main-content`), not user-controlled input. No XSS vector. |
</threat_model>

<verification>
- SkipToContent component exists and is exported from packages/ui
- Customer dashboard layout: has `<header>`, `<nav aria-label="Main navigation">`, `<main id="main-content" tabIndex={-1}>`
- Customer auth layout: has `<main id="main-content">` and SkipToContent
- Admin layout: has `<nav aria-label="Main navigation">`, main has id/tabIndex, SkipToContent present
- Both focus hooks exist and reference "main-content"
</verification>

<success_criteria>
Both apps have semantic landmarks (nav, header, main) per WCAG 2.1 AA. Skip-to-content links are first focusable elements. Route changes move focus to main content. All per D-01, D-02, D-03, D-04, D-06.
</success_criteria>

<output>
After completion, create `.planning/phases/07-accessibility/07-01-SUMMARY.md`
</output>
