---
phase: 07-accessibility
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/(auth)/login/page.tsx
  - apps/web/app/(auth)/signup/page.tsx
  - apps/web/app/(auth)/forgot-password/page.tsx
  - apps/web/app/(auth)/reset-password/page.tsx
  - apps/web/app/(auth)/magic-link/page.tsx
  - apps/admin/src/routes/login.tsx
autonomous: true
gap_closure: true
requirements:
  - A11Y-01
must_haves:
  truths:
    - "Every auth page has exactly one h1 heading element visible to screen readers"
    - "Heading hierarchy on auth pages goes h1 (page title) with no skipped levels"
  artifacts:
    - path: "apps/web/app/(auth)/login/page.tsx"
      provides: "h1 heading for login page"
      contains: "<h1"
    - path: "apps/web/app/(auth)/signup/page.tsx"
      provides: "h1 heading for signup page"
      contains: "<h1"
    - path: "apps/web/app/(auth)/forgot-password/page.tsx"
      provides: "h1 heading for forgot password page"
      contains: "<h1"
    - path: "apps/web/app/(auth)/reset-password/page.tsx"
      provides: "h1 heading for reset password page"
      contains: "<h1"
    - path: "apps/web/app/(auth)/magic-link/page.tsx"
      provides: "h1 heading for magic link page"
      contains: "<h1"
    - path: "apps/admin/src/routes/login.tsx"
      provides: "h1 heading for admin login page"
      contains: "<h1"
  key_links: []
---

<objective>
Close the heading hierarchy gap on auth pages identified in 07-VERIFICATION.md.

Purpose: SC-1 requires "correct heading hierarchy (h1 through h3, no skipped levels)" on every page. Auth pages currently use CardTitle which renders as a `<div>`, providing no semantic heading for screen reader navigation.

Output: All 6 auth pages have a semantic `<h1>` element as their page heading.
</objective>

<execution_context>
@C:/Projetos/baseworks/.claude/get-shit-done/workflows/execute-plan.md
@C:/Projetos/baseworks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-accessibility/07-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add h1 headings to customer app auth pages</name>
  <files>
    apps/web/app/(auth)/login/page.tsx
    apps/web/app/(auth)/signup/page.tsx
    apps/web/app/(auth)/forgot-password/page.tsx
    apps/web/app/(auth)/reset-password/page.tsx
    apps/web/app/(auth)/magic-link/page.tsx
  </files>
  <read_first>
    apps/web/app/(auth)/login/page.tsx
    apps/web/app/(auth)/signup/page.tsx
    apps/web/app/(auth)/forgot-password/page.tsx
    apps/web/app/(auth)/reset-password/page.tsx
    apps/web/app/(auth)/magic-link/page.tsx
    packages/ui/src/components/card.tsx
  </read_first>
  <action>
In each auth page, replace the CardTitle element that serves as the page heading with an h1 element inside CardTitle. The pattern is:

BEFORE:
```tsx
<CardTitle className="text-2xl font-semibold">Sign in</CardTitle>
```

AFTER:
```tsx
<CardTitle asChild>
  <h1 className="text-2xl font-semibold">Sign in</h1>
</CardTitle>
```

Wait -- CardTitle in this codebase does NOT support asChild (it is a plain div, not a Radix Slot). Instead, replace CardTitle with a raw h1 that keeps the same styling:

AFTER (correct approach):
```tsx
<h1 className="text-2xl font-semibold leading-none tracking-tight">Sign in</h1>
```

Replace the `<CardTitle>` line with `<h1>` using the combined class names from both CardTitle's base styles (`text-2xl font-semibold leading-none tracking-tight`) and any page-specific classes.

Specific replacements per file:

1. `apps/web/app/(auth)/login/page.tsx` line 73:
   Replace `<CardTitle className="text-2xl font-semibold">Sign in</CardTitle>`
   With `<h1 className="text-2xl font-semibold leading-none tracking-tight">Sign in</h1>`
   Also remove CardTitle from the import on line 18.

2. `apps/web/app/(auth)/signup/page.tsx`:
   Replace `<CardTitle className="text-2xl font-semibold">Create account</CardTitle>`
   With `<h1 className="text-2xl font-semibold leading-none tracking-tight">Create account</h1>`
   Remove CardTitle from import.

3. `apps/web/app/(auth)/forgot-password/page.tsx` (has TWO CardTitle instances -- one for "Forgot password" form view, one for "Check your email" success view):
   Replace BOTH `<CardTitle className="text-2xl font-semibold">...</CardTitle>` with `<h1 className="text-2xl font-semibold leading-none tracking-tight">...</h1>`
   Remove CardTitle from import.

4. `apps/web/app/(auth)/reset-password/page.tsx` (has TWO CardTitle instances -- "Reset password" and "Invalid link"):
   Replace BOTH with h1 elements using the same class pattern.
   Remove CardTitle from import.

5. `apps/web/app/(auth)/magic-link/page.tsx` (has TWO CardTitle instances -- "Magic link" and "Check your email"):
   Replace BOTH with h1 elements using the same class pattern.
   Remove CardTitle from import.

IMPORTANT: Keep the exact same text content. Only change the element from div (via CardTitle) to h1. The CardTitle component's base classes are `text-2xl font-semibold leading-none tracking-tight` -- use these combined with any page-specific overrides.
  </action>
  <verify>
    <automated>grep -r "&lt;h1" apps/web/app/\(auth\)/ --include="*.tsx" | wc -l</automated>
    Expect: at least 7 (login: 1, signup: 1, forgot-password: 2, reset-password: 2, magic-link: 2 = 8 total h1 tags; some pages may have conditional rendering reducing visible count)
    Also: `grep -r "CardTitle" apps/web/app/\(auth\)/ --include="*.tsx"` should return zero matches.
  </verify>
  <acceptance_criteria>
    - Each of the 5 auth page files contains at least one `<h1` element
    - Zero occurrences of `CardTitle` in any file under `apps/web/app/(auth)/`
    - The h1 elements have class `text-2xl font-semibold leading-none tracking-tight`
    - No other heading elements (h2, h3) are introduced -- only h1 replacements
  </acceptance_criteria>
  <done>All 5 customer app auth pages render an h1 semantic heading instead of a div-based CardTitle</done>
</task>

<task type="auto">
  <name>Task 2: Add h1 heading to admin login page</name>
  <files>
    apps/admin/src/routes/login.tsx
  </files>
  <read_first>
    apps/admin/src/routes/login.tsx
    packages/ui/src/components/card.tsx
  </read_first>
  <action>
In `apps/admin/src/routes/login.tsx` line 82, replace:

```tsx
<CardTitle className="text-2xl font-semibold">Admin</CardTitle>
```

With:

```tsx
<h1 className="text-2xl font-semibold leading-none tracking-tight">Admin</h1>
```

Remove `CardTitle` from the import statement on line 8 (the import from `@baseworks/ui`). Keep all other imports (Card, CardContent, CardDescription, CardHeader, Button, Input, Label).
  </action>
  <verify>
    <automated>grep "&lt;h1" apps/admin/src/routes/login.tsx</automated>
    Expect: one line containing `<h1 className="text-2xl font-semibold leading-none tracking-tight">Admin</h1>`
    Also: `grep "CardTitle" apps/admin/src/routes/login.tsx` should return zero matches.
  </verify>
  <acceptance_criteria>
    - `apps/admin/src/routes/login.tsx` contains exactly one `<h1` element
    - Zero occurrences of `CardTitle` in `apps/admin/src/routes/login.tsx`
    - The h1 has class `text-2xl font-semibold leading-none tracking-tight`
    - The h1 text content is "Admin"
  </acceptance_criteria>
  <done>Admin login page renders an h1 semantic heading instead of a div-based CardTitle</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected. Changes are purely semantic HTML element swaps (div to h1) with no data flow, authentication, or input handling modifications.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | T (Tampering) | Auth page HTML | accept | Element swap from div to h1 has no security impact; no new attack surface |
</threat_model>

<verification>
After both tasks complete:
1. `grep -r "<h1" apps/web/app/\(auth\)/ --include="*.tsx"` returns matches in all 5 auth page files
2. `grep "<h1" apps/admin/src/routes/login.tsx` returns exactly one match
3. `grep -r "CardTitle" apps/web/app/\(auth\)/ apps/admin/src/routes/login.tsx --include="*.tsx"` returns zero matches
4. `cd packages/ui && bun run vitest run` -- all existing a11y tests still pass (no regressions)
</verification>

<success_criteria>
- All 6 auth pages across both apps have semantic h1 headings
- SC-1 heading hierarchy gap from 07-VERIFICATION.md is fully closed
- No visual regression (h1 uses identical classes to former CardTitle)
- All existing vitest-axe tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-accessibility/07-04-SUMMARY.md`
</output>
