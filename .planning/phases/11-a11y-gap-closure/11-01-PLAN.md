---
phase: 11-a11y-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/(auth)/invite/[token]/page.tsx
autonomous: true
requirements:
  - A11Y-01
tags:
  - accessibility
  - nextjs
  - frontend

must_haves:
  truths:
    - "Every card state on the invite accept page renders an <h1> at the top of its primary card"
    - "The invite accept page contains zero <CardTitle> usages after refactor"
    - "Heading font size/weight/tracking matches the canonical login page h1 pattern (visual parity)"
  artifacts:
    - path: "apps/web/app/(auth)/invite/[token]/page.tsx"
      provides: "Five <h1> elements (one per card state) using text-2xl font-semibold leading-none tracking-tight"
      contains: "<h1"
  key_links:
    - from: "apps/web/app/(auth)/invite/[token]/page.tsx"
      to: "canonical h1 pattern from apps/web/app/(auth)/login/page.tsx:85"
      via: "identical className string"
      pattern: "text-2xl font-semibold leading-none tracking-tight"
---

<objective>
Replace all 5 `<CardTitle>` usages on the invite accept page with `<h1>` elements matching the canonical auth-page heading pattern. This closes A11Y-01 by ensuring the invite accept page has a real top-level heading that screen readers can announce and that heading-order automation can detect.

Purpose: `<CardTitle>` from shadcn/ui renders as a `<div>` (not a heading). The Phase 07-04 fix updated login/signup/forgot-password/reset-password/magic-link to inline `<h1>` but missed the invite accept page added later in Phase 9. The v1.1 milestone audit flagged this as a partial A11Y-01 regression that must ship before milestone close.

Output: One modified file — `apps/web/app/(auth)/invite/[token]/page.tsx` — with all 5 card-state titles rendered as `<h1>` and the `CardTitle` import removed.
</objective>

<execution_context>
@C:/Projetos/baseworks/.claude/get-shit-done/workflows/execute-plan.md
@C:/Projetos/baseworks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-a11y-gap-closure/11-CONTEXT.md
@.planning/v1.1-MILESTONE-AUDIT.md

<!-- File being modified -->
@apps/web/app/(auth)/invite/[token]/page.tsx

<!-- Reference pattern to mirror (DO NOT modify) -->
@apps/web/app/(auth)/login/page.tsx

<interfaces>
<!-- Canonical auth h1 pattern extracted from login/page.tsx:85 -->
<!-- Use this EXACT JSX shape when replacing each CardTitle -->

```tsx
<h1 className="text-2xl font-semibold leading-none tracking-tight">
  {/* content */}
</h1>
```

<!-- Note: login page omits the extra `mt-4` / `text-2xl` wrapper classes that
     current CardTitle usages apply. For invite accept page, preserve existing
     layout spacing by keeping any `mt-4` on the h1 itself where it already
     appears on the CardTitle (lines 152, 218, 289 have `mt-4 text-2xl`;
     lines 171, 189 have just `text-2xl`). -->

<!-- Existing CardTitle call sites in apps/web/app/(auth)/invite/[token]/page.tsx: -->
<!--
  line 152: <CardTitle className="mt-4 text-2xl">        (invalid-state card)
  line 171: <CardTitle className="text-2xl">             (already-member card)
  line 189: <CardTitle className="text-2xl">             (declined card)
  line 218: <CardTitle className="mt-4 text-2xl">        (logged-in accept card)
  line 289: <CardTitle className="mt-4 text-2xl">        (not-logged-in card)
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace all 5 CardTitle usages with h1 and drop CardTitle import</name>
  <files>apps/web/app/(auth)/invite/[token]/page.tsx</files>
  <read_first>
    - apps/web/app/(auth)/invite/[token]/page.tsx (the full file — note all 5 CardTitle sites at lines 152, 171, 189, 218, 289 and the import on line 18)
    - apps/web/app/(auth)/login/page.tsx (specifically line 85 — this is the canonical h1 className pattern to mirror)
  </read_first>
  <action>
    Perform two changes in apps/web/app/(auth)/invite/[token]/page.tsx:

    1. **Remove `CardTitle` from the import block on line 18.** The current import block is:
       ```tsx
       import {
         Avatar,
         AvatarFallback,
         Badge,
         Button,
         Card,
         CardContent,
         CardDescription,
         CardFooter,
         CardHeader,
         CardTitle,       // <- DELETE this line
         Dialog,
         DialogContent,
         DialogDescription,
         DialogFooter,
         DialogHeader,
         DialogTitle,
         Skeleton,
       } from "@baseworks/ui";
       ```
       Delete the `CardTitle,` line exactly.

    2. **Replace each of the 5 `<CardTitle>` usages with an `<h1>`** that uses the canonical auth-page className `"text-2xl font-semibold leading-none tracking-tight"`, preserving the existing layout margin where the CardTitle had `mt-4`:

       **Line 152 (invalid-state card):**
       ```tsx
       <CardTitle className="mt-4 text-2xl">
         {t("accept.invalidTitle")}
       </CardTitle>
       ```
       becomes:
       ```tsx
       <h1 className="mt-4 text-2xl font-semibold leading-none tracking-tight">
         {t("accept.invalidTitle")}
       </h1>
       ```

       **Line 171 (already-member card):**
       ```tsx
       <CardTitle className="text-2xl">
         {t("accept.alreadyMember")}
       </CardTitle>
       ```
       becomes:
       ```tsx
       <h1 className="text-2xl font-semibold leading-none tracking-tight">
         {t("accept.alreadyMember")}
       </h1>
       ```

       **Line 189 (declined card):**
       ```tsx
       <CardTitle className="text-2xl">{t("accept.declined")}</CardTitle>
       ```
       becomes:
       ```tsx
       <h1 className="text-2xl font-semibold leading-none tracking-tight">
         {t("accept.declined")}
       </h1>
       ```

       **Line 218 (logged-in accept card):**
       ```tsx
       <CardTitle className="mt-4 text-2xl">{orgName}</CardTitle>
       ```
       becomes:
       ```tsx
       <h1 className="mt-4 text-2xl font-semibold leading-none tracking-tight">
         {orgName}
       </h1>
       ```

       **Line 289 (not-logged-in card):**
       ```tsx
       <CardTitle className="mt-4 text-2xl">{orgName}</CardTitle>
       ```
       becomes:
       ```tsx
       <h1 className="mt-4 text-2xl font-semibold leading-none tracking-tight">
         {orgName}
       </h1>
       ```

    Notes:
    - Do NOT change any other JSX. Do NOT touch `CardHeader`, `CardDescription`, `CardContent`, `CardFooter`, or any button/dialog wiring.
    - Do NOT add a heading to the `<Card>` inside the Dialog at lines 249-274 — that's a modal dialog with `DialogTitle`, which is the correct semantic.
    - Font size `text-2xl` is preserved exactly; `font-semibold`, `leading-none`, and `tracking-tight` are the shared auth-page h1 classes from login/page.tsx:85 and produce identical visual output to the previous CardTitle.
  </action>
  <verify>
    <automated>cd C:/Projetos/baseworks && grep -c "CardTitle" "apps/web/app/(auth)/invite/[token]/page.tsx"</automated>
    <automated>cd C:/Projetos/baseworks && grep -c "&lt;h1" "apps/web/app/(auth)/invite/[token]/page.tsx"</automated>
    <automated>cd C:/Projetos/baseworks && bun run --filter=@baseworks/web typecheck 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CardTitle" apps/web/app/\(auth\)/invite/\[token\]/page.tsx` returns exactly `0`
    - `grep -c '<h1' apps/web/app/\(auth\)/invite/\[token\]/page.tsx` returns exactly `5`
    - `grep -c "text-2xl font-semibold leading-none tracking-tight" apps/web/app/\(auth\)/invite/\[token\]/page.tsx` returns exactly `5`
    - No lingering `CardTitle,` in the import block (grep returns 0)
    - `bun run --filter=@baseworks/web typecheck` exits 0 (no TypeScript errors from the edit)
    - `bun test` exits 0 (existing packages/ui a11y tests still pass — they guard the FormMessage primitive transitively relied on by Plan 11-02, but we run them here to catch any accidental regression)
  </acceptance_criteria>
  <done>
    All 5 card states on the invite accept page render `<h1>` at the top of the primary Card. Visual output is unchanged (same font-size/weight/tracking). `CardTitle` is no longer imported. Typecheck passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. This is an internal refactor of presentational JSX on an existing page.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | N/A | `apps/web/app/(auth)/invite/[token]/page.tsx` | accept | No new threats introduced — structural refactor only. No new user input, no new network calls, no auth/authz changes, no secrets touched. Existing invite token validation and rendering logic is unchanged. |
</threat_model>

<verification>
Run after the task completes:

```bash
cd C:/Projetos/baseworks

# A11Y-01 grep assertions
grep -c "CardTitle" "apps/web/app/(auth)/invite/[token]/page.tsx"   # expect 0
grep -c '<h1' "apps/web/app/(auth)/invite/[token]/page.tsx"         # expect 5

# Typecheck
bun run --filter=@baseworks/web typecheck

# Existing tests still pass
bun test
```
</verification>

<success_criteria>
- Invite accept page has exactly 5 `<h1>` elements (one per card state)
- Invite accept page has zero `CardTitle` usages and no CardTitle import
- `@baseworks/web` typecheck passes
- Existing `bun test` suite passes (no regressions)
- Visual parity: heading text size/weight/tracking unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/11-a11y-gap-closure/11-01-SUMMARY.md`
</output>
