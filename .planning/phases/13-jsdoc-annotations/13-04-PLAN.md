---
phase: 13-jsdoc-annotations
plan: 04
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - apps/api/src/core/cqrs.ts
  - apps/api/src/core/event-bus.ts
  - apps/api/src/core/registry.ts
  - apps/api/src/core/middleware/tenant.ts
  - apps/api/src/core/middleware/error.ts
  - apps/api/src/core/middleware/request-trace.ts
autonomous: true
requirements:
  - JSDOC-04
  - JSDOC-05

must_haves:
  truths:
    - "CqrsBus class has method-level JSDoc on all public methods including execute and query"
    - "CqrsBus.execute and CqrsBus.query have @example blocks"
    - "TypedEventBus has method-level JSDoc on emit, on, and off"
    - "TypedEventBus.emit and TypedEventBus.on have @example blocks"
    - "ModuleRegistry has JSDoc on all public methods including attachRoutes, getCqrs, getEventBus, getLoaded, getLoadedNames"
    - "All 3 middleware files have normalized JSDoc per style guide"
    - "Error middleware documents HTTP status mapping behavior"
  artifacts:
    - path: "apps/api/src/core/cqrs.ts"
      provides: "Full method-level JSDoc with @example on execute/query"
      contains: "@example"
    - path: "apps/api/src/core/event-bus.ts"
      provides: "Full method-level JSDoc with @example on emit/on"
      contains: "@example"
    - path: "apps/api/src/core/registry.ts"
      provides: "JSDoc on all public methods"
      contains: "attachRoutes"
    - path: "apps/api/src/core/middleware/error.ts"
      provides: "Expanded JSDoc documenting status mapping"
      contains: "status"
  key_links:
    - from: "apps/api/src/core/cqrs.ts"
      to: "packages/shared/src/types/cqrs.ts"
      via: "CqrsBus dispatches to CommandHandler/QueryHandler types"
      pattern: "CommandHandler"
    - from: "apps/api/src/core/event-bus.ts"
      to: "packages/shared/src/types/events.ts"
      via: "TypedEventBus uses DomainEvents interface for type safety"
      pattern: "DomainEvents"
---

<objective>
Annotate all core infrastructure: CqrsBus, TypedEventBus, ModuleRegistry, and all middleware files.

Purpose: Core infrastructure (JSDOC-04) is the backbone of the module system. CqrsBus and EventBus are the two most-used APIs -- their @example blocks (JSDOC-05) will appear in IDE tooltips for every developer working with the codebase. This plan completes the remaining @example targets.

Output: 6 core files fully annotated with method-level JSDoc and 4+ @example blocks.
</objective>

<execution_context>
@C:/Projetos/baseworks/.claude/get-shit-done/workflows/execute-plan.md
@C:/Projetos/baseworks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-jsdoc-annotations/13-CONTEXT.md
@.planning/phases/13-jsdoc-annotations/13-PATTERNS.md
@.planning/phases/13-jsdoc-annotations/13-01-SUMMARY.md
@docs/jsdoc-style-guide.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Annotate CqrsBus and TypedEventBus with @example blocks</name>
  <files>
    apps/api/src/core/cqrs.ts,
    apps/api/src/core/event-bus.ts
  </files>
  <read_first>
    apps/api/src/core/cqrs.ts,
    apps/api/src/core/event-bus.ts,
    packages/shared/src/types/cqrs.ts,
    packages/shared/src/types/events.ts,
    docs/jsdoc-style-guide.md,
    .planning/phases/13-jsdoc-annotations/13-PATTERNS.md
  </read_first>
  <action>
**apps/api/src/core/cqrs.ts -- CqrsBus class:**

Read the file. The class currently has a one-line class block and NO method-level JSDoc. Add JSDoc to all public methods:

1. **Class-level block** (rewrite existing): "Command-Query Responsibility Segregation bus. Routes typed command and query dispatches to registered handlers. Each handler is registered with a namespaced key (e.g., 'auth:create-tenant') and receives validated input plus HandlerContext."

2. **registerCommand** method: "Register a command handler under a namespaced key. Called by ModuleRegistry during module loading." @param key - Namespaced command identifier (e.g., "auth:create-tenant"), @param handler - CommandHandler function, @returns void.

3. **registerQuery** method: "Register a query handler under a namespaced key. Called by ModuleRegistry during module loading." @param key, @param handler, @returns void.

4. **execute** method: "Dispatch a command to its registered handler. Validates that the command key exists and passes input + context to the handler." @param key - Namespaced command identifier, @param input - Validated command input, @param ctx - HandlerContext with tenantId, userId, db, emit. @returns Promise<Result<T>> from the handler. @throws if command key is not registered. Add @example (per D-05):
   ```
   @example
   const result = await bus.execute("auth:create-tenant", { name: "Acme" }, ctx);
   if (!result.success) throw new Error(result.error);
   return result.data; // { id: "...", name: "Acme" }
   ```

5. **query** method: "Dispatch a query to its registered handler. Validates that the query key exists and passes input + context to the handler." @param key, @param input, @param ctx. @returns Promise<Result<T>>. @throws if query key not registered. Add @example:
   ```
   @example
   const result = await bus.query("auth:get-tenant", { tenantId: ctx.tenantId }, ctx);
   if (!result.success) return notFound();
   return result.data;
   ```

6. Any other public methods (getRegisteredCommands, getRegisteredQueries, etc.): Add one-line JSDoc if they exist.

**apps/api/src/core/event-bus.ts -- TypedEventBus class:**

Read the file. Class has a good block; methods emit/on/off have NO JSDoc. Add:

1. **Class-level block**: Verify existing is comprehensive. Normalize tag order if needed. Keep reference to DomainEvents declaration merging.

2. **emit** method: "Emit a typed domain event to all registered listeners. Executes listeners asynchronously with error isolation -- a failing listener does not prevent other listeners from executing." @param event - Event name (key of DomainEvents interface), @param payload - Event payload matching the DomainEvents type. @returns Promise<void>. Add @example:
   ```
   @example
   await eventBus.emit("auth:tenant-created", { tenantId: "t-123", name: "Acme" });
   ```

3. **on** method: "Register a listener for a typed domain event. Returns an unsubscribe function." @param event - Event name, @param handler - Async callback receiving the event payload. @returns () => void (unsubscribe function). Add @example:
   ```
   @example
   const unsubscribe = eventBus.on("auth:tenant-created", async (payload) => {
     await createBillingCustomer(payload.tenantId);
   });
   ```

4. **off** method: "Remove a specific listener for an event. Prefer using the unsubscribe function returned by `on()`." @param event, @param handler. @returns void.
  </action>
  <verify>
    <automated>grep -c "@example" apps/api/src/core/cqrs.ts</automated>
    <automated>grep -c "@param" apps/api/src/core/cqrs.ts</automated>
    <automated>grep -c "@example" apps/api/src/core/event-bus.ts</automated>
    <automated>grep -c "@param" apps/api/src/core/event-bus.ts</automated>
    <automated>cd C:/Projetos/baseworks && bunx biome check apps/api/src/core/cqrs.ts apps/api/src/core/event-bus.ts --max-diagnostics=20</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/src/core/cqrs.ts contains at least 2 "@example" blocks (execute + query)
    - apps/api/src/core/cqrs.ts contains at least 6 "@param" occurrences (across all methods)
    - apps/api/src/core/event-bus.ts contains at least 2 "@example" blocks (emit + on)
    - apps/api/src/core/event-bus.ts contains at least 4 "@param" occurrences (across emit/on/off)
    - Both files pass `bunx biome check` with no errors
  </acceptance_criteria>
  <done>CqrsBus has method-level JSDoc on all public methods with @example on execute and query. TypedEventBus has method-level JSDoc with @example on emit and on. 4 @example blocks added (completing the 10-15 target with Plans 01-03).</done>
</task>

<task type="auto">
  <name>Task 2: Annotate ModuleRegistry and middleware files</name>
  <files>
    apps/api/src/core/registry.ts,
    apps/api/src/core/middleware/tenant.ts,
    apps/api/src/core/middleware/error.ts,
    apps/api/src/core/middleware/request-trace.ts
  </files>
  <read_first>
    apps/api/src/core/registry.ts,
    apps/api/src/core/middleware/tenant.ts,
    apps/api/src/core/middleware/error.ts,
    apps/api/src/core/middleware/request-trace.ts,
    docs/jsdoc-style-guide.md,
    .planning/phases/13-jsdoc-annotations/13-PATTERNS.md
  </read_first>
  <action>
**apps/api/src/core/registry.ts -- ModuleRegistry class:**

Read the file. Class has good class-level block and 3 documented methods (getAuthRoutes, getModuleRoutes, getBillingRoutes or similar). Per PATTERNS.md section 5.6, these methods need JSDoc: `attachRoutes`, `getCqrs`, `getEventBus`, `getLoaded`, `getLoadedNames`.

1. **Class-level block**: Verify existing. Normalize if needed. Keep "Config-driven module registry" description.

2. **Already documented methods** (getAuthRoutes, getModuleRoutes, getBillingRoutes): Verify they match D-01/D-03. Add @param/@returns if missing.

3. **loadAll** (or equivalent): "Load all modules configured in the config. Imports each module, registers its commands and queries in the CqrsBus, and stores the loaded module definition." @param config or similar, @returns Promise<void>. Note: 13-PATTERNS.md lists this as an @example candidate -- add if it makes sense:
   ```
   @example
   const registry = new ModuleRegistry(cqrs, eventBus, db, logger);
   await registry.loadAll(config);
   ```

4. **attachRoutes**: "Attach loaded module routes to the Elysia app instance. Must be called after loadAll(). Returns void -- routes are mounted as side effects." @param app - Elysia instance, @returns void.

5. **getCqrs**: "Returns the CqrsBus instance used by this registry." @returns CqrsBus. One-liner JSDoc is fine for this accessor.

6. **getEventBus**: "Returns the TypedEventBus instance used by this registry." @returns TypedEventBus. One-liner.

7. **getLoaded**: "Returns the array of loaded ModuleDefinition objects." @returns ModuleDefinition[]. One-liner.

8. **getLoadedNames**: "Returns the names of all loaded modules as a string array." @returns string[]. One-liner.

**Middleware files (3 files):**

9. **middleware/tenant.ts** -- Already has a comprehensive block per PATTERNS.md. Normalize: verify tag order, ensure Per D-16/T-02-05/T-02-09 references are present. Per D-03, if the export is a function or factory, add @returns describing the Elysia plugin with `tenantId` in derive context.

10. **middleware/error.ts** -- Currently has a one-line doc. Expand to document HTTP status mapping behavior: "Global error middleware mapping application errors to consistent JSON responses. Maps Error instances to 500, validation errors to 422, not-found to 404. Returns `{ error: string, statusCode: number }` shape." Add @returns or describe the behavior in prose. Document the `as: 'global'` scoping.

11. **middleware/request-trace.ts** -- Already has a full block. Normalize tag order per style guide. Ensure description covers what the trace ID is used for (logging, debugging, response headers if applicable).
  </action>
  <verify>
    <automated>grep -c "/\*\*" apps/api/src/core/registry.ts</automated>
    <automated>grep "attachRoutes\|getCqrs\|getEventBus\|getLoaded\|getLoadedNames" apps/api/src/core/registry.ts | head -10</automated>
    <automated>grep -c "status\|422\|500\|404" apps/api/src/core/middleware/error.ts</automated>
    <automated>cd C:/Projetos/baseworks && bunx biome check apps/api/src/core/ --max-diagnostics=20</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/src/core/registry.ts contains at least 8 JSDoc blocks (class + 7 methods)
    - apps/api/src/core/registry.ts JSDoc blocks cover "attachRoutes", "getCqrs", "getEventBus" (grep each word returns >= 1 in a JSDoc context)
    - apps/api/src/core/middleware/error.ts contains "500" or "422" or "status" in a JSDoc block (documenting HTTP mapping)
    - apps/api/src/core/middleware/error.ts JSDoc block is at least 3 lines (not just one-liner)
    - apps/api/src/core/middleware/tenant.ts contains "Per D-16" or "activeOrganizationId" in JSDoc
    - apps/api/src/core/middleware/request-trace.ts contains "/** " (JSDoc preserved/normalized)
    - All files in apps/api/src/core/ pass `bunx biome check` with no errors
  </acceptance_criteria>
  <done>ModuleRegistry has JSDoc on all public methods including previously undocumented ones. Error middleware expanded to document status mapping. All middleware normalized. Core infrastructure (JSDOC-04) is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected -- documentation-only changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | I (Information Disclosure) | Core infra JSDoc | accept | No secrets in documentation. @example blocks use placeholder data. |
</threat_model>

<verification>
1. CqrsBus: all public methods documented, @example on execute + query
2. TypedEventBus: all public methods documented, @example on emit + on
3. ModuleRegistry: all public methods documented (including 5 previously undocumented)
4. Error middleware: expanded to document HTTP status mapping
5. Tenant and request-trace middleware: normalized per style guide
6. `bunx biome check apps/api/src/core/` passes
</verification>

<success_criteria>
- All 6 core infrastructure files fully annotated per JSDOC-04
- 4+ @example blocks added (CqrsBus execute/query, EventBus emit/on), completing JSDOC-05 target of 10-15 total across all plans
- All annotations follow D-01/D-02/D-03/D-04
- Biome passes on all modified files
</success_criteria>

<output>
After completion, create `.planning/phases/13-jsdoc-annotations/13-04-SUMMARY.md`
</output>
