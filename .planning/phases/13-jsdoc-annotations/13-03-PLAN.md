---
phase: 13-jsdoc-annotations
plan: 03
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - packages/modules/billing/src/commands/create-checkout-session.ts
  - packages/modules/billing/src/commands/cancel-subscription.ts
  - packages/modules/billing/src/commands/change-subscription.ts
  - packages/modules/billing/src/commands/create-one-time-payment.ts
  - packages/modules/billing/src/commands/create-portal-session.ts
  - packages/modules/billing/src/commands/record-usage.ts
  - packages/modules/billing/src/queries/get-subscription-status.ts
  - packages/modules/billing/src/queries/get-billing-history.ts
  - packages/modules/billing/src/ports/payment-provider.ts
  - packages/modules/billing/src/ports/types.ts
  - packages/modules/billing/src/adapters/stripe/stripe-adapter.ts
  - packages/modules/billing/src/adapters/stripe/stripe-webhook-mapper.ts
  - packages/modules/billing/src/adapters/pagarme/pagarme-adapter.ts
  - packages/modules/billing/src/adapters/pagarme/pagarme-webhook-mapper.ts
  - packages/modules/billing/src/provider-factory.ts
  - packages/modules/billing/src/hooks/on-tenant-created.ts
  - packages/modules/billing/src/jobs/process-webhook.ts
  - packages/modules/billing/src/jobs/sync-usage.ts
  - packages/modules/billing/src/jobs/send-email.ts
  - packages/modules/billing/src/routes.ts
  - packages/modules/billing/src/schema.ts
  - packages/modules/example/src/commands/create-example.ts
  - packages/modules/example/src/queries/list-examples.ts
  - packages/modules/example/src/routes.ts
autonomous: true
requirements:
  - JSDOC-02
  - JSDOC-03
  - JSDOC-01
  - JSDOC-05

must_haves:
  truths:
    - "All 6 billing command handlers have standardized JSDoc with @param, @returns, and event references"
    - "All 2 billing query handlers have standardized JSDoc with @param and @returns"
    - "PaymentProvider interface and all 14 billing port types have JSDoc"
    - "Both adapters (Stripe, Pagarme) have class/method-level JSDoc"
    - "getPaymentProvider has @example block"
    - "registerBillingHooks has @returns tag"
    - "Example module handlers have full JSDoc blocks"
  artifacts:
    - path: "packages/modules/billing/src/ports/types.ts"
      provides: "JSDoc on all 14 billing port interfaces"
      contains: "/** "
    - path: "packages/modules/billing/src/ports/payment-provider.ts"
      provides: "JSDoc on PaymentProvider interface methods"
      contains: "@param"
    - path: "packages/modules/billing/src/provider-factory.ts"
      provides: "getPaymentProvider with @example"
      contains: "@example"
    - path: "packages/modules/example/src/commands/create-example.ts"
      provides: "Full handler JSDoc block"
      contains: "@param"
  key_links:
    - from: "packages/modules/billing/src/ports/payment-provider.ts"
      to: "adapters/stripe/ and adapters/pagarme/"
      via: "Adapter classes implement PaymentProvider interface"
      pattern: "implements PaymentProvider"
---

<objective>
Annotate all billing module exports (handlers, ports, adapters, jobs, hooks) and example module exports.

Purpose: The billing module has the most complex domain logic (payment provider abstraction, webhook processing, usage tracking). Documenting the port/adapter boundary and handler contracts makes the provider-agnostic architecture understandable. The example module serves as a template for new modules and needs documentation for that purpose.

Output: 21 billing files + 3 example module files annotated.
</objective>

<execution_context>
@C:/Projetos/baseworks/.claude/get-shit-done/workflows/execute-plan.md
@C:/Projetos/baseworks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-jsdoc-annotations/13-CONTEXT.md
@.planning/phases/13-jsdoc-annotations/13-PATTERNS.md
@.planning/phases/13-jsdoc-annotations/13-01-SUMMARY.md
@docs/jsdoc-style-guide.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Annotate billing command/query handlers and ports/types (10 files)</name>
  <files>
    packages/modules/billing/src/commands/create-checkout-session.ts,
    packages/modules/billing/src/commands/cancel-subscription.ts,
    packages/modules/billing/src/commands/change-subscription.ts,
    packages/modules/billing/src/commands/create-one-time-payment.ts,
    packages/modules/billing/src/commands/create-portal-session.ts,
    packages/modules/billing/src/commands/record-usage.ts,
    packages/modules/billing/src/queries/get-subscription-status.ts,
    packages/modules/billing/src/queries/get-billing-history.ts,
    packages/modules/billing/src/ports/payment-provider.ts,
    packages/modules/billing/src/ports/types.ts
  </files>
  <read_first>
    packages/modules/billing/src/commands/create-checkout-session.ts,
    packages/modules/billing/src/commands/cancel-subscription.ts,
    packages/modules/billing/src/commands/change-subscription.ts,
    packages/modules/billing/src/commands/create-one-time-payment.ts,
    packages/modules/billing/src/commands/create-portal-session.ts,
    packages/modules/billing/src/commands/record-usage.ts,
    packages/modules/billing/src/queries/get-subscription-status.ts,
    packages/modules/billing/src/queries/get-billing-history.ts,
    packages/modules/billing/src/ports/payment-provider.ts,
    packages/modules/billing/src/ports/types.ts,
    docs/jsdoc-style-guide.md
  </read_first>
  <action>
**Billing command handlers (6 files) -- apply defineCommand template:**

1. **create-checkout-session.ts** -- "Create a Stripe/provider checkout session for subscription signup. Delegates to the configured PaymentProvider adapter." @param input (priceId, successUrl, cancelUrl or similar), @param ctx, @returns Result<{ url: string }> with checkout redirect URL.

2. **cancel-subscription.ts** -- "Cancel the active subscription for the current tenant. Delegates cancellation to the configured PaymentProvider." @param input, @param ctx, @returns Result. Note if it emits SubscriptionCancelled event.

3. **change-subscription.ts** -- "Change the current tenant's subscription to a different plan/price. Handles proration via the PaymentProvider adapter." @param input (newPriceId or similar), @param ctx, @returns Result.

4. **create-one-time-payment.ts** -- "Create a one-time payment checkout session (not a subscription). Used for add-ons or one-off purchases." @param input, @param ctx, @returns Result<{ url: string }>.

5. **create-portal-session.ts** -- "Create a customer portal session URL for self-service billing management. Redirects to the provider's hosted portal (e.g., Stripe Customer Portal)." @param input, @param ctx, @returns Result<{ url: string }>.

6. **record-usage.ts** -- "Record a metered usage event for the current tenant. Stored in usage_records table and synced to the payment provider." @param input (metric, quantity, timestamp), @param ctx, @returns Result.

**Billing query handlers (2 files) -- apply defineQuery template:**

7. **get-subscription-status.ts** -- "Retrieve the current subscription status for the current tenant. Returns plan details, status, and billing period." @param input, @param ctx, @returns Result<{ subscription }> or err if no subscription.

8. **get-billing-history.ts** -- "Retrieve billing history (invoices/payments) for the current tenant." @param input (pagination if any), @param ctx, @returns Result<{ history: BillingEvent[] }>.

**Port files (2 files) -- apply interface template:**

9. **ports/payment-provider.ts** -- Already has file-level and interface overview. Per D-03, add @param/@returns to EACH method signature in the PaymentProvider interface:
   - `createCheckoutSession(@param input, @returns Promise<{ url }>)`
   - `cancelSubscription(@param subscriptionId, @returns Promise<void>)`
   - `changeSubscription(...)` etc.
   Each method doc should be 1-2 lines explaining what it does in provider-agnostic terms.

10. **ports/types.ts** -- Already has file-level block. Add interface-level JSDoc to all 14 interfaces. For each interface: one sentence describing what domain concept it represents. Example for `CheckoutSessionInput`: "Input for creating a provider checkout session. Contains pricing, URLs, and customer reference." Keep descriptions provider-agnostic per the billing abstraction design.

**Rules:** D-01/D-02/D-03/D-04, tag order, 100-char lines.
  </action>
  <verify>
    <automated>for f in packages/modules/billing/src/commands/*.ts; do echo "$f:"; grep -c "@param" "$f"; done</automated>
    <automated>for f in packages/modules/billing/src/queries/*.ts; do echo "$f:"; grep -c "@returns" "$f"; done</automated>
    <automated>grep -c "@param" packages/modules/billing/src/ports/payment-provider.ts</automated>
    <automated>grep -c "/\*\*" packages/modules/billing/src/ports/types.ts</automated>
    <automated>cd C:/Projetos/baseworks && bunx biome check packages/modules/billing/src/commands/ packages/modules/billing/src/queries/ packages/modules/billing/src/ports/ --max-diagnostics=20</automated>
  </verify>
  <acceptance_criteria>
    - All 6 billing command handler files contain "@param" and "@returns"
    - Both billing query handler files contain "@param" and "@returns"
    - packages/modules/billing/src/ports/payment-provider.ts contains at least 5 "@param" occurrences (one per interface method)
    - packages/modules/billing/src/ports/types.ts contains at least 14 JSDoc blocks ("/** " count >= 14)
    - All modified files pass `bunx biome check` with no errors
  </acceptance_criteria>
  <done>All 8 billing CQRS handlers documented. PaymentProvider interface methods have @param/@returns. All 14 port types have interface-level JSDoc.</done>
</task>

<task type="auto">
  <name>Task 2: Annotate billing adapters, jobs, hooks, and example module (14 files)</name>
  <files>
    packages/modules/billing/src/adapters/stripe/stripe-adapter.ts,
    packages/modules/billing/src/adapters/stripe/stripe-webhook-mapper.ts,
    packages/modules/billing/src/adapters/pagarme/pagarme-adapter.ts,
    packages/modules/billing/src/adapters/pagarme/pagarme-webhook-mapper.ts,
    packages/modules/billing/src/provider-factory.ts,
    packages/modules/billing/src/hooks/on-tenant-created.ts,
    packages/modules/billing/src/jobs/process-webhook.ts,
    packages/modules/billing/src/jobs/sync-usage.ts,
    packages/modules/billing/src/jobs/send-email.ts,
    packages/modules/billing/src/routes.ts,
    packages/modules/billing/src/schema.ts,
    packages/modules/example/src/commands/create-example.ts,
    packages/modules/example/src/queries/list-examples.ts,
    packages/modules/example/src/routes.ts
  </files>
  <read_first>
    packages/modules/billing/src/adapters/stripe/stripe-adapter.ts,
    packages/modules/billing/src/adapters/stripe/stripe-webhook-mapper.ts,
    packages/modules/billing/src/adapters/pagarme/pagarme-adapter.ts,
    packages/modules/billing/src/adapters/pagarme/pagarme-webhook-mapper.ts,
    packages/modules/billing/src/provider-factory.ts,
    packages/modules/billing/src/hooks/on-tenant-created.ts,
    packages/modules/billing/src/jobs/process-webhook.ts,
    packages/modules/billing/src/jobs/sync-usage.ts,
    packages/modules/billing/src/jobs/send-email.ts,
    packages/modules/billing/src/routes.ts,
    packages/modules/billing/src/schema.ts,
    packages/modules/example/src/commands/create-example.ts,
    packages/modules/example/src/queries/list-examples.ts,
    packages/modules/example/src/routes.ts,
    docs/jsdoc-style-guide.md
  </read_first>
  <action>
**Adapter files (4 files) -- apply class template:**

1. **stripe-adapter.ts** -- Class-level: "Stripe implementation of the PaymentProvider interface. Wraps the Stripe Node.js SDK for checkout, subscription management, portal, and usage recording." Method-level JSDoc on each public method that implements PaymentProvider, referencing Stripe-specific behavior (e.g., "Uses stripe.checkout.sessions.create with mode='subscription'"). @param/@returns on each.

2. **stripe-webhook-mapper.ts** -- "Maps Stripe webhook event types to normalized billing domain events. Handles checkout.session.completed, customer.subscription.updated/deleted, invoice.payment_succeeded/failed." Document the mapping function with @param (Stripe event) and @returns (normalized event or null).

3. **pagarme-adapter.ts** -- Same pattern as Stripe adapter but for Pagar.me. Class-level: "Pagar.me implementation of the PaymentProvider interface." Method-level docs on public methods.

4. **pagarme-webhook-mapper.ts** -- Same pattern as Stripe webhook mapper but for Pagar.me events.

**Infrastructure files (5 files):**

5. **provider-factory.ts** -- Already has good JSDoc. Per D-03, add @param/@returns to all 3 exported functions. Per D-05, add @example to `getPaymentProvider`:
   ```
   @example
   const provider = getPaymentProvider();
   const session = await provider.createCheckoutSession(input);
   ```
   Document the lazy singleton pattern: "Returns the cached PaymentProvider instance, creating it on first call based on PAYMENT_PROVIDER env var."

6. **hooks/on-tenant-created.ts** -- Already has good JSDoc on `registerBillingHooks`. Per D-03, add @returns. Normalize tone per D-02.

7. **jobs/process-webhook.ts** -- Already has file-level + sub-handler docs. Per D-03, add @param (job data with provider, event payload), @returns, @throws (document that it re-throws for BullMQ retry). Ensure sub-handler functions have @param/@returns.

8. **jobs/sync-usage.ts** -- Read and audit. Add full block: "Synchronize recorded usage events to the payment provider. Processes pending usage_records and submits them via the PaymentProvider adapter." @param (job data if any), @returns.

9. **jobs/send-email.ts** -- Read and audit. Add full block: "Send a transactional email via Resend. Processes email jobs from the BullMQ queue." @param (job data: to, subject, template), @returns.

**Billing supporting files (2 files):**

10. **routes.ts** -- Apply Elysia plugin template. "Billing module routes plugin. Mounts CQRS command/query endpoints for checkout, subscription management, portal, and billing history."

11. **schema.ts** -- If separate from packages/db schema, add file-level block describing what billing-specific schema this file defines or re-exports.

**Example module (3 files):**

12. **create-example.ts** -- Currently has NO JSDoc. Add full handler block: "Create a new example record for the current tenant. Demonstrates the standard defineCommand pattern for new module development." @param input (name/data fields), @param ctx, @returns Result<{ example }>.

13. **list-examples.ts** -- Read and audit. Add: "List all example records for the current tenant. Demonstrates the standard defineQuery pattern." @param input, @param ctx, @returns Result<{ examples: Example[] }>.

14. **example/routes.ts** -- Apply Elysia plugin template: "Example module routes plugin. Demonstrates the standard route pattern for new modules with CQRS dispatch."
  </action>
  <verify>
    <automated>grep -c "@example" packages/modules/billing/src/provider-factory.ts</automated>
    <automated>grep -c "/\*\*" packages/modules/billing/src/adapters/stripe/stripe-adapter.ts</automated>
    <automated>grep -c "@param" packages/modules/example/src/commands/create-example.ts</automated>
    <automated>grep -c "/\*\*" packages/modules/billing/src/jobs/process-webhook.ts</automated>
    <automated>cd C:/Projetos/baseworks && bunx biome check packages/modules/billing/src/ packages/modules/example/src/ --max-diagnostics=20</automated>
  </verify>
  <acceptance_criteria>
    - packages/modules/billing/src/adapters/stripe/stripe-adapter.ts contains at least 3 JSDoc blocks (class + methods)
    - packages/modules/billing/src/adapters/pagarme/pagarme-adapter.ts contains at least 3 JSDoc blocks
    - packages/modules/billing/src/provider-factory.ts contains "@example" (grep >= 1)
    - packages/modules/billing/src/hooks/on-tenant-created.ts contains "@returns"
    - packages/modules/billing/src/jobs/process-webhook.ts contains "@throws"
    - packages/modules/example/src/commands/create-example.ts contains "@param" and "@returns"
    - packages/modules/example/src/queries/list-examples.ts contains "@param" and "@returns"
    - All files pass `bunx biome check` with no errors
  </acceptance_criteria>
  <done>All billing adapters, jobs, hooks, routes, schema documented. Example module fully annotated as a template reference. getPaymentProvider has @example (8th of 10-15 target).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected -- documentation-only changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | I (Information Disclosure) | Adapter JSDoc | accept | No API keys or webhook secrets documented in JSDoc. Examples use placeholder values. |
</threat_model>

<verification>
1. All 8 billing CQRS handlers have @param and @returns
2. PaymentProvider methods have @param/@returns
3. All 14 port types have JSDoc
4. Both adapters have class + method-level JSDoc
5. getPaymentProvider has @example
6. Example module has full handler JSDoc
7. `bunx biome check packages/modules/billing/src/ packages/modules/example/src/` passes
</verification>

<success_criteria>
- 8 billing handlers + 2 billing queries annotated per D-01/D-03
- 14 port types documented
- 4 adapter files documented
- 5 infrastructure/job files documented
- 3 example module files documented
- 1 @example added (getPaymentProvider)
- Biome passes on all modified files
</success_criteria>

<output>
After completion, create `.planning/phases/13-jsdoc-annotations/13-03-SUMMARY.md`
</output>
