---
phase: 09-team-invites
plan: 01
type: execute
wave: 1
depends_on:
  - "00"
files_modified:
  - packages/modules/auth/src/auth.ts
  - packages/modules/billing/src/templates/team-invite.tsx
  - packages/modules/billing/src/jobs/send-email.ts
  - packages/i18n/src/index.ts
  - packages/i18n/src/locales/en/invite.json
  - packages/i18n/src/locales/pt-BR/invite.json
  - packages/ui/src/components/ui/switch.tsx
autonomous: true
requirements:
  - INVT-01
  - INVT-02

must_haves:
  truths:
    - "sendInvitationEmail callback in better-auth org plugin enqueues team-invite email via BullMQ"
    - "sendInvitationEmail callback skips email enqueueing when email ends with @internal (link mode suppression)"
    - "Email template renders org name, inviter name, role, and accept link"
    - "Invitation expiration is effectively disabled per D-11"
    - "All invite UI strings exist in en and pt-BR translation files"
    - "Switch component is available from @baseworks/ui"
  artifacts:
    - path: "packages/modules/auth/src/auth.ts"
      provides: "sendInvitationEmail callback + invitationExpiresIn config + @internal email suppression"
      contains: "sendInvitationEmail"
    - path: "packages/modules/billing/src/templates/team-invite.tsx"
      provides: "React Email template for team invitations"
      exports: ["TeamInviteEmail"]
    - path: "packages/modules/billing/src/jobs/send-email.ts"
      provides: "team-invite template registered in email handler"
      contains: "team-invite"
    - path: "packages/i18n/src/locales/en/invite.json"
      provides: "English invite translations"
    - path: "packages/i18n/src/locales/pt-BR/invite.json"
      provides: "Portuguese invite translations"
    - path: "packages/i18n/src/index.ts"
      provides: "invite namespace registered"
      contains: "invite"
  key_links:
    - from: "packages/modules/auth/src/auth.ts"
      to: "packages/modules/billing/src/jobs/send-email.ts"
      via: "BullMQ email:send queue with template: team-invite"
      pattern: "queue\\.add.*team-invite"
---

<objective>
Wire the backend invitation infrastructure: better-auth sendInvitationEmail callback (with @internal email suppression for link mode), email template, i18n translations for the entire invite feature, and install the Switch UI component.

Purpose: Provide the foundation that all subsequent plans depend on -- email delivery, translation strings, and UI primitives. The @internal email suppression contract is critical: Plan 02 uses `link-invite-{nanoid}@internal` placeholder emails for shareable link mode, and this callback must detect that suffix and skip email enqueueing.
Output: Modified auth.ts with invitation config, new email template, new i18n namespace, Switch component installed.
</objective>

<execution_context>
@C:/Projetos/baseworks/.claude/get-shit-done/workflows/execute-plan.md
@C:/Projetos/baseworks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-team-invites/09-CONTEXT.md
@.planning/phases/09-team-invites/09-RESEARCH.md
@.planning/phases/09-team-invites/09-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From packages/modules/auth/src/auth.ts:
```typescript
// Organization plugin config (lines 84-88) -- add sendInvitationEmail + invitationExpiresIn here
organization({
  allowUserToCreateOrganization: true,
  creatorRole: "owner",
  organizationLimit: 5,
})
```

From packages/modules/billing/src/jobs/send-email.ts:
```typescript
// Templates map (line 8-13) -- add "team-invite" entry here
const templates: Record<string, (data: any) => JSX.Element> = {
  "welcome": (data) => WelcomeEmail(data),
  "password-reset": (data) => PasswordResetEmail(data),
  "magic-link": (data) => PasswordResetEmail({ ...data, userName: data.email }),
  "billing-notification": (data) => BillingNotificationEmail(data),
};
// Subjects map (line 15-20) -- add "team-invite" entry here
const subjects: Record<string, string> = { ... };
```

From packages/modules/billing/src/templates/password-reset.tsx:
```typescript
// Follow this exact pattern for the team-invite template
export function PasswordResetEmail({ url, userName = "there" }: PasswordResetEmailProps) { ... }
```

From packages/i18n/src/index.ts:
```typescript
export const namespaces = ["common", "auth", "dashboard", "billing", "admin"] as const;
// Must add "invite" to this array
// Must add exports for enInvite and ptBRInvite
```

Email suppression contract (established by this plan, consumed by Plan 02 and Plan 03):
```
- Shareable link invitations use placeholder email: `link-invite-{nanoid}@internal`
- sendInvitationEmail callback checks: if email ends with "@internal", skip email enqueueing
- Plan 03 uses the same "@internal" suffix to detect link-type vs email-type invitations in the UI
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire sendInvitationEmail callback with @internal suppression, email template, and email handler registration</name>
  <files>
    packages/modules/auth/src/auth.ts,
    packages/modules/billing/src/templates/team-invite.tsx,
    packages/modules/billing/src/jobs/send-email.ts
  </files>
  <read_first>
    packages/modules/auth/src/auth.ts,
    packages/modules/billing/src/templates/password-reset.tsx,
    packages/modules/billing/src/jobs/send-email.ts
  </read_first>
  <action>
**1. Create `packages/modules/billing/src/templates/team-invite.tsx`:**

Create a React Email template following the exact pattern of password-reset.tsx. The component must be named `TeamInviteEmail` and accept these props:

```typescript
interface TeamInviteEmailProps {
  inviteLink: string;
  organizationName: string;
  inviterName: string;
  role: string;
}
```

Template structure:
- Html > Head > Body (fontFamily: "Arial, sans-serif", backgroundColor: "#f4f4f5") > Container (maxWidth: "480px", margin: "0 auto", padding: "20px", backgroundColor: "#ffffff")
- Text heading (fontSize: "24px", fontWeight: "bold"): "You're invited to {organizationName}"
- Text body: "{inviterName} has invited you to join {organizationName} as a {role}."
- Button CTA (href={inviteLink}, backgroundColor: "#18181b", color: "#ffffff", padding: "12px 20px", borderRadius: "6px", textDecoration: "none"): "Accept Invitation"
- Hr separator
- Text disclaimer (color: "#71717a", fontSize: "14px"): "If you were not expecting this invitation, you can ignore this email."

Import from `@react-email/components`: Html, Head, Body, Container, Text, Button, Hr.

**2. Register template in `packages/modules/billing/src/jobs/send-email.ts`:**

Add import at top:
```typescript
import { TeamInviteEmail } from "../templates/team-invite";
```

Add to `templates` map:
```typescript
"team-invite": (data) => TeamInviteEmail(data),
```

Add to `subjects` map:
```typescript
"team-invite": "You're Invited to Join a Team",
```

**3. Modify `packages/modules/auth/src/auth.ts`:**

In the `organization()` plugin call (around line 84), add two new properties. **CRITICAL: The sendInvitationEmail callback MUST check for @internal email suffix and skip enqueueing for link-mode invitations.** This is the email suppression contract that Plan 02 and Plan 03 depend on.

```typescript
organization({
  allowUserToCreateOrganization: true,
  creatorRole: "owner",
  organizationLimit: 5,
  invitationExpiresIn: 315360000, // ~10 years in seconds, effectively no expiration per D-11
  sendInvitationEmail: async (data) => {
    // Email suppression for shareable link mode:
    // Plan 02 creates link invitations with placeholder email `link-invite-{nanoid}@internal`
    // When we detect the @internal suffix, skip email enqueueing entirely.
    if (data.email.endsWith("@internal")) {
      console.log(`[AUTH] Link-mode invite (no email): ${data.email}`);
      return;
    }

    const queue = getEmailQueue();
    const inviteLink = `${env.WEB_URL}/invite/${data.id}`;
    if (queue) {
      await queue.add("team-invite", {
        to: data.email,
        template: "team-invite",
        data: {
          inviteLink,
          organizationName: data.organization.name,
          inviterName: data.inviter.user.name || data.inviter.user.email,
          role: data.role,
        },
      });
    } else {
      console.log(`[AUTH] Team invite for ${data.email}: ${inviteLink}`);
    }
  },
})
```

Note: `env.WEB_URL` should already exist in the env config (used by other parts of the app). If it doesn't exist, check env and use `env.NEXT_PUBLIC_APP_URL` or the appropriate web URL env var. The `getEmailQueue()` function is already defined at the top of auth.ts.
  </action>
  <verify>
    <automated>cd C:/Projetos/baseworks && grep -n "sendInvitationEmail" packages/modules/auth/src/auth.ts && grep -n "invitationExpiresIn" packages/modules/auth/src/auth.ts && grep -n "@internal" packages/modules/auth/src/auth.ts && grep -n "team-invite" packages/modules/billing/src/jobs/send-email.ts && grep -n "TeamInviteEmail" packages/modules/billing/src/templates/team-invite.tsx</automated>
  </verify>
  <acceptance_criteria>
    - packages/modules/auth/src/auth.ts contains `sendInvitationEmail: async (data)`
    - packages/modules/auth/src/auth.ts contains `invitationExpiresIn: 315360000`
    - packages/modules/auth/src/auth.ts contains `data.email.endsWith("@internal")` check that returns early
    - packages/modules/auth/src/auth.ts contains `queue.add("team-invite"`
    - packages/modules/billing/src/templates/team-invite.tsx exports `TeamInviteEmail`
    - packages/modules/billing/src/templates/team-invite.tsx contains `inviteLink`
    - packages/modules/billing/src/templates/team-invite.tsx contains `organizationName`
    - packages/modules/billing/src/jobs/send-email.ts contains `"team-invite": (data) => TeamInviteEmail(data)`
    - packages/modules/billing/src/jobs/send-email.ts contains `"team-invite": "You're Invited to Join a Team"`
  </acceptance_criteria>
  <done>sendInvitationEmail callback enqueues team-invite email via BullMQ for real emails, skips enqueueing for @internal placeholder emails (link mode), template renders invitation details, email handler recognizes team-invite template, invitation expiration effectively disabled.</done>
</task>

<task type="auto">
  <name>Task 2: Create invite i18n namespace and install Switch component</name>
  <files>
    packages/i18n/src/locales/en/invite.json,
    packages/i18n/src/locales/pt-BR/invite.json,
    packages/i18n/src/index.ts,
    packages/ui/src/components/ui/switch.tsx
  </files>
  <read_first>
    packages/i18n/src/index.ts,
    packages/i18n/src/locales/en/common.json,
    packages/i18n/src/locales/en/auth.json
  </read_first>
  <action>
**1. Install Switch component:**

Run from packages/ui directory:
```bash
cd packages/ui && npx shadcn@latest add switch --yes
```

Verify the switch.tsx file was created in `packages/ui/src/components/ui/switch.tsx`. If shadcn installs to a different path, move it to the correct location. Ensure it is exported from the UI package barrel export (check packages/ui/src/index.ts or equivalent and add `export * from "./components/ui/switch"` if not auto-added).

**2. Create `packages/i18n/src/locales/en/invite.json`:**

All translation keys from UI-SPEC copywriting contract. Structure:

```json
{
  "settings": {
    "title": "Settings",
    "tabs": {
      "team": "Team"
    }
  },
  "members": {
    "title": "Members",
    "removeMember": "Remove member",
    "removeConfirmTitle": "Remove this member?",
    "removeConfirmBody": "This member will lose access to the organization.",
    "removeConfirmButton": "Yes, Remove Member",
    "removed": "Member removed"
  },
  "pending": {
    "title": "Pending Invitations",
    "empty": "No pending invitations",
    "emptyDescription": "Invite team members to collaborate in your organization.",
    "cancelled": "Invitation cancelled",
    "resent": "Invitation resent to {email}",
    "typeEmail": "Email",
    "typeLink": "Link"
  },
  "dialog": {
    "title": "Invite to {orgName}",
    "description": "Send an email invite or generate a shareable link.",
    "modeLabel": "Invitation type",
    "emailMode": "Email",
    "linkMode": "Link",
    "emailLabel": "Email address",
    "emailPlaceholder": "colleague@example.com",
    "roleLabel": "Role",
    "validation": {
      "emailRequired": "Email address is required",
      "emailInvalid": "Please enter a valid email address"
    }
  },
  "roles": {
    "owner": "Owner",
    "admin": "Admin",
    "member": "Member"
  },
  "actions": {
    "inviteMember": "Invite Team Member",
    "sendInvite": "Send Invite",
    "generateLink": "Generate Link",
    "copyLink": "Copy Link",
    "linkCopied": "Copied!",
    "accept": "Accept Invitation",
    "decline": "Decline Invitation",
    "loginToAccept": "Log in to Accept",
    "createAccountToJoin": "Create Account to Join",
    "cancelInvitation": "Cancel Invitation",
    "resendInvite": "Resend Invite"
  },
  "toast": {
    "inviteSent": "Invitation sent to {email}",
    "linkCopied": "Invite link copied to clipboard",
    "linkGenerated": "Invite link generated"
  },
  "accept": {
    "invitedBy": "{name} invited you to join {orgName}",
    "roleAssigned": "You'll join as",
    "invalidTitle": "This invitation is no longer valid",
    "invalidBody": "It may have been cancelled or already used. Contact the person who invited you for a new link.",
    "alreadyMember": "You're already a member of this organization.",
    "goHome": "Go to Home",
    "goToDashboard": "Go to Dashboard",
    "declineConfirmTitle": "Decline this invitation?",
    "declineConfirmBody": "You can ask for a new invitation later if you change your mind.",
    "declineConfirmButton": "Yes, Decline Invitation",
    "declined": "Invitation declined"
  },
  "cancel": {
    "confirmTitle": "Cancel this invitation?",
    "confirmBody": "This will revoke the invitation. The recipient will no longer be able to join using this invite.",
    "confirmButton": "Yes, Cancel Invitation"
  }
}
```

**3. Create `packages/i18n/src/locales/pt-BR/invite.json`:**

Same structure with Portuguese translations:

```json
{
  "settings": {
    "title": "Configuracoes",
    "tabs": {
      "team": "Equipe"
    }
  },
  "members": {
    "title": "Membros",
    "removeMember": "Remover membro",
    "removeConfirmTitle": "Remover este membro?",
    "removeConfirmBody": "Este membro perdera acesso a organizacao.",
    "removeConfirmButton": "Sim, Remover Membro",
    "removed": "Membro removido"
  },
  "pending": {
    "title": "Convites Pendentes",
    "empty": "Nenhum convite pendente",
    "emptyDescription": "Convide membros da equipe para colaborar na sua organizacao.",
    "cancelled": "Convite cancelado",
    "resent": "Convite reenviado para {email}",
    "typeEmail": "Email",
    "typeLink": "Link"
  },
  "dialog": {
    "title": "Convidar para {orgName}",
    "description": "Envie um convite por email ou gere um link compartilhavel.",
    "modeLabel": "Tipo de convite",
    "emailMode": "Email",
    "linkMode": "Link",
    "emailLabel": "Endereco de email",
    "emailPlaceholder": "colega@exemplo.com",
    "roleLabel": "Funcao",
    "validation": {
      "emailRequired": "Endereco de email e obrigatorio",
      "emailInvalid": "Por favor, insira um endereco de email valido"
    }
  },
  "roles": {
    "owner": "Proprietario",
    "admin": "Admin",
    "member": "Membro"
  },
  "actions": {
    "inviteMember": "Convidar Membro",
    "sendInvite": "Enviar Convite",
    "generateLink": "Gerar Link",
    "copyLink": "Copiar Link",
    "linkCopied": "Copiado!",
    "accept": "Aceitar Convite",
    "decline": "Recusar Convite",
    "loginToAccept": "Entrar para Aceitar",
    "createAccountToJoin": "Criar Conta para Participar",
    "cancelInvitation": "Cancelar Convite",
    "resendInvite": "Reenviar Convite"
  },
  "toast": {
    "inviteSent": "Convite enviado para {email}",
    "linkCopied": "Link de convite copiado",
    "linkGenerated": "Link de convite gerado"
  },
  "accept": {
    "invitedBy": "{name} convidou voce para {orgName}",
    "roleAssigned": "Voce entrara como",
    "invalidTitle": "Este convite nao e mais valido",
    "invalidBody": "Ele pode ter sido cancelado ou ja utilizado. Entre em contato com quem o convidou para um novo link.",
    "alreadyMember": "Voce ja e membro desta organizacao.",
    "goHome": "Ir para Inicio",
    "goToDashboard": "Ir para Painel",
    "declineConfirmTitle": "Recusar este convite?",
    "declineConfirmBody": "Voce pode pedir um novo convite depois se mudar de ideia.",
    "declineConfirmButton": "Sim, Recusar Convite",
    "declined": "Convite recusado"
  },
  "cancel": {
    "confirmTitle": "Cancelar este convite?",
    "confirmBody": "Isso revogara o convite. O destinatario nao podera mais ingressar usando este convite.",
    "confirmButton": "Sim, Cancelar Convite"
  }
}
```

**4. Update `packages/i18n/src/index.ts`:**

- Add `"invite"` to the namespaces array: `export const namespaces = ["common", "auth", "dashboard", "billing", "admin", "invite"] as const;`
- Add English export: `export { default as enInvite } from "./locales/en/invite.json";`
- Add Portuguese export: `export { default as ptBRInvite } from "./locales/pt-BR/invite.json";`

Place the new exports after the existing admin exports, following the same pattern.
  </action>
  <verify>
    <automated>cd C:/Projetos/baseworks && grep -n "invite" packages/i18n/src/index.ts && test -f packages/i18n/src/locales/en/invite.json && test -f packages/i18n/src/locales/pt-BR/invite.json && test -f packages/ui/src/components/ui/switch.tsx && echo "ALL CHECKS PASS"</automated>
  </verify>
  <acceptance_criteria>
    - packages/i18n/src/index.ts contains `"invite"` in namespaces array
    - packages/i18n/src/index.ts contains `export { default as enInvite }`
    - packages/i18n/src/index.ts contains `export { default as ptBRInvite }`
    - packages/i18n/src/locales/en/invite.json exists and contains keys: settings.title, members.title, pending.title, dialog.title, roles.admin, roles.member, actions.inviteMember, accept.invitedBy
    - packages/i18n/src/locales/pt-BR/invite.json exists and contains keys: settings.title, members.title, pending.title, dialog.title
    - packages/ui/src/components/ui/switch.tsx exists
    - Switch component is exported from packages/ui barrel export
  </acceptance_criteria>
  <done>invite namespace with en/pt-BR translations registered in i18n package, Switch component installed and exported from @baseworks/ui.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| better-auth callback -> email queue | Internal, trusted -- callback runs server-side |
| Email template data | Template receives org name, inviter name, role -- no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | team-invite email template | mitigate | Template receives only public data (org name, inviter name, role). No passwords, tokens, or internal IDs in email body. Invite link uses better-auth's opaque invitation ID. |
| T-09-02 | Denial of Service | sendInvitationEmail callback | accept | Rate limiting handled by better-auth's invitationLimit config (default 100 per org). BullMQ queue provides backpressure. Low risk for starter kit. |
</threat_model>

<verification>
- `grep "sendInvitationEmail" packages/modules/auth/src/auth.ts` returns match
- `grep "invitationExpiresIn" packages/modules/auth/src/auth.ts` returns match
- `grep "@internal" packages/modules/auth/src/auth.ts` returns match (email suppression)
- `grep "team-invite" packages/modules/billing/src/jobs/send-email.ts` returns matches for both template and subject
- `test -f packages/modules/billing/src/templates/team-invite.tsx` passes
- `grep "invite" packages/i18n/src/index.ts` returns namespace registration
- Both invite.json locale files exist with correct structure
- Switch component file exists
</verification>

<success_criteria>
- Email sending infrastructure wired: auth.ts callback -> BullMQ queue -> send-email handler -> team-invite template
- Email suppression for link mode: @internal suffix detected and email skipped
- Translation files cover all UI strings from UI-SPEC copywriting contract
- Switch component available for invite dialog mode toggle
- No new npm packages needed (all pre-installed except Switch via shadcn CLI)
</success_criteria>

<output>
After completion, create `.planning/phases/09-team-invites/09-01-SUMMARY.md`
</output>
