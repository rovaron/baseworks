---
phase: 09-team-invites
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/modules/auth/src/__tests__/invitation.test.ts
autonomous: true
requirements:
  - INVT-01
  - INVT-02
  - INVT-03
  - INVT-04
  - INVT-05

must_haves:
  truths:
    - "invitation.test.ts exists with failing test stubs for all 5 INVT requirements"
    - "Test file follows existing auth test patterns (bun test compatible)"
    - "Running the tests produces failures (RED phase of TDD)"
  artifacts:
    - path: "packages/modules/auth/src/__tests__/invitation.test.ts"
      provides: "Failing test stubs for invitation lifecycle"
      contains: "describe"
  key_links:
    - from: "packages/modules/auth/src/__tests__/invitation.test.ts"
      to: "packages/modules/auth/src/commands/create-invitation.ts"
      via: "import of createInvitation command"
      pattern: "create-invitation"
---

<objective>
Create the test scaffold for invitation lifecycle with failing stubs covering all INVT requirements.

Purpose: Satisfy Nyquist compliance -- all subsequent plans reference `bun test packages/modules/auth/src/__tests__/invitation.test.ts` in their verify commands. This Wave 0 plan ensures the test file exists before execution begins.
Output: invitation.test.ts with describe blocks and failing test stubs for INVT-01 through INVT-05.
</objective>

<execution_context>
@C:/Projetos/baseworks/.claude/get-shit-done/workflows/execute-plan.md
@C:/Projetos/baseworks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-team-invites/09-CONTEXT.md
@.planning/phases/09-team-invites/09-RESEARCH.md

<interfaces>
<!-- Existing test patterns in the auth module -->

From packages/modules/auth/src/__tests__/ (existing test directory):
Look for existing test files to follow the same import pattern, describe/it structure,
and mock setup conventions. The tests use `bun test` (not Vitest) with `describe`, `it`,
`expect` from bun's built-in test runner.

From packages/modules/auth/src/commands/create-tenant.ts (command pattern):
```typescript
import { defineCommand, ok, err } from "@baseworks/shared";
// Commands return ok(data) or err(message)
```

From packages/modules/auth/src/queries/list-members.ts (query pattern):
```typescript
import { defineQuery, ok, err } from "@baseworks/shared";
// Queries return ok(data) or err(message)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create invitation.test.ts with failing stubs for INVT-01 through INVT-05</name>
  <files>
    packages/modules/auth/src/__tests__/invitation.test.ts
  </files>
  <read_first>
    packages/modules/auth/src/__tests__/,
    packages/modules/auth/src/commands/create-tenant.ts,
    packages/modules/auth/src/queries/list-members.ts
  </read_first>
  <action>
Create `packages/modules/auth/src/__tests__/invitation.test.ts` with failing test stubs.

Read any existing test files in `packages/modules/auth/src/__tests__/` first to match the exact import/mock patterns used in this project.

The test file must have describe blocks covering each INVT requirement. Each test stub should use `it.todo()` or a minimal failing assertion so that running the tests produces clear output about what needs to be implemented.

Structure:

```typescript
import { describe, it, expect, mock, beforeEach } from "bun:test";

// These imports will fail until Plans 01 and 02 create the files,
// which is expected -- the test file exists as a scaffold.
// Uncomment imports as commands/queries are created:
// import { createInvitation } from "../commands/create-invitation";
// import { acceptInvitation } from "../commands/accept-invitation";
// import { rejectInvitation } from "../commands/reject-invitation";
// import { cancelInvitation } from "../commands/cancel-invitation";
// import { listInvitations } from "../queries/list-invitations";
// import { getInvitation } from "../queries/get-invitation";

describe("Invitation Lifecycle", () => {

  describe("INVT-01: Create invitation with email + role", () => {
    it.todo("creates an invitation with admin role via auth.api.createInvitation");
    it.todo("creates an invitation with member role");
    it.todo("emits invitation.created event on success");
    it.todo("returns error when auth.api.createInvitation fails");
  });

  describe("INVT-02: Email sent via queue on invitation", () => {
    it.todo("sendInvitationEmail callback enqueues team-invite email via BullMQ");
    it.todo("email job includes inviteLink, organizationName, inviterName, role");
    it.todo("skips email enqueueing for @internal placeholder emails (link mode)");
  });

  describe("INVT-03: Shareable link generation (no email sent)", () => {
    it.todo("creates invitation with link-invite-{nanoid}@internal placeholder email");
    it.todo("sendInvitationEmail callback does NOT enqueue email for @internal addresses");
    it.todo("returns invitation ID for URL construction");
  });

  describe("INVT-04: Accept invite joins org", () => {
    it.todo("acceptInvitation calls auth.api.acceptInvitation with invitationId");
    it.todo("emits invitation.accepted event on success");
    it.todo("rejectInvitation calls auth.api.rejectInvitation with invitationId");
    it.todo("emits invitation.rejected event on success");
  });

  describe("INVT-05: List/cancel/resend invitations", () => {
    it.todo("listInvitations returns pending invitations for an org");
    it.todo("cancelInvitation calls auth.api.cancelInvitation");
    it.todo("emits invitation.cancelled event on success");
    it.todo("getInvitation returns invitation details by ID (public query)");
  });

});
```

All tests are `it.todo()` stubs -- they will show as "todo" (not failures that block execution) but clearly document what needs testing. Plans 01 and 02 will progressively implement these tests as they create the commands and queries.

**IMPORTANT:** Check if `packages/modules/auth/src/__tests__/` directory exists. If not, create it. Check existing test files for any shared mock setup (e.g., `auth-setup.test.ts` or similar) and import it if relevant.
  </action>
  <verify>
    <automated>cd C:/Projetos/baseworks && test -f packages/modules/auth/src/__tests__/invitation.test.ts && grep "INVT-01" packages/modules/auth/src/__tests__/invitation.test.ts && grep "INVT-02" packages/modules/auth/src/__tests__/invitation.test.ts && grep "INVT-03" packages/modules/auth/src/__tests__/invitation.test.ts && grep "INVT-04" packages/modules/auth/src/__tests__/invitation.test.ts && grep "INVT-05" packages/modules/auth/src/__tests__/invitation.test.ts && echo "ALL CHECKS PASS"</automated>
  </verify>
  <done>invitation.test.ts exists with todo stubs covering all 5 INVT requirements. Running `bun test packages/modules/auth/src/__tests__/invitation.test.ts` succeeds (todos are not failures) and reports the test scaffold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Test scaffold only -- no runtime boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | Test-only plan, no security surface |
</threat_model>

<verification>
- `test -f packages/modules/auth/src/__tests__/invitation.test.ts` passes
- `bun test packages/modules/auth/src/__tests__/invitation.test.ts` runs without hard errors
- File contains describe blocks for all 5 INVT requirements
</verification>

<success_criteria>
- invitation.test.ts exists and is runnable by bun test
- All 5 INVT requirements have corresponding describe blocks with todo stubs
- Subsequent plans (01, 02) can reference this file in their verify commands
</success_criteria>

<output>
After completion, create `.planning/phases/09-team-invites/09-00-SUMMARY.md`
</output>
