---
phase: 08-internationalization
plan: 03
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - apps/admin/src/routes/login.tsx
  - apps/admin/src/routes/users/list.tsx
  - apps/admin/src/routes/users/detail.tsx
  - apps/admin/src/routes/tenants/list.tsx
  - apps/admin/src/routes/tenants/detail.tsx
  - apps/admin/src/routes/billing/overview.tsx
  - apps/admin/src/routes/system/health.tsx
  - apps/admin/src/layouts/admin-layout.tsx
autonomous: true
requirements:
  - I18N-03
must_haves:
  truths:
    - "Admin dashboard (Vite) renders all user-facing strings from translation files via react-i18next, with no hardcoded English in JSX"
  artifacts:
    - path: "apps/admin/src/routes/login.tsx"
      provides: "translated admin login page"
      contains: "useTranslation"
    - path: "apps/admin/src/routes/users/list.tsx"
      provides: "translated users list page"
      contains: "useTranslation"
    - path: "apps/admin/src/layouts/admin-layout.tsx"
      provides: "translated admin layout"
      contains: "useTranslation"
  key_links:
    - from: "apps/admin/src/routes/login.tsx"
      to: "packages/i18n/src/locales/en/admin.json"
      via: "useTranslation('admin')"
---

<objective>
Replace all hardcoded English strings in the admin dashboard (apps/admin) with react-i18next translation calls.

Purpose: I18N-03 requires that the admin dashboard renders all user-facing strings from translation files with no hardcoded English in JSX.

Output: Every user-facing string in apps/admin uses `useTranslation()` from react-i18next, referencing keys in the en/pt-BR JSON files.
</objective>

<execution_context>
@C:/Projetos/baseworks/.claude/get-shit-done/workflows/execute-plan.md
@C:/Projetos/baseworks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-internationalization/08-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate admin login and layout</name>
  <files>
    apps/admin/src/routes/login.tsx
    apps/admin/src/layouts/admin-layout.tsx
  </files>
  <read_first>
    apps/admin/src/routes/login.tsx
    apps/admin/src/layouts/admin-layout.tsx
    packages/i18n/src/locales/en/admin.json
    packages/i18n/src/locales/en/common.json
  </read_first>
  <action>
**react-i18next pattern** (different from next-intl):

```typescript
import { useTranslation } from "react-i18next";

// Inside component:
const { t } = useTranslation("admin"); // namespace
const { t: tc } = useTranslation("common"); // for shared strings
```

**Admin login page (apps/admin/src/routes/login.tsx):**

1. Add `import { useTranslation } from "react-i18next";`
2. Move `loginSchema` inside the component (same Zod-in-component pattern as Plan 02)
3. Add `const { t } = useTranslation("admin");` and `const { t: tAuth } = useTranslation("auth");`
4. Replace strings:

| Hardcoded | Translation key |
|-----------|-----------------|
| `"Admin"` (heading) | `t("title")` |
| `"Sign in to Admin"` | `t("signInToAdmin")` |
| `"Email"` | `tAuth("email")` |
| `"admin@example.com"` | `t("adminEmailPlaceholder")` |
| `"Password"` | `tAuth("password")` |
| `"Sign in to Admin"` (button) | `t("signInToAdmin")` |
| `"Signing in..."` | `t("signingIn")` |
| Zod: email message | `tAuth("validation.emailRequired")` |
| Zod: password message | `tAuth("validation.passwordRequired")` |
| toast: `"Invalid email or password"` | `t("toast.invalidCredentials")` |
| toast: `"You do not have admin privileges..."` | `t("toast.noAdminPrivileges")` |
| toast: `"An unexpected error..."` | `t("toast.unexpectedError")` |

**Admin layout (apps/admin/src/layouts/admin-layout.tsx):**

1. Add `import { useTranslation } from "react-i18next";`
2. Add `const { t } = useTranslation("admin");` and `const { t: tc } = useTranslation("common");`
3. Replace strings:

| Hardcoded | Translation key |
|-----------|-----------------|
| `"Admin"` (sidebar header) | `t("title")` |
| `"Tenants"` | `t("nav.tenants")` |
| `"Users"` | `t("nav.users")` |
| `"Billing"` | `t("nav.billing")` |
| `"System"` | `t("nav.system")` |
| `"Sign out"` | `tc("signOut")` |
  </action>
  <verify>
    <automated>grep "useTranslation" apps/admin/src/routes/login.tsx apps/admin/src/layouts/admin-layout.tsx</automated>
    Expect: Both files contain useTranslation imports/calls.
  </verify>
  <acceptance_criteria>
    - Admin login page uses useTranslation for all strings
    - Admin layout uses useTranslation for nav items and sign out
    - Zero hardcoded English in these two files
    - `bun run build` succeeds
  </acceptance_criteria>
  <done>Admin login and layout render all strings from translation files</done>
</task>

<task type="auto">
  <name>Task 2: Translate users pages (list + detail)</name>
  <files>
    apps/admin/src/routes/users/list.tsx
    apps/admin/src/routes/users/detail.tsx
  </files>
  <read_first>
    apps/admin/src/routes/users/list.tsx
    apps/admin/src/routes/users/detail.tsx
    packages/i18n/src/locales/en/admin.json
  </read_first>
  <action>
**Users list page (apps/admin/src/routes/users/list.tsx):**

1. Add `import { useTranslation } from "react-i18next";`
2. Add `const { t } = useTranslation("admin");` and `const { t: tc } = useTranslation("common");`
3. Replace ALL strings using `admin.users.*` keys:

| Category | Hardcoded | Translation key |
|----------|-----------|-----------------|
| Heading | `"Users"` | `t("users.title")` |
| Search | `"Search users..."` | `t("users.searchPlaceholder")` |
| Column | `"Name"` | `t("users.columns.name")` |
| Column | `"Email"` | `t("users.columns.email")` |
| Column | `"Created"` | `t("users.columns.created")` |
| Column | `"Status"` | `t("users.columns.status")` |
| Badge | `"banned"` | `t("users.status.banned")` |
| Badge | `"active"` | `t("users.status.active")` |
| Menu | `"Open menu"` | `tc("openMenu")` |
| Menu | `"View details"` | `t("users.actions.viewDetails")` |
| Menu | `"Impersonate"` | `t("users.actions.impersonate")` |
| Menu | `"Ban user"` / `"Unban user"` | `t("users.actions.banUser")` / `t("users.actions.unbanUser")` |
| Empty | `"No accounts registered..."` | `t("users.empty")` |
| Dialog | ban/unban titles | `t("users.banDialog.banTitle")` / `t("users.banDialog.unbanTitle")` |
| Dialog | ban/unban descriptions | `t("users.banDialog.banDescription", { email })` / `t("users.banDialog.unbanDescription", { email })` |
| Dialog | keep/cancel buttons | `tc("cancel")` / `t("users.banDialog.keepActive")` |
| Dialog | confirm buttons | `t("users.actions.banUser")` / `t("users.actions.unbanUser")` |
| Dialog | `"Updating..."` | `t("users.banDialog.updating")` |
| Dialog | impersonate title/desc | `t("users.impersonateDialog.title")` / `t("users.impersonateDialog.description", { email })` |
| Dialog | `"Return to admin"` | `t("users.impersonateDialog.returnToAdmin")` |
| Dialog | `"Start impersonation"` | `t("users.impersonateDialog.startImpersonation")` |
| Dialog | `"Starting..."` | `t("users.impersonateDialog.starting")` |
| Toast | all toast messages | `t("users.toast.*")` |

**Users detail page (apps/admin/src/routes/users/detail.tsx):**

1. Add `import { useTranslation } from "react-i18next";`
2. Add `const { t } = useTranslation("admin");`
3. Replace strings using `admin.users.detail.*` and `admin.users.*` keys:

| Hardcoded | Translation key |
|-----------|-----------------|
| `"Back to users"` | `t("users.detail.backToUsers")` |
| `"Back"` | `tc("back")` |
| `"User not found."` | `t("users.detail.notFound")` |
| `"User Information"` | `t("users.detail.userInfo")` |
| `"Created"` | `t("users.detail.created")` |
| `"Ban reason"` | `t("users.detail.banReason")` |
| `"Organization Memberships"` | `t("users.detail.memberships")` |
| `"Actions"` | `t("users.detail.actions")` |
| `"Ban user"` / `"Unban user"` | `t("users.actions.banUser")` / `t("users.actions.unbanUser")` |
| `"Impersonate"` | `t("users.actions.impersonate")` |
| Dialog strings | Same as list page (reuse same keys) |
| Toast messages | Same as list page |
  </action>
  <verify>
    <automated>grep "useTranslation" apps/admin/src/routes/users/list.tsx apps/admin/src/routes/users/detail.tsx</automated>
    Expect: Both files contain useTranslation.
    Also: `grep -n '"Users"\|"Search users"\|"Ban user"\|"User not found"\|"Back to users"' apps/admin/src/routes/users/list.tsx apps/admin/src/routes/users/detail.tsx` returns zero.
  </verify>
  <acceptance_criteria>
    - Users list and detail pages use useTranslation for all strings
    - Zero hardcoded English in users pages
    - Dynamic values (email, names) use interpolation: `t("key", { email })`
    - `bun run build` succeeds
  </acceptance_criteria>
  <done>Admin users list and detail pages render all strings from translation files</done>
</task>

<task type="auto">
  <name>Task 3: Translate tenants pages (list + detail)</name>
  <files>
    apps/admin/src/routes/tenants/list.tsx
    apps/admin/src/routes/tenants/detail.tsx
  </files>
  <read_first>
    apps/admin/src/routes/tenants/list.tsx
    apps/admin/src/routes/tenants/detail.tsx
    packages/i18n/src/locales/en/admin.json
  </read_first>
  <action>
Same pattern as Task 2 but using `admin.tenants.*` namespace keys.

**Tenants list page (apps/admin/src/routes/tenants/list.tsx):**

1. Add `import { useTranslation } from "react-i18next";`
2. Add `const { t } = useTranslation("admin");` and `const { t: tc } = useTranslation("common");`
3. Replace ALL strings:

| Category | Hardcoded | Translation key |
|----------|-----------|-----------------|
| Heading | `"Tenants"` | `t("tenants.title")` |
| Search | `"Search tenants..."` | `t("tenants.searchPlaceholder")` |
| Column | `"Name"` | `t("tenants.columns.name")` |
| Column | `"Slug"` | `t("tenants.columns.slug")` |
| Column | `"Created"` | `t("tenants.columns.created")` |
| Column | `"Status"` | `t("tenants.columns.status")` |
| Badge | `"deactivated"` / `"active"` | `t("tenants.status.deactivated")` / `t("tenants.status.active")` |
| Menu | `"View details"` | `t("tenants.actions.viewDetails")` |
| Menu | `"Deactivate"` | `t("tenants.actions.deactivate")` |
| Empty | `"Your platform has no tenants..."` | `t("tenants.empty")` |
| Dialog | title/description/buttons | `t("tenants.deactivateDialog.*")` |
| Toast | all toast messages | `t("tenants.toast.*")` |

**Tenants detail page (apps/admin/src/routes/tenants/detail.tsx):**

| Hardcoded | Translation key |
|-----------|-----------------|
| `"Back to tenants"` | `t("tenants.detail.backToTenants")` |
| `"Back"` | `tc("back")` |
| `"Tenant not found."` | `t("tenants.detail.notFound")` |
| `"Tenant Information"` | `t("tenants.detail.tenantInfo")` |
| `"Name"` | `t("tenants.detail.name")` |
| `"Slug"` | `t("tenants.detail.slug")` |
| `"Created"` | `t("tenants.detail.created")` |
| `"Members"` | `t("tenants.detail.members")` |
| `"Actions"` | `t("tenants.detail.actions")` |
| `"Reactivate tenant"` / `"Deactivate tenant"` | `t("tenants.detail.reactivate")` / `t("tenants.detail.deactivate")` |
| Dialog strings | `t("tenants.detail.reactivateDialog.*")` / `t("tenants.detail.deactivateDialog.*")` |
| Toast messages | `t("tenants.toast.*")` |
  </action>
  <verify>
    <automated>grep "useTranslation" apps/admin/src/routes/tenants/list.tsx apps/admin/src/routes/tenants/detail.tsx</automated>
    Expect: Both files contain useTranslation.
  </verify>
  <acceptance_criteria>
    - Tenants list and detail pages use useTranslation for all strings
    - Zero hardcoded English in tenants pages
    - Dynamic values (name) use interpolation
    - `bun run build` succeeds
  </acceptance_criteria>
  <done>Admin tenants list and detail pages render all strings from translation files</done>
</task>

<task type="auto">
  <name>Task 4: Translate billing overview and system health pages</name>
  <files>
    apps/admin/src/routes/billing/overview.tsx
    apps/admin/src/routes/system/health.tsx
  </files>
  <read_first>
    apps/admin/src/routes/billing/overview.tsx
    apps/admin/src/routes/system/health.tsx
    packages/i18n/src/locales/en/admin.json
  </read_first>
  <action>
**Billing overview (apps/admin/src/routes/billing/overview.tsx):**

1. Add `import { useTranslation } from "react-i18next";`
2. Add `const { t } = useTranslation("admin");` and `const { t: tc } = useTranslation("common");`
3. Replace strings using `admin.billingOverview.*` keys:

| Category | Hardcoded | Translation key |
|----------|-----------|-----------------|
| Heading | `"Billing Overview"` | `t("billingOverview.title")` |
| Error | `"Failed to load data..."` | `t("billingOverview.loadError")` |
| Button | `"Retry"` | `tc("retry")` |
| Loading | `"Loading..."` | `tc("loading")` |
| Card | `"Total Subscribers"` | `t("billingOverview.totalSubscribers")` |
| Card | `"Monthly Recurring Revenue"` | `t("billingOverview.mrr")` |
| Card | `"Active Subscriptions"` | `t("billingOverview.activeSubscriptions")` |
| Card | `"Subscription Distribution"` | `t("billingOverview.subscriptionDistribution")` |
| Empty | `"No billing activity..."` | `t("billingOverview.emptyBilling")` |
| Empty | `"No subscription plans..."` | `t("billingOverview.noPlans")` |
| Badge | `"{count} subscribers"` | `t("billingOverview.subscribers", { count })` |
| Card | `"Recent Subscriptions"` | `t("billingOverview.recentSubscriptions")` |
| Text | `"Invoice"` | `t("billingOverview.invoice")` |
| Column | Tenant/Plan/Status/Date | `t("billingOverview.columns.*")` |

**System health (apps/admin/src/routes/system/health.tsx):**

1. Add `import { useTranslation } from "react-i18next";`
2. Add `const { t } = useTranslation("admin");` and `const { t: tc } = useTranslation("common");`
3. Replace ALL strings using `admin.systemHealth.*` keys:

| Category | Hardcoded | Translation key |
|----------|-----------|-----------------|
| Heading | `"System Health"` | `t("systemHealth.title")` |
| Error | `"Failed to load data..."` | `t("systemHealth.loadError")` |
| Button | `"Retry"` | `tc("retry")` |
| Loading | `"Loading..."` | `tc("loading")` |
| Badge | `"Healthy"` / `"Degraded"` / `"Unhealthy"` | `t("systemHealth.status.*")` |
| Message | status descriptions | `t("systemHealth.statusMessages.*")` |
| Section | `"Queues"` | `t("systemHealth.queues")` |
| Label | Waiting/Active/Completed/Failed | `t("systemHealth.queueMetrics.*")` |
| Card | `"Redis"` | `t("systemHealth.redis.title")` |
| Label | Memory/Clients/Uptime | `t("systemHealth.redis.*")` |
| Badge | `"connected"` / `"disconnected"` | `t("systemHealth.redis.connected")` / `t("systemHealth.redis.disconnected")` |
| Text | `"No Redis data..."` | `t("systemHealth.redis.noData")` |
| Card | `"API Server"` | `t("systemHealth.apiServer.title")` |
| Label | `"Loaded modules"` | `t("systemHealth.apiServer.loadedModules")` |
  </action>
  <verify>
    <automated>grep "useTranslation" apps/admin/src/routes/billing/overview.tsx apps/admin/src/routes/system/health.tsx</automated>
    Expect: Both files contain useTranslation.
    Also: `cd apps/admin && bun run build 2>&1 | tail -5` succeeds.
  </verify>
  <acceptance_criteria>
    - Billing overview and system health pages use useTranslation for all strings
    - Zero hardcoded English in these files
    - Dynamic values (count, name) use interpolation
    - `bun run build` succeeds
  </acceptance_criteria>
  <done>Admin billing overview and system health pages render all strings from translation files</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. Same as Plan 02.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | T (Tampering) | Translation interpolation | accept | react-i18next escapeValue is false (safe with React). No dangerouslySetInnerHTML used. |
</threat_model>

<verification>
After all tasks complete:
1. `grep -rn "useTranslation" apps/admin/src/ --include="*.tsx" | wc -l` shows all route/layout files
2. Search for remaining hardcoded strings: `grep -rn '"Users"\|"Tenants"\|"Admin"\|"Sign in"\|"System Health"\|"Billing Overview"' apps/admin/src/routes/ apps/admin/src/layouts/ --include="*.tsx"` returns zero
3. `cd apps/admin && bun run build` succeeds
</verification>

<success_criteria>
- All user-facing strings in apps/admin use react-i18next translation calls (I18N-03)
- No hardcoded English strings in JSX across all 7 route files and 1 layout file
- Zod validation messages and toast messages are translated
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/08-internationalization/08-03-SUMMARY.md`
</output>
