---
status: passed
phase: 20.1-close-v13-milestone-gaps
source: [20.1-VERIFICATION.md]
started: 2026-04-26T22:14:00Z
updated: 2026-04-27T01:02:00Z
---

## Current Test

[all tests passed]

## Tests

### 1. Live API+worker traceId continuity (SC#3 production gate)
expected: Run `bun run api` and `bun run worker` in separate shells. Send an authenticated request that enqueues a BullMQ job. Grep the API log line and the worker log line for that request and confirm both carry the same `traceId` (a single 32-hex string).
result: passed

evidence:
- Booted `bun run apps/api/src/index.ts` (PID 983, port 3000) and `bun run apps/api/src/worker.ts` (PID 984) against `docker compose` postgres + redis on 2026-04-27.
- `/health` reported `database.status=up, redis.status=up`.
- Signed up `uat-1777251693@example.com` via `POST /api/auth/sign-up/email` → 200 + session cookie.
- `POST /examples` with the cookie → 200 + body `{success:true, data:{id:163720de-4f1c-4124-919a-0f97b2970261, tenantId:gUrrxTzeQCRyi6I9PhVjHMu1Nc50Ra1b, ...}}`. The hook fired and enqueued `example-process-followup` on Redis.
- Grepping `/tmp/baseworks-uat/api.log` for `requestId: "8dc01faa-c5a8-4d7a-b07d-7761ec5165db"` → API "request completed" line carries `traceId: "3d6390dcd78b42a2924d69e11c656da8"`, `path: "/examples"`, `status: 200`.
- Grepping `/tmp/baseworks-uat/worker.log` for the same `requestId` → Worker "Job started" + "Job completed" lines carry `traceId: "3d6390dcd78b42a2924d69e11c656da8"` — **identical 32-hex string** to the API line.
- `tenantId` and `userId` also match across both sides (`gUrrxTzeQCRyi6I9PhVjHMu1Nc50Ra1b` / `bptyLY0NkCblrOvXAGrjibZ5M4S289fY`); `spanId` correctly differs (separate spans within the same trace).

corroborated by: `bun test apps/api/__tests__/observability-bullmq-trace.test.ts` (D-14 lock-in regression gate added by Plan 20.1-03) — 20/20 expect() calls passed at HEAD, asserting the same producer-consumer traceId equality at the test-runner level.

## Summary

total: 1
passed: 1
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps
