---
phase: 15
plan: 03
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - docs/getting-started.md
  - docs/architecture.md
autonomous: true
requirements: [DOCS-01, DOCS-02]
must_haves:
  truths:
    - "A new developer can follow docs/getting-started.md end to end to install prerequisites, set env vars, start Postgres + Redis via docker compose, run migrations, start the API, start the worker, and run the backend test suite"
    - "docs/architecture.md contains exactly four Mermaid diagrams (module system, CQRS flow, request lifecycle, tenant scoping), each using concrete code-identifier labels (ModuleRegistry, CqrsBus, TypedEventBus, scopedDb, defineCommand, defineQuery, HandlerContext, tenantMiddleware) readers can grep"
    - "docs/architecture.md does NOT re-explain integrations in depth (no Stripe webhook flow, no better-auth plugin details) — those live in Plan 05 integration docs"
    - "docs/getting-started.md uses only `bun` commands (no npm/yarn/pnpm) and cites exact root-package.json script names"
    - "Both docs link back to docs/README.md tone/citation/Mermaid contracts (per Plan 01)"
  artifacts:
    - path: "docs/getting-started.md"
      provides: "DOCS-01 — Prerequisites, install, env setup, dev server, worker, tests"
      min_lines: 80
      contains: "bun install"
    - path: "docs/architecture.md"
      provides: "DOCS-02 — Module system, CQRS flow, request lifecycle, tenant scoping with 4 Mermaid diagrams"
      min_lines: 120
      contains: "```mermaid"
  key_links:
    - from: "docs/getting-started.md"
      to: "docs/architecture.md"
      via: "next-step link in a closing section"
      pattern: "architecture\\.md"
    - from: "docs/architecture.md"
      to: "packages/db/src/helpers/scoped-db.ts"
      via: "code citation in the tenant scoping diagram explanation"
      pattern: "scoped-db\\.ts"
    - from: "docs/architecture.md"
      to: "apps/api/src/core/registry.ts"
      via: "code citation in module system diagram"
      pattern: "registry\\.ts"
---

<objective>
Author the onboarding and architecture foundation of Phase 15 documentation. DOCS-01 (`docs/getting-started.md`) walks a new developer from clone to running API + worker + tests. DOCS-02 (`docs/architecture.md`) pins the four mandated Mermaid diagrams (module system, CQRS flow, request lifecycle, tenant scoping) with named code anchors per D-02, and provides supporting prose without drifting into integration-specific content (per Pitfall 6 in RESEARCH.md).

Purpose: These two documents are the prerequisites cited by the guides in Plan 04 and the integration docs in Plan 05. D-06 makes `docs/architecture.md` an explicit prerequisite of `docs/add-a-module.md`.

Output: Two markdown files under `docs/` implementing DOCS-01 and DOCS-02, both conforming to the contracts locked in `docs/README.md` (Plan 01).
</objective>

<execution_context>
@C:/Projetos/baseworks/.claude/get-shit-done/workflows/execute-plan.md
@C:/Projetos/baseworks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-developer-documentation/15-CONTEXT.md
@.planning/phases/15-developer-documentation/15-RESEARCH.md
@.planning/phases/15-developer-documentation/15-PATTERNS.md
@.planning/phases/15-developer-documentation/15-VALIDATION.md
@docs/README.md
@docs/jsdoc-style-guide.md
@package.json
@docker-compose.yml
@packages/config/src/env.ts
@apps/api/src/index.ts
@apps/api/src/worker.ts
@apps/api/src/core/registry.ts
@apps/api/src/core/cqrs.ts
@apps/api/src/core/event-bus.ts
@packages/db/src/helpers/scoped-db.ts
@packages/shared/src/types/cqrs.ts
@packages/shared/src/types/module.ts
</context>

<interfaces>
<!-- Concrete anchors the docs must cite. Pulled from the codebase this session. -->

### DOCS-01 anchors

Root `package.json:5-22` scripts (verbatim names the guide MUST use):

- `bun api` — Watch-mode API server on port 3000 (from `apps/api/src/index.ts`)
- `bun worker` — BullMQ worker entrypoint (`apps/api/src/worker.ts`)
- `bun db:generate` — Generate Drizzle SQL migrations
- `bun db:migrate` — Apply pending migrations to `DATABASE_URL`
- `bun db:push` — Dev-only: push schema straight to DB (no migration file)
- `bun dev:web` — Next.js customer app (`apps/web`)
- `bun dev:admin` — Vite admin SPA (`apps/admin`)
- `bun docker:up` / `bun docker:down` — Start/stop Postgres 16 + Redis 7 via `docker-compose.yml`
- `bun typecheck` — `tsc --noEmit`
- `bun lint` / `bun lint:fix` — Biome
- `bun test` — Backend unit tests (Bun's built-in test runner)

Minimum env vars to start locally (from `packages/config/src/env.ts:10-32`):

- `DATABASE_URL` (required, URL) — Postgres connection string
- `BETTER_AUTH_SECRET` (required, min 32 chars)
- `BETTER_AUTH_URL` (default `http://localhost:3000`)
- `REDIS_URL` (required when `INSTANCE_ROLE` is `worker` or `all`) — default role is `all`
- `WEB_URL` (default `http://localhost:3000`)
- `ADMIN_URL` (default `http://localhost:5173`)
- `PAYMENT_PROVIDER` (default `stripe`); requires `STRIPE_SECRET_KEY` unless `NODE_ENV=test`
- `RESEND_API_KEY` (optional; email dispatcher gracefully skips when absent)

`docker-compose.yml` launches `postgres:16` + `redis:7` — confirm versions by running `docker compose ps` after `bun docker:up`.

### DOCS-02 anchors (for the 4 diagrams)

| Diagram | Anchors (concrete identifiers — no abstract labels) |
|---------|-----------------------------------------------------|
| **Module system** | `ModuleRegistry` (`apps/api/src/core/registry.ts`), `ModuleDefinition` (`packages/shared/src/types/module.ts`), `moduleImportMap` (`apps/api/src/core/registry.ts:10-16`), `registry.loadAll` (`apps/api/src/index.ts:31`) |
| **CQRS flow** | `CqrsBus.execute` / `CqrsBus.query` (`apps/api/src/core/cqrs.ts`), `defineCommand` / `defineQuery` (`packages/shared/src/types/cqrs.ts`), `HandlerContext` (same file, line 26-31), `TypedEventBus.emit` (`apps/api/src/core/event-bus.ts`) |
| **Request lifecycle** | `errorMiddleware`, `requestTraceMiddleware`, `localeMiddleware`, `cors`, auth routes, `tenantMiddleware` (`apps/api/src/core/middleware/tenant.ts`), `derive handlerCtx` (`apps/api/src/index.ts`), module routes |
| **Tenant scoping** | `scopedDb` (`packages/db/src/helpers/scoped-db.ts`), `tenantMiddleware` session → `activeOrganizationId` → `tenantId`, `injectTenantId` inside `scopedDb.insert` |

### Tone contract

From `docs/jsdoc-style-guide.md:12-23`:

```markdown
- **Technical-precise tone:** Describe what the code does, not what the developer should feel.
- **One declarative sentence opener:** Use present tense, active voice.
- Forbidden filler: "basically", "simply", "just".
```

DOCS-01 has discretion for a slightly warmer onboarding voice (CONTEXT.md). DOCS-02 MUST follow the strict reference-doc tone.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/getting-started.md (DOCS-01)</name>
  <files>docs/getting-started.md</files>
  <read_first>
    - docs/README.md (tone, citation, Mermaid contracts locked in Plan 01)
    - docs/jsdoc-style-guide.md:12-23 (tone rules — DOCS-01 has warmth discretion within these bounds)
    - package.json (exact script names used in commands)
    - packages/config/src/env.ts:10-32 (required env vars and defaults)
    - docker-compose.yml (Postgres 16 + Redis 7 versions)
    - .planning/phases/15-developer-documentation/15-RESEARCH.md §"Common Pitfalls" — especially Pitfall 7 (Windows/POSIX shell note)
    - .planning/phases/15-developer-documentation/15-PATTERNS.md §"docs/getting-started.md" (onboarding layout guidance)
  </read_first>
  <action>
Create `docs/getting-started.md` following this exact structure. Target length ~90-130 lines. Tone: technical-precise, declarative, present tense; slight warmth (e.g., addressing the reader as "you" is allowed, but no exclamations, no emojis, no filler words).

Required sections (headings verbatim):

1. `# Getting Started` (H1)
2. One-paragraph lead: one sentence naming what Baseworks is (reference `.planning/PROJECT.md` only for your own context — do NOT link it from a doc), one sentence naming what this guide covers (clone → env → run API/worker/tests), one sentence stating the prerequisites are listed below.
3. `---` separator
4. `## Prerequisites` (H2). A 3-column markdown table: `Tool | Version | Purpose`. Rows:
   - `[Bun](https://bun.sh)` | `1.1+` | `Runtime, package manager, test runner. Native TypeScript; no separate build step.`
   - `[Docker](https://docs.docker.com/get-docker/)` | `latest` | `Runs local Postgres 16 and Redis 7 via \`docker-compose.yml\`.`
   - `Git` | `any` | `Clone the repository.`

   Add a short paragraph after the table: commands below assume a POSIX-compatible shell (bash, zsh, Git Bash on Windows, or WSL2). Root `bun` scripts are cross-platform; direct shell pipelines assume POSIX.

5. `## 1. Clone and install` (H2). Fenced bash block:

   ```bash
   git clone <your-repo-url>
   cd baseworks
   bun install
   ```

   One-paragraph note: `bun install` resolves all workspaces (`apps/*`, `packages/*`, `packages/modules/*`) in one pass. The resulting `bun.lockb` is committed.

6. `## 2. Configure environment variables` (H2). State that env vars are loaded by Bun natively plus validated at startup by `packages/config/src/env.ts` (cite the file, do not re-document the Zod schema). Provide a minimal `.env` template to copy into `.env` at the repo root:

   ```bash
   # Required
   DATABASE_URL=postgres://postgres:postgres@localhost:5432/baseworks
   BETTER_AUTH_SECRET=replace-with-a-random-string-at-least-32-chars
   REDIS_URL=redis://localhost:6379

   # Optional — populate as you enable features
   STRIPE_SECRET_KEY=sk_test_replace_me
   STRIPE_WEBHOOK_SECRET=whsec_replace_me
   RESEND_API_KEY=re_replace_me
   ```

   Add one paragraph stating the full list and defaults live in `packages/config/src/env.ts` (cite the file; no duplication). All secret values above are placeholders. Include `sk_test_` / `whsec_` / `re_` prefixes so readers know the shape, but no real keys.

7. `## 3. Start Postgres and Redis` (H2). Fenced bash block:

   ```bash
   bun docker:up
   ```

   One-paragraph note: this invokes `docker compose up -d` and starts a `postgres:16` container and a `redis:7` container as declared in `docker-compose.yml`. Verify with `docker compose ps`.

8. `## 4. Apply database migrations` (H2). Fenced bash block:

   ```bash
   bun db:migrate
   ```

   One-paragraph note: `bun db:migrate` runs the Drizzle migrator against `DATABASE_URL`. For a clean dev DB you may use `bun db:push` instead to push the current schema without creating a migration file.

9. `## 5. Run the API server` (H2). Fenced bash block:

   ```bash
   bun api
   ```

   One-paragraph note: starts the Elysia API from `apps/api/src/index.ts` in watch mode on port 3000 (configurable via `PORT`). Swagger UI is mounted at `/swagger` when the swagger plugin is enabled.

10. `## 6. Run the BullMQ worker` (H2). Fenced bash block:

    ```bash
    bun worker
    ```

    One-paragraph note: starts `apps/api/src/worker.ts`. Requires `REDIS_URL` (asserted at startup by `assertRedisUrl` in `packages/config/src/env.ts`). Exposes a health endpoint on `WORKER_HEALTH_PORT` (default 3001).

11. `## 7. Run the tests` (H2). Fenced bash block:

    ```bash
    bun test
    ```

    One-paragraph note: Bun's built-in test runner auto-discovers `*.test.ts` files across the monorepo. React component tests via Vitest are planned but not yet wired — all current tests run under `bun test`. Cite `docs/testing.md` for the full testing guide (Plan 04 will create this file).

12. `## 8. Run the frontends (optional)` (H2). Two fenced bash blocks:

    ```bash
    bun dev:web
    ```

    ```bash
    bun dev:admin
    ```

    One-paragraph note: `dev:web` starts the Next.js customer app on port 3000 (conflicts with `bun api`; start them on different `PORT` values when running both). `dev:admin` starts the Vite admin SPA on port 5173.

13. `## Next steps` (H2). A short bulleted list linking to the next docs:
    - [Architecture overview](./architecture.md) — understand the module system, CQRS flow, request lifecycle, and tenant scoping.
    - [Add a module](./add-a-module.md) — follow an annotated walkthrough of `packages/modules/example`.
    - [Configuration](./configuration.md) — full env var reference and module loading.
    - [Testing](./testing.md) — mock patterns and test runner scope.
    - [Integrations](./integrations/) — better-auth, billing, BullMQ, email.

14. `## Troubleshooting` (H2). Three subsections (`###`):

    `### Port 3000 already in use` — set `PORT=3001 bun api`.

    `### REDIS_URL missing when starting worker` — export `REDIS_URL=redis://localhost:6379` or ensure `bun docker:up` completed successfully.

    `### STRIPE_SECRET_KEY not set warning in tests` — expected in `NODE_ENV=test`; `validatePaymentProviderEnv` in `packages/config/src/env.ts:49-84` logs a warning instead of throwing in test mode.

Formatting constraints:
- Single `#` H1 at top; `##` for sections; `###` for troubleshooting subsections.
- All commands use `bun` — no `npm`, `yarn`, `pnpm`.
- Every env placeholder value follows the shape `sk_test_xxx`, `whsec_xxx`, `re_xxx` — no real keys.
- No filler words (`basically`, `simply`, `just`).
- No emojis.
- Relative links use `./` prefix.
  </action>
  <verify>
    <automated>cd "C:/Projetos/baseworks" &amp;&amp; test -f docs/getting-started.md &amp;&amp; grep -q "^# Getting Started$" docs/getting-started.md &amp;&amp; grep -q "^## Prerequisites$" docs/getting-started.md &amp;&amp; grep -q "^## 1\. Clone and install$" docs/getting-started.md &amp;&amp; grep -q "^## 2\. Configure environment variables$" docs/getting-started.md &amp;&amp; grep -q "^## 3\. Start Postgres and Redis$" docs/getting-started.md &amp;&amp; grep -q "^## 4\. Apply database migrations$" docs/getting-started.md &amp;&amp; grep -q "^## 5\. Run the API server$" docs/getting-started.md &amp;&amp; grep -q "^## 6\. Run the BullMQ worker$" docs/getting-started.md &amp;&amp; grep -q "^## 7\. Run the tests$" docs/getting-started.md &amp;&amp; grep -q "^## Next steps$" docs/getting-started.md &amp;&amp; grep -q "^## Troubleshooting$" docs/getting-started.md &amp;&amp; grep -q "bun install" docs/getting-started.md &amp;&amp; grep -q "bun docker:up" docs/getting-started.md &amp;&amp; grep -q "bun db:migrate" docs/getting-started.md &amp;&amp; grep -q "bun api" docs/getting-started.md &amp;&amp; grep -q "bun worker" docs/getting-started.md &amp;&amp; grep -q "bun test" docs/getting-started.md &amp;&amp; grep -q "architecture\.md" docs/getting-started.md &amp;&amp; ! grep -nE "\bnpm install\b|\byarn \b|\bpnpm \b" docs/getting-started.md &amp;&amp; ! grep -nE "\b(basically|simply|just)\b" docs/getting-started.md &amp;&amp; ! grep -nE "sk_live_|sk_test_[a-zA-Z0-9]{20,}|re_[a-zA-Z0-9]{15,}|whsec_[a-zA-Z0-9]{20,}" docs/getting-started.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/getting-started.md` exists
    - All 12 required `##` headings present (`Prerequisites`, `1. Clone and install`, `2. Configure environment variables`, `3. Start Postgres and Redis`, `4. Apply database migrations`, `5. Run the API server`, `6. Run the BullMQ worker`, `7. Run the tests`, `8. Run the frontends (optional)`, `Next steps`, `Troubleshooting`)
    - Uses `bun` commands exclusively: `bun install`, `bun docker:up`, `bun db:migrate`, `bun api`, `bun worker`, `bun test` (all grep-matched)
    - Links to `./architecture.md`, `./add-a-module.md`, `./configuration.md`, `./testing.md`, `./integrations/` in Next steps section
    - Cites `packages/config/src/env.ts` for the env var source of truth
    - `grep -nE "\bnpm install\b|\byarn \b|\bpnpm \b" docs/getting-started.md` returns zero
    - `grep -nE "\b(basically|simply|just)\b" docs/getting-started.md` returns zero
    - `grep -nE "sk_live_|sk_test_[a-zA-Z0-9]{20,}|re_[a-zA-Z0-9]{15,}|whsec_[a-zA-Z0-9]{20,}" docs/getting-started.md` returns zero (no real-shaped secrets; only short placeholders like `sk_test_replace_me` which has fewer than 20 chars after the prefix)
  </acceptance_criteria>
  <done>`docs/getting-started.md` exists with all required sections, uses only `bun` commands, contains no filler words, contains no real-shaped secret strings, and links onward to the architecture and other guides.</done>
</task>

<task type="auto">
  <name>Task 2: Author docs/architecture.md (DOCS-02) with 4 Mermaid diagrams</name>
  <files>docs/architecture.md</files>
  <read_first>
    - docs/README.md (Mermaid syntax + tone contracts)
    - apps/api/src/core/registry.ts (ModuleRegistry + moduleImportMap for the module-system diagram)
    - apps/api/src/core/cqrs.ts (CqrsBus.execute / query for the CQRS diagram)
    - apps/api/src/core/event-bus.ts (TypedEventBus for the CQRS diagram)
    - apps/api/src/index.ts:1-131 (request lifecycle middleware chain for the request-lifecycle diagram)
    - apps/api/src/core/middleware/tenant.ts (tenantMiddleware for tenant-scoping diagram)
    - packages/db/src/helpers/scoped-db.ts (scopedDb for tenant-scoping diagram)
    - packages/shared/src/types/cqrs.ts:26-31 (HandlerContext shape — tenantId/userId/db/emit/enqueue)
    - packages/shared/src/types/module.ts (ModuleDefinition — for module-system diagram)
    - .planning/phases/15-developer-documentation/15-RESEARCH.md §"Pattern 1" and §"Architectural Responsibility Map"
    - .planning/phases/15-developer-documentation/15-PATTERNS.md §"docs/architecture.md"
  </read_first>
  <action>
Create `docs/architecture.md` following this exact structure. Target length ~180-260 lines including diagrams. Tone: strict reference-doc (no warmth discretion here, per D-11). Present tense, declarative, no filler.

Required sections (headings verbatim):

1. `# Architecture Overview` (H1)
2. One-paragraph lead: single sentence stating this document describes the backend architecture of Baseworks — module system, CQRS flow, request lifecycle, tenant scoping. Second sentence: each subsystem is illustrated with one Mermaid diagram using concrete code identifiers so readers can grep for the names in the source tree.
3. `---`
4. `## Audience and prerequisites` (H2). One paragraph: this document assumes familiarity with Elysia, Drizzle, and BullMQ. Every example cites the source file it describes. The full stack overview lives in the project root `CLAUDE.md` §"Technology Stack".
5. `## 1. Module system` (H2).

   Lead paragraph: Every subsystem beyond core infrastructure is a **module**. A module is a `ModuleDefinition` (see `packages/shared/src/types/module.ts`) exporting routes, CQRS commands, CQRS queries, BullMQ jobs, and a list of emitted event names. The `ModuleRegistry` (`apps/api/src/core/registry.ts`) loads each module declared in the `modules: []` array in `apps/api/src/index.ts:27` and `apps/api/src/worker.ts:23`.

   Include ONE Mermaid diagram (flowchart LR). Every box label uses an actual file or class name.

   ```mermaid
   flowchart LR
     Cfg[modules array<br/>apps/api/src/index.ts:27]
     Reg[ModuleRegistry<br/>apps/api/src/core/registry.ts]
     Map[moduleImportMap<br/>registry.ts:10-16]
     Auth[auth module<br/>packages/modules/auth]
     Bill[billing module<br/>packages/modules/billing]
     Ex[example module<br/>packages/modules/example]
     MD[ModuleDefinition<br/>packages/shared/src/types/module.ts]

     Cfg --> Reg
     Reg --> Map
     Map --> Auth
     Map --> Bill
     Map --> Ex
     Auth --> MD
     Bill --> MD
     Ex --> MD
   ```

   After the diagram: three short subsections:
   - Subsection "What a module contributes" — bulleted list of the five ModuleDefinition surfaces (routes, commands, queries, jobs, events) citing `packages/shared/src/types/module.ts`.
   - Subsection "How modules are registered" — cite `apps/api/src/core/registry.ts:10-16` for the static import map and `apps/api/src/index.ts:25-28` for the active modules list.
   - Subsection "Adding a module" — one sentence pointing to `add-a-module.md`.

6. `## 2. CQRS flow` (H2).

   Lead paragraph: Incoming requests dispatch to command or query handlers through a typed bus (`CqrsBus` in `apps/api/src/core/cqrs.ts`). Commands mutate state and may emit domain events through `TypedEventBus` (`apps/api/src/core/event-bus.ts`). Queries return `Result<T>` without side effects.

   Include ONE Mermaid diagram (sequenceDiagram). Every participant uses a code identifier.

   ```mermaid
   sequenceDiagram
     participant Route as Elysia route handler
     participant Bus as CqrsBus
     participant Cmd as defineCommand handler
     participant DB as scopedDb
     participant EB as TypedEventBus
     participant Q as BullMQ queue

     Route->>Bus: bus.execute("example:create", input, ctx)
     Bus->>Cmd: handler(input, ctx)
     Cmd->>DB: ctx.db.insert(table).values(...)
     DB-->>Cmd: Result row
     Cmd->>EB: ctx.emit("example.created", payload)
     Cmd->>Q: ctx.enqueue("example:process-followup", payload)
     Cmd-->>Bus: ok(data)
     Bus-->>Route: Result<T>
   ```

   After the diagram: two short subsections:
   - Subsection "HandlerContext" — cite `packages/shared/src/types/cqrs.ts:26-31`. Inline snippet (≤ 10 lines) of the HandlerContext interface.
   - Subsection "Queries vs commands" — one paragraph: `defineCommand` / `defineQuery` both return `Promise<Result<T>>`; commands may emit events and enqueue jobs, queries have no side effects. Cite `packages/shared/src/types/cqrs.ts`.

7. `## 3. Request lifecycle` (H2).

   Lead paragraph: Every HTTP request passes through a fixed middleware chain declared in `apps/api/src/index.ts`. The chain captures error handling, request tracing, locale, CORS, auth, tenant resolution, and finally the handler context that module routes consume.

   Include ONE Mermaid diagram (flowchart TD). Labels match exact middleware names.

   ```mermaid
   flowchart TD
     Req[HTTP Request] --> EM[errorMiddleware<br/>apps/api/src/core/middleware/error.ts]
     EM --> RT[requestTraceMiddleware<br/>request-trace.ts]
     RT --> LM[localeMiddleware<br/>packages/modules/auth/src/locale-context.ts]
     LM --> CR[cors<br/>@elysiajs/cors]
     CR --> AR[auth routes<br/>better-auth .mount]
     AR --> TM[tenantMiddleware<br/>apps/api/src/core/middleware/tenant.ts]
     TM --> DV[derive handlerCtx<br/>tenantId, userId, db, emit, enqueue]
     DV --> MR[module routes]
     MR --> Bus[CqrsBus.execute / CqrsBus.query]
   ```

   After the diagram: one paragraph per middleware citing its source file with one-line purpose (no deep explanation — D-08 principle: the code IS the reference). Order matches the diagram.

8. `## 4. Tenant scoping` (H2).

   Lead paragraph: Tenant isolation is enforced by `scopedDb` (`packages/db/src/helpers/scoped-db.ts`). The wrapper auto-injects `tenantId` into INSERTs and auto-applies `WHERE tenant_id = ?` to SELECTs, UPDATEs, and DELETEs. Row-level security at the PostgreSQL layer is available if ever required but is NOT currently used (per CLAUDE.md).

   Include ONE Mermaid diagram (flowchart LR).

   ```mermaid
   flowchart LR
     Sess[better-auth session<br/>activeOrganizationId]
     TM[tenantMiddleware<br/>apps/api/src/core/middleware/tenant.ts]
     Ctx[HandlerContext<br/>tenantId, userId, db]
     SD[scopedDb<br/>packages/db/src/helpers/scoped-db.ts]
     Tbl[Drizzle table<br/>e.g. examples]

     Sess --> TM
     TM --> Ctx
     Ctx --> SD
     SD --> Tbl
     SD -. "auto-inject tenantId on insert" .- Tbl
     SD -. "auto-apply WHERE tenant_id" .- Tbl
   ```

   After the diagram: two short subsections:
   - Subsection "Why scopedDb over RLS" — one paragraph: application-level enforcement is easier to test, debug, and mock than PostgreSQL RLS. Cite `packages/db/src/helpers/scoped-db.ts` for the implementation.
   - Subsection "Bypass (admin routes)" — one paragraph: `packages/db/src/helpers/unscoped-db.ts` exposes the raw Drizzle client for admin routes that must cross tenant boundaries. Never import it from a module command or query.

9. `## Next steps` (H2). Bulleted list:
    - [Add a module](./add-a-module.md) — hands-on walkthrough of the module system using `packages/modules/example`.
    - [Configuration](./configuration.md) — module loading and provider selection.
    - [Testing](./testing.md) — how the CQRS bus and `HandlerContext` are mocked in unit tests.
    - Integration docs under [integrations/](./integrations/) for better-auth, billing, BullMQ, and email.

Formatting constraints:
- Exactly 4 Mermaid fenced blocks. All use `flowchart` or `sequenceDiagram` (never `graph`, never `classDiagram`).
- Every diagram box label contains at least one code identifier that exists in the repo (e.g., `ModuleRegistry`, `CqrsBus`, `scopedDb`).
- All code citations use the format `path:line` or `path::functionName` (D-10).
- Every inline code snippet begins with a comment line naming its source file.
- No integration-specific content (no Stripe webhook flow, no better-auth plugin details) — those live in Plan 05.
- No filler words.
- No emojis.
  </action>
  <verify>
    <automated>cd "C:/Projetos/baseworks" &amp;&amp; test -f docs/architecture.md &amp;&amp; grep -q "^# Architecture Overview$" docs/architecture.md &amp;&amp; grep -q "^## 1\. Module system$" docs/architecture.md &amp;&amp; grep -q "^## 2\. CQRS flow$" docs/architecture.md &amp;&amp; grep -q "^## 3\. Request lifecycle$" docs/architecture.md &amp;&amp; grep -q "^## 4\. Tenant scoping$" docs/architecture.md &amp;&amp; test "$(grep -c '^```mermaid$' docs/architecture.md)" = "4" &amp;&amp; grep -q "ModuleRegistry" docs/architecture.md &amp;&amp; grep -q "CqrsBus" docs/architecture.md &amp;&amp; grep -q "TypedEventBus" docs/architecture.md &amp;&amp; grep -q "scopedDb" docs/architecture.md &amp;&amp; grep -q "tenantMiddleware" docs/architecture.md &amp;&amp; grep -q "HandlerContext" docs/architecture.md &amp;&amp; grep -q "registry\.ts" docs/architecture.md &amp;&amp; grep -q "scoped-db\.ts" docs/architecture.md &amp;&amp; ! grep -q '^```graph' docs/architecture.md &amp;&amp; ! grep -q 'classDiagram' docs/architecture.md &amp;&amp; ! grep -nE "\b(basically|simply|just)\b" docs/architecture.md &amp;&amp; ! grep -nE "\bnpm\b|\byarn\b|\bpnpm\b" docs/architecture.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/architecture.md` exists
    - All 4 required `##` diagram sections present (`1. Module system`, `2. CQRS flow`, `3. Request lifecycle`, `4. Tenant scoping`)
    - Exactly 4 ```mermaid fenced blocks (count: `grep -c '^```mermaid$' docs/architecture.md` returns 4)
    - Concrete code identifiers appear as Mermaid box labels (all of: `ModuleRegistry`, `CqrsBus`, `TypedEventBus`, `scopedDb`, `tenantMiddleware`, `HandlerContext` present)
    - File paths cited: `registry.ts`, `scoped-db.ts`, plus at least one more (`cqrs.ts`, `event-bus.ts`, or middleware file)
    - No deprecated Mermaid `graph` keyword used (`grep -q '^```graph' docs/architecture.md` fails)
    - No `classDiagram` used
    - No filler words (`basically`, `simply`, `just`)
    - No `npm`/`yarn`/`pnpm`
    - Links forward to `./add-a-module.md`, `./configuration.md`, `./testing.md`, `./integrations/`
  </acceptance_criteria>
  <done>`docs/architecture.md` has exactly 4 Mermaid diagrams with named code anchors, cites the canonical source files, stays within the 4-diagram scope, and links forward to the guides and integrations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → public reader | Both docs are committed to the repo and readable by anyone. Getting Started shows env var examples that could leak real secrets if a placeholder is replaced with a production key. |
| document → reader copying code | Reader may copy diagrams or shell commands verbatim. Any insecure pattern documented becomes a propagated pattern in downstream projects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07 | Information Disclosure | `docs/getting-started.md` env var examples. | mitigate | All secret values in the `.env` template are obvious placeholders using the correct-shape prefixes but short suffixes (`sk_test_replace_me`, `re_replace_me`, `whsec_replace_me`). Acceptance criteria greps for `sk_live_` and for `sk_test_[a-zA-Z0-9]{20,}` (real-shape lengths ≥ 20 chars after prefix) — zero matches required. `BETTER_AUTH_SECRET` uses the literal string `replace-with-a-random-string-at-least-32-chars` (prose, not a real secret). |
| T-15-08 | Information Disclosure | `docs/getting-started.md` reveals production URLs / infrastructure. | mitigate | `WEB_URL` and `ADMIN_URL` examples use only `localhost:3000` and `localhost:5173` dev defaults from `env.ts`. No staging/production domains referenced. |
| T-15-09 | Tampering | `docs/architecture.md` inaccurately describes tenant isolation, leading readers to skip `scopedDb` and write raw SQL. | mitigate | Section "4. Tenant scoping" explicitly names `scopedDb` as the isolation boundary, cites `packages/db/src/helpers/scoped-db.ts`, and the "Bypass" subsection warns readers to never import `unscoped-db.ts` from module commands/queries. Reviewer confirms wording before merge. |
| T-15-10 | Elevation of Privilege | Reader learns a pattern for writing handlers that bypasses `HandlerContext` and reaches directly into `createDb(env.DATABASE_URL)`. | mitigate | Every CQRS-flow diagram and sample in architecture.md uses `ctx.db` (`scopedDb`). No diagram path reaches `createDb(env.DATABASE_URL)` directly from a handler. Plan 04's add-a-module tutorial reinforces this. |
</threat_model>

<verification>
Automated (acceptance-criteria grep chains run as `<automated>` in each task):
- File existence, heading structure, required identifiers, forbidden words, forbidden syntax (`graph`, `classDiagram`), exact Mermaid block count.

Manual-only (per 15-VALIDATION.md):
- Reviewer pushes branch and opens `docs/architecture.md` in GitHub PR preview; confirms all 4 Mermaid diagrams render without errors on github.com.
- Reviewer reads `docs/getting-started.md` on a fresh clone and runs every command verbatim: `bun install`, `bun docker:up`, `bun db:migrate`, `bun api`, `bun worker`, `bun test`. All must succeed.
- Reviewer confirms every code citation in `docs/architecture.md` (e.g., `registry.ts:10-16`) points to the intended code by opening each cited range.
</verification>

<success_criteria>
1. `docs/getting-started.md` exists with all 12 sections, uses only `bun` commands, contains no real-shaped secrets, and passes all forbidden-word greps.
2. `docs/architecture.md` exists with exactly 4 Mermaid diagrams, each using concrete code-identifier labels matching `ModuleRegistry`, `CqrsBus`, `TypedEventBus`, `scopedDb`, and `tenantMiddleware`.
3. `docs/architecture.md` cites `registry.ts`, `scoped-db.ts`, and at least one of `cqrs.ts`/`event-bus.ts`/`middleware/tenant.ts`.
4. Neither doc uses `npm`/`yarn`/`pnpm`, filler words, or emojis.
5. Both docs link forward to the guides that Plan 04 creates (`add-a-module.md`, `configuration.md`, `testing.md`) and the integrations directory.
</success_criteria>

<output>
After completion, create `.planning/phases/15-developer-documentation/15-03-SUMMARY.md` summarizing:
- DOCS-01 delivered (getting-started.md) with exact command surface documented
- DOCS-02 delivered (architecture.md) with 4 Mermaid diagrams and their anchor identifiers
- Known open cross-links to Plan 04 files (add-a-module.md, configuration.md, testing.md) — GitHub will show relative-link warnings until those land
- Downstream: Plan 04 creates the guides that DOCS-02 links to; Plan 05 creates integration docs referenced from both.
</output>
