---
phase: 06-responsive-layouts
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - apps/web/app/(dashboard)/dashboard/page.tsx
  - apps/web/app/(dashboard)/dashboard/billing/page.tsx
  - apps/admin/src/routes/billing/overview.tsx
  - apps/admin/src/routes/tenants/detail.tsx
  - apps/admin/src/routes/users/detail.tsx
  - apps/admin/src/routes/system/health.tsx
autonomous: false
requirements:
  - RESP-05
  - RESP-06

must_haves:
  truths:
    - "All customer app pages render without horizontal overflow at 375px, 768px, and 1440px"
    - "All admin dashboard pages render without horizontal overflow at 375px, 768px, and 1440px"
    - "Stat cards stack vertically on mobile and use grid on desktop"
    - "Billing plans grid stacks on mobile"
    - "Admin billing recent subscriptions table is readable on mobile via JS conditional render"
    - "Detail pages use single column on mobile, two columns on desktop"
  artifacts:
    - path: "apps/web/app/(dashboard)/dashboard/billing/page.tsx"
      provides: "Responsive billing page"
      contains: "lg:grid-cols"
    - path: "apps/admin/src/routes/billing/overview.tsx"
      provides: "Responsive billing overview with JS-conditional mobile subscription layout"
      contains: "useIsMobile"
    - path: "apps/admin/src/routes/tenants/detail.tsx"
      provides: "Responsive tenant detail page"
      contains: "lg:grid-cols-2"
    - path: "apps/admin/src/routes/users/detail.tsx"
      provides: "Responsive user detail page"
      contains: "lg:grid-cols-2"
    - path: "apps/admin/src/routes/system/health.tsx"
      provides: "Responsive system health page"
      contains: "grid-cols-1"
  key_links:
    - from: "apps/web/app/(dashboard)/dashboard/billing/page.tsx"
      to: "@baseworks/ui Card components"
      via: "responsive grid classes"
      pattern: "grid-cols-1.*lg:grid-cols"
    - from: "apps/admin/src/routes/billing/overview.tsx"
      to: "packages/ui/src/hooks/use-mobile.tsx"
      via: "useIsMobile for JS conditional rendering"
      pattern: "useIsMobile"
---

<objective>
Audit and fix all remaining pages in both apps for responsive layout at 375px, 768px, and 1440px viewports.

Purpose: Ensure every page in both the customer app and admin dashboard renders without horizontal overflow or content clipping at all three breakpoints (RESP-05, RESP-06). The sidebar work from Plan 01 and card-based tables from Plan 02 handle the major structural issues; this plan covers page-level spacing, grid stacking, and the billing overview's raw table.

Output: All pages responsive across all breakpoints. Final visual checkpoint to confirm.
</objective>

<execution_context>
@C:/Projetos/baseworks/.claude/get-shit-done/workflows/execute-plan.md
@C:/Projetos/baseworks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-responsive-layouts/06-CONTEXT.md
@.planning/phases/06-responsive-layouts/06-RESEARCH.md
@.planning/phases/06-responsive-layouts/06-UI-SPEC.md
@.planning/phases/06-responsive-layouts/06-01-SUMMARY.md
@.planning/phases/06-responsive-layouts/06-02-SUMMARY.md

<interfaces>
<!-- Page-level responsive patterns from UI-SPEC -->

Spacing rules per breakpoint:
| Element | Mobile (<768px) | Tablet (768-1023px) | Desktop (>=1024px) |
|---------|-----------------|---------------------|--------------------|
| Page content padding | 16px (px-4) | 24px (px-6) | 32px (px-8) |
| Section vertical gap | 24px (gap-6) | 24px (gap-6) | 32px (gap-8) |
| Grid columns (detail) | 1 column | 1 column | 2 columns (lg:grid-cols-2) |

Stacking rules:
- Grid layouts with lg:grid-cols-2/3 collapse to single column below lg (1024px)
- Stat cards: stack vertically on mobile, 2-col on tablet, 3-4 col on desktop
- Billing plans: stack vertically on mobile, horizontal on desktop

From apps/web/app/(dashboard)/layout.tsx (after Plan 01):
```typescript
// Uses SidebarInset with: <div className="mx-auto max-w-4xl p-6">
```

From apps/admin/src/layouts/admin-layout.tsx (after Plan 01):
```typescript
// Uses SidebarInset with: <main className="flex-1 p-6">
```

From apps/admin/src/routes/system/health.tsx (current structure):
```typescript
// Line 97: <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4"> for queue cards
// Line 114: <div className="space-y-6"> main container
// Line 119: CardContent with flex items-center gap-4 for status bar
// Line 144: Queues section with grid-cols-1 md:grid-cols-2 lg:grid-cols-3
// Line 158: <div className="grid grid-cols-2 gap-2 text-sm"> inside each queue card for stats
```

From apps/admin/src/routes/tenants/detail.tsx (current structure):
```typescript
// Line 96: <div className="grid grid-cols-1 lg:grid-cols-2 gap-6"> -- already responsive
// Line 82-88: flex items-center gap-4 for header with back button
```

From apps/admin/src/routes/users/detail.tsx (current structure):
```typescript
// Line 82: <div className="grid grid-cols-1 lg:grid-cols-2 gap-6"> for skeleton
// Line 124: <div className="grid grid-cols-1 lg:grid-cols-2 gap-6"> for content -- already responsive
// Line 130: flex items-center gap-4 for avatar section
// Line 162: flex items-center justify-between for membership rows
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make all customer app pages responsive and fix admin page-level layouts</name>
  <files>apps/web/app/(dashboard)/dashboard/page.tsx, apps/web/app/(dashboard)/dashboard/billing/page.tsx, apps/admin/src/routes/billing/overview.tsx, apps/admin/src/routes/tenants/detail.tsx, apps/admin/src/routes/users/detail.tsx, apps/admin/src/routes/system/health.tsx</files>
  <read_first>
    apps/web/app/(dashboard)/dashboard/page.tsx
    apps/web/app/(dashboard)/dashboard/billing/page.tsx
    apps/admin/src/routes/billing/overview.tsx
    apps/admin/src/routes/tenants/detail.tsx
    apps/admin/src/routes/users/detail.tsx
    apps/admin/src/routes/system/health.tsx
    apps/web/app/(dashboard)/layout.tsx
    apps/admin/src/layouts/admin-layout.tsx
    .planning/phases/06-responsive-layouts/06-UI-SPEC.md
  </read_first>
  <action>
**Customer App Pages (RESP-05):**

**1. Dashboard page (`apps/web/app/(dashboard)/dashboard/page.tsx`)**
- Already simple (single Card). Verify it has no fixed-width elements.
- The parent layout wraps in `max-w-4xl p-6`. On mobile this is fine since max-w constrains.
- Change parent layout padding to responsive: the layout.tsx `p-6` should be `p-4 md:p-6 lg:p-8` per UI-SPEC spacing rules. **Wait -- layout.tsx is Plan 01's file. Instead, ensure the page content itself works at all widths.** If the layout padding is `p-6`, it works at 375px (24px each side = 327px content). This is fine. No changes needed to dashboard page.

**2. Billing page (`apps/web/app/(dashboard)/dashboard/billing/page.tsx`)**
- Plans grid already uses `grid grid-cols-1 gap-6 lg:grid-cols-3` -- this correctly stacks on mobile. Verify this is present; if so, no change needed.
- SubscriptionCard: Already responsive (Card with flex layout). No changes needed.
- BillingHistory: Uses Card per item with `flex items-center justify-between py-4`. On very narrow screens the flex may wrap. Add `flex-wrap` to the CardContent className, or ensure text truncates. Specifically:
  - Change `<CardContent className="flex items-center justify-between py-4">` to `<CardContent className="flex flex-wrap items-center justify-between gap-2 py-4">`
  - This ensures on narrow screens the amount wraps below the description instead of overflowing.
- TabsList: On mobile, the three tabs ("Subscription", "History", "Usage") may overflow. Add `className="w-full"` to TabsList so tabs fill available width, and each TabsTrigger gets `className="flex-1"` for equal distribution.

**Admin Dashboard Pages (RESP-06):**

**3. Billing overview (`apps/admin/src/routes/billing/overview.tsx`)**
- Stat cards grid already uses `grid grid-cols-1 lg:grid-cols-3 gap-6`. Good.
- The "Recent Subscriptions" section uses a raw `<Table>` (not DataTable). This has 4 columns (Tenant, Plan, Status, Date) which can overflow at 375px.
- Solution: Use JS conditional rendering with `useIsMobile()` (NOT the CSS `hidden md:block` / `block md:hidden` dual-render pattern, which RESEARCH.md explicitly lists as an anti-pattern to avoid since it renders both views in the DOM).
  - Import `useIsMobile` from `@baseworks/ui/hooks/use-mobile`
  - Add `const isMobile = useIsMobile()` at the top of the component
  - Wrap the table/card rendering in a conditional:
    ```tsx
    {isMobile ? (
      <div className="space-y-3">
        {recentSubscriptions.slice(0, 10).map((sub: any, i: number) => (
          <div key={sub.id ?? i} className="flex items-center justify-between rounded-lg border p-3">
            <div className="space-y-1">
              <p className="text-sm font-medium">{sub.tenantName ?? sub.tenantId}</p>
              <p className="text-xs text-muted-foreground">{sub.plan} &middot; {sub.date ? new Date(sub.date).toLocaleDateString() : "N/A"}</p>
            </div>
            <Badge variant={sub.status === "active" ? "default" : "secondary"}>
              {sub.status}
            </Badge>
          </div>
        ))}
      </div>
    ) : (
      <Table>
        {/* existing table markup unchanged */}
      </Table>
    )}
    ```
  This renders only the active view -- no hidden DOM waste. The admin dashboard is a Vite SPA, so no SSR concerns with the JS conditional.

**4. Tenant detail (`apps/admin/src/routes/tenants/detail.tsx`)**
- The detail grid already uses `grid grid-cols-1 lg:grid-cols-2 gap-6` (line 96) -- this correctly stacks to single column on mobile. No grid changes needed.
- Header section (line 82-88): uses `flex items-center gap-4` for back button + title. Add `flex-wrap` so on very narrow screens the title wraps below the back button if needed: change to `flex flex-wrap items-center gap-4`.
- Ensure all text in detail cards uses `break-words` class on long values (slugs can be lengthy). Add `className="break-all"` to the slug value `<p>` element.
- The action Card with deactivation controls: already uses standard Card layout, no changes needed.

**5. User detail (`apps/admin/src/routes/users/detail.tsx`)**
- The detail grid already uses `grid grid-cols-1 lg:grid-cols-2 gap-6` (line 124) -- correctly stacks on mobile. No grid changes needed.
- The skeleton loading state also already uses `grid grid-cols-1 lg:grid-cols-2 gap-6` (line 82). Good.
- Header section (line 111): uses `flex items-center gap-4`. Add `flex-wrap` for safety: `flex flex-wrap items-center gap-4`.
- Membership rows (line 162): `flex items-center justify-between` could overflow if tenant name is long. Add `min-w-0` on the text container and `truncate` on the tenant name text to prevent overflow.
- Dialog footers (lines 207, 235): already have `gap-2 sm:gap-0` which handles mobile button spacing. No changes needed.

**6. System health (`apps/admin/src/routes/system/health.tsx`)**
- Queue cards grid (line 144): already uses `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4` -- correctly stacks on mobile. No changes needed.
- Status bar card (line 119): uses `flex items-center gap-4` -- fine on mobile since it's just a badge + text.
- Inside each queue card, stats use `grid grid-cols-2 gap-2` (line 158). At 375px with sidebar hidden, this gives ~167px per column which is sufficient for "Waiting: 0" style content. No changes needed.
- Page heading (line 115): plain `text-2xl font-semibold`, responsive by default. No changes needed.
- No fixed-width elements found in this file. No changes needed beyond confirming the existing responsive grid classes are correct.

**General responsive audit for all files above:**
- No element should have a fixed width greater than ~300px without responsive override
- All flex containers with horizontal layout should have `flex-wrap` or switch to column on mobile
- Text content should use `truncate`, `break-words`, or `break-all` where appropriate
- `overflow-hidden` on page-level containers to prevent horizontal scroll
  </action>
  <verify>
    <automated>cd /c/Projetos/baseworks && grep -c "useIsMobile\|isMobile" apps/admin/src/routes/billing/overview.tsx && grep -c "lg:grid-cols" apps/web/app/\(dashboard\)/dashboard/billing/page.tsx && grep -c "flex-wrap" apps/admin/src/routes/tenants/detail.tsx && grep -c "truncate\|min-w-0" apps/admin/src/routes/users/detail.tsx</automated>
  </verify>
  <acceptance_criteria>
    - apps/admin/src/routes/billing/overview.tsx contains `useIsMobile` import and JS conditional render (NOT `hidden md:block` CSS dual-render)
    - apps/admin/src/routes/billing/overview.tsx does NOT contain `hidden md:block` or `block md:hidden` for subscription table switching
    - apps/web/app/(dashboard)/dashboard/billing/page.tsx PlanSelection grid contains `grid-cols-1` (mobile) and `lg:grid-cols-3` (desktop)
    - apps/admin/src/routes/tenants/detail.tsx header contains `flex-wrap`
    - apps/admin/src/routes/tenants/detail.tsx slug value contains `break-all`
    - apps/admin/src/routes/users/detail.tsx header contains `flex-wrap`
    - apps/admin/src/routes/users/detail.tsx membership name contains `truncate` or `min-w-0`
    - apps/admin/src/routes/system/health.tsx queue grid contains `grid-cols-1` for mobile stacking
    - No file contains fixed-width elements wider than 300px without responsive overrides
  </acceptance_criteria>
  <done>
    All pages in both apps render without horizontal overflow at 375px, 768px, and 1440px. Billing overview uses JS conditional rendering (useIsMobile) for mobile-friendly subscription layout. Detail pages have text overflow protection. System health page queue grid stacks on mobile.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of responsive layouts across all breakpoints</name>
  <files>none</files>
  <action>
    Human visually verifies all responsive layout changes across both apps at three breakpoints (375px, 768px, 1440px). No code changes in this task -- verification only.
  </action>
  <what-built>
    Complete responsive layout system across both apps:
    - Three-tier sidebar (desktop full/collapsed, tablet icon rail with transient hover-expand, mobile Sheet drawer)
    - Data table cards on mobile for tenants and users lists with sort dropdown and filter chips
    - All page layouts responsive at 375px, 768px, and 1440px
    - localStorage sidebar persistence (not corrupted by tablet hover)
    - Auto-dismiss Sheet on navigation
  </what-built>
  <how-to-verify>
    Start both apps:
    1. `cd apps/web && bun dev` (customer app, usually http://localhost:3000)
    2. `cd apps/admin && bun dev` (admin dashboard, usually http://localhost:5173)

    **Desktop (1440px):**
    1. Open admin dashboard at 1440px
    2. Verify sidebar is visible alongside content (not overlaying)
    3. Click sidebar toggle -- should collapse to icon rail (48px)
    4. Refresh -- sidebar should still be collapsed (localStorage)
    5. Open customer app dashboard at 1440px -- same sidebar behavior

    **Mobile (375px via Chrome DevTools):**
    6. Resize admin dashboard to 375px (or use device toolbar iPhone SE)
    7. Verify hamburger icon in top bar
    8. Tap hamburger -- Sheet drawer opens from left
    9. Tap a nav item -- Sheet dismisses and navigates
    10. Navigate to Tenants list -- should see card layout, not table
    11. Tap a tenant card -- should expand to show slug
    12. Use Sort dropdown above cards
    13. Navigate to Billing overview -- should see stacked subscription items (not table)
    14. Resize customer app to 375px -- verify hamburger and Sheet
    15. Check billing page -- plans should stack vertically

    **Tablet (800px):**
    16. Resize admin to 800px
    17. Verify icon-only rail sidebar
    18. Hover over rail -- should expand after ~200ms delay
    19. Move mouse away -- should collapse
    20. Check DevTools > Application > Local Storage -- sidebar_state value should NOT change during hover expand/collapse

    **Overflow check:**
    21. At 375px, scroll right on every page -- NO horizontal scroll should be possible
    22. Check admin: tenants list, users list, tenant detail, user detail, billing, system health
    23. Check customer: dashboard, billing (all tabs)
  </how-to-verify>
  <verify>
    <automated>echo "Checkpoint: human visual verification required"</automated>
  </verify>
  <done>Human confirms all pages render correctly at 375px, 768px, and 1440px with no horizontal overflow, correct sidebar behavior at each breakpoint, card-based mobile tables, and localStorage not corrupted by tablet hover.</done>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. All changes are CSS class modifications and JS conditional rendering. No data flow changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | D (Denial of Service) | JS conditional rendering | accept | Billing overview uses `useIsMobile()` for conditional render -- only one view rendered at a time. No duplicate DOM. Negligible performance impact. |
</threat_model>

<verification>
1. No horizontal scrollbar at 375px on any page in either app
2. No horizontal scrollbar at 768px on any page in either app
3. No horizontal scrollbar at 1440px on any page in either app
4. All grids stack to single column on mobile
5. Billing plans visible and readable on mobile
6. Admin billing recent subscriptions readable on mobile (JS conditional stacked layout, not CSS dual-render)
7. Detail pages use responsive column layout
8. System health queue cards stack on mobile
</verification>

<success_criteria>
- Zero horizontal overflow at 375px, 768px, and 1440px across all pages in both apps
- Human visual verification confirms correct layout at all three breakpoints
- All page content accessible (not hidden behind sidebar or clipped)
- Billing overview uses JS conditional render (useIsMobile), NOT CSS hidden/block dual-render
</success_criteria>

<output>
After completion, create `.planning/phases/06-responsive-layouts/06-03-SUMMARY.md`
</output>
