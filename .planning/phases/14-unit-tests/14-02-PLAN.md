---
phase: 14-unit-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/core/__tests__/cqrs.test.ts
  - apps/api/src/core/__tests__/event-bus.test.ts
  - apps/api/src/core/__tests__/registry.test.ts
  - packages/db/src/__tests__/scoped-db.test.ts
  - packages/config/src/__tests__/env.test.ts
autonomous: true
requirements:
  - TEST-06
  - TEST-07
  - TEST-08
must_haves:
  truths:
    - "CqrsBus edge cases tested: duplicate registration, handler throws, unknown command"
    - "EventBus edge cases tested: multiple subscribers same event, emit with no subscribers, subscriber error isolation"
    - "Registry edge cases tested: duplicate module load, module with missing commands"
    - "ScopedDb edge cases tested: empty tenantId behavior, raw property access"
    - "Config validation tested: validatePaymentProviderEnv and assertRedisUrl"
  artifacts:
    - path: "apps/api/src/core/__tests__/cqrs.test.ts"
      provides: "Expanded CQRS bus edge case tests"
      contains: "duplicate registration"
    - path: "apps/api/src/core/__tests__/event-bus.test.ts"
      provides: "Expanded event bus edge case tests"
      contains: "no subscribers"
    - path: "apps/api/src/core/__tests__/registry.test.ts"
      provides: "Expanded registry edge case tests"
      contains: "duplicate module"
    - path: "packages/db/src/__tests__/scoped-db.test.ts"
      provides: "Expanded scoped-db edge case tests"
      contains: "empty.*tenantId"
    - path: "packages/config/src/__tests__/env.test.ts"
      provides: "Config validation tests"
      contains: "validatePaymentProviderEnv\\|assertRedisUrl"
  key_links:
    - from: "apps/api/src/core/__tests__/cqrs.test.ts"
      to: "apps/api/src/core/cqrs-bus.ts"
      via: "import"
      pattern: "import.*CqrsBus\\|cqrs"
---

<objective>
Expand existing core infrastructure, scoped-db, and config test files with edge case coverage.

Purpose: These shared foundations underpin all modules. Deeper testing catches subtle bugs in tenant isolation, event dispatch, and configuration validation. Per D-04, core/scoped-db tests go deeper than handler tests.
Output: 5 existing test files expanded with new edge case tests.
</objective>

<execution_context>
@C:/Projetos/baseworks/.claude/get-shit-done/workflows/execute-plan.md
@C:/Projetos/baseworks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-unit-tests/14-CONTEXT.md
@.planning/phases/14-unit-tests/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand CqrsBus, EventBus, and Registry edge case tests</name>
  <files>
    apps/api/src/core/__tests__/cqrs.test.ts
    apps/api/src/core/__tests__/event-bus.test.ts
    apps/api/src/core/__tests__/registry.test.ts
  </files>
  <read_first>
    apps/api/src/core/__tests__/cqrs.test.ts
    apps/api/src/core/__tests__/event-bus.test.ts
    apps/api/src/core/__tests__/registry.test.ts
    apps/api/src/core/cqrs-bus.ts
    apps/api/src/core/event-bus.ts
    apps/api/src/core/registry.ts
  </read_first>
  <action>
Read each existing test file and its corresponding source file. Add new `describe` blocks for edge cases (per D-04, TEST-07). Do NOT delete existing tests -- only append new ones.

**cqrs.test.ts -- add these edge case tests:**
- "returns error result when handler throws an unhandled exception" -- register a handler that `throw new Error("boom")`, execute it, verify result has `success: false` and error contains "boom"
- "returns error for unknown command/query name" -- call `bus.execute("nonexistent:cmd", {}, ctx)` and verify it returns an error result (not a crash)
- "logs warning on duplicate command registration" -- register the same command name twice, verify the bus still works (second registration overwrites or warns). Read the source to determine exact behavior, then test accordingly.
- "handles concurrent execution of different commands" -- register two handlers, execute both via `Promise.all`, verify both resolve independently

**event-bus.test.ts -- add these edge case tests:**
- "delivers event to multiple subscribers of the same event type" -- subscribe two handlers to the same event, emit once, verify both called
- "does not error when emitting with no subscribers" -- emit an event with no registered handlers, verify no errors thrown
- "isolates subscriber errors" -- subscribe two handlers where the first throws, emit the event, verify the second handler still executes (async error isolation)
- "off/unsubscribe removes a listener" -- if the event bus has an `off` method, test it. If not, skip this test. Read the source first.

**registry.test.ts -- add these edge case tests:**
- "handles loading a module with empty commands and queries" -- create a module definition with `commands: {}` and `queries: {}`, load it, verify registry does not crash
- "handles duplicate module name gracefully" -- load the same module name twice, verify behavior (overwrite or error). Read source to determine expected behavior.
- "lists loaded modules" -- load 2+ modules, verify `registry.modules` or equivalent accessor returns them all
  </action>
  <verify>
    <automated>cd C:/Projetos/baseworks && bun test apps/api/src/core/__tests__/cqrs.test.ts apps/api/src/core/__tests__/event-bus.test.ts apps/api/src/core/__tests__/registry.test.ts --bail</automated>
  </verify>
  <acceptance_criteria>
    - `cqrs.test.ts` contains test for "unknown command" or "nonexistent" returning an error
    - `cqrs.test.ts` contains test for "handler throws" producing an error result
    - `event-bus.test.ts` contains test for "multiple subscribers" receiving the same event
    - `event-bus.test.ts` contains test for "no subscribers" emitting without error
    - `registry.test.ts` contains test for "empty commands" or "empty queries" not crashing
    - All three test files pass: `bun test apps/api/src/core/__tests__/*.test.ts` exits 0
  </acceptance_criteria>
  <done>CqrsBus has 4+ new edge case tests, EventBus has 3+ new edge case tests, Registry has 3+ new edge case tests. All pass.</done>
</task>

<task type="auto">
  <name>Task 2: Expand scoped-db edge case tests</name>
  <files>
    packages/db/src/__tests__/scoped-db.test.ts
  </files>
  <read_first>
    packages/db/src/__tests__/scoped-db.test.ts
    packages/db/src/helpers/scoped-db.ts
  </read_first>
  <action>
Read the existing scoped-db test file and the scoped-db source. Add new edge case tests (per D-04, TEST-06). Do NOT delete existing tests.

**Add these edge case tests:**
- "exposes raw property for unscoped access" -- create a scopedDb, verify `scopedDb.raw` returns the underlying unscoped db instance
- "handles empty string tenantId" -- create a scopedDb with `tenantId: ""`, verify behavior. Read the source to determine if it throws, returns empty results, or passes through. Test the actual behavior.
- "handles undefined tenantId" -- if the function accepts undefined, test behavior. If TypeScript prevents it, add a comment noting the type guard.
- "tenantId is accessible on the scoped db" -- verify `scopedDb.tenantId` returns the tenantId passed during creation
- "select always scopes to tenant" -- if the existing tests don't already verify that queries include a WHERE clause with tenantId, add a test that verifies the scoping mechanism (may require reading how scopedDb implements the filter)

Read the source carefully -- scopedDb may use Drizzle's `db.$with()` or a proxy pattern. Tailor tests to the actual implementation.
  </action>
  <verify>
    <automated>cd C:/Projetos/baseworks && bun test packages/db/src/__tests__/scoped-db.test.ts --bail</automated>
  </verify>
  <acceptance_criteria>
    - `scoped-db.test.ts` contains test for "raw" property access
    - `scoped-db.test.ts` contains test for empty string or edge case tenantId
    - `scoped-db.test.ts` contains test for `tenantId` accessor
    - `bun test packages/db/src/__tests__/scoped-db.test.ts` exits 0
  </acceptance_criteria>
  <done>scoped-db.test.ts has 3+ new edge case tests covering raw access, empty tenantId behavior, and tenantId accessor. All pass.</done>
</task>

<task type="auto">
  <name>Task 3: Expand config/env validation tests</name>
  <files>
    packages/config/src/__tests__/env.test.ts
  </files>
  <read_first>
    packages/config/src/__tests__/env.test.ts
    packages/config/src/index.ts
    packages/config/src/env.ts
  </read_first>
  <action>
Read the existing env.test.ts and the config package source files. Add tests for `validatePaymentProviderEnv()` and `assertRedisUrl()` functions (per TEST-08).

Read the source to find these functions (they may be in `env.ts`, `index.ts`, or a separate validation file). Then add tests:

**validatePaymentProviderEnv tests (if function exists):**
- "throws when PAYMENT_PROVIDER=pagarme without PAGARME_SECRET_KEY in non-test NODE_ENV" -- mock env, call function, expect throw
- "throws when PAYMENT_PROVIDER=stripe without STRIPE_SECRET_KEY in non-test NODE_ENV" -- same pattern
- "warns but does not throw in test NODE_ENV" -- mock NODE_ENV=test, call function, no throw
- "passes when all required keys present" -- set all env vars, no throw

**assertRedisUrl tests (if function exists):**
- "throws for worker role without REDIS_URL" -- mock SERVER_ROLE=worker, no REDIS_URL, expect throw
- "throws for all role without REDIS_URL" -- mock SERVER_ROLE=all, no REDIS_URL, expect throw  
- "does not throw for api role without REDIS_URL" -- mock SERVER_ROLE=api, no REDIS_URL, no throw
- "passes for any role when REDIS_URL present" -- set REDIS_URL, no throw

**NOTE:** These functions may use `process.env` directly or receive env as parameter. The existing env.test.ts likely uses `Bun.spawn` for subprocess isolation (since env validation crashes the process). Follow the existing test pattern -- if subprocess-based, create new subprocess tests. If direct function calls, use direct calls.

If `validatePaymentProviderEnv` or `assertRedisUrl` do not exist as separate functions, look for the validation logic inline in the env creation and test whatever validation paths exist in the config package.
  </action>
  <verify>
    <automated>cd C:/Projetos/baseworks && bun test packages/config/src/__tests__/env.test.ts --bail</automated>
  </verify>
  <acceptance_criteria>
    - `env.test.ts` contains at least 3 new test cases beyond what existed before
    - Tests cover payment provider env validation (missing keys scenario)
    - Tests cover Redis URL validation (role-based requirement)
    - `bun test packages/config/src/__tests__/env.test.ts` exits 0
  </acceptance_criteria>
  <done>env.test.ts has new tests for payment provider env validation and Redis URL validation. All pass. Config edge cases covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Not applicable -- expanding existing unit tests.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | I (Information Disclosure) | Test files | accept | No real secrets in test code. |
</threat_model>

<verification>
1. `bun test apps/api/src/core/__tests__/*.test.ts` -- all core tests pass
2. `bun test packages/db/src/__tests__/scoped-db.test.ts` -- all scoped-db tests pass
3. `bun test packages/config/src/__tests__/env.test.ts` -- all config tests pass
</verification>

<success_criteria>
- Core infrastructure (CqrsBus, EventBus, Registry) each have 3+ new edge case tests
- scoped-db has 3+ new edge case tests
- Config env validation has 3+ new test cases
- All existing tests still pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/14-unit-tests/14-02-SUMMARY.md`
</output>
