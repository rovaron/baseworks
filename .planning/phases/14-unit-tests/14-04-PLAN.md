---
phase: 14-unit-tests
plan: 04
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - packages/modules/auth/src/__tests__/get-tenant.test.ts
  - packages/modules/auth/src/__tests__/list-tenants.test.ts
  - packages/modules/auth/src/__tests__/get-profile.test.ts
  - packages/modules/auth/src/__tests__/list-members.test.ts
  - packages/modules/auth/src/__tests__/get-invitation.test.ts
  - packages/modules/auth/src/__tests__/list-invitations.test.ts
autonomous: true
requirements:
  - TEST-02
must_haves:
  truths:
    - "All 6 auth query handlers have behavioral unit tests"
    - "Each query test has 1 success path + 1-2 error cases"
    - "get-profile test handles its special db import pattern (not ctx.db)"
  artifacts:
    - path: "packages/modules/auth/src/__tests__/get-tenant.test.ts"
      provides: "get-tenant behavioral tests"
      contains: "describe(\"getTenant\""
    - path: "packages/modules/auth/src/__tests__/list-tenants.test.ts"
      provides: "list-tenants behavioral tests"
      contains: "describe(\"listTenants\""
    - path: "packages/modules/auth/src/__tests__/get-profile.test.ts"
      provides: "get-profile behavioral tests"
      contains: "describe(\"getProfile\""
    - path: "packages/modules/auth/src/__tests__/list-members.test.ts"
      provides: "list-members behavioral tests"
      contains: "describe(\"listMembers\""
    - path: "packages/modules/auth/src/__tests__/get-invitation.test.ts"
      provides: "get-invitation behavioral tests"
      contains: "describe(\"getInvitation\""
    - path: "packages/modules/auth/src/__tests__/list-invitations.test.ts"
      provides: "list-invitations behavioral tests"
      contains: "describe(\"listInvitations\""
  key_links:
    - from: "packages/modules/auth/src/__tests__/get-profile.test.ts"
      to: "@baseworks/db"
      via: "mock.module for createDb"
      pattern: "mock\\.module.*@baseworks/db"
---

<objective>
Create behavioral unit tests for all 6 auth query handlers.

Purpose: Completes TEST-02. Auth queries follow the same mock.module("../auth") pattern as commands, except get-profile which uses direct DB access (requires mocking @baseworks/db and @baseworks/config instead).
Output: 6 new test files.
</objective>

<execution_context>
@C:/Projetos/baseworks/.claude/get-shit-done/workflows/execute-plan.md
@C:/Projetos/baseworks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-unit-tests/14-CONTEXT.md
@.planning/phases/14-unit-tests/14-RESEARCH.md
@.planning/phases/14-unit-tests/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests for tenant/member query handlers</name>
  <files>
    packages/modules/auth/src/__tests__/get-tenant.test.ts
    packages/modules/auth/src/__tests__/list-tenants.test.ts
    packages/modules/auth/src/__tests__/list-members.test.ts
  </files>
  <read_first>
    packages/modules/auth/src/queries/get-tenant.ts
    packages/modules/auth/src/queries/list-tenants.ts
    packages/modules/auth/src/queries/list-members.ts
    packages/modules/auth/src/__tests__/create-tenant.test.ts
    packages/modules/__test-utils__/mock-context.ts
    packages/modules/__test-utils__/assert-result.ts
  </read_first>
  <action>
Create 3 test files following the validated mock.module("../auth") pattern (per D-06, D-03).

**get-tenant.test.ts:**
Read `get-tenant.ts` to find which `auth.api.*` method it calls (likely `getFullOrganization`). Tests:
- "returns tenant data on success" -- mock auth.api method to resolve with `{ id: "org-1", name: "Test", slug: "test", members: [] }`, verify `assertResultOk(result)` and `result.data.id === "org-1"`
- "returns error when tenant not found" -- mock auth.api to return null/undefined or throw, verify `assertResultErr(result)` 
- "returns error when auth.api throws" -- mock rejection with error message, verify err contains it

**list-tenants.test.ts:**
Read `list-tenants.ts` (likely calls `auth.api.listOrganizations`). Tests:
- "returns list of tenants" -- mock to resolve with array of orgs, verify Result ok with data array
- "returns empty array when user has no tenants" -- mock to resolve with `[]`, verify Result ok with empty data
- "returns error when auth.api throws" -- mock rejection

**list-members.test.ts:**
Read `list-members.ts` (likely calls `auth.api.getFullOrganization` and extracts members). Tests:
- "returns members list for tenant" -- mock to resolve with org containing members array, verify data
- "returns error when tenant not found" -- mock to return null/throw
- "returns error when auth.api throws" -- mock rejection
  </action>
  <verify>
    <automated>cd C:/Projetos/baseworks && bun test packages/modules/auth/src/__tests__/get-tenant.test.ts packages/modules/auth/src/__tests__/list-tenants.test.ts packages/modules/auth/src/__tests__/list-members.test.ts --bail</automated>
  </verify>
  <acceptance_criteria>
    - `get-tenant.test.ts` contains `describe("getTenant"` with at least 2 tests
    - `list-tenants.test.ts` contains `describe("listTenants"` with at least 2 tests including empty array case
    - `list-members.test.ts` contains `describe("listMembers"` with at least 2 tests
    - All files use `mock.module("../auth"` and import from `__test-utils__/`
    - `bun test` on all three exits 0
  </acceptance_criteria>
  <done>get-tenant, list-tenants, list-members each have 2-3 behavioral tests. All pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests for get-profile, get-invitation, list-invitations query handlers</name>
  <files>
    packages/modules/auth/src/__tests__/get-profile.test.ts
    packages/modules/auth/src/__tests__/get-invitation.test.ts
    packages/modules/auth/src/__tests__/list-invitations.test.ts
  </files>
  <read_first>
    packages/modules/auth/src/queries/get-profile.ts
    packages/modules/auth/src/queries/get-invitation.ts
    packages/modules/auth/src/queries/list-invitations.ts
    packages/modules/auth/src/__tests__/create-tenant.test.ts
    packages/modules/__test-utils__/mock-context.ts
    packages/modules/__test-utils__/assert-result.ts
  </read_first>
  <action>
Create 3 test files. get-profile requires SPECIAL mocking (Pitfall 6 from research).

**get-profile.test.ts (SPECIAL -- does NOT use ctx.db):**
Read `get-profile.ts` carefully. It imports `createDb` from `@baseworks/db`, `user as userTable`, `env` from `@baseworks/config`, and `eq` from `drizzle-orm`. It creates its OWN db connection at module level: `const db = createDb(env.DATABASE_URL)`.

Mock strategy (different from other auth handlers):
1. `mock.module("@baseworks/config", () => ({ env: { DATABASE_URL: "postgres://test@localhost/test", NODE_ENV: "test" } }))`
2. Create a mock db object that supports `db.select().from(userTable).where(eq(...))` chain
3. `mock.module("@baseworks/db", () => ({ createDb: () => mockDb, user: { id: "id_col", name: "name_col", email: "email_col" } }))` -- return the mock db from createDb and export mock table columns
4. You may also need to mock `drizzle-orm` if `eq()` is used in a way that requires real column references. Read the source to determine if you can provide simple mock column objects.
5. Dynamic import: `const { getProfile } = await import("../queries/get-profile")`

Tests:
- "returns user profile when found" -- mock db.select chain to resolve with `[{ id: "user-1", name: "Test User", email: "test@test.com" }]`, provide `ctx.userId = "user-1"`, verify Result ok
- "returns error when user not authenticated (no userId)" -- set `ctx.userId = undefined`, verify Result err
- "returns error when user not found in db" -- mock db.select chain to resolve with `[]`, verify Result err

**get-invitation.test.ts:**
Standard auth.api pattern (likely `auth.api.getInvitation`). Tests:
- "returns invitation on success" -- mock resolve, verify ok
- "returns error when invitation not found" -- mock to return null or throw
- "returns error when auth.api throws" -- mock rejection

**list-invitations.test.ts:**
Standard auth.api pattern (likely `auth.api.listInvitations`). Tests:
- "returns invitations list" -- mock resolve with array, verify ok
- "returns empty array when no invitations" -- mock resolve with `[]`
- "returns error when auth.api throws" -- mock rejection
  </action>
  <verify>
    <automated>cd C:/Projetos/baseworks && bun test packages/modules/auth/src/__tests__/get-profile.test.ts packages/modules/auth/src/__tests__/get-invitation.test.ts packages/modules/auth/src/__tests__/list-invitations.test.ts --bail</automated>
  </verify>
  <acceptance_criteria>
    - `get-profile.test.ts` contains `mock.module("@baseworks/config"` AND `mock.module("@baseworks/db"` (special pattern)
    - `get-profile.test.ts` contains `describe("getProfile"` with at least 3 tests including "not authenticated" case
    - `get-invitation.test.ts` contains `describe("getInvitation"` with at least 2 tests
    - `list-invitations.test.ts` contains `describe("listInvitations"` with at least 2 tests
    - `bun test` on all three exits 0
    - No "connection refused" or "DATABASE_URL" errors (proves mocking works for get-profile)
  </acceptance_criteria>
  <done>get-profile (with special mocking), get-invitation, list-invitations each have 2-3 behavioral tests. All pass. TEST-02 fully complete (6/6 auth query handlers tested).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Not applicable -- unit tests only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | I (Information Disclosure) | Test files | accept | Mock values only. |
</threat_model>

<verification>
1. `bun test packages/modules/auth/src/__tests__/get-*.test.ts packages/modules/auth/src/__tests__/list-*.test.ts` -- all 6 query handler tests pass
2. get-profile test specifically does NOT attempt real DB connection
</verification>

<success_criteria>
- 6 new auth query test files
- Each has 2-3 behavioral tests per D-03
- get-profile handles its special direct-db pattern correctly
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/14-unit-tests/14-04-SUMMARY.md`
</output>
