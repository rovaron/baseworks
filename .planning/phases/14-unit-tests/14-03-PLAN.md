---
phase: 14-unit-tests
plan: 03
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - packages/modules/auth/src/__tests__/update-tenant.test.ts
  - packages/modules/auth/src/__tests__/delete-tenant.test.ts
  - packages/modules/auth/src/__tests__/update-profile.test.ts
  - packages/modules/auth/src/__tests__/create-invitation.test.ts
  - packages/modules/auth/src/__tests__/accept-invitation.test.ts
  - packages/modules/auth/src/__tests__/cancel-invitation.test.ts
  - packages/modules/auth/src/__tests__/reject-invitation.test.ts
  - packages/modules/auth/src/__tests__/tenant-crud.test.ts
  - packages/modules/auth/src/__tests__/invitation.test.ts
  - packages/modules/auth/src/__tests__/profile.test.ts
autonomous: true
requirements:
  - TEST-01
must_haves:
  truths:
    - "All 8 auth command handlers have behavioral unit tests"
    - "Each handler test has 1 success path + 1-2 error cases (~3 tests each)"
    - "Old registration-only test files are deleted"
    - "All tests use createMockContext from shared __test-utils__"
  artifacts:
    - path: "packages/modules/auth/src/__tests__/update-tenant.test.ts"
      provides: "update-tenant behavioral tests"
      contains: "describe(\"updateTenant\""
    - path: "packages/modules/auth/src/__tests__/delete-tenant.test.ts"
      provides: "delete-tenant behavioral tests"
      contains: "describe(\"deleteTenant\""
    - path: "packages/modules/auth/src/__tests__/update-profile.test.ts"
      provides: "update-profile behavioral tests"
      contains: "describe(\"updateProfile\""
    - path: "packages/modules/auth/src/__tests__/create-invitation.test.ts"
      provides: "create-invitation behavioral tests"
      contains: "describe(\"createInvitation\""
    - path: "packages/modules/auth/src/__tests__/accept-invitation.test.ts"
      provides: "accept-invitation behavioral tests"
      contains: "describe(\"acceptInvitation\""
    - path: "packages/modules/auth/src/__tests__/cancel-invitation.test.ts"
      provides: "cancel-invitation behavioral tests"
      contains: "describe(\"cancelInvitation\""
    - path: "packages/modules/auth/src/__tests__/reject-invitation.test.ts"
      provides: "reject-invitation behavioral tests"
      contains: "describe(\"rejectInvitation\""
  key_links:
    - from: "packages/modules/auth/src/__tests__/*.test.ts"
      to: "packages/modules/__test-utils__/mock-context.ts"
      via: "import createMockContext"
      pattern: "from.*__test-utils__/mock-context"
---

<objective>
Create behavioral unit tests for the remaining 7 auth command handlers and clean up old registration-only test files.

Purpose: Completes TEST-01 (auth command handler coverage). create-tenant.test.ts was validated in Plan 01; this plan applies the proven pattern to the remaining 7 handlers and deletes obsolete registration-only tests per D-08.
Output: 7 new test files, 3 old files deleted.
</objective>

<execution_context>
@C:/Projetos/baseworks/.claude/get-shit-done/workflows/execute-plan.md
@C:/Projetos/baseworks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-unit-tests/14-CONTEXT.md
@.planning/phases/14-unit-tests/14-RESEARCH.md
@.planning/phases/14-unit-tests/14-01-SUMMARY.md

<interfaces>
<!-- The mock.module pattern validated in Plan 01 -->
<!-- All auth command handlers import { auth } from "../auth" -->
<!-- All use defineCommand from @baseworks/shared -->
<!-- All return Result<T> via ok()/err() -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests for tenant CRUD + profile command handlers</name>
  <files>
    packages/modules/auth/src/__tests__/update-tenant.test.ts
    packages/modules/auth/src/__tests__/delete-tenant.test.ts
    packages/modules/auth/src/__tests__/update-profile.test.ts
  </files>
  <read_first>
    packages/modules/auth/src/commands/update-tenant.ts
    packages/modules/auth/src/commands/delete-tenant.ts
    packages/modules/auth/src/commands/update-profile.ts
    packages/modules/auth/src/__tests__/create-tenant.test.ts
    packages/modules/__test-utils__/mock-context.ts
    packages/modules/__test-utils__/assert-result.ts
  </read_first>
  <action>
Create 3 test files following the exact pattern validated in Plan 01's create-tenant.test.ts (per D-06, D-03). Each file:
1. Imports `describe, test, expect, mock, beforeEach` from `"bun:test"`
2. Imports `createMockContext` from `"../../../__test-utils__/mock-context"`
3. Imports `assertResultOk, assertResultErr` from `"../../../__test-utils__/assert-result"`
4. Creates mock functions for the specific `auth.api.*` method(s) the handler calls
5. Calls `mock.module("../auth", ...)` with those mocks (use the SAME path pattern that worked in Plan 01 -- check 14-01-SUMMARY.md for which path was validated)
6. Dynamically imports the handler: `const { handlerName } = await import("../commands/handler-name")`
7. Writes ~3 tests per handler

**update-tenant.test.ts:**
Read `update-tenant.ts` to find which `auth.api.*` method it calls (likely `updateOrganization`). Tests:
- "updates tenant successfully and returns updated org" -- mock auth.api method to resolve, verify Result ok, verify mock called with expected body
- "returns error when auth.api throws" -- mock rejection, verify Result err contains message
- If handler emits events, verify `ctx.emit` called. If not, test only success + error.

**delete-tenant.test.ts:**
Read `delete-tenant.ts` to find which `auth.api.*` method it calls (likely `deleteOrganization`). Tests:
- "deletes tenant and emits tenant.deleted event" -- mock resolve, verify Result ok, verify `ctx.emit` called with "tenant.deleted"
- "returns error when auth.api throws" -- mock rejection
- "passes tenantId correctly to auth.api" -- verify mock called with the tenantId from input

**update-profile.test.ts:**
Read `update-profile.ts` carefully -- it may call `auth.api.updateUser` AND optionally `auth.api.changePassword`. Tests:
- "updates user name successfully" -- mock updateUser, verify Result ok
- "changes password when currentPassword and newPassword provided" -- mock changePassword, verify called
- "returns error when auth.api throws" -- mock rejection
  </action>
  <verify>
    <automated>cd C:/Projetos/baseworks && bun test packages/modules/auth/src/__tests__/update-tenant.test.ts packages/modules/auth/src/__tests__/delete-tenant.test.ts packages/modules/auth/src/__tests__/update-profile.test.ts --bail</automated>
  </verify>
  <acceptance_criteria>
    - `update-tenant.test.ts` contains `describe("updateTenant"` with at least 2 tests
    - `delete-tenant.test.ts` contains `describe("deleteTenant"` with at least 2 tests
    - `update-profile.test.ts` contains `describe("updateProfile"` with at least 2 tests
    - All three files import from `__test-utils__/mock-context`
    - All three files use `mock.module("../auth"` (or the validated path from Plan 01)
    - `bun test` on all three exits 0
  </acceptance_criteria>
  <done>update-tenant, delete-tenant, and update-profile each have 2-3 behavioral tests covering success and error paths. All pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests for invitation command handlers + delete old registration files</name>
  <files>
    packages/modules/auth/src/__tests__/create-invitation.test.ts
    packages/modules/auth/src/__tests__/accept-invitation.test.ts
    packages/modules/auth/src/__tests__/cancel-invitation.test.ts
    packages/modules/auth/src/__tests__/reject-invitation.test.ts
    packages/modules/auth/src/__tests__/tenant-crud.test.ts
    packages/modules/auth/src/__tests__/invitation.test.ts
    packages/modules/auth/src/__tests__/profile.test.ts
  </files>
  <read_first>
    packages/modules/auth/src/commands/create-invitation.ts
    packages/modules/auth/src/commands/accept-invitation.ts
    packages/modules/auth/src/commands/cancel-invitation.ts
    packages/modules/auth/src/commands/reject-invitation.ts
    packages/modules/auth/src/__tests__/create-tenant.test.ts
    packages/modules/auth/src/__tests__/profile.test.ts
    packages/modules/__test-utils__/mock-context.ts
  </read_first>
  <action>
Create 4 invitation handler test files following the validated pattern (per D-06, D-03). Then delete old registration-only files (per D-08).

**create-invitation.test.ts:**
Read `create-invitation.ts` -- it imports `nanoid` and calls `auth.api.createInvitation`. It has two modes: email mode and link mode (generates `link-invite-{nanoid}@internal`). Mock both `auth.api.createInvitation` AND `nanoid` (via `mock.module("nanoid", () => ({ nanoid: () => "mock-id-123" }))`). Tests:
- "creates email invitation and emits invitation.created event" -- provide email, verify auth.api called, verify ctx.emit
- "creates link invitation with @internal placeholder email" -- provide no email / link mode indicator, verify auth.api called with email containing "@internal"
- "returns error when auth.api.createInvitation throws" -- mock rejection

**accept-invitation.test.ts:**
Read `accept-invitation.ts` -- calls `auth.api.acceptInvitation`. Note: may access `ctx.headers` (Pitfall 5 from research). Tests:
- "accepts invitation and emits invitation.accepted event" -- mock resolve, verify
- "returns error when auth.api throws" -- mock rejection

**cancel-invitation.test.ts:**
Read `cancel-invitation.ts`. Tests:
- "cancels invitation and emits invitation.cancelled event" -- mock resolve, verify
- "returns error when auth.api throws" -- mock rejection

**reject-invitation.test.ts:**
Read `reject-invitation.ts`. Note: may access `ctx.headers` (Pitfall 5). Tests:
- "rejects invitation and emits invitation.rejected event" -- mock resolve, verify
- "returns error when auth.api throws" -- mock rejection

**Delete old files (per D-08):**
- Delete `tenant-crud.test.ts` (registration-only -- verified)
- Delete `invitation.test.ts` (todo stubs only -- verified)
- Read `profile.test.ts` first: if it contains ONLY registration checks and auth config verification, delete it. If it has behavioral tests, keep them and note in summary. (From earlier read: it has registration checks + auth config verification but NO behavioral tests, so delete it.)

Keep `auth-setup.test.ts` (module definition tests) and `tenant-session.test.ts` (integration tests with real DB).
  </action>
  <verify>
    <automated>cd C:/Projetos/baseworks && bun test packages/modules/auth/src/__tests__/create-invitation.test.ts packages/modules/auth/src/__tests__/accept-invitation.test.ts packages/modules/auth/src/__tests__/cancel-invitation.test.ts packages/modules/auth/src/__tests__/reject-invitation.test.ts --bail</automated>
  </verify>
  <acceptance_criteria>
    - `create-invitation.test.ts` contains `describe("createInvitation"` with at least 3 tests
    - `accept-invitation.test.ts` contains `describe("acceptInvitation"` with at least 2 tests
    - `cancel-invitation.test.ts` contains `describe("cancelInvitation"` with at least 2 tests
    - `reject-invitation.test.ts` contains `describe("rejectInvitation"` with at least 2 tests
    - `tenant-crud.test.ts` does NOT exist (deleted per D-08)
    - `invitation.test.ts` does NOT exist (deleted per D-08)
    - `profile.test.ts` does NOT exist (deleted per D-08)
    - All four new test files pass: exits 0
  </acceptance_criteria>
  <done>4 invitation handler test files created with 2-3 tests each. 3 old registration-only files deleted. All new tests pass. TEST-01 fully complete (8/8 auth command handlers tested).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Not applicable -- unit tests only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | I (Information Disclosure) | Test files | accept | Mock values only, no real secrets. |
</threat_model>

<verification>
1. `bun test packages/modules/auth/src/__tests__/*.test.ts` -- all auth tests pass (new + kept existing)
2. Old files `tenant-crud.test.ts`, `invitation.test.ts`, `profile.test.ts` do not exist
3. 8 auth command handlers each have a dedicated test file
</verification>

<success_criteria>
- 7 new auth command test files (+ create-tenant from Plan 01 = 8 total)
- Each has 2-3 behavioral tests per D-03
- 3 old registration-only files deleted per D-08
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/14-unit-tests/14-03-SUMMARY.md`
</output>
