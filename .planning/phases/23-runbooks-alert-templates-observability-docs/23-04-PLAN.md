---
phase: 23
plan: 04
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - docs/alerts/sentry/db-down.json
  - docs/alerts/sentry/redis-down.json
  - docs/alerts/sentry/queue-backing-up.json
  - docs/alerts/sentry/webhook-failures.json
  - docs/alerts/sentry/auth-outage.json
  - docs/alerts/sentry/otel-exporter-failing.json
  - docs/alerts/sentry/bull-board-inaccessible.json
  - docs/alerts/sentry/high-error-rate.json
  - docs/alerts/sentry/slow-checkout.json
  - docs/alerts/sentry/README.md
autonomous: true
requirements:
  - DOC-04
must_haves:
  truths:
    - "All 9 Sentry alert JSON files exist at docs/alerts/sentry/<slug>.json with the canonical kebab-case slugs matching the runbooks (D-14)."
    - "Every JSON file is parseable by JSON.parse (NO comments — D-15 wrong; SLO note ships as `_baseworks_meta.slo_note` per RESEARCH §Q1 + Research Finding 4)."
    - "Every JSON file has a top-level `runbook_url` string field whose value is `../../runbooks/<slug>.md` (resolves to docs/runbooks/<slug>.md from docs/alerts/sentry/<slug>.json)."
    - "Every JSON file has a `_baseworks_meta` wrapper with `endpoint`, `slo_note`, and `priority` keys (D-15 + Discretion-default priorities)."
    - "5 alerts use the Issue Alert shape (project-level POST /api/0/projects/{org}/{project}/rules/): auth-outage, webhook-failures, high-error-rate, otel-exporter-failing, bull-board-inaccessible."
    - "4 alerts use the Metric Alert shape (org-level POST /api/0/organizations/{org}/alert-rules/): db-down, redis-down, queue-backing-up, slow-checkout."
    - "docs/alerts/sentry/README.md (~30+ lines per D-16) documents THREE import paths (sentry-cli api POST passthrough, raw curl, UI click-through — Research Finding 3) AND the strip-before-import idiom `jq 'del(.runbook_url, ._baseworks_meta)'`."
    - "Pass B of validate-docs.ts validates every runbook_url and exits 0 (Plan 03 ships the 9 runbook .md files in parallel; both plans must merge before Pass B can pass — verified at Plan 05's full-suite check)."
  artifacts:
    - path: "docs/alerts/sentry/db-down.json"
      provides: "Metric Alert for DB latency / availability"
      contains: '"runbook_url": "../../runbooks/db-down.md"'
    - path: "docs/alerts/sentry/redis-down.json"
      provides: "Metric Alert for Redis"
      contains: '"runbook_url": "../../runbooks/redis-down.md"'
    - path: "docs/alerts/sentry/queue-backing-up.json"
      provides: "Metric Alert for queue depth"
      contains: '"runbook_url": "../../runbooks/queue-backing-up.md"'
    - path: "docs/alerts/sentry/webhook-failures.json"
      provides: "Issue Alert for webhook signature/delivery failures"
      contains: '"runbook_url": "../../runbooks/webhook-failures.md"'
    - path: "docs/alerts/sentry/auth-outage.json"
      provides: "Issue Alert for auth event spike"
      contains: '"runbook_url": "../../runbooks/auth-outage.md"'
    - path: "docs/alerts/sentry/otel-exporter-failing.json"
      provides: "Issue Alert for OTEL exporter errors"
      contains: '"runbook_url": "../../runbooks/otel-exporter-failing.md"'
    - path: "docs/alerts/sentry/bull-board-inaccessible.json"
      provides: "Issue Alert for bull-board 401/403"
      contains: '"runbook_url": "../../runbooks/bull-board-inaccessible.md"'
    - path: "docs/alerts/sentry/high-error-rate.json"
      provides: "Issue Alert for general error rate"
      contains: '"runbook_url": "../../runbooks/high-error-rate.md"'
    - path: "docs/alerts/sentry/slow-checkout.json"
      provides: "Metric Alert for p95(transaction.duration) on checkout"
      contains: '"runbook_url": "../../runbooks/slow-checkout.md"'
    - path: "docs/alerts/sentry/README.md"
      provides: "import guide (3 paths) + SLO-burn-rate translation (D-15)"
      min_lines: 30
  key_links:
    - from: "docs/alerts/sentry/<slug>.json"
      to: "docs/runbooks/<slug>.md"
      via: "top-level runbook_url field"
      pattern: '"runbook_url"'
    - from: "docs/alerts/sentry/README.md"
      to: "docs/alerts/sentry/<any>.json"
      via: "Strip-before-import jq command"
      pattern: "jq 'del"
---

<objective>
Wave 2: ship the 9 Sentry alert JSON templates at docs/alerts/sentry/<slug>.json + a README.md (D-16) that documents the three operator-side import paths (sentry-cli api POST passthrough / raw curl / Sentry UI click-through — per Research Finding 3, since `sentry-cli alerts import` does NOT exist).

Each JSON file is the body the operator POSTs to the appropriate Sentry REST endpoint after stripping the runbook_url and _baseworks_meta fields with `jq 'del(.runbook_url, ._baseworks_meta)'`. Every file MUST be strict JSON (no comments — Research Finding 4 corrects D-15) parseable by `JSON.parse`. The runbook_url field is what Pass B of validate-docs.ts hard-fails on.

5 alerts are Issue Alerts (project-level, event-frequency-based): auth-outage, webhook-failures, high-error-rate, otel-exporter-failing, bull-board-inaccessible.
4 alerts are Metric Alerts (org-level, threshold-based): db-down, redis-down, queue-backing-up, slow-checkout.

Output: 10 files in docs/alerts/sentry/ — 9 JSON files + 1 README.md. Wave-0 test scripts/__tests__/alert-files-present.test.ts (10 tests: 9 alerts + 1 README) goes RED → GREEN. Pass B of validate-docs.ts validates every runbook_url. Pass B requires the runbook target to exist; Plan 03 ships the runbook .md files in parallel. The merge order between Plans 03 and 04 is irrelevant within Wave 2; Plan 05's final smoke check verifies both have landed.

This plan creates files under docs/alerts/sentry/ ONLY. No file overlap with Plan 02 (docs/observability/) or Plan 03 (docs/runbooks/). Wave-2 parallel-safe.
</objective>

<execution_context>
@C:/Projetos/baseworks/.claude/get-shit-done/workflows/execute-plan.md
@C:/Projetos/baseworks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/23-runbooks-alert-templates-observability-docs/23-CONTEXT.md
@.planning/phases/23-runbooks-alert-templates-observability-docs/23-RESEARCH.md
@.planning/phases/23-runbooks-alert-templates-observability-docs/23-PATTERNS.md
@.planning/phases/23-runbooks-alert-templates-observability-docs/23-VALIDATION.md
@.planning/phases/23-runbooks-alert-templates-observability-docs/23-01-SUMMARY.md
@docs/integrations/bullmq.md

VERBATIM Issue Alert skeleton (RESEARCH §Q1 — copy structure into 5 Issue Alert files; customize per-alert):

```json
{
  "name": "<Alert display name>",
  "actionMatch": "all",
  "filterMatch": "all",
  "frequency": 5,
  "environment": "production",
  "conditions": [
    {
      "id": "sentry.rules.conditions.event_frequency.EventFrequencyCondition",
      "value": <threshold integer>,
      "interval": "5m"
    }
  ],
  "filters": [
    {
      "id": "sentry.rules.filters.tagged_event.TaggedEventFilter",
      "key": "<tag key>",
      "match": "eq",
      "value": "<tag value>"
    }
  ],
  "actions": [
    {
      "id": "sentry.mail.actions.NotifyEmailAction",
      "targetType": "Team",
      "fallthroughType": "AllMembers",
      "targetIdentifier": "OPERATOR_TEAM_ID"
    }
  ],
  "runbook_url": "../../runbooks/<slug>.md",
  "_baseworks_meta": {
    "endpoint": "POST /api/0/projects/{org}/{project}/rules/",
    "slo_note": "<one-line SLO math>",
    "priority": "<high | medium | low>"
  }
}
```

VERBATIM Metric Alert skeleton (RESEARCH §Q1 — copy structure into 4 Metric Alert files):

```json
{
  "name": "<Alert display name>",
  "dataset": "<events | transactions | metrics>",
  "query": "<Sentry search query>",
  "aggregate": "<p95(transaction.duration) | count() | etc>",
  "timeWindow": 5,
  "thresholdType": 0,
  "resolveThreshold": <auto-resolve number | null>,
  "triggers": [
    {
      "label": "warning",
      "alertThreshold": <warning threshold>,
      "thresholdType": 0,
      "actions": []
    },
    {
      "label": "critical",
      "alertThreshold": <critical threshold>,
      "thresholdType": 0,
      "actions": [
        { "type": "email", "targetType": "team", "targetIdentifier": "OPERATOR_TEAM_ID" }
      ]
    }
  ],
  "projects": ["YOUR_PROJECT_SLUG"],
  "environment": "production",
  "runbook_url": "../../runbooks/<slug>.md",
  "_baseworks_meta": {
    "endpoint": "POST /api/0/organizations/{org}/alert-rules/",
    "slo_note": "<one-line SLO math>",
    "priority": "<high | medium | low>"
  }
}
```

Per-alert customization (locked from CONTEXT.md Discretion-default priorities + RESEARCH alert-type assignments):

| slug | type | priority | endpoint | per-alert customization |
|---|---|---|---|---|
| auth-outage | Issue | high | project-rules | name: "Auth outage — login error spike", value: 50, filter: module=auth, slo_note: "Fires when ≥50 auth-tagged events occur within a 5m window." |
| webhook-failures | Issue | medium | project-rules | name: "Webhook failures — Stripe/Pagar.me delivery errors", value: 10, filter: route=/api/webhooks/*, slo_note: "Fires when ≥10 webhook-route events occur within a 5m window." |
| high-error-rate | Issue | medium | project-rules | name: "High error rate — overall events spike", value: 100, filter: level=error, slo_note: "Fires when ≥100 error-level events occur within a 5m window across the project." |
| otel-exporter-failing | Issue | low | project-rules | name: "OTEL exporter failing — observability silent", value: 5, filter: logger=observability, slo_note: "Fires when ≥5 observability-logger events occur within a 5m window (transport errors)." |
| bull-board-inaccessible | Issue | low | project-rules | name: "Bull-board inaccessible — admin queue UI failures", value: 5, filter: route=/admin/bull-board, slo_note: "Fires when ≥5 bull-board-route 4xx events occur within a 5m window." |
| db-down | Metric | high | org-rules | name: "DB down — Postgres availability", dataset=metrics, query: "metric:health.db.status", aggregate: "count()", warning=1, critical=3, slo_note: "Fast-burn alert: 3 consecutive 5m windows with health.db.status=down." |
| redis-down | Metric | high | org-rules | name: "Redis down — cache/queue backend", dataset=metrics, query: "metric:health.redis.status", aggregate: "count()", warning=1, critical=3, slo_note: "Fast-burn alert: 3 consecutive 5m windows with health.redis.status=down." |
| queue-backing-up | Metric | low | org-rules | name: "Queue backing up — BullMQ depth runaway", dataset=metrics, query: "metric:queue.waiting_count", aggregate: "max()", warning=500, critical=1000, slo_note: "Fast-burn alert: max queue waiting count >1000 over a 5m window." |
| slow-checkout | Metric | medium | org-rules | name: "Slow checkout — p95 latency", dataset=transactions, query: "event.type:transaction transaction:/api/billing/checkout", aggregate: "p95(transaction.duration)", warning=2000, critical=5000, resolveThreshold=1500, slo_note: "Fast-burn alert: ≥2% monthly latency budget over a 5m window. timeWindow=5 + alertThreshold gating equivalent to Prometheus `for: 5m`." |

CRITICAL constraints:
- ALL JSON files MUST be parseable by `JSON.parse` — NO comments, NO trailing commas. The "// SLO note" form from D-15 is WRONG — per Research Finding 4, the SLO note ships as `_baseworks_meta.slo_note` field.
- The `runbook_url` value is repo-relative-from-the-JSON-file: from `docs/alerts/sentry/db-down.json` the value `"../../runbooks/db-down.md"` resolves (via `node:path.join + dirname`) to `docs/runbooks/db-down.md`.
- Pre-merge validation: `node -e "JSON.parse(require('fs').readFileSync('docs/alerts/sentry/<slug>.json'))"` exits 0 for every file.
- All `targetIdentifier` placeholders are `OPERATOR_TEAM_ID` and project slugs are `YOUR_PROJECT_SLUG` — operator replaces these during import (validate-docs.ts invariant 2 will catch any accidentally committed real Sentry team/project IDs that match its secret-shape regexes).

`docs/alerts/sentry/README.md` content per D-16 + RESEARCH §Q2 — must include:
1. Header: "# Sentry Alert Templates"
2. Overview paragraph: what these alerts are + why they exist + the runbook_url annotation cross-link.
3. Upstream Documentation section: links to docs.sentry.io/api/alerts/ for Issue Alerts and Metric Alerts.
4. Env vars table (mirror docs/integrations/bullmq.md:17-21 shape):
   | Env var | Required | Purpose |
   | --- | --- | --- |
   | SENTRY_AUTH_TOKEN | yes for CLI/curl import | scope: alerts:write or project:write |
   | SENTRY_ORG | yes for CLI/curl import | your Sentry org slug |
   | SENTRY_PROJECT | yes for project-level (Issue) alerts | your Sentry project slug |
5. THREE import paths section (Research Finding 3):
   - **Path A — sentry-cli api POST (RECOMMENDED for CLI users):**
     ```bash
     # Issue alerts (project-level):
     jq 'del(.runbook_url, ._baseworks_meta)' docs/alerts/sentry/auth-outage.json \
       | sentry-cli api --auth-token "$SENTRY_AUTH_TOKEN" \
           POST "/projects/$SENTRY_ORG/$SENTRY_PROJECT/rules/" \
           --data @-

     # Metric alerts (org-level):
     jq 'del(.runbook_url, ._baseworks_meta)' docs/alerts/sentry/slow-checkout.json \
       | sentry-cli api --auth-token "$SENTRY_AUTH_TOKEN" \
           POST "/organizations/$SENTRY_ORG/alert-rules/" \
           --data @-
     ```
     **NOTE:** `sentry-cli alerts import` does NOT exist. The generic `sentry-cli api` REST passthrough is the canonical CLI import path.
   - **Path B — raw curl (for environments without sentry-cli):**
     ```bash
     jq 'del(.runbook_url, ._baseworks_meta)' docs/alerts/sentry/auth-outage.json \
       | curl -X POST "https://sentry.io/api/0/projects/$SENTRY_ORG/$SENTRY_PROJECT/rules/" \
           -H "Authorization: Bearer $SENTRY_AUTH_TOKEN" \
           -H "Content-Type: application/json" \
           --data @-
     ```
   - **Path C — Sentry UI click-through (manual fallback):**
     1. Sentry → Project (or Organization) → Alerts → Create Alert.
     2. Choose "Issue Alert" or "Metric Alert" per the file's `_baseworks_meta.endpoint`.
     3. Build the rule via wizard using the values in the JSON file as a guide.
     4. Save.
     **NOTE:** Newer Sentry UI versions may have hidden the JSON-edit toggle; if so, fall back to wizard-driven entry using the JSON values as a reference.
6. SLO burn-rate translation explainer (D-15, ~10-30 lines):
   - Sentry's API does not speak Prometheus burn-rate directly.
   - Short-window detection → Issue Alert with `actionMatch: "all"` and 5-15m frequency.
   - Equivalent of Prometheus `for: 5m` → `triggers[].alertThreshold` paired with `timeWindow: 5` (Metric Alert).
   - Burn-rate math (e.g., "fast burn = >2% of monthly budget in 1h") is documented in each alert JSON's `_baseworks_meta.slo_note` field.
7. Forward-looking note (D-13): "When a fork user wires OTLP/Grafana in v1.4+, they author Grafana YAML themselves. The observability ports are vendor-agnostic so these Sentry templates remain useful even after Grafana is added."
8. The strip-before-import idiom: `jq 'del(.runbook_url, ._baseworks_meta)'` is the canonical strip command. Document why: Sentry's API may reject unknown top-level fields (RESEARCH §Q1 footnote) — strip first, import second.
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → Sentry SaaS | Operator POSTs JSON bodies to sentry.io. Threat surface = real auth tokens / org IDs / team IDs leaking into the repo if the templates are accidentally filled in. |
| validate-docs.ts → docs/alerts/sentry/*.json | Validator reads files via Bun.Glob + node:fs/promises.readFile + JSON.parse. Path traversal bounded to ROOT. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-18 | Information Disclosure | docs/alerts/sentry/*.json containing real Sentry org/team/project IDs | mitigate | All targetIdentifier values use the placeholder `OPERATOR_TEAM_ID`; all projects values use `YOUR_PROJECT_SLUG`. validate-docs.ts invariant 2 catches sk_live_* / re_* / whsec_* shapes if accidentally pasted. The 9 files are templates, not provisioned config. |
| T-23-19 | Tampering | malformed JSON breaking Pass B | mitigate | Wave-0 test alert-files-present.test.ts uses JSON.parse on every file — RED until valid JSON lands. Pass B's JSON.parse-then-validate-runbook_url is the second gate. CI fails red on any malformed file. |
| T-23-20 | Information Disclosure | docs/alerts/sentry/README.md leaking real auth tokens in CLI examples | mitigate | All command examples use `$SENTRY_AUTH_TOKEN`, `$SENTRY_ORG`, `$SENTRY_PROJECT` shell-variable references. No literal credentials. |
| T-23-21 | Tampering | runbook_url path traversal | mitigate | `runbook_url` values are constrained to `../../runbooks/<slug>.md`. The validator's `existsSync(join(ROOT, dirname(relPath), runbookUrl))` resolves within ROOT bounds; Bun's `path.join` handles `..` safely against ROOT. No symlink following (the validator uses `existsSync`, not `realpath`, but the docs/ tree is repo-controlled). |
| T-23-22 | Repudiation | Sentry rejecting unknown top-level fields silently | accept | Documented in README path A/B with `jq 'del(.runbook_url, ._baseworks_meta)'` as the strip-before-import idiom. Operator UAT confirms; if Sentry strict-rejects, the strip command is the canonical workaround. |
| T-23-23 | Denial of Service | extremely large alert JSON files | accept | Templates are bounded in size (~50-150 lines each). No untrusted external input flows. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create the 5 Issue Alert JSON files (auth-outage, webhook-failures, high-error-rate, otel-exporter-failing, bull-board-inaccessible)</name>
  <files>
    docs/alerts/sentry/auth-outage.json,
    docs/alerts/sentry/webhook-failures.json,
    docs/alerts/sentry/high-error-rate.json,
    docs/alerts/sentry/otel-exporter-failing.json,
    docs/alerts/sentry/bull-board-inaccessible.json
  </files>
  <read_first>
    - .planning/phases/23-runbooks-alert-templates-observability-docs/23-RESEARCH.md §Q1 (Issue Alert JSON shape) — full skeleton
    - .planning/phases/23-runbooks-alert-templates-observability-docs/23-PATTERNS.md "docs/alerts/sentry/<slug>.json" section
    - The per-alert customization table in this plan's `<context>` block
  </read_first>
  <action>
    Create 5 JSON files using the VERBATIM Issue Alert skeleton from `<context>`. Per-alert customization comes from the table in `<context>`.

    For each file:
    1. Copy the Issue Alert skeleton structure verbatim.
    2. Replace `<Alert display name>`, `<threshold integer>`, `<tag key>`, `<tag value>`, `<slug>`, `<one-line SLO math>`, `<high|medium|low>` with the per-alert values from the customization table.
    3. Set `runbook_url` to `../../runbooks/<slug>.md` (e.g., `../../runbooks/auth-outage.md`).
    4. Set `_baseworks_meta.endpoint` to `"POST /api/0/projects/{org}/{project}/rules/"` (Issue Alerts go to project-rules).
    5. NO COMMENTS. JSON.parse must succeed on every file. The SLO note goes in `_baseworks_meta.slo_note` as a string value.

    Example final shape for auth-outage.json (use this exactly, then customize the other 4 per the table):

    ```json
    {
      "name": "Auth outage — login error spike",
      "actionMatch": "all",
      "filterMatch": "all",
      "frequency": 5,
      "environment": "production",
      "conditions": [
        {
          "id": "sentry.rules.conditions.event_frequency.EventFrequencyCondition",
          "value": 50,
          "interval": "5m"
        }
      ],
      "filters": [
        {
          "id": "sentry.rules.filters.tagged_event.TaggedEventFilter",
          "key": "module",
          "match": "eq",
          "value": "auth"
        }
      ],
      "actions": [
        {
          "id": "sentry.mail.actions.NotifyEmailAction",
          "targetType": "Team",
          "fallthroughType": "AllMembers",
          "targetIdentifier": "OPERATOR_TEAM_ID"
        }
      ],
      "runbook_url": "../../runbooks/auth-outage.md",
      "_baseworks_meta": {
        "endpoint": "POST /api/0/projects/{org}/{project}/rules/",
        "slo_note": "Fires when ≥50 auth-tagged events occur within a 5m window.",
        "priority": "high"
      }
    }
    ```

    Repeat for webhook-failures, high-error-rate, otel-exporter-failing, bull-board-inaccessible — each with its own threshold, filter key/value, name, slo_note, priority per the table.

    Tag/filter mappings:
    - webhook-failures: filter on tag `route` `eq` `/api/webhooks/*` (or `module` `eq` `billing` if route tag isn't yet on events — researcher-discretion at exec time). value: 10.
    - high-error-rate: filter on tag `level` `eq` `error`. value: 100.
    - otel-exporter-failing: filter on tag `logger` `eq` `observability`. value: 5.
    - bull-board-inaccessible: filter on tag `route` `eq` `/admin/bull-board`. value: 5.

    Use the Write tool. Validate JSON parseability after each file with `node -e "JSON.parse(require('fs').readFileSync('docs/alerts/sentry/<slug>.json'))"`.
  </action>
  <verify>
    <automated>for f in auth-outage webhook-failures high-error-rate otel-exporter-failing bull-board-inaccessible; do node -e "JSON.parse(require('fs').readFileSync('docs/alerts/sentry/$f.json'))" || exit 1; done && echo "all 5 valid JSON"</automated>
  </verify>
  <acceptance_criteria>
    - All 5 files exist under docs/alerts/sentry/.
    - For each file: `node -e "JSON.parse(require('fs').readFileSync('docs/alerts/sentry/<slug>.json'))"` exits 0.
    - For each file: `jq -e '.runbook_url' docs/alerts/sentry/<slug>.json` exits 0 and emits the expected `../../runbooks/<slug>.md`.
    - For each file: `jq -e '._baseworks_meta.endpoint' docs/alerts/sentry/<slug>.json` exits 0 and emits the project-rules endpoint string.
    - For each file: `jq -e '._baseworks_meta.slo_note' docs/alerts/sentry/<slug>.json` exits 0.
    - For each file: `jq -e '._baseworks_meta.priority' docs/alerts/sentry/<slug>.json` exits 0 and emits one of `high`, `medium`, `low` matching the table.
    - For each file: `jq -e '.actionMatch' docs/alerts/sentry/<slug>.json` returns `"all"`.
    - For each file: `grep -c "//" docs/alerts/sentry/<slug>.json` returns 0 (NO comments — D-15 misreading correction).
    - For each file: `grep -c "OPERATOR_TEAM_ID" docs/alerts/sentry/<slug>.json` returns at least 1 (placeholder used).
  </acceptance_criteria>
  <done>5 Issue Alert JSON files ship. Each is valid JSON, has the runbook_url + _baseworks_meta wrapper, and uses placeholder org/team IDs.</done>
</task>

<task type="auto">
  <name>Task 2: Create the 4 Metric Alert JSON files (db-down, redis-down, queue-backing-up, slow-checkout)</name>
  <files>
    docs/alerts/sentry/db-down.json,
    docs/alerts/sentry/redis-down.json,
    docs/alerts/sentry/queue-backing-up.json,
    docs/alerts/sentry/slow-checkout.json
  </files>
  <read_first>
    - .planning/phases/23-runbooks-alert-templates-observability-docs/23-RESEARCH.md §Q1 (Metric Alert JSON shape) — full skeleton
    - The per-alert customization table in this plan's `<context>` block
  </read_first>
  <action>
    Create 4 JSON files using the VERBATIM Metric Alert skeleton from `<context>`. Per-alert customization from the table:

    Example final shape for slow-checkout.json (the canonical Metric Alert example from RESEARCH §Q1):

    ```json
    {
      "name": "Slow checkout — p95 latency",
      "dataset": "transactions",
      "query": "event.type:transaction transaction:/api/billing/checkout",
      "aggregate": "p95(transaction.duration)",
      "timeWindow": 5,
      "thresholdType": 0,
      "resolveThreshold": 1500,
      "triggers": [
        { "label": "warning", "alertThreshold": 2000, "thresholdType": 0, "actions": [] },
        {
          "label": "critical",
          "alertThreshold": 5000,
          "thresholdType": 0,
          "actions": [
            { "type": "email", "targetType": "team", "targetIdentifier": "OPERATOR_TEAM_ID" }
          ]
        }
      ],
      "projects": ["YOUR_PROJECT_SLUG"],
      "environment": "production",
      "runbook_url": "../../runbooks/slow-checkout.md",
      "_baseworks_meta": {
        "endpoint": "POST /api/0/organizations/{org}/alert-rules/",
        "slo_note": "Fast-burn alert: ≥2% monthly latency budget over a 5m window. timeWindow=5 + alertThreshold gating equivalent to Prometheus `for: 5m`.",
        "priority": "medium"
      }
    }
    ```

    For db-down, redis-down, queue-backing-up: customize per the table:

    db-down.json:
    - name: "DB down — Postgres availability"
    - dataset: "metrics", query: "metric:health.db.status", aggregate: "count()"
    - triggers: warning alertThreshold=1, critical alertThreshold=3
    - resolveThreshold: 0 (or null/omit)
    - runbook_url: "../../runbooks/db-down.md"
    - priority: "high"

    redis-down.json: parallel structure to db-down with `metric:health.redis.status` query and `redis-down.md` runbook_url.

    queue-backing-up.json:
    - name: "Queue backing up — BullMQ depth runaway"
    - dataset: "metrics", query: "metric:queue.waiting_count", aggregate: "max()"
    - triggers: warning=500, critical=1000
    - runbook_url: "../../runbooks/queue-backing-up.md"
    - priority: "low"

    NOTE on dataset: Sentry's metrics dataset for custom metrics may not yet be wired in v1.3 (Phase 21 deferred). The `dataset: "metrics"` + `query: "metric:health.db.status"` queries are forward-compatible — when v1.4+ wires OTLP, these queries will resolve against custom metrics. For v1.3 operators using only Sentry's built-in error tracking, the db-down/redis-down/queue-backing-up alerts MAY need to be reconfigured to use Sentry's transaction or events dataset; document this caveat in `_baseworks_meta.slo_note` for these three files (e.g., "Forward-looking: requires custom metrics wiring (v1.4+ OTLP). For v1.3, monitor via /health/detailed manually or convert to Issue Alert on `health.db.status=down` log events.").

    All 4 files use `_baseworks_meta.endpoint = "POST /api/0/organizations/{org}/alert-rules/"`.

    NO COMMENTS. JSON.parse must succeed.
  </action>
  <verify>
    <automated>for f in db-down redis-down queue-backing-up slow-checkout; do node -e "JSON.parse(require('fs').readFileSync('docs/alerts/sentry/$f.json'))" || exit 1; done && bun test scripts/__tests__/alert-files-present.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 4 files exist under docs/alerts/sentry/.
    - For each file: `node -e "JSON.parse(...)"` exits 0.
    - For each file: `jq -e '.runbook_url' docs/alerts/sentry/<slug>.json` exits 0 and emits `../../runbooks/<slug>.md`.
    - For each file: `jq -e '._baseworks_meta.endpoint' docs/alerts/sentry/<slug>.json` returns the org-rules endpoint string.
    - For each file: `jq -e '.triggers[0].alertThreshold' docs/alerts/sentry/<slug>.json` returns a number.
    - For each file: `jq -e '.projects[0]' docs/alerts/sentry/<slug>.json` returns "YOUR_PROJECT_SLUG".
    - alert-files-present.test.ts: 9/10 tests pass (9 alert files + the README test, which is still RED until Task 3).
    - For each file: `grep -c "//" docs/alerts/sentry/<slug>.json` returns 0.
  </acceptance_criteria>
  <done>4 Metric Alert JSON files ship. Combined with Task 1's 5 Issue Alerts: all 9 alerts in place. alert-files-present.test.ts is 9/10 GREEN (README pending Task 3).</done>
</task>

<task type="auto">
  <name>Task 3: Create docs/alerts/sentry/README.md (~50-100 lines, 3 import paths + SLO burn-rate explainer)</name>
  <files>docs/alerts/sentry/README.md</files>
  <read_first>
    - docs/integrations/bullmq.md (mirror header pattern lines 1-11, env-var table lines 17-21, smoke-test pattern lines 27-35)
    - .planning/phases/23-runbooks-alert-templates-observability-docs/23-RESEARCH.md §Q2 (3 import paths, lines 300-322 verbatim)
    - .planning/phases/23-runbooks-alert-templates-observability-docs/23-PATTERNS.md "docs/alerts/sentry/README.md" section
    - .planning/phases/23-runbooks-alert-templates-observability-docs/23-CONTEXT.md D-13 (forward-looking Grafana note) + D-15 (SLO translation) + D-16 (README scope)
  </read_first>
  <action>
    Create docs/alerts/sentry/README.md (~50-100 lines).

    Use the structure outlined in `<context>` "docs/alerts/sentry/README.md content" — apply VERBATIM the 8-section sequence:

    1. **# Sentry Alert Templates** (H1).

    2. **Overview** (~5-line paragraph): explain the 9 alert templates, their relationship to the runbooks, and the operator's role (these are templates the operator imports; one per runbook).

    3. **Upstream Documentation** section (mirror docs/integrations/bullmq.md:7-11):
       - [Sentry Alerts API](https://docs.sentry.io/api/alerts/)
       - [Issue Alert config](https://docs.sentry.io/product/alerts/create-alerts/issue-alert-config/)
       - [Metric Alert (alert rules)](https://docs.sentry.io/product/alerts/create-alerts/metric-alert-config/)

    4. **Required environment variables** (table — mirror docs/integrations/bullmq.md:17-21):
       ```markdown
       | Env var | Required | Purpose |
       | --- | --- | --- |
       | `SENTRY_AUTH_TOKEN` | yes for CLI/curl import | Sentry auth token with scope `alerts:write` (project) or `org:write` (org-level alerts). |
       | `SENTRY_ORG` | yes for CLI/curl import | Your Sentry organization slug. |
       | `SENTRY_PROJECT` | yes for project-level alerts | Your Sentry project slug. Required by Issue Alerts (project-level endpoint). |
       ```

    5. **Importing alerts — three paths** section. CRITICAL header note up front: `sentry-cli alerts import` does NOT exist (Research Finding 3). The three paths are:

       **Path A — sentry-cli api POST passthrough (recommended for CLI):**
       Verbatim shell block from `<context>` Path A.

       **Path B — raw curl (no CLI dep):**
       Verbatim shell block from `<context>` Path B.

       **Path C — Sentry UI click-through (manual fallback):**
       Verbatim 4-step list from `<context>` Path C.

    6. **The strip-before-import idiom** section (~10 lines): explain why `jq 'del(.runbook_url, ._baseworks_meta)'` is required. Quote: "Sentry's REST API may reject unknown top-level fields. Strip before import; runbook_url is for Baseworks's validate-docs.ts gate, _baseworks_meta is for operator reference."

    7. **SLO burn-rate translation** section (~15-20 lines, D-15):
       - Sentry does not speak Prometheus burn-rate. Translation reference:
         - Short-window detection → Issue Alert with `actionMatch: "all"` and 5-15m frequency.
         - Equivalent of Prometheus `for: 5m` → Metric Alert `triggers[].alertThreshold` paired with `timeWindow: 5`.
         - Burn-rate math (e.g., "fast burn = >2% of monthly budget in 1h") is documented in each alert JSON's `_baseworks_meta.slo_note` field.

    8. **Forward-looking note (Grafana)** (~5-10 lines, D-13): "When a fork user wires OTLP/Grafana in v1.4+, they author Grafana YAML themselves. The observability ports are vendor-agnostic so these Sentry templates remain useful as a reference for naming conventions and threshold defaults even after Grafana is added."

    9. **Customizing runbook_url for forks** (~5 lines, D-16): If the operator forks docs to a different path layout, they update each `runbook_url` value to match. The validate-docs.ts Pass B uses repo-relative `path.join(ROOT, dirname(jsonPath), runbookUrl)` — the value is path-resolved from the JSON file's directory.

    Tone: declarative, technical-precise. No frontmatter. No screenshots.
  </action>
  <verify>
    <automated>test -f docs/alerts/sentry/README.md && grep -c "sentry-cli alerts import" docs/alerts/sentry/README.md && grep -c "jq 'del" docs/alerts/sentry/README.md && bun test scripts/__tests__/alert-files-present.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File docs/alerts/sentry/README.md exists.
    - `grep -c "^# Sentry Alert Templates" docs/alerts/sentry/README.md` returns 1.
    - `grep -c "sentry-cli api" docs/alerts/sentry/README.md` returns at least 1 (Path A documented).
    - `grep -c "does NOT exist" docs/alerts/sentry/README.md` returns at least 1 (the negative claim about `sentry-cli alerts import` is explicit).
    - `grep -c "jq 'del" docs/alerts/sentry/README.md` returns at least 1 (strip idiom documented).
    - `grep -c "_baseworks_meta" docs/alerts/sentry/README.md` returns at least 1.
    - `grep -c "Path A" docs/alerts/sentry/README.md` returns at least 1.
    - `grep -c "Path B" docs/alerts/sentry/README.md` returns at least 1.
    - `grep -c "Path C" docs/alerts/sentry/README.md` returns at least 1.
    - `grep -c "burn-rate" docs/alerts/sentry/README.md` returns at least 1.
    - `grep -c "frequency: 5" docs/alerts/sentry/README.md` returns at least 1 OR `grep -c "timeWindow: 5" docs/alerts/sentry/README.md` returns at least 1.
    - File line count ≥ 30 (`wc -l docs/alerts/sentry/README.md` ≥ 30).
    - No frontmatter: first line is not `---`.
    - No markdown image refs.
    - alert-files-present.test.ts: all 10/10 tests pass GREEN (9 alerts + README).
  </acceptance_criteria>
  <done>The README ships with the 3-path import guide, strip idiom, SLO burn-rate explainer, and Grafana forward-looking note. The `sentry-cli alerts import` non-existence is documented explicitly. alert-files-present.test.ts is fully GREEN.</done>
</task>

</tasks>

<verification>
At the end of this plan:
- 9 alert JSON files + 1 README.md under docs/alerts/sentry/.
- All 10 tests in scripts/__tests__/alert-files-present.test.ts pass GREEN.
- Every JSON file is parseable by JSON.parse (no comments).
- Every JSON file has the top-level runbook_url string + _baseworks_meta wrapper.
- The 5 Issue Alerts use endpoint `POST /api/0/projects/{org}/{project}/rules/`; the 4 Metric Alerts use `POST /api/0/organizations/{org}/alert-rules/`.
- README documents the 3 import paths, strip idiom, and SLO burn-rate translation.
- `bun run validate` exits 0 ONCE Plan 03 has merged (Pass B requires the runbook .md targets to exist; until Plan 03 merges, Pass B fails on the 9 dangling runbook_url paths). Plan 05 verifies the full suite.
</verification>

<success_criteria>
- All 3 tasks complete with their `<acceptance_criteria>` met.
- All 9 JSON files have validated JSON.parse-ability (no comments per Research Finding 4).
- README documents `sentry-cli alerts import` does NOT exist (Research Finding 3).
- All `runbook_url` values are `../../runbooks/<matching-slug>.md` (so Pass B resolves correctly once Plan 03 lands).
- `tsc --noEmit` and `biome check .` pass (no source changes; pure docs + JSON).
</success_criteria>

<output>
After completion, create `.planning/phases/23-runbooks-alert-templates-observability-docs/23-04-SUMMARY.md` documenting:
- Final per-alert customization (name, threshold, filter key/value, priority, slo_note) in a table.
- README final line count + heading structure.
- Any deviation from the per-alert customization table in this plan's `<context>` (e.g., if a tag like `module=auth` does not yet exist in actual emitted events at exec time, document the substitute used and the operator-side action needed to align).
- Any auto-fix Rule-1 deviations from the plan text.
- Final Wave-0 test status: 10/10 alert tests GREEN.
- Note on dataset/metrics availability for db-down/redis-down/queue-backing-up (forward-looking caveat for v1.4+ OTLP).
</output>
