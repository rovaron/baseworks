---
phase: 23
plan: 05
type: execute
wave: 3
depends_on: [23-01, 23-02, 23-03, 23-04]
files_modified:
  - docs/README.md
autonomous: false
requirements:
  - DOC-03
  - DOC-04
must_haves:
  truths:
    - "docs/README.md has a new ## Operations section listing entries for docs/observability/, docs/runbooks/, and docs/alerts/sentry/."
    - "Full-suite `bun run validate` exits 0 against the complete corpus (4th invariant Pass A validates 9 runbooks' cross-links; Pass B validates 9 Sentry alert JSONs' runbook_url fields; Mermaid floor 11 satisfied)."
    - "All 6 Wave-0 unit-test files pass GREEN: validate-docs.test.ts (Plan 01), runbook-files-present.test.ts, runbook-section-shape.test.ts, runbook-no-screenshots.test.ts, alert-files-present.test.ts, observability-docs-present.test.ts."
    - "Smoke-test PR (manual checkpoint) confirms validate.yml fails red on a deliberately-broken runbook_url and goes green when reverted."
  artifacts:
    - path: "docs/README.md"
      provides: "appended ## Operations section linking to obs/runbooks/alerts"
      contains: "## Operations"
  key_links:
    - from: "docs/README.md"
      to: "docs/observability/README.md"
      via: "Operations table entry"
      pattern: "observability/README.md"
    - from: "docs/README.md"
      to: "docs/runbooks/"
      via: "Operations table entry"
      pattern: "runbooks/"
    - from: "docs/README.md"
      to: "docs/alerts/sentry/README.md"
      via: "Operations table entry"
      pattern: "alerts/sentry/README.md"
---

<objective>
Wave 3: append the Operations section to docs/README.md (per RESEARCH §Q6 + PATTERNS.md "docs/README.md") linking to the three new docs surfaces (observability, runbooks, alerts/sentry) shipped in Plans 02/03/04. Then perform the final phase-level validation: run the full Wave-0 test suite, run `bun run validate` end-to-end, and execute the manual smoke-test PR checkpoint per VALIDATION.md "Manual-Only Verifications" — submit a deliberately-broken-runbook_url PR, watch validate.yml fail red, revert, watch it go green.

Purpose: Phase 23 verification gate. After Plans 01-04 land, the operator-facing surface is in place. Plan 05 indexes it from the top-level docs README and proves the CI gate works end-to-end.

Output: 1-line edit to docs/README.md (append a section). 1 manual-checkpoint task. The phase is complete after Plan 05.

This plan does NOT modify scripts/validate-docs.ts, any obs concept doc, any runbook, or any Sentry alert JSON. It only updates docs/README.md and runs verification.
</objective>

<execution_context>
@C:/Projetos/baseworks/.claude/get-shit-done/workflows/execute-plan.md
@C:/Projetos/baseworks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/23-runbooks-alert-templates-observability-docs/23-CONTEXT.md
@.planning/phases/23-runbooks-alert-templates-observability-docs/23-VALIDATION.md
@.planning/phases/23-runbooks-alert-templates-observability-docs/23-01-SUMMARY.md
@.planning/phases/23-runbooks-alert-templates-observability-docs/23-02-SUMMARY.md
@.planning/phases/23-runbooks-alert-templates-observability-docs/23-03-SUMMARY.md
@.planning/phases/23-runbooks-alert-templates-observability-docs/23-04-SUMMARY.md
@docs/README.md

VERBATIM Operations section to append (RESEARCH §Q6 lines 619-634):

```markdown
## Operations

| Document | Purpose |
| --- | --- |
| [observability/README.md](./observability/README.md) | Index for the observability concept docs (attributes, cardinality, trace propagation). |
| [observability/attributes.md](./observability/attributes.md) | Glossary of legitimate context attributes (lives on span/log/metric, type, cardinality risk). |
| [observability/cardinality.md](./observability/cardinality.md) | Cardinality rules + Baseworks-specific high-card values forbidden as metric labels. |
| [observability/trace-propagation.md](./observability/trace-propagation.md) | Single-trace flow API → DB → enqueue → worker (Mermaid). |
| [runbooks/](./runbooks/) | 9 incident runbooks (DB down, Redis down, queue backing up, webhook failures, auth outage, OTEL exporter failing, bull-board inaccessible, high error rate, slow checkout). |
| [alerts/sentry/README.md](./alerts/sentry/README.md) | Sentry alert templates — import via sentry-cli api / curl / UI; SLO-burn-rate translation. |
```

Insertion point in docs/README.md: AFTER the existing `## Contents` table (lines 11-24) and BEFORE the existing `## Tone` section (line 26). The current docs/README.md structure (lines 1-44 confirmed): H1 → blurb → Reading Order → Contents → Tone → Code Citations → Mermaid Diagrams. Insert "## Operations" between Contents and Tone.
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/README.md → reader | Plain markdown. Threat surface = stale or incorrect index entries leading the operator to a wrong place. |
| Smoke-test PR → CI | Operator submits a PR containing a deliberately-broken runbook_url; CI runs validate.yml. The PR is reverted before merge. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-24 | Tampering | docs/README.md Operations entries pointing to wrong files | mitigate | Each entry uses the canonical path. The validator's existing 3 invariants check the README markdown; while Pass A is scoped to docs/runbooks/ (not docs/), the entries must still resolve at click time — verified manually in Task 1's acceptance grep. |
| T-23-25 | Repudiation | smoke-test PR accidentally merging to main | mitigate | The smoke-test is a draft PR per checkpoint instructions. Resume signal explicitly says "revert before merge." Manual checkpoint with human approval gate. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Append the ## Operations section to docs/README.md</name>
  <files>docs/README.md</files>
  <read_first>
    - docs/README.md (read full file to understand the existing structure; insertion point is between `## Contents` and `## Tone`)
    - .planning/phases/23-runbooks-alert-templates-observability-docs/23-RESEARCH.md §Q6 (lines 619-634 verbatim Operations section)
  </read_first>
  <action>
    Use the Edit tool to insert a new `## Operations` section into docs/README.md between the existing `## Contents` table (ends at line 24) and `## Tone` (starts at line 26).

    The new section text is the VERBATIM block from `<context>`. Insert with a leading blank line and a trailing blank line so the section is visually separated:

    ```markdown
    
    ## Operations
    
    | Document | Purpose |
    | --- | --- |
    | [observability/README.md](./observability/README.md) | Index for the observability concept docs (attributes, cardinality, trace propagation). |
    | [observability/attributes.md](./observability/attributes.md) | Glossary of legitimate context attributes (lives on span/log/metric, type, cardinality risk). |
    | [observability/cardinality.md](./observability/cardinality.md) | Cardinality rules + Baseworks-specific high-card values forbidden as metric labels. |
    | [observability/trace-propagation.md](./observability/trace-propagation.md) | Single-trace flow API → DB → enqueue → worker (Mermaid). |
    | [runbooks/](./runbooks/) | 9 incident runbooks (DB down, Redis down, queue backing up, webhook failures, auth outage, OTEL exporter failing, bull-board inaccessible, high error rate, slow checkout). |
    | [alerts/sentry/README.md](./alerts/sentry/README.md) | Sentry alert templates — import via sentry-cli api / curl / UI; SLO-burn-rate translation. |
    
    ```

    Verify with `bun run validate` that the corpus is still passing (the new docs/README.md links to docs/observability/* and docs/alerts/sentry/README.md and docs/runbooks/ — none of these are validated by Pass A or Pass B because:
    - Pass A only scans docs/runbooks/ files. docs/README.md is not in that scope.
    - Pass B only scans docs/alerts/sentry/*.json. docs/README.md is not in that scope.
    - Generic broken-link detection across all of docs/ is explicitly OUT of scope per D-10 (deferred to v1.4 docs-quality phase).

    However, all linked files MUST exist (Plans 02/03/04 should have shipped them). If any are missing at the time this task runs, the task fails — file an issue back to the appropriate plan rather than working around.

    After the edit, run the full Wave-0 test suite to confirm everything is GREEN:
    ```bash
    bun test scripts/__tests__/
    bun run validate
    ```
  </action>
  <verify>
    <automated>grep -c "^## Operations" docs/README.md && grep -c "observability/README.md" docs/README.md && grep -c "alerts/sentry/README.md" docs/README.md && bun test scripts/__tests__/ && bun run validate</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Operations" docs/README.md` returns 1.
    - `grep -c "observability/README.md" docs/README.md` returns at least 1.
    - `grep -c "observability/attributes.md" docs/README.md` returns at least 1.
    - `grep -c "observability/cardinality.md" docs/README.md` returns at least 1.
    - `grep -c "observability/trace-propagation.md" docs/README.md` returns at least 1.
    - `grep -c "runbooks/" docs/README.md` returns at least 1.
    - `grep -c "alerts/sentry/README.md" docs/README.md` returns at least 1.
    - `bun test scripts/__tests__/` reports all tests passing GREEN. Specifically:
      - validate-docs.test.ts: 7/7 passing.
      - runbook-files-present.test.ts: 9/9 passing.
      - runbook-section-shape.test.ts: 9/9 passing.
      - runbook-no-screenshots.test.ts: 9/9 passing.
      - alert-files-present.test.ts: 10/10 passing.
      - observability-docs-present.test.ts: 5/5 passing.
      - TOTAL: 49/49 unit tests GREEN.
    - `bun run validate` exits 0. Output includes:
      - "OK: found 11 Mermaid fenced blocks" (or similar — count is 11).
      - No `[validate-docs] FAIL:` lines.
      - Final line `[validate-docs] PASS`.
    - All 9 entries in the Operations table point to files that exist (visual + path-spot-check).
    - `tsc --noEmit` and `biome check .` both pass.
  </acceptance_criteria>
  <done>docs/README.md indexes all three new documentation surfaces. The full Wave-0 unit-test suite is GREEN. `bun run validate` is GREEN against the complete corpus. The local CI gate is fully proven.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Smoke-test the validate.yml CI gate against a deliberately-broken runbook_url</name>
  <files>(no files modified — manual CI smoke-test against the corpus shipped in Plans 01-04 + Task 1 of this plan)</files>
  <action>This is a manual checkpoint. See <how-to-verify> below for the smoke-test procedure. The executor halts here, waits for the operator to perform the steps, then resumes when the operator types the resume signal.</action>
  <what-built>
    - .github/workflows/validate.yml (Plan 01) running on PR + push to main.
    - 4th invariant in scripts/validate-docs.ts (Plan 01) detecting broken runbook_url + cross-runbook links.
    - Full content corpus (Plans 02/03/04) covering 4 obs concept docs, 9 runbooks, 9 alert JSONs, sentry README, top-level README index update.

    The CI gate has not yet been exercised on a real GitHub Actions run. This checkpoint proves the gate works end-to-end via the smoke-test described in 23-VALIDATION.md "Manual-Only Verifications".
  </what-built>
  <how-to-verify>
    Perform the smoke test in two phases:

    **Phase 1 — RED smoke (gate fails on broken runbook_url):**

    1. Create a branch from main called `smoke/23-validate-broken-runbook-url`.
    2. Edit ONE Sentry alert JSON to have a deliberately-broken runbook_url. Example: in `docs/alerts/sentry/db-down.json`, change `"runbook_url": "../../runbooks/db-down.md"` to `"runbook_url": "../../runbooks/this-does-not-exist.md"`.
    3. Commit + push the branch + open a PR against main.
    4. Watch the `Validate — docs + cross-link integrity` workflow run on the PR.
    5. **EXPECTED:** the `validate-docs` job FAILS RED. Click into the failed job → "Run docs validator" step → confirm stderr contains:
       ```
       [validate-docs] FAIL: docs/alerts/sentry/db-down.json: runbook_url "../../runbooks/this-does-not-exist.md" → target not found at <abs path>
       [validate-docs] 1 failure(s). Exit 1.
       ```
    6. The PR's "Checks" tab shows the red X. Do NOT merge.

    **Phase 2 — GREEN smoke (gate passes on revert):**

    7. On the same branch, revert the change (restore the correct runbook_url).
    8. Push the revert commit.
    9. Watch the `Validate — docs + cross-link integrity` workflow run again.
    10. **EXPECTED:** the `validate-docs` job PASSES GREEN. The job's "Run docs validator" step ends with:
       ```
       [validate-docs] OK: found 11 Mermaid fenced blocks across docs/ (>= 11 required).
       [validate-docs] PASS
       ```
    11. Close the PR (do NOT merge — this was a smoke test). Optionally delete the branch.

    Record both observations in 23-VERIFICATION.md as completed manual-UAT items.

    **Additional manual checks** (not blocking for this checkpoint, but recommended):
    - View `docs/observability/trace-propagation.md` on the GREEN PR's diff page in GitHub. Confirm both Mermaid diagrams render without "Syntax error" boxes.
    - Open `docs/runbooks/db-down.md` on GitHub. Confirm the cross-link to `../alerts/sentry/db-down.json` (in the "Source alert" line) resolves on click.
  </how-to-verify>
  <resume-signal>Type "approved" once both Phase 1 (RED smoke) and Phase 2 (GREEN smoke) are confirmed. If the gate behaves unexpectedly (e.g., GREEN on broken runbook_url, or RED on a clean corpus), describe the divergence and which file/line triggered it.</resume-signal>
  <verify><automated>echo "manual checkpoint — operator confirms RED smoke + GREEN smoke per <how-to-verify>; no automated command"</automated></verify>
  <done>Operator approved the checkpoint. Both Phase 1 (RED smoke: validate.yml fails red on broken runbook_url) and Phase 2 (GREEN smoke: validate.yml passes on revert) recorded in 23-VERIFICATION.md.</done>
</task>

</tasks>

<verification>
At the end of this plan:
- docs/README.md has the new ## Operations section.
- All 6 Wave-0 unit-test files pass GREEN (49/49 tests).
- `bun run validate` exits 0 against the complete corpus.
- The validate.yml CI gate has been smoke-tested in both directions (RED on broken runbook_url, GREEN on clean corpus).
- DOC-03 acceptance: 9 runbooks shipped under docs/runbooks/ with the 5-section template — fully met.
- DOC-04 acceptance: Sentry alert templates with runbook_url annotations + observability concepts doc set — fully met. Grafana YAML scope dropped per D-13 (Phase 21 deferral).
</verification>

<success_criteria>
- Task 1 complete with all acceptance grep checks passing and the full Wave-0 test suite GREEN.
- Task 2 (manual checkpoint) approved: both RED smoke and GREEN smoke observations recorded.
- Phase 23's `<verification>` items in 23-VALIDATION.md are addressed (manual-UAT smoke-test PR captured).
- `tsc --noEmit` and `biome check .` clean.
- No new dependencies, no source-code changes (pure docs + index update).
</success_criteria>

<output>
After completion, create `.planning/phases/23-runbooks-alert-templates-observability-docs/23-05-SUMMARY.md` documenting:
- Final shape of docs/README.md ## Operations section.
- Final Wave-0 test count (49/49 expected).
- Smoke-test PR URL (RED phase) and revert commit URL (GREEN phase) for the verification record.
- Any auto-fix Rule-1 deviations from the plan text.
- Phase-close confirmation: DOC-03 + DOC-04 are fully addressed; Phase 23 is ready for `/gsd-verify-work`.
</output>
