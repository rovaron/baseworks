---
phase: 23
plan: 03
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - docs/runbooks/db-down.md
  - docs/runbooks/redis-down.md
  - docs/runbooks/queue-backing-up.md
  - docs/runbooks/webhook-failures.md
  - docs/runbooks/auth-outage.md
  - docs/runbooks/otel-exporter-failing.md
  - docs/runbooks/bull-board-inaccessible.md
  - docs/runbooks/high-error-rate.md
  - docs/runbooks/slow-checkout.md
autonomous: true
requirements:
  - DOC-03
must_haves:
  truths:
    - "All 9 runbooks exist at canonical paths under docs/runbooks/ with the locked kebab-case slugs (db-down, redis-down, queue-backing-up, webhook-failures, auth-outage, otel-exporter-failing, bull-board-inaccessible, high-error-rate, slow-checkout)."
    - "Each runbook contains exactly 5 H2 sections in this order: Trigger, Symptoms, Triage, Resolution, Escalation (per D-03 + Wave-0 test runbook-section-shape.test.ts)."
    - "Each runbook is 150-300 lines, second-person imperative, opens with the 'Source alert' note linking to the matching Sentry alert JSON file."
    - "Each runbook's Triage section opens with the docker-compose-assumed paragraph (D-02) AND the 'wait 5 minutes' note (D-03)."
    - "Every cross-runbook markdown link inside docs/runbooks/*.md resolves to an existing file. Pass A of validate-docs.ts hard-fails any broken link."
    - "No screenshots or markdown image refs (D-04 + Wave-0 test runbook-no-screenshots.test.ts)."
    - "Operator-relevant Phase 22 surfaces are inline-cited where used: /health/detailed field shapes, bull-board mount path, worker:heartbeat:* keys, ringbuffer recentErrors shape."
  artifacts:
    - path: "docs/runbooks/db-down.md"
      provides: "Postgres unreachable triage + resolution"
      min_lines: 80
    - path: "docs/runbooks/redis-down.md"
      provides: "Redis unreachable triage + resolution"
      min_lines: 80
    - path: "docs/runbooks/queue-backing-up.md"
      provides: "BullMQ queue depth runaway + worker heartbeat checks"
      min_lines: 80
    - path: "docs/runbooks/webhook-failures.md"
      provides: "Stripe / Pagar.me webhook signature + delivery triage"
      min_lines: 80
    - path: "docs/runbooks/auth-outage.md"
      provides: "better-auth login failure spike triage"
      min_lines: 80
    - path: "docs/runbooks/otel-exporter-failing.md"
      provides: "validateObservabilityEnv + OTLP/Sentry DSN misconfig triage"
      min_lines: 80
    - path: "docs/runbooks/bull-board-inaccessible.md"
      provides: "RBAC 401/403 + CSP frame-ancestors + static-asset failure modes (Phase 22 D-01..04)"
      min_lines: 80
    - path: "docs/runbooks/high-error-rate.md"
      provides: "ringbuffer recentErrors + Sentry/GlitchTip cross-replica triage"
      min_lines: 80
    - path: "docs/runbooks/slow-checkout.md"
      provides: "p95 latency at /api/billing/checkout triage"
      min_lines: 80
  key_links:
    - from: "docs/runbooks/db-down.md"
      to: "docs/alerts/sentry/db-down.json"
      via: "Source alert link in opener"
      pattern: "../alerts/sentry/db-down.json"
    - from: "docs/runbooks/queue-backing-up.md"
      to: "docs/observability/cardinality.md"
      via: "cross-runbook markdown link to obs concept doc"
      pattern: "../observability/cardinality.md"
    - from: "docs/runbooks/otel-exporter-failing.md"
      to: "packages/config/src/env.ts:122"
      via: "validateObservabilityEnv citation"
      pattern: "validateObservabilityEnv"
    - from: "docs/runbooks/bull-board-inaccessible.md"
      to: "apps/api/src/routes/bull-board.ts:42"
      via: "mount path citation"
      pattern: "/admin/bull-board"
    - from: "docs/runbooks/high-error-rate.md"
      to: "/health/detailed"
      via: "recentErrors field reference (Phase 22 D-15)"
      pattern: "recentErrors"
---

<objective>
Wave 2: ship all 9 incident runbooks under docs/runbooks/ using the locked Trigger → Symptoms → Triage → Resolution → Escalation template (D-03). Each runbook is ~150-300 lines, second-person imperative, docker-compose-first command examples (D-02), text-only with optional Mermaid (D-04), no frontmatter, links to its matching Sentry alert JSON file (Plan 04 ships those JSON files).

Purpose: DOC-03 acceptance — operator paged at 3am has a Triage checklist for the 9 most likely v1.3 failure modes. Tone: "wait 5 minutes before acting" + concrete docker compose / curl / redis-cli commands rather than prose narratives.

Output: 9 markdown files. Wave-0 tests runbook-files-present.test.ts (9/9), runbook-section-shape.test.ts (9/9), runbook-no-screenshots.test.ts (9/9) all go RED → GREEN. Pass A of validate-docs.ts validates every cross-runbook markdown link.

This plan creates files under docs/runbooks/ ONLY. files_modified is the 9 runbooks; zero overlap with Plan 02 (docs/observability/) and Plan 04 (docs/alerts/sentry/) so all three Wave-2 plans run in parallel.

Cross-runbook link policy: each runbook MAY link to other runbooks via [text](./other-slug.md). All targets in this plan are siblings (same directory), so paths are ./peer-slug.md. Cross-link to obs concept docs uses ../observability/<file>.md. Forward-link to docs/alerts/sentry/<slug>.json uses ../alerts/sentry/<slug>.json. The validator's Pass A checks every match of the locked regex `\]\((\.\.?\/[\w\/.-]+\.md)(?:#[\w-]+)?\)` — only .md targets are checked, .json links are tolerated.
</objective>

<execution_context>
@C:/Projetos/baseworks/.claude/get-shit-done/workflows/execute-plan.md
@C:/Projetos/baseworks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/23-runbooks-alert-templates-observability-docs/23-CONTEXT.md
@.planning/phases/23-runbooks-alert-templates-observability-docs/23-RESEARCH.md
@.planning/phases/23-runbooks-alert-templates-observability-docs/23-PATTERNS.md
@.planning/phases/23-runbooks-alert-templates-observability-docs/23-VALIDATION.md
@.planning/phases/23-runbooks-alert-templates-observability-docs/23-01-SUMMARY.md
@docs/architecture.md
@docs/integrations/billing.md
@docs/integrations/bullmq.md
@docs/integrations/better-auth.md
@apps/api/src/routes/health-detailed.ts
@apps/api/src/routes/bull-board.ts
@apps/api/src/index.ts
@apps/api/src/worker.ts
@packages/config/src/env.ts
@packages/observability/src/lib/ring-buffer-error-tracker.ts

VERBATIM runbook section template (RESEARCH §"Runbook section template", lines 893-931 — copy structure into EVERY runbook scaffold):

```
# <Alert name>

> Source alert: [docs/alerts/sentry/<slug>.json](../alerts/sentry/<slug>.json)

## Trigger

<2 lines: which alert fires, matching Sentry template path>

## Symptoms

- <bullet 1: what the operator sees>
- <bullet 2>

## Triage

> These commands assume `docker-compose.yml` from the repo root. K8s/PaaS users translate to `kubectl exec` / equivalent.

> **Wait 5 minutes before action** to confirm this is not a deploy blip. The alert's frequency window is 5m (matches Sentry alert template's `frequency: 5`).

1. `docker compose ps` — confirm services up
2. ...

## Resolution

### Most likely: <root cause 1>

<command + rationale>

### If that did not work: <root cause 2>

<command + rationale>

## Escalation

If stuck >30 minutes:

- Open an issue / post in repo discussions
- Check upstream provider status page (<link>)
- Page yourself for the next attempt
```

Per-runbook surfaces and citations (verified per RESEARCH "File Refs Verified"):

- db-down: `/health/detailed` `data.db` subobject + `data.status`. Cite `apps/api/src/index.ts:90-110` for the `/health` Docker probe (unauthenticated) which db-down references as the operator's first triage. Triage commands include `docker compose exec postgres pg_isready -U $POSTGRES_USER -d $POSTGRES_DB`, `docker compose logs postgres --tail 100`, `docker compose restart postgres`.

- redis-down: `/health/detailed` `data.redis` subobject. Triage: `docker compose exec redis redis-cli ping`, `docker compose logs redis --tail 100`, `docker compose restart redis`.

- queue-backing-up: cite `packages/config/src/env.ts:52` for `WORKER_HEARTBEAT_INTERVAL_MS`, `apps/api/src/worker.ts:112-130` for the heartbeat publisher, Phase 22 D-12 worker:heartbeat:* keys. Triage: `docker compose exec redis redis-cli --scan --pattern 'worker:heartbeat:*'` and `redis-cli get worker:heartbeat:<instanceId>`. Cross-link to ../observability/cardinality.md (job throughput metrics — what's safe to label with).

- webhook-failures: cite `packages/observability/src/lib/scrub-pii.ts:128-134` for the webhook-route data drop (operator must know request bodies are NEVER forwarded). Cross-link to ../integrations/billing.md.

- auth-outage: better-auth login error spike. Triage: `docker compose logs api --tail 200 -f | grep auth`, check `/health/detailed`'s auth contributor (slot exists per Phase 22 D-08).

- otel-exporter-failing: cite `packages/config/src/env.ts:122` for `validateObservabilityEnv`, `apps/api/src/index.ts:1-2` for the line-1 telemetry pattern. Document expected error messages: "SENTRY_DSN is required when ERROR_TRACKER=sentry". Cross-link to ../observability/trace-propagation.md.

- bull-board-inaccessible: cite `apps/api/src/routes/bull-board.ts:42-43` for mount path `/admin/bull-board`, `apps/api/src/routes/bull-board.ts:73-75` for CSP frame-ancestors enforcement, `packages/config/src/env.ts:49` for `BULL_BOARD_READ_ONLY`. Walk through Phase 22 D-01..04 failure modes inline (3-line summaries each per RESEARCH Open Question 3): 401 (no session), 403 (wrong role), CSP frame-ancestors block, static-asset 401. Cross-link to ../observability/trace-propagation.md (forward-looking).

- high-error-rate: cite `packages/observability/src/lib/ring-buffer-error-tracker.ts:13-24` for RingBufferEntry interface. Document the surfaced shape (per RESEARCH Q8 ringbuffer caveat): `/health/detailed`'s `recentErrors` is `{timestamp, message, source, count}` (firstFrame omitted). Cross-link to Sentry/GlitchTip dashboards as cross-replica deeper-look surface. Cross-link to ../observability/cardinality.md.

- slow-checkout: cite the p95(transaction.duration) Sentry alert (Plan 04 ships matching JSON). Triage: `docker compose logs api --tail 200 -f | grep checkout`, check `/health/detailed`'s billing contributor.

Common Triage commands operators use across multiple runbooks:
- `docker compose ps` — service liveness
- `docker compose logs <service> --tail 100 -f` — tail logs
- `docker compose exec postgres psql -U $POSTGRES_USER -d $POSTGRES_DB` — DB shell
- `docker compose exec redis redis-cli ping` — Redis liveness
- `docker compose exec redis redis-cli --scan --pattern 'worker:heartbeat:*'` — worker discovery
- `curl http://localhost:3000/health` — unauthenticated Docker probe (apps/api/src/index.ts:100-133)
- `curl -H "cookie: ..." http://localhost:3000/health/detailed` — owner-RBAC detailed health
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → reader (operator at 3am) | Plain markdown rendered by GitHub. Threat surface = stale or wrong commands sending the operator down a dead end during a real incident. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-12 | Information Disclosure | Triage commands accidentally leaking real secrets/credentials in examples | mitigate | All commands use shell-variable references ($POSTGRES_USER, $POSTGRES_DB, $SENTRY_AUTH_TOKEN, etc.) — never literal credentials. validate-docs.ts invariant 2 catches sk_live_*, re_*, whsec_* shapes. Test fixtures never contain real DSNs. |
| T-23-13 | Tampering | Stale runbook commands after Phase 22 surface changes | mitigate | Each runbook cites the source file:line that defines the command surface (e.g., bull-board.ts:42 for the mount path). PR diffs against those source files surface the need to update runbooks. v1.4 docs-quality phase will revisit. |
| T-23-14 | Repudiation | webhook-failures.md misleading the operator about request body availability | mitigate | The runbook EXPLICITLY cites scrub-pii.ts:128-134's webhook-route rule (request.data dropped on /api/webhooks/**) so the operator knows the body is NOT in Sentry events — they must reproduce locally OR check upstream provider dashboards. |
| T-23-15 | Information Disclosure | Cross-replica error data limitations not surfaced | mitigate | high-error-rate.md documents the ringbuffer is process-local (Phase 22 deferred cross-replica aggregation). Operator routed to Sentry/GlitchTip for cross-replica view. |
| T-23-16 | Denial of Service | Triage commands accidentally hammering production | mitigate | Each runbook's `wait 5 minutes` opener mitigates panic-driven repeated restarts. Triage commands are read-mostly (ps, logs, ping, scan) — destructive commands (restart) appear in Resolution sections only, gated by "if that did not work." |
| T-23-17 | Elevation of Privilege | bull-board-inaccessible.md leaking how to bypass RBAC | mitigate | The runbook documents the failure modes (401/403/CSP) for diagnostic purposes — does NOT document how to bypass requireRole("owner"). The fix path is "log in as owner," not "disable the gate." Phase 22 D-01..04 RBAC discipline preserved. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create the 5 infrastructure runbooks (db-down, redis-down, queue-backing-up, webhook-failures, otel-exporter-failing)</name>
  <files>
    docs/runbooks/db-down.md,
    docs/runbooks/redis-down.md,
    docs/runbooks/queue-backing-up.md,
    docs/runbooks/webhook-failures.md,
    docs/runbooks/otel-exporter-failing.md
  </files>
  <read_first>
    - apps/api/src/routes/health-detailed.ts (verify the response shape — db-down/redis-down/queue-backing-up embed the relevant slice inline)
    - apps/api/src/index.ts (verify lines 90-110 for /health Docker probe and 100-133 for the unauthenticated probe)
    - apps/api/src/worker.ts (verify lines 112-130 for heartbeat publisher and the 4 queues enumerated: example-process-followup, billing-process-webhook, billing-sync-usage, email-send)
    - packages/config/src/env.ts (verify line 52 for WORKER_HEARTBEAT_INTERVAL_MS, line 122 for validateObservabilityEnv)
    - packages/observability/src/lib/scrub-pii.ts (verify lines 128-134 for webhook-route data drop)
    - .planning/phases/23-runbooks-alert-templates-observability-docs/23-RESEARCH.md "Runbook section template" + "File Refs Verified"
    - docs/integrations/billing.md (cross-link target for webhook-failures.md)
    - docs/integrations/bullmq.md (cross-link target for queue-backing-up.md)
  </read_first>
  <action>
    Create 5 runbook markdown files. Each follows the VERBATIM template from `<context>` "VERBATIM runbook section template." Replace placeholders per-runbook.

    Per-runbook content:

    ## docs/runbooks/db-down.md
    - Title: `# DB down — Postgres unreachable`
    - Source alert link: `[docs/alerts/sentry/db-down.json](../alerts/sentry/db-down.json)`
    - Trigger (2 lines): "Sentry Metric Alert `db-down` fires when /health responses include `data.db.status='down'` for 5m. Maps to docs/alerts/sentry/db-down.json."
    - Symptoms (5 bullets): API returns 500/503 on routes that hit DB; `/health` returns non-200; `/health/detailed` shows `data.db.status='down'` with err message; pgAdmin / psql connections refused; pino logs include "ECONNREFUSED 127.0.0.1:5432" or similar.
    - Triage opens with the docker-compose paragraph + "wait 5m" note. Numbered steps:
      1. `docker compose ps` — confirm postgres service is "Up"
      2. `docker compose exec postgres pg_isready -U $POSTGRES_USER -d $POSTGRES_DB` — confirm Postgres accepting connections
      3. `docker compose logs postgres --tail 100` — look for OOM, disk full, FATAL errors
      4. `curl -s http://localhost:3000/health | jq` — confirm API agrees DB is down
      5. (If API is up) `curl -s -H "cookie: <owner session>" http://localhost:3000/health/detailed | jq .data.db` — see the err string
    - Embed the relevant `/health/detailed` `data.db` subobject inline (per RESEARCH Q6 recommendation): a small JSON snippet showing `{ status: "up" | "down", err?: string, latencyMs?: number }`.
    - Resolution sections: "Most likely: Postgres container exited" → `docker compose up -d postgres` + check disk. "If that did not work: Connection-string mismatch" → check $DATABASE_URL env, drizzle config.
    - Escalation: per template. Upstream link: PostgreSQL release notes / Postgres Docker image issues.

    ## docs/runbooks/redis-down.md
    - Title: `# Redis down — queue + cache backend unreachable`
    - Source alert: ../alerts/sentry/redis-down.json
    - Symptoms: BullMQ enqueue failures, session reads failing (if Redis is session store), `/health/detailed.data.redis.status='down'`, pino logs with "ECONNREFUSED 127.0.0.1:6379"
    - Triage: `docker compose ps`, `docker compose exec redis redis-cli ping` (expect PONG), `docker compose logs redis --tail 100`, `curl -s http://localhost:3000/health/detailed | jq .data.redis`
    - Resolution: "Most likely: Redis OOM" → check `redis-cli info memory` for maxmemory; "If that did not work: persistence misconfig" → check redis.conf appendonly settings.
    - Cross-link: forward-looking [queue-backing-up.md](./queue-backing-up.md) for queue-side symptoms.

    ## docs/runbooks/queue-backing-up.md
    - Title: `# Queue backing up — BullMQ depth runaway`
    - Source alert: ../alerts/sentry/queue-backing-up.json
    - Symptoms: queue waiting count > 1000 (default threshold per Phase 22), workers idle, jobs not progressing, /health/detailed.data.workers shows 0 active heartbeats.
    - Triage:
      1. `docker compose ps` — confirm api, worker, redis up
      2. `docker compose exec redis redis-cli --scan --pattern 'worker:heartbeat:*'` — list active workers
      3. (For each key) `docker compose exec redis redis-cli get worker:heartbeat:<instanceId>` — inspect JSON `{instanceId, queues: [...], lastHeartbeat: ISO, version?}`
      4. `docker compose logs worker --tail 200 -f` — look for processor errors
      5. `bunx bull-board http://localhost:3000/admin/bull-board` — visual inspect (per Phase 22 D-01..03 — owner session required)
    - Embed the heartbeat key shape inline: cite Phase 22 D-12 `worker:heartbeat:{instanceId}` with TTL=2× $WORKER_HEARTBEAT_INTERVAL_MS, value JSON `{ instanceId, queues: string[], lastHeartbeat: ISO }`. Cite `apps/api/src/worker.ts:112-130` and `packages/config/src/env.ts:52`.
    - Cite the 4 queues that workers enumerate: example-process-followup, billing-process-webhook, billing-sync-usage, email-send (per Phase 22 verification on 2026-04-27).
    - Resolution: "Most likely: worker crashed" → `docker compose restart worker`; "If that did not work: poison message" → bull-board to mark a job failed and inspect job.data.
    - Cross-link to [../observability/cardinality.md](../observability/cardinality.md) for what's safe to label on queue-related metrics.
    - Cross-link to [bull-board-inaccessible.md](./bull-board-inaccessible.md) when bull-board itself isn't reachable.

    ## docs/runbooks/webhook-failures.md
    - Title: `# Webhook failures — Stripe / Pagar.me delivery or signature errors`
    - Source alert: ../alerts/sentry/webhook-failures.json
    - Symptoms: Stripe Dashboard webhooks tab shows red deliveries; pino logs with `webhook signature verification failed` or 4xx returns from /api/webhooks/stripe; payment status divergence (Stripe says paid, our DB says pending).
    - Triage:
      1. Check Stripe Dashboard → Developers → Webhooks → recent deliveries; filter by status: failed
      2. `docker compose logs api --tail 200 -f | grep -i webhook`
      3. Verify signing secret matches Dashboard: `echo $STRIPE_WEBHOOK_SECRET | head -c 10` (don't print full)
    - CRITICAL inline note: cite `packages/observability/src/lib/scrub-pii.ts:128-134` — request bodies on /api/webhooks/** are dropped before forwarding to Sentry. Operator must reproduce locally or use Stripe Dashboard's "Resend" feature to inspect.
    - Resolution: "Most likely: signing secret rotated" → update env + restart; "If that did not work: webhook endpoint URL mismatch" → check Stripe Dashboard endpoint config.
    - Cross-link: [../integrations/billing.md](../integrations/billing.md) for the canonical billing flow.

    ## docs/runbooks/otel-exporter-failing.md
    - Title: `# OTEL exporter failing — observability silent`
    - Source alert: ../alerts/sentry/otel-exporter-failing.json
    - Symptoms: Sentry/GlitchTip stops receiving events; pino logs with "Sentry transport error" or "OTLP exporter failed"; on boot, `validateObservabilityEnv()` may have crash-hard rejected (Phase 17 D-09).
    - Triage:
      1. Check API boot logs: `docker compose logs api --tail 50` — is the process running, or did it exit on `validateObservabilityEnv` rejection?
      2. (If running) `printenv | grep -E 'ERROR_TRACKER|SENTRY_DSN|GLITCHTIP_DSN|OTLP'` — verify DSN/endpoint env vars are set
      3. (If misconfigured) document expected error messages: "SENTRY_DSN is required when ERROR_TRACKER=sentry" (cite `packages/config/src/env.ts:122` for `validateObservabilityEnv`)
      4. Check that `apps/api/src/index.ts:1-2` follows the line-1 telemetry pattern: `import "./telemetry"; import { ..., validateObservabilityEnv } from "@baseworks/config";`
    - Resolution: "Most likely: missing DSN" → set $SENTRY_DSN or $GLITCHTIP_DSN; "If that did not work: outbound network blocked" → check egress firewall to sentry.io / glitchtip endpoint.
    - Cross-link: [../observability/trace-propagation.md](../observability/trace-propagation.md) for context on what stops flowing.
    - Cross-link: [../observability/cardinality.md](../observability/cardinality.md) for what NOT to label.

    All 5 files: NO frontmatter, NO screenshots, second-person imperative tone, ~150-300 lines each.
  </action>
  <verify>
    <automated>bun test scripts/__tests__/runbook-files-present.test.ts scripts/__tests__/runbook-section-shape.test.ts scripts/__tests__/runbook-no-screenshots.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - All 5 files exist under docs/runbooks/.
    - For each file: `grep -E "^## (Trigger|Symptoms|Triage|Resolution|Escalation)$" docs/runbooks/<slug>.md` returns exactly 5 lines.
    - For each file: `head -1 docs/runbooks/<slug>.md` is a single H1 line (not `---` frontmatter).
    - For each file: `grep -E "Source alert: \[docs/alerts/sentry/<slug>.json\]\(\.\./alerts/sentry/<slug>.json\)" docs/runbooks/<slug>.md` returns 1.
    - For each file: `grep -E '!\[.*\]\(' docs/runbooks/<slug>.md` returns nothing (no images).
    - For each file: line count between 80 and 400 lines (`wc -l` falls in range).
    - `grep -c "WORKER_HEARTBEAT_INTERVAL_MS" docs/runbooks/queue-backing-up.md` returns at least 1.
    - `grep -c "validateObservabilityEnv" docs/runbooks/otel-exporter-failing.md` returns at least 1.
    - `grep -c "scrub-pii.ts" docs/runbooks/webhook-failures.md` returns at least 1.
    - 5 of 9 tests in runbook-files-present.test.ts pass; 5 of 9 in runbook-section-shape.test.ts pass.
  </acceptance_criteria>
  <done>5 infrastructure runbooks shipped. Each follows the 5-section template, cites verified file:line refs, and links to its matching Sentry alert JSON (which Plan 04 ships in parallel). Wave-0 tests partial-GREEN — 4 more runbooks land in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Create the 4 application runbooks (auth-outage, bull-board-inaccessible, high-error-rate, slow-checkout)</name>
  <files>
    docs/runbooks/auth-outage.md,
    docs/runbooks/bull-board-inaccessible.md,
    docs/runbooks/high-error-rate.md,
    docs/runbooks/slow-checkout.md
  </files>
  <read_first>
    - apps/api/src/routes/bull-board.ts (verify lines 42-43 for mount path, 73-75 for CSP)
    - apps/api/src/routes/admin.ts (verify line 328 for deprecated /api/admin/system/health alias)
    - packages/config/src/env.ts (verify line 49 for BULL_BOARD_READ_ONLY)
    - packages/observability/src/lib/ring-buffer-error-tracker.ts (verify lines 13-24 for RingBufferEntry interface)
    - apps/api/src/routes/health-detailed.ts (verify recentErrors field projection — RESEARCH Q8 says firstFrame is omitted on the surfaced shape)
    - docs/integrations/better-auth.md (cross-link target for auth-outage.md)
    - docs/integrations/billing.md (cross-link target for slow-checkout.md)
  </read_first>
  <action>
    Create 4 runbook markdown files following the VERBATIM template.

    ## docs/runbooks/auth-outage.md
    - Title: `# Auth outage — better-auth login failure spike`
    - Source alert: ../alerts/sentry/auth-outage.json
    - Symptoms: Sentry Issue Alert `auth-outage` fires (>=50 auth-tagged events in 5m); /api/auth/* returns 500/401 unexpectedly; users report "cannot log in"; pino logs with "auth: invalid session" or DB errors from auth tables.
    - Triage:
      1. `docker compose ps` — confirm api + postgres + redis up
      2. `docker compose logs api --tail 200 -f | grep -i 'auth\|better-auth'`
      3. `curl -i http://localhost:3000/api/auth/session` (unauthenticated; should 401)
      4. Check Sentry/GlitchTip for the auth-outage Issue Alert events; group by error message
      5. (If owner session available) `curl -H "cookie: <owner>" http://localhost:3000/health/detailed | jq .data.auth`
    - Resolution: "Most likely: better-auth schema drift" → run `bun run db:migrate` to apply auth-table migrations; "If that did not work: session cookie domain mismatch" → check `BETTER_AUTH_SECRET` and `BETTER_AUTH_TRUSTED_ORIGINS`.
    - Cross-link: [../integrations/better-auth.md](../integrations/better-auth.md) for the canonical auth flow.

    ## docs/runbooks/bull-board-inaccessible.md
    - Title: `# Bull-board inaccessible — admin queue UI not loading`
    - Source alert: ../alerts/sentry/bull-board-inaccessible.json
    - Symptoms: Admin → Jobs page shows 401/403/blank iframe; bull-board static assets return 401 (per Phase 22 verification); browser console shows CSP `frame-ancestors` violation.
    - Triage:
      1. `docker compose ps` — confirm api up
      2. `curl -i http://localhost:3000/admin/bull-board` (unauthenticated → expect 401)
      3. `curl -i -H "cookie: <owner session>" http://localhost:3000/admin/bull-board` (expect 200 HTML)
      4. `curl -i -H "cookie: <owner>" http://localhost:3000/admin/bull-board/static/main.js` (expect 200; if 401, RBAC is rejecting static assets — Phase 22 D-01..04 expected behavior, not a bug)
      5. Browser DevTools → Console → look for "Refused to frame ... violates Content Security Policy directive: frame-ancestors"
    - Cite Phase 22 D-01..04 surfaces inline:
      - Mount path: `/admin/bull-board` (per `apps/api/src/routes/bull-board.ts:42-43`)
      - CSP header: `frame-ancestors` self + admin-origin (per `apps/api/src/routes/bull-board.ts:73-75`)
      - Read-only mode: $BULL_BOARD_READ_ONLY env (per `packages/config/src/env.ts:49`, default "true")
    - Walk through the 4 failure modes inline (3-line summaries each per RESEARCH Open Question 3):
      1. **401 (no session)** — log in as owner first, then revisit.
      2. **403 (wrong role)** — user does not have `owner` role on the active organization. Use `getFullOrganization()` workaround or assign owner role via better-auth admin UI.
      3. **CSP frame-ancestors block** — the iframe origin (admin dashboard) doesn't match the allowed list. Check `$ADMIN_ORIGIN` env vs the actual admin URL.
      4. **Static asset 401** — expected behavior, not a bug. The owner session must be present for static asset requests.
    - Resolution: "Most likely: not logged in as owner" → log in via admin UI; "If that did not work: CSP origin mismatch" → align `$ADMIN_ORIGIN` env with the admin Vite dev server URL.

    ## docs/runbooks/high-error-rate.md
    - Title: `# High error rate — error count spike across replicas`
    - Source alert: ../alerts/sentry/high-error-rate.json
    - Symptoms: Sentry Issue Alert `high-error-rate` fires (>=N events in 5m, threshold tunable per fork); /health/detailed shows non-zero `recentErrors`.
    - Triage:
      1. `curl -H "cookie: <owner>" http://localhost:3000/health/detailed | jq .data.recentErrors`
      2. Embed the surfaced shape (per RESEARCH Q8 — DIFFERENT from the internal RingBufferEntry; `firstFrame` is omitted on /health/detailed):
         ```json
         {
           "timestamp": "2026-04-28T12:00:00Z",
           "message": "string (truncated to 500 chars)",
           "source": "cqrs | http | worker | global",
           "count": 1
         }
         ```
      3. CRITICAL note: the ringbuffer is process-local (Phase 22 deferred cross-replica aggregation). With multiple replicas, each replica has its own ringbuffer — operator must check ALL replicas' /health/detailed OR rely on Sentry/GlitchTip for cross-replica aggregation.
      4. `docker compose logs api --tail 500 -f | grep -i error` — tail current process logs
      5. Sentry/GlitchTip dashboard: filter by environment, group by issue.message, sort by count.
    - Cite `packages/observability/src/lib/ring-buffer-error-tracker.ts:13-24` for the internal interface (with the field-projection caveat).
    - Resolution: "Most likely: recent deploy regression" → check git log + bisect; "If that did not work: dependency-of-a-dependency change" → check `bun.lock` recent diff.
    - Cross-link: [../observability/cardinality.md](../observability/cardinality.md) for which error tags are safe to dimension.

    ## docs/runbooks/slow-checkout.md
    - Title: `# Slow checkout — p95 latency above 5s on /api/billing/checkout`
    - Source alert: ../alerts/sentry/slow-checkout.json
    - Symptoms: Sentry Metric Alert `slow-checkout` fires when `p95(transaction.duration)` on the checkout transaction exceeds 5s for 5m; Stripe Dashboard checkout sessions take longer than usual; users abandon checkout.
    - Triage:
      1. `docker compose logs api --tail 200 -f | grep checkout`
      2. (If owner session) `curl -H "cookie: <owner>" http://localhost:3000/health/detailed | jq .data.billing` — check upstream Stripe latency
      3. Sentry → Performance → Transactions → filter `transaction:/api/billing/checkout` → p95 chart over 1h
      4. Check Stripe Status page (status.stripe.com) for upstream incidents
      5. Inspect recent traces (Sentry Performance) for the checkout span — find the slow child span (DB? Stripe API? webhook send?)
    - Resolution: "Most likely: Stripe API slow" → wait it out + monitor status.stripe.com; "If that did not work: DB lock contention on subscriptions table" → `docker compose exec postgres psql -U $POSTGRES_USER -d $POSTGRES_DB -c "SELECT * FROM pg_stat_activity WHERE state = 'active'"`.
    - Cross-link: [../integrations/billing.md](../integrations/billing.md) for the canonical billing flow.
    - Cross-link: [webhook-failures.md](./webhook-failures.md) when checkout latency correlates with failed webhooks.

    All 4 files: NO frontmatter, NO screenshots, second-person imperative tone, ~150-300 lines each.
  </action>
  <verify>
    <automated>bun test scripts/__tests__/runbook-files-present.test.ts scripts/__tests__/runbook-section-shape.test.ts scripts/__tests__/runbook-no-screenshots.test.ts && bun run validate</automated>
  </verify>
  <acceptance_criteria>
    - All 4 files exist under docs/runbooks/.
    - All 9 runbook-files-present.test.ts tests now pass GREEN (combined with Task 1).
    - All 9 runbook-section-shape.test.ts tests now pass GREEN.
    - All 9 runbook-no-screenshots.test.ts tests now pass GREEN.
    - For each file: `grep -E "^## (Trigger|Symptoms|Triage|Resolution|Escalation)$" docs/runbooks/<slug>.md` returns exactly 5.
    - `grep -c "/admin/bull-board" docs/runbooks/bull-board-inaccessible.md` returns at least 1.
    - `grep -c "frame-ancestors" docs/runbooks/bull-board-inaccessible.md` returns at least 1.
    - `grep -c "BULL_BOARD_READ_ONLY" docs/runbooks/bull-board-inaccessible.md` returns at least 1.
    - `grep -c "ring-buffer-error-tracker.ts" docs/runbooks/high-error-rate.md` returns at least 1.
    - `grep -c "p95" docs/runbooks/slow-checkout.md` returns at least 1.
    - `bun run validate` exits 0 — Pass A scans 9 runbooks for cross-runbook .md links and finds none broken (every cross-link target exists either as another runbook or as a docs/observability/* file or docs/integrations/* file).
    - No markdown image refs in any of the 4 files.
  </acceptance_criteria>
  <done>All 9 runbooks ship. Wave-0 tests for runbook presence/shape/no-screenshots are fully GREEN. Pass A of validate-docs.ts validates every cross-runbook link and exits 0. The complete DOC-03 surface is in place.</done>
</task>

</tasks>

<verification>
At the end of this plan:
- 9 runbooks under docs/runbooks/.
- All 3 Wave-0 runbook tests GREEN: runbook-files-present.test.ts, runbook-section-shape.test.ts, runbook-no-screenshots.test.ts.
- `bun run validate` exits 0 (4th invariant Pass A finds no broken cross-runbook links; Pass B has no docs/alerts/sentry/*.json yet — that comes from Plan 04 in parallel).
- No frontmatter, no screenshots, all citations use the verified paths from RESEARCH "File Refs Verified".
</verification>

<success_criteria>
- All 2 tasks complete with their `<acceptance_criteria>` met.
- All 9 runbooks have the 5 H2 sections in canonical order.
- All cross-runbook .md links resolve (validated by Pass A).
- DOC-03 acceptance language fully addressed: 9 runbooks under docs/runbooks/ with the 5-section template.
- `tsc --noEmit` and `biome check .` pass (no source changes; pure docs).
</success_criteria>

<output>
After completion, create `.planning/phases/23-runbooks-alert-templates-observability-docs/23-03-SUMMARY.md` documenting:
- Final line count per runbook (verify each is in the 150-300 range or document why some are shorter/longer).
- Cross-runbook link map: which runbooks link to which siblings or to docs/observability/* / docs/integrations/*.
- Re-verified file:line citations (paths still accurate at exec time).
- Any auto-fix Rule-1 deviations from the plan text.
- Final Wave-0 test status: 27/27 runbook tests GREEN (9 × 3 test files).
</output>
