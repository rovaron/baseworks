---
phase: 23
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - docs/observability/README.md
  - docs/observability/attributes.md
  - docs/observability/cardinality.md
  - docs/observability/trace-propagation.md
  - scripts/validate-docs.ts
autonomous: true
requirements:
  - DOC-04
must_haves:
  truths:
    - "Operator opening docs/observability/README.md sees a 30-line index linking to attributes.md, cardinality.md, trace-propagation.md plus a one-paragraph 'what observability looks like in this codebase' intro."
    - "docs/observability/attributes.md is the canonical glossary table (Name | Lives on | Type | Example | Cardinality risk) citing packages/observability/src/context.ts:43-51 (the corrected path per RESEARCH Finding 2)."
    - "docs/observability/cardinality.md enumerates the 9 Baseworks-specific high-cardinality values (D-08) with cross-links to packages/observability/src/lib/scrub-pii.ts:34-52."
    - "docs/observability/trace-propagation.md contains exactly 2 Mermaid fenced blocks (one sequenceDiagram + one stateDiagram-v2) per D-06."
    - "scripts/validate-docs.ts Mermaid floor literal raised from 8 to 11 IN THE SAME COMMIT as the trace-propagation.md diagrams (Research Finding 5: half-merged state breaks CI)."
    - "Total Mermaid count post-plan: 11 (8 existing + 2 trace-propagation + 1 README flowchart). bun run validate exits 0 with floor literal 11."
  artifacts:
    - path: "docs/observability/README.md"
      provides: "obs concept index + 1 small flowchart for 11-floor compliance"
      contains: "trace-propagation.md"
      min_lines: 25
    - path: "docs/observability/attributes.md"
      provides: "5-column glossary table"
      contains: "Cardinality risk"
      min_lines: 60
    - path: "docs/observability/cardinality.md"
      provides: "rules + Baseworks-specific high-card values"
      contains: "tenantId"
      min_lines: 50
    - path: "docs/observability/trace-propagation.md"
      provides: "sequenceDiagram + stateDiagram-v2 + walkthrough"
      contains: "wrapQueue"
      min_lines: 80
    - path: "scripts/validate-docs.ts"
      provides: "Mermaid floor raised 8 to 11"
      contains: "mermaidTotal < 11"
  key_links:
    - from: "docs/observability/trace-propagation.md"
      to: "packages/observability/src/wrappers/wrap-queue.ts"
      via: "file:line citation + 5-line snippet"
      pattern: "wrap-queue.ts:74-83"
    - from: "docs/observability/cardinality.md"
      to: "packages/observability/src/lib/scrub-pii.ts"
      via: "DEFAULT_DENY_KEYS cross-reference"
      pattern: "scrub-pii.ts:34"
    - from: "docs/observability/attributes.md"
      to: "packages/observability/src/context.ts"
      via: "ObservabilityContext interface citation"
      pattern: "context.ts:43-51"
---

<objective>
Wave 2: ship the 4 observability concept docs under docs/observability/ (D-05) and bump the validate-docs.ts Mermaid floor from 8 to 11 in the SAME commit as the new diagrams (D-06 + Research Finding 5). README.md is a 30-line index plus a small flowchart; attributes.md is the 5-column glossary table; cardinality.md is the rules-and-anti-patterns guide; trace-propagation.md ships the 2 mandated Mermaid diagrams (sequenceDiagram + stateDiagram-v2) plus walkthrough citing the corrected file:line refs (Research Finding 2).

Purpose: developer-facing reference set. cardinality.md and attributes.md are deep-linked from the runbooks (Plan 03), so this plan must land BEFORE Plan 03 references any anchor in these files. trace-propagation.md is the canonical "single trace API to DB to enqueue to worker" walkthrough closing the Phase 19 + 20 + 20.1 storyline.

Output: 4 markdown files (3 plain text + 1 with Mermaid) + 1-line edit to validate-docs.ts (literal 8 to 11). Wave-0 test scripts/__tests__/observability-docs-present.test.ts goes from RED to GREEN.

CRITICAL: D-06 sets the Mermaid floor literal to 11. Existing diagrams (8) + trace-propagation.md (2) = 10. To satisfy floor=11, this plan MUST add at least 1 more Mermaid block. The third block lands in docs/observability/README.md as a small flowchart showing "where observability lives in the codebase." Total final count: 8 existing + 2 trace-propagation + 1 README flowchart = 11. Floor = 11. Validator passes.
</objective>

<execution_context>
@C:/Projetos/baseworks/.claude/get-shit-done/workflows/execute-plan.md
@C:/Projetos/baseworks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/23-runbooks-alert-templates-observability-docs/23-CONTEXT.md
@.planning/phases/23-runbooks-alert-templates-observability-docs/23-RESEARCH.md
@.planning/phases/23-runbooks-alert-templates-observability-docs/23-PATTERNS.md
@.planning/phases/23-runbooks-alert-templates-observability-docs/23-VALIDATION.md
@.planning/phases/23-runbooks-alert-templates-observability-docs/23-01-SUMMARY.md
@docs/architecture.md
@docs/jsdoc-style-guide.md
@docs/README.md
@packages/observability/src/context.ts
@packages/observability/src/wrappers/wrap-queue.ts
@packages/observability/src/lib/scrub-pii.ts
@packages/queue/src/index.ts
@scripts/validate-docs.ts

VERIFIED interfaces from RESEARCH "File Refs Verified" — use THESE in citations, NOT the wrong CONTEXT.md paths:

- ObservabilityContext interface lives at `packages/observability/src/context.ts:43-51`
- obsContext ALS instance lives at `packages/observability/src/context.ts:57`
- W3C carrier write site: `packages/observability/src/wrappers/wrap-queue.ts:74-83`
- DEFAULT_DENY_KEYS array: `packages/observability/src/lib/scrub-pii.ts:34-52`
- wrapProcessorWithAls: `packages/queue/src/index.ts` (around line 70+ — re-verify exact line during implementation)

DO NOT cite `packages/queue/src/wrap-queue.ts` (CONTEXT.md is wrong) or `packages/observability/src/lib/obs-context.ts` (CONTEXT.md is wrong).

VERBATIM Mermaid diagrams (copy-paste from RESEARCH §Q3 — do NOT improvise):

Diagram 1 — sequenceDiagram for trace-propagation.md:

```
sequenceDiagram
    autonumber
    participant Client
    participant BunServe as Bun.serve fetch wrapper
    participant ObsMW as observabilityMiddleware
    participant CqrsBus as wrapCqrsBus
    participant Handler as command handler
    participant Drizzle as scopedDb (Drizzle)
    participant WrapQueue as wrapQueue (PRODUCER span)
    participant Redis as BullMQ / Redis
    participant Worker as wrapProcessorWithAls (CONSUMER span)
    participant JobHandler as job processor

    Client->>BunServe: HTTP POST /api/example<br/>traceparent: 00-abc...
    BunServe->>ObsMW: obsContext.run({ traceId, requestId, ... })
    ObsMW->>CqrsBus: bus.dispatch(CreateExampleCommand)
    CqrsBus->>Handler: command handler invoked<br/>(child span "cqrs.dispatch CreateExampleCommand")
    Handler->>Drizzle: scopedDb.insert(...)<br/>(span — Phase 21+)
    Drizzle-->>Handler: row
    Handler->>WrapQueue: queue.add("send-email", data)
    WrapQueue->>Redis: BullMQ enqueue<br/>job.data._otel = { traceparent }
    Redis-->>Worker: poll → job available
    Worker->>Worker: propagation.extract(job.data._otel) → parent ctx<br/>obsContext.run({ traceId: parent, ... })
    Worker->>JobHandler: processor(job)
    JobHandler-->>Worker: ok
    Worker-->>Redis: ack
    BunServe-->>Client: 200 OK<br/>traceparent emitted on response
```

Diagram 2 — stateDiagram-v2 for trace-propagation.md (INCLUDE the two `note right of` blocks):

```
stateDiagram-v2
    [*] --> NoFrame: process boot

    NoFrame --> ActiveFrame: obsContext.run(seed, fn)<br/>(only legal seed path — Plan 19-08 lint ban)
    ActiveFrame --> ActiveFrame: setTenantContext(...)<br/>setSpan(...)<br/>(in-place mutation, no new frame)

    ActiveFrame --> ForkedFrame: setImmediate / queueMicrotask<br/>Promise.then
    ForkedFrame --> ForkedFrame: ALS auto-propagates<br/>(node:async_hooks contract)
    ForkedFrame --> NoFrame: callback returns

    ActiveFrame --> EnqueueBoundary: wrapQueue.add(jobName, data)<br/>propagation.inject → job.data._otel
    EnqueueBoundary --> NoFrame: ALS frame ends with HTTP request
    NoFrame --> WorkerSeeded: worker.process(job)<br/>propagation.extract(job.data._otel)<br/>obsContext.run(jobCtx, processor)
    WorkerSeeded --> NoFrame: processor returns

    ActiveFrame --> [*]: HTTP response sent

    note right of ActiveFrame
        BANNED: obsContext.enterWith(...)
        Lint rule fails CI on introduction
        (Phase 19 / CTX-01)
    end note

    note right of EnqueueBoundary
        W3C carrier shape:
        { traceparent, tracestate? }
        Verified at packages/observability/src/wrappers/wrap-queue.ts:74-83
    end note
```

Diagram 3 — flowchart for docs/observability/README.md (small, brings count to 11):

```
flowchart LR
    Code[Application code] -->|"obsContext.run / setSpan"| Ports[Observability Ports]
    Ports -->|env-selected adapter| Adapters[Adapters]
    Adapters --> Sentry[SentryErrorTracker]
    Adapters --> Pino[PinoErrorTracker]
    Adapters --> Noop[NoopAdapters]
    Code -->|"wrapQueue / wrapProcessorWithAls"| Wrappers[ALS-aware wrappers]
    Wrappers --> Ports
```

Box labels MUST match real symbols (docs/README.md:43): obsContext, setSpan, SentryErrorTracker, PinoErrorTracker, wrapQueue, wrapProcessorWithAls are all real. Group nouns ("Application code", "Adapters", "Wrappers") follow the same usage as docs/architecture.md.
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → reader | Plain markdown rendered by GitHub. No untrusted input. The threat surface is incorrect or stale information misleading an operator at 3am. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-07 | Information Disclosure | docs/observability/cardinality.md leaking real PII patterns | mitigate | All "values that get scrubbed" examples use synthetic placeholders (e.g., user@example.com, cus_xxxxx). The 17-key denylist is referenced by name + line range — no real scrubbed-value examples. Existing scripts/validate-docs.ts invariant 2 catches sk_live_*, re_*, whsec_* if accidentally committed. |
| T-23-08 | Tampering | stale file:line refs across refactors | mitigate | All citations use the verified paths from RESEARCH "File Refs Verified" (2026-04-28). Citations include both file path AND line range; D-07 mandates 5-line snippets so context drift is visible in PR diffs. Future refactors that move these symbols surface in code-review while validate-docs.ts continues to pass — manual fix-up acknowledged as v1.4 docs-quality concern. |
| T-23-09 | Information Disclosure | example values in attributes.md | mitigate | Example column uses tenant_abc123, req_12345, cus_test — all synthetic placeholders. No real customer or internal data. |
| T-23-10 | Repudiation | Mermaid renderer divergence (local vs GitHub) | mitigate | Both diagrams use syntax already shipped in this repo (sequenceDiagram in 8 places; stateDiagram-v2 per RESEARCH Q3 verified GitHub-renderable). Pre-merge UAT: planner previews on draft PR before merge. |
| T-23-11 | Tampering | Mermaid floor mismatch breaking CI | mitigate | This plan ships the floor bump (8 to 11) IN THE SAME COMMIT as the new diagrams (Research Finding 5). Total Mermaid count post-plan: 11. Validator passes. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/observability/README.md (index + small flowchart) and docs/observability/attributes.md (glossary table)</name>
  <files>docs/observability/README.md, docs/observability/attributes.md</files>
  <read_first>
    - docs/README.md (mirror "Reading Order" + "Contents" pattern, lines 1-25)
    - docs/jsdoc-style-guide.md (Phase 13 D-01 single-canonical-style-guide precedent for attributes.md opener pattern, lines 1-23)
    - packages/observability/src/context.ts (verify ObservabilityContext interface lines — RESEARCH says 43-51, re-verify before quoting)
    - packages/observability/src/lib/scrub-pii.ts (verify DEFAULT_DENY_KEYS lines — RESEARCH says 34-52)
    - .planning/phases/23-runbooks-alert-templates-observability-docs/23-PATTERNS.md docs/observability/attributes.md section
  </read_first>
  <action>
    PART A — Create docs/observability/README.md (~30-50 lines including the flowchart).

    Open with H1 "Observability in Baseworks" + 1-paragraph "what observability looks like in this codebase" intro that names the v1.3 stack: ports = Tracer/MetricsProvider/ErrorTracker; adapters = Sentry, GlitchTip, pino, noop; wrappers = wrapQueue, wrapProcessorWithAls, wrapCqrsBus; ALS = obsContext.

    Add "Where observability lives in the code" section with the flowchart from `<context>` Diagram 3 (use a triple-backtick fenced mermaid block).

    Add "Reading Order" section with text: "Start with attributes.md to learn what context fields exist on every span/log/metric. Read cardinality.md next to learn which of those values must NOT become metric labels. Read trace-propagation.md last to see the end-to-end flow from HTTP request through CQRS dispatch through Drizzle query through BullMQ enqueue and back to the worker."

    Add "Contents" table with markdown links:
      - attributes.md — Glossary of legitimate context attributes (lives on span/log/metric, type, cardinality risk).
      - cardinality.md — Cardinality rules + Baseworks-specific high-card values forbidden as metric labels.
      - trace-propagation.md — Single-trace flow API to DB to enqueue to worker (Mermaid).

    Tone: declarative, present tense, technical-precise (per docs/README.md:26-28). No emojis, no "basically/simply/just."

    PART B — Create docs/observability/attributes.md (~60-100 lines).

    Open with H1 "Observability Attributes Glossary" + 4-line opener (mirror jsdoc-style-guide.md:1-9 shape):
      "This guide is the canonical reference for context attributes that flow through Baseworks's observability ports. Every value listed here lives on at least one of: a span (OTEL), a structured log (pino), or a metric (forward-looking — Phase 21+). The Cardinality risk column is decisive: HIGH-cardinality values must NEVER become metric labels (see cardinality.md)."

    Add "General Rules" section (mirror jsdoc-style-guide.md:10-23 bullet shape):
      - Source of truth: the ObservabilityContext interface at `packages/observability/src/context.ts:43-51`. New fields land there first; this glossary is updated in the same PR.
      - No PII in metric labels: see cardinality.md and the scrubber denylist at `packages/observability/src/lib/scrub-pii.ts:34-52`.
      - Lives-on convention: "span" = OTEL span attribute, "log" = pino structured log field (auto-injected from obsContext), "metric" = OTEL metric label (forward-looking).
      - Type column: TypeScript type (matches the source interface).
      - Cardinality risk: LOW (≤ ~10 values), MEDIUM (≤ ~1000), HIGH (unbounded). HIGH = forbidden as metric label, OK on span/log.

    Embed the 5-line ObservabilityContext snippet (D-07) starting with the source comment "// From packages/observability/src/context.ts:43-51" (use a fenced typescript code block).

    Add the 5-column glossary table with header `| Name | Lives on | Type | Example value | Cardinality risk |` and rows for at least the 9 D-08 values plus locale, traceId, spanId. Use synthetic placeholders for example values (T-23-09 mitigation):
      | requestId | span, log | string | req_a8f3b1 | HIGH |
      | traceId | span, log | string (W3C) | 4bf92f3577b34da6a3ce929d0e0e4736 | HIGH |
      | spanId | span, log | string (W3C) | 00f067aa0ba902b7 | HIGH |
      | tenantId | span, log | string | tenant_abc123 | HIGH |
      | userId | span, log | string | user_xyz789 | HIGH |
      | locale | span, log | string | pt-BR | LOW |
      | command | span | string | example.create | MEDIUM |
      | queryName | span | string | getSubscriptionStatus | MEDIUM |
      | jobId | span, log | string | bull_q1_4567 | HIGH |
      | stripeCustomerId | log | string | cus_test_xxxx | HIGH |
      | pagarmeCustomerId | log | string | pagarme_cust_xxxx | HIGH |

    Add "Cross-references" section: link to cardinality.md (HIGH-card enforcement) and trace-propagation.md (traceId/spanId flow).

    No frontmatter (Established Patterns).
  </action>
  <verify>
    <automated>test -f docs/observability/README.md && test -f docs/observability/attributes.md && grep -cE "^```mermaid$" docs/observability/README.md && grep -cE "^\| Name \| Lives on " docs/observability/attributes.md && grep -c "packages/observability/src/context.ts" docs/observability/attributes.md</automated>
  </verify>
  <acceptance_criteria>
    - Files docs/observability/README.md and docs/observability/attributes.md exist.
    - `grep -cE "^```mermaid$" docs/observability/README.md` returns 1.
    - `grep -cE "^# " docs/observability/README.md` returns 1 (single H1).
    - `grep -cE "^## " docs/observability/README.md` returns at least 3.
    - `grep -cE "^\| Name \| Lives on \| Type \| Example value \| Cardinality risk \|" docs/observability/attributes.md` returns 1.
    - `grep -c "tenantId" docs/observability/attributes.md` returns at least 1.
    - `grep -c "packages/observability/src/context.ts" docs/observability/attributes.md` returns at least 1.
    - `grep -c "packages/observability/src/lib/obs-context.ts" docs/observability/attributes.md` returns 0 (the WRONG path from CONTEXT.md must NOT appear).
    - No YAML frontmatter on either file: first line of each is not `---`.
    - No markdown image refs: `grep -E '!\[.*\]\(' docs/observability/README.md docs/observability/attributes.md` returns nothing.
  </acceptance_criteria>
  <done>The obs index page and the attributes glossary are live. The README ships 1 Mermaid block (flowchart) which counts toward the floor of 11. The attributes glossary cites the corrected file:line paths from RESEARCH.</done>
</task>

<task type="auto">
  <name>Task 2: Author docs/observability/cardinality.md and docs/observability/trace-propagation.md</name>
  <files>docs/observability/cardinality.md, docs/observability/trace-propagation.md</files>
  <read_first>
    - docs/architecture.md (mirror Mermaid + citation fidelity for trace-propagation.md)
    - packages/observability/src/wrappers/wrap-queue.ts (verify lines 43-46 and 74-83 before quoting)
    - packages/queue/src/index.ts (verify wrapProcessorWithAls location — RESEARCH says ~line 70+; re-verify)
    - packages/observability/src/context.ts (verify line 57 is the obsContext ALS instance; verify lines 75-84 for setTenantContext)
    - packages/observability/src/lib/scrub-pii.ts (verify DEFAULT_DENY_KEYS lines 34-52)
    - .planning/phases/23-runbooks-alert-templates-observability-docs/23-CONTEXT.md (D-08 — the 9 high-cardinality values list)
    - .planning/phases/23-runbooks-alert-templates-observability-docs/23-RESEARCH.md Q3 (Mermaid diagrams text — copy verbatim)
    - .planning/phases/23-runbooks-alert-templates-observability-docs/23-PATTERNS.md cardinality.md and trace-propagation.md sections
  </read_first>
  <action>
    PART A — Create docs/observability/cardinality.md (~50-80 lines).

    Open with H1 "Observability Cardinality Guide" + 4-line opener:
      "Cardinality is the number of unique values an attribute can take. HIGH-cardinality values create unbounded label spaces in metrics backends — they must NEVER become metric labels. They MAY appear on spans (one trace at a time) and structured logs (one event at a time), but a metric like requests_total{tenantId='...'} would explode the metrics backend."

    Add "Rules" section with 4 bullets:
      - Span attributes: any cardinality is fine. One span instance carries one set of values.
      - Structured log fields: any cardinality is fine. One log event carries one set of values.
      - Metric labels: LOW or MEDIUM cardinality only. HIGH-cardinality values are forbidden.
      - PII denylist overlap: every value on the scrubber denylist (`packages/observability/src/lib/scrub-pii.ts:34-52`) is HIGH-cardinality by definition. The two lists are not identical — `requestId` is high-card but not PII.

    Embed the 5-line scrub-pii snippet (D-07) starting with source comment "// From packages/observability/src/lib/scrub-pii.ts:34-38" inside a fenced typescript code block.

    Add "Baseworks-specific HIGH-cardinality values" section listing all 9 from D-08 verbatim:
      - tenantId — one per tenant; with N tenants per fork, label space = N. Forbidden.
      - userId — one per user; unbounded.
      - requestId — generated per-request via crypto.randomUUID(); unbounded.
      - email — one per user; unbounded; ALSO PII (scrubbed in error events).
      - command — string per CQRS command (e.g., example.create). MEDIUM at v1.3 (~50), but treated as HIGH out of caution.
      - queryName — same as command but for queries.
      - jobId — BullMQ-assigned per job; unbounded.
      - stripeCustomerId — one per Stripe customer; unbounded.
      - pagarmeCustomerId — one per Pagar.me customer; unbounded.

    Add "Anti-patterns" section: 3-4 examples of common cardinality blowups (e.g., latency_ms{path="/api/users/123"} should be latency_ms{route="/api/users/:id"} — matched-route template, not concrete URL).

    Add "Forward-looking: when OTLP wires" section (per Discretion-recommended-yes): note that v1.4+ OTLP exporters can introduce a Biome GritQL lint rule similar to Phase 19's enterWith ban for mechanical enforcement.

    Add "Cross-references" section: link to attributes.md (the glossary), and forward-looking link to ../runbooks/otel-exporter-failing.md (this target does not yet exist; Plan 03 ships it; the validator's Pass A only enforces cross-runbook links INSIDE docs/runbooks/, so links FROM docs/observability/ to docs/runbooks/ are NOT validated by Pass A).

    PART B — Create docs/observability/trace-propagation.md (~80-150 lines).

    Open with H1 "Trace Propagation in Baseworks" + 5-line opener:
      "A request enters at the Bun.serve fetch boundary, opens an obsContext frame seeded with traceId/requestId/tenantId/userId, dispatches a CQRS command, queries the database, enqueues a BullMQ job, and returns. The same traceId flows from the HTTP request through to the worker that processes the job. This document maps every hop with verified file:line refs to the v1.3 implementation."

    Add "Diagram 1: HTTP request flow" section. Embed Diagram 1 from `<context>` (sequenceDiagram, VERBATIM) inside a fenced mermaid block.

    Add a 4-6 paragraph walkthrough of Diagram 1, citing the corrected paths:
      - "ObsMW: obsContext.run()" — cite `packages/observability/src/context.ts:57` for the ALS instance.
      - "WrapQueue → Redis: BullMQ enqueue" — cite `packages/observability/src/wrappers/wrap-queue.ts:74-83` and embed the 5-line snippet showing carrier injection.
      - "Worker: propagation.extract" — cite `packages/queue/src/index.ts` wrapProcessorWithAls (verify exact line during implementation).

    Add "Diagram 2: ALS lifecycle" section. Embed Diagram 2 from `<context>` (stateDiagram-v2, VERBATIM, INCLUDING the two `note right of` blocks) inside a fenced mermaid block.

    Add a 3-4 paragraph walkthrough of Diagram 2:
      - The legal-seed-only invariant: cite Phase 19's enterWith ban + lint rule.
      - The W3C carrier shape: { traceparent, tracestate? } — cite wrap-queue.ts:74-83.
      - The fork → restore lifecycle: ALS auto-propagates over setImmediate / queueMicrotask / Promise.then.

    Embed the 5-line wrap-queue carrier snippet starting with source comment "// From packages/observability/src/wrappers/wrap-queue.ts:74-83" inside a fenced typescript code block.

    Add "Cross-references" section: link to attributes.md (field shape), cardinality.md (HIGH-card enforcement), and forward-looking link to ../runbooks/otel-exporter-failing.md.

    NO frontmatter. NO screenshots. Box labels in Mermaid MUST match real symbols (per docs/README.md:43) — RESEARCH §Q3 confirms every participant is a real symbol.

    Tone: declarative, technical-precise.
  </action>
  <verify>
    <automated>test -f docs/observability/cardinality.md && test -f docs/observability/trace-propagation.md && [ "$(grep -cE '^```mermaid$' docs/observability/trace-propagation.md)" = "2" ] && grep -c "tenantId" docs/observability/cardinality.md && grep -c "packages/observability/src/wrappers/wrap-queue.ts" docs/observability/trace-propagation.md</automated>
  </verify>
  <acceptance_criteria>
    - Files docs/observability/cardinality.md and docs/observability/trace-propagation.md exist.
    - `grep -cE "^```mermaid$" docs/observability/trace-propagation.md` returns exactly 2.
    - `grep -c "sequenceDiagram" docs/observability/trace-propagation.md` returns at least 1.
    - `grep -c "stateDiagram-v2" docs/observability/trace-propagation.md` returns at least 1.
    - `grep -c "tenantId" docs/observability/cardinality.md` returns at least 1.
    - `grep -c "stripeCustomerId" docs/observability/cardinality.md` returns at least 1 (D-08 9-value coverage).
    - `grep -c "pagarmeCustomerId" docs/observability/cardinality.md` returns at least 1.
    - `grep -c "packages/observability/src/wrappers/wrap-queue.ts" docs/observability/trace-propagation.md` returns at least 1.
    - `grep -c "packages/queue/src/wrap-queue.ts" docs/observability/trace-propagation.md` returns 0 (WRONG path must NOT appear).
    - `grep -c "packages/observability/src/lib/scrub-pii.ts" docs/observability/cardinality.md` returns at least 1.
    - No YAML frontmatter (first line is not `---`).
    - No markdown image refs in either file.
  </acceptance_criteria>
  <done>The cardinality guide and trace-propagation walkthrough are live. trace-propagation.md ships exactly 2 Mermaid blocks. Cardinality enumerates the full D-08 9-value list with corrected scrub-pii citation. Both files use the corrected paths.</done>
</task>

<task type="auto">
  <name>Task 3: Bump Mermaid floor literal in scripts/validate-docs.ts from 8 to 11 and verify full docs corpus</name>
  <files>scripts/validate-docs.ts</files>
  <read_first>
    - scripts/validate-docs.ts (current Mermaid floor at line 69: `if (mermaidTotal < 8)`)
    - .planning/phases/23-runbooks-alert-templates-observability-docs/23-CONTEXT.md D-06 ("8 → 11" decisive)
    - .planning/phases/23-runbooks-alert-templates-observability-docs/23-RESEARCH.md §Q3 last paragraph (existing 8 + 2 new + 1 buffer = 11)
  </read_first>
  <action>
    Edit scripts/validate-docs.ts. Two literal changes:

    1. Line 69 (the if condition): change `if (mermaidTotal < 8) {` to `if (mermaidTotal < 11) {`.

    2. Line 71 (the error message inside the if-body): update the message to reference 11 and the new trace-propagation.md source. Replace:
       `[validate-docs] FAIL: found ${mermaidTotal} Mermaid fenced blocks across docs/; D-01 requires at least 8 (4 in docs/architecture.md + 1 per integration doc).`
       with:
       `[validate-docs] FAIL: found ${mermaidTotal} Mermaid fenced blocks across docs/; floor is 11 (4 in docs/architecture.md + 1 per integration doc + 2 in docs/observability/trace-propagation.md + 1 in docs/observability/README.md, with 1 buffer).`

    3. Update the JSDoc header at top of file: change "at least 8 Mermaid fenced code blocks" to "at least 11 Mermaid fenced code blocks" in invariant 3's documentation. The post-edit JSDoc invariant-3 line should read approximately:
       `*   3. Mermaid coverage floor: per D-01 (Phase 15) + D-06 (Phase 23), at least 11 Mermaid fenced code blocks appear across docs/ (4 architecture diagrams + 4 integration sequenceDiagrams + 2 trace-propagation + 1 obs README, with 1 buffer).`

    Use the Edit tool for these literal changes. Do NOT touch other parts of the file — Plan 01 already added the 4th invariant Pass A and Pass B; this task only bumps the floor literal.

    AFTER the edit, run `bun run validate` to verify the corpus passes:
    - 8 existing Mermaid blocks (4 architecture + 4 integrations) + 2 in trace-propagation.md + 1 in README.md = 11. Floor = 11. Validator emits OK message and exits 0.
    - If count is < 11 (e.g., one of the new blocks didn't render its `^```mermaid$` line because of nested triple-backticks or wrong fencing), this task fails — go back to Task 1 or 2 and fix the affected file.
  </action>
  <verify>
    <automated>grep -c "if (mermaidTotal < 11)" scripts/validate-docs.ts && bun run validate</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "if (mermaidTotal < 11)" scripts/validate-docs.ts` returns exactly 1.
    - `grep -c "if (mermaidTotal < 8)" scripts/validate-docs.ts` returns 0 (old literal removed).
    - `grep -c "floor is 11" scripts/validate-docs.ts` returns 1 (new error message).
    - `grep -c "at least 11 Mermaid" scripts/validate-docs.ts` returns 1 (JSDoc updated).
    - `bun run validate` exits 0 with stdout/stderr containing `OK: found 11 Mermaid fenced blocks across docs/ (>= 11 required)` (or similar — text wording may vary slightly with the OK-branch existing message; the count must be exactly 11).
    - `bun test scripts/__tests__/observability-docs-present.test.ts` reports 5/5 passing (previously RED, now GREEN).
    - `bun test scripts/__tests__/validate-docs.test.ts` continues to pass 7/7 (Plan 01's tests still GREEN).
  </acceptance_criteria>
  <done>The Mermaid floor literal is 11. Total docs corpus has exactly 11 Mermaid blocks. Validator passes. Plan 01's existing tests still pass. Plan 01's observability-docs-present.test.ts is now fully GREEN. The floor bump is in the same commit as the new diagrams (Research Finding 5: half-merged state avoided).</done>
</task>

</tasks>

<verification>
At the end of this plan:
- 4 markdown files exist under docs/observability/.
- Total Mermaid fenced blocks across docs/: 11 (4 architecture + 4 integrations + 2 trace-propagation + 1 README).
- `bun run validate` exits 0.
- `bun test scripts/__tests__/observability-docs-present.test.ts` reports 5/5 passing.
- `bun test scripts/__tests__/validate-docs.test.ts` reports 7/7 passing.
- Plans 03 and 04 (running in Wave 2 alongside this plan) reference attributes.md/cardinality.md/trace-propagation.md anchors via cross-runbook links — those are validated by Pass A. Wave 2 ordering: Plans 02/03/04 are parallel; Plan 03 ships runbook files that link to docs/observability/*; Pass A in Plan 01's validator validates those cross-doc links AT MERGE TIME. The order within Wave 2 is irrelevant as long as all three land before Plan 05's CI smoke check.
</verification>

<success_criteria>
- All 3 tasks complete with their `<acceptance_criteria>` met.
- Mermaid floor literal is 11; corpus count is 11; validator passes.
- All citations use the corrected paths (no `packages/queue/src/wrap-queue.ts`, no `packages/observability/src/lib/obs-context.ts`).
- `bun run typecheck` passes (no new TS files added; only doc edits).
- `biome check .` passes on the validate-docs.ts edit.
</success_criteria>

<output>
After completion, create `.planning/phases/23-runbooks-alert-templates-observability-docs/23-02-SUMMARY.md` documenting:
- Final line counts of the 4 obs concept docs.
- Confirmed final Mermaid block count (must be 11).
- Any deviation from the verbatim Mermaid diagrams in RESEARCH §Q3 (with rationale).
- Any auto-fix Rule-1 deviations from the plan text.
- Verified file:line references at exec time (re-confirmed paths if symbols moved between research date and execution date).
</output>
