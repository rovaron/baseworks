---
phase: 13-jsdoc-annotations
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - packages/modules/auth/src/commands/create-invitation.ts
  - packages/modules/auth/src/commands/accept-invitation.ts
  - packages/modules/auth/src/commands/reject-invitation.ts
  - packages/modules/auth/src/commands/cancel-invitation.ts
  - packages/modules/auth/src/commands/create-tenant.ts
  - packages/modules/auth/src/commands/delete-tenant.ts
  - packages/modules/auth/src/commands/update-tenant.ts
  - packages/modules/auth/src/commands/update-profile.ts
  - packages/modules/auth/src/queries/get-tenant.ts
  - packages/modules/auth/src/queries/list-tenants.ts
  - packages/modules/auth/src/queries/list-members.ts
  - packages/modules/auth/src/queries/get-profile.ts
  - packages/modules/auth/src/queries/get-invitation.ts
  - packages/modules/auth/src/queries/list-invitations.ts
  - packages/modules/auth/src/middleware.ts
  - packages/modules/auth/src/routes.ts
  - packages/modules/auth/src/auth.ts
  - packages/modules/auth/src/locale-context.ts
autonomous: true
requirements:
  - JSDOC-02
  - JSDOC-03
  - JSDOC-05

must_haves:
  truths:
    - "All 8 auth command handlers have standardized JSDoc with @param, @returns, and events emitted where applicable"
    - "All 6 auth query handlers have standardized JSDoc with @param and @returns"
    - "Auth middleware (betterAuthPlugin, requireRole) has JSDoc with @example on requireRole"
    - "Auth routes.ts has JSDoc on the plugin export and makeCtx helper"
  artifacts:
    - path: "packages/modules/auth/src/commands/create-invitation.ts"
      provides: "Gold standard handler JSDoc with @param/@returns added per D-03"
      contains: "@param"
    - path: "packages/modules/auth/src/commands/delete-tenant.ts"
      provides: "Handler JSDoc documenting TenantDeleted event emission"
      contains: "@returns"
    - path: "packages/modules/auth/src/middleware.ts"
      provides: "requireRole JSDoc with @example"
      contains: "@example"
  key_links:
    - from: "docs/jsdoc-style-guide.md"
      to: "all auth module files"
      via: "Handler template from style guide applied to all defineCommand/defineQuery exports"
      pattern: "defineCommand"
---

<objective>
Annotate all auth module exports: 8 command handlers, 6 query handlers, middleware, routes, and supporting files.

Purpose: The auth module is the largest module with 14 CQRS handlers. Standardizing their JSDoc per D-01/D-03 ensures consistent documentation for the most complex domain in the codebase. Satisfies JSDOC-02 (commands) and JSDOC-03 (queries) for the auth module.

Output: 18 files annotated with standardized JSDoc following the style guide from Plan 01.
</objective>

<execution_context>
@C:/Projetos/baseworks/.claude/get-shit-done/workflows/execute-plan.md
@C:/Projetos/baseworks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-jsdoc-annotations/13-CONTEXT.md
@.planning/phases/13-jsdoc-annotations/13-PATTERNS.md
@.planning/phases/13-jsdoc-annotations/13-01-SUMMARY.md
@docs/jsdoc-style-guide.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Annotate auth command handlers (8 files)</name>
  <files>
    packages/modules/auth/src/commands/create-invitation.ts,
    packages/modules/auth/src/commands/accept-invitation.ts,
    packages/modules/auth/src/commands/reject-invitation.ts,
    packages/modules/auth/src/commands/cancel-invitation.ts,
    packages/modules/auth/src/commands/create-tenant.ts,
    packages/modules/auth/src/commands/delete-tenant.ts,
    packages/modules/auth/src/commands/update-tenant.ts,
    packages/modules/auth/src/commands/update-profile.ts
  </files>
  <read_first>
    packages/modules/auth/src/commands/create-invitation.ts,
    packages/modules/auth/src/commands/accept-invitation.ts,
    packages/modules/auth/src/commands/reject-invitation.ts,
    packages/modules/auth/src/commands/cancel-invitation.ts,
    packages/modules/auth/src/commands/create-tenant.ts,
    packages/modules/auth/src/commands/delete-tenant.ts,
    packages/modules/auth/src/commands/update-tenant.ts,
    packages/modules/auth/src/commands/update-profile.ts,
    docs/jsdoc-style-guide.md
  </read_first>
  <action>
For each of the 8 command handler files, read the file first, then add or rewrite JSDoc per D-01/D-03/D-04 using the defineCommand handler template from the style guide.

**Template to apply (from style guide section 2.1):**
```typescript
/**
 * [One-line action statement -- verb + object + outcome.]
 *
 * [Optional: elaborate on non-obvious logic, modes, or side effects.]
 *
 * @param input - [Brief description of validated input shape]
 * @param ctx   - Handler context: tenantId, userId, db, emit
 * @returns     - Result<[OutputType]> -- [describe what data is in success case]
 * @throws      - [Only if handler re-throws instead of returning err()]
 *
 * Per X-YY: [Design reference if relevant.]
 */
```

**Per-file specifics:**

1. **create-invitation.ts** -- Already has gold-standard prose. ADD `@param input - CreateInvitationInput with email/mode, role, organizationId`, `@param ctx - Handler context with tenantId, userId, db, emit`, `@returns Result<{ invitation: Invitation }> with invitation ID for link mode or void for email mode`. Keep existing prose and Per D-XX references.

2. **accept-invitation.ts** -- Read existing block. Add/ensure: opening line "Accept a pending invitation and add the user as a member of the organization.", @param input (invitationId), @param ctx, @returns Result with member record or error. Document that it emits InvitationAccepted event if applicable.

3. **reject-invitation.ts** -- If missing JSDoc, add full block: "Reject a pending invitation. Marks the invitation as rejected without adding the user to the organization." @param input (invitationId), @param ctx, @returns Result.

4. **cancel-invitation.ts** -- If missing, add: "Cancel a pending invitation sent by the organization. Only the inviting organization can cancel." @param, @param ctx, @returns.

5. **create-tenant.ts** -- Read existing. Add/ensure @param input (name, slug or similar), @param ctx, @returns Result<{ tenant }> with created tenant. Document TenantCreated event emission.

6. **delete-tenant.ts** -- Read existing. Add/ensure @param, @param ctx, @returns. Document TenantDeleted event emission and that it requires owner role.

7. **update-tenant.ts** -- Audit; add full block if missing: "Update tenant display name or settings." @param, @param ctx, @returns Result with updated tenant.

8. **update-profile.ts** -- Read existing. Add/ensure @param input (name, avatar, etc.), @param ctx, @returns Result with updated profile.

**Rules for all files:**
- Per D-02: Technical-precise tone. No tutorial language.
- Per D-03: Always include @param and @returns even if TypeScript already expresses types.
- Per D-04: Rewrite any existing JSDoc that doesn't match the standard.
- Keep "Per X-YY:" references that already exist in the code.
- Tag order: description, @param, @returns, @throws, @example, @see, Per references.
- Lines must not exceed 100 characters (Biome line width).
  </action>
  <verify>
    <automated>for f in packages/modules/auth/src/commands/*.ts; do echo "$f:"; grep -c "@param" "$f"; done</automated>
    <automated>for f in packages/modules/auth/src/commands/*.ts; do echo "$f:"; grep -c "@returns" "$f"; done</automated>
    <automated>cd C:/Projetos/baseworks && bunx biome check packages/modules/auth/src/commands/ --max-diagnostics=20</automated>
  </verify>
  <acceptance_criteria>
    - All 8 files in packages/modules/auth/src/commands/ contain "@param input" (grep returns >= 1 per file)
    - All 8 files contain "@returns" (grep returns >= 1 per file)
    - create-invitation.ts still contains "Per D-04" or "Per D-13" (existing references preserved)
    - delete-tenant.ts contains "TenantDeleted" in its JSDoc block
    - create-tenant.ts contains "TenantCreated" in its JSDoc block
    - All files pass `bunx biome check` with no errors
  </acceptance_criteria>
  <done>All 8 auth command handlers have standardized JSDoc with @param, @returns, and event documentation per D-01/D-03 standard.</done>
</task>

<task type="auto">
  <name>Task 2: Annotate auth queries and supporting files (10 files)</name>
  <files>
    packages/modules/auth/src/queries/get-tenant.ts,
    packages/modules/auth/src/queries/list-tenants.ts,
    packages/modules/auth/src/queries/list-members.ts,
    packages/modules/auth/src/queries/get-profile.ts,
    packages/modules/auth/src/queries/get-invitation.ts,
    packages/modules/auth/src/queries/list-invitations.ts,
    packages/modules/auth/src/middleware.ts,
    packages/modules/auth/src/routes.ts,
    packages/modules/auth/src/auth.ts,
    packages/modules/auth/src/locale-context.ts
  </files>
  <read_first>
    packages/modules/auth/src/queries/get-tenant.ts,
    packages/modules/auth/src/queries/list-tenants.ts,
    packages/modules/auth/src/queries/list-members.ts,
    packages/modules/auth/src/queries/get-profile.ts,
    packages/modules/auth/src/queries/get-invitation.ts,
    packages/modules/auth/src/queries/list-invitations.ts,
    packages/modules/auth/src/middleware.ts,
    packages/modules/auth/src/routes.ts,
    packages/modules/auth/src/auth.ts,
    packages/modules/auth/src/locale-context.ts,
    docs/jsdoc-style-guide.md
  </read_first>
  <action>
**Query handlers (6 files):**

Apply the defineQuery handler template from the style guide to each:

1. **get-tenant.ts** -- Read existing. Add/ensure: "Retrieve a single tenant by ID within the current tenant scope." @param input (tenantId or similar), @param ctx, @returns Result<{ tenant }> or err if not found.

2. **list-tenants.ts** -- "List all tenants the authenticated user belongs to." @param input (pagination/filter params if any), @param ctx, @returns Result<{ tenants: Tenant[] }>.

3. **list-members.ts** -- "List all members of the current tenant organization." @param input, @param ctx, @returns Result<{ members: Member[] }>.

4. **get-profile.ts** -- "Retrieve the authenticated user's profile. Uses ctx.userId for direct DB query instead of session re-fetch." @param input, @param ctx, @returns Result<{ profile }>.

5. **get-invitation.ts** -- "Retrieve a single invitation by ID." @param input (invitationId), @param ctx, @returns Result<{ invitation }> or err if not found.

6. **list-invitations.ts** -- "List all pending invitations for the current tenant organization." @param input, @param ctx, @returns Result<{ invitations: Invitation[] }>.

**Supporting files (4 files):**

7. **middleware.ts** -- Already has JSDoc on betterAuthPlugin and requireRole. Per D-03, add @param/@returns to requireRole. Per D-05, add @example to requireRole:
   ```
   @example
   app.use(requireRole("owner", "admin")).delete("/tenant", handler);
   ```
   Ensure betterAuthPlugin has @returns describing what it derives (session, user into context).

8. **routes.ts** -- Already has plugin-level block. Add @param/@returns to `makeCtx` helper function describing how it builds HandlerContext from Elysia context. Use Elysia plugin template for the routes export.

9. **auth.ts** -- This is the better-auth configuration file. Add file-level block: "better-auth instance configuration for Baseworks. Configures email/password, OAuth (Google/GitHub), magic link, organization plugin, and database hooks." Document any exported symbols per style guide.

10. **locale-context.ts** -- Read and audit. Add JSDoc per the appropriate template (likely a plain function or Elysia plugin). Describe what locale context it provides.

**Rules:** Same as Task 1 -- D-01/D-02/D-03/D-04, tag order, 100-char line width.
  </action>
  <verify>
    <automated>for f in packages/modules/auth/src/queries/*.ts; do echo "$f:"; grep -c "@param" "$f"; done</automated>
    <automated>grep -c "@example" packages/modules/auth/src/middleware.ts</automated>
    <automated>grep -c "/\*\*" packages/modules/auth/src/auth.ts</automated>
    <automated>cd C:/Projetos/baseworks && bunx biome check packages/modules/auth/src/ --max-diagnostics=20</automated>
  </verify>
  <acceptance_criteria>
    - All 6 files in packages/modules/auth/src/queries/ contain "@param" and "@returns"
    - packages/modules/auth/src/middleware.ts contains "@example" (grep returns >= 1)
    - packages/modules/auth/src/middleware.ts contains "@param" for requireRole
    - packages/modules/auth/src/routes.ts contains "@param" for makeCtx
    - packages/modules/auth/src/auth.ts contains "/** " (at least one JSDoc block)
    - packages/modules/auth/src/locale-context.ts contains "/** " (at least one JSDoc block)
    - All files pass `bunx biome check` with no errors
  </acceptance_criteria>
  <done>All 6 auth query handlers have standardized JSDoc. Auth middleware has @example on requireRole (7th of 10-15 target). Auth supporting files (routes, auth config, locale-context) fully documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected -- documentation-only changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02 | I (Information Disclosure) | JSDoc comments | accept | No secrets or internal URLs in documentation. Low risk. |
</threat_model>

<verification>
1. All 8 auth command handlers have @param and @returns tags
2. All 6 auth query handlers have @param and @returns tags
3. requireRole has an @example block
4. makeCtx in routes.ts has @param/@returns
5. auth.ts and locale-context.ts have JSDoc blocks
6. `bunx biome check packages/modules/auth/src/` passes
</verification>

<success_criteria>
- 14 CQRS handlers fully annotated per D-01/D-02/D-03/D-04
- 4 supporting files annotated
- 1 @example block added (requireRole)
- Biome check passes on entire auth module
</success_criteria>

<output>
After completion, create `.planning/phases/13-jsdoc-annotations/13-02-SUMMARY.md`
</output>
