---
phase: 14-unit-tests
plan: 05
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - packages/modules/billing/src/__tests__/create-checkout-session.test.ts
  - packages/modules/billing/src/__tests__/cancel-subscription.test.ts
  - packages/modules/billing/src/__tests__/change-subscription.test.ts
  - packages/modules/billing/src/__tests__/create-one-time-payment.test.ts
  - packages/modules/billing/src/__tests__/create-portal-session.test.ts
  - packages/modules/billing/src/__tests__/record-usage.test.ts
  - packages/modules/billing/src/__tests__/get-subscription-status.test.ts
  - packages/modules/billing/src/__tests__/get-billing-history.test.ts
  - packages/modules/billing/src/__tests__/stripe-adapter.test.ts
autonomous: true
requirements:
  - TEST-03
  - TEST-04
  - TEST-05
must_haves:
  truths:
    - "All 6 billing command handlers have behavioral unit tests"
    - "Both billing query handlers have behavioral unit tests"
    - "Stripe adapter has conformance tests mirroring pagarme-adapter.test.ts structure"
    - "Each handler test uses createMockContext/createMockDb from shared __test-utils__"
    - "Billing tests use setPaymentProvider/resetPaymentProvider for provider injection"
  artifacts:
    - path: "packages/modules/billing/src/__tests__/create-checkout-session.test.ts"
      provides: "create-checkout-session behavioral tests"
      contains: "describe(\"createCheckoutSession\""
    - path: "packages/modules/billing/src/__tests__/cancel-subscription.test.ts"
      provides: "cancel-subscription behavioral tests"
      contains: "describe(\"cancelSubscription\""
    - path: "packages/modules/billing/src/__tests__/change-subscription.test.ts"
      provides: "change-subscription behavioral tests"
      contains: "describe(\"changeSubscription\""
    - path: "packages/modules/billing/src/__tests__/create-one-time-payment.test.ts"
      provides: "create-one-time-payment behavioral tests"
      contains: "describe(\"createOneTimePayment\""
    - path: "packages/modules/billing/src/__tests__/create-portal-session.test.ts"
      provides: "create-portal-session behavioral tests"
      contains: "describe(\"createPortalSession\""
    - path: "packages/modules/billing/src/__tests__/record-usage.test.ts"
      provides: "record-usage behavioral tests"
      contains: "describe(\"recordUsage\""
    - path: "packages/modules/billing/src/__tests__/get-subscription-status.test.ts"
      provides: "get-subscription-status behavioral tests"
      contains: "describe(\"getSubscriptionStatus\""
    - path: "packages/modules/billing/src/__tests__/get-billing-history.test.ts"
      provides: "get-billing-history behavioral tests"
      contains: "describe(\"getBillingHistory\""
    - path: "packages/modules/billing/src/__tests__/stripe-adapter.test.ts"
      provides: "Stripe adapter conformance tests"
      contains: "describe(\"StripeAdapter\""
  key_links:
    - from: "packages/modules/billing/src/__tests__/*.test.ts"
      to: "packages/modules/__test-utils__/mock-context.ts"
      via: "import createMockContext, createMockDb"
      pattern: "from.*__test-utils__/mock-context"
    - from: "packages/modules/billing/src/__tests__/*.test.ts"
      to: "packages/modules/billing/src/provider-factory.ts"
      via: "import setPaymentProvider, resetPaymentProvider"
      pattern: "from.*provider-factory"
---

<objective>
Create behavioral unit tests for all 8 billing handlers and Stripe adapter conformance tests.

Purpose: Completes TEST-03 (billing commands), TEST-04 (billing queries), and TEST-05 (Stripe adapter parity with Pagarme). Billing handlers use ctx.db (scoped-db chainable API) + getPaymentProvider(), requiring a different mock strategy than auth handlers.
Output: 9 new test files.
</objective>

<execution_context>
@C:/Projetos/baseworks/.claude/get-shit-done/workflows/execute-plan.md
@C:/Projetos/baseworks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-unit-tests/14-CONTEXT.md
@.planning/phases/14-unit-tests/14-RESEARCH.md
@.planning/phases/14-unit-tests/14-01-SUMMARY.md

<interfaces>
<!-- Billing handler mock strategy (from RESEARCH.md): -->
<!-- 1. mock.module("@baseworks/config", ...) to prevent env crash -->
<!-- 2. mock.module("stripe", ...) to prevent Stripe constructor crash -->
<!-- 3. Dynamic import handler after mocks -->
<!-- 4. setPaymentProvider(mockProvider) in beforeEach for handler tests -->
<!-- 5. createMockDb({ select: [...] }) for db chain returns -->
<!-- 6. resetPaymentProvider() in beforeEach to clear singleton -->

<!-- Billing handlers use: ctx.db.select().from(table).where(eq(...)).limit(1) -->
<!-- Returns array, destructured as: const [customer] = await ctx.db.select()... -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create billing command handler tests (6 handlers)</name>
  <files>
    packages/modules/billing/src/__tests__/create-checkout-session.test.ts
    packages/modules/billing/src/__tests__/cancel-subscription.test.ts
    packages/modules/billing/src/__tests__/change-subscription.test.ts
    packages/modules/billing/src/__tests__/create-one-time-payment.test.ts
    packages/modules/billing/src/__tests__/create-portal-session.test.ts
    packages/modules/billing/src/__tests__/record-usage.test.ts
  </files>
  <read_first>
    packages/modules/billing/src/commands/create-checkout-session.ts
    packages/modules/billing/src/commands/cancel-subscription.ts
    packages/modules/billing/src/commands/change-subscription.ts
    packages/modules/billing/src/commands/create-one-time-payment.ts
    packages/modules/billing/src/commands/create-portal-session.ts
    packages/modules/billing/src/commands/record-usage.ts
    packages/modules/billing/src/provider-factory.ts
    packages/modules/billing/src/__tests__/billing.test.ts
    packages/modules/__test-utils__/mock-context.ts
    packages/modules/__test-utils__/mock-payment-provider.ts
    packages/modules/__test-utils__/assert-result.ts
  </read_first>
  <action>
Create 6 test files for billing command handlers (per D-06, D-03). Each file follows the billing mock pattern:

**Common pattern for ALL billing handler tests:**
```
1. import { describe, test, expect, mock, beforeEach } from "bun:test"
2. import { createMockContext, createMockDb } from "../../../__test-utils__/mock-context"
3. import { createMockPaymentProvider } from "../../../__test-utils__/mock-payment-provider"
4. import { assertResultOk, assertResultErr } from "../../../__test-utils__/assert-result"
5. mock.module("@baseworks/config", () => ({ env: { DATABASE_URL: "postgres://test@localhost/test", NODE_ENV: "test" } }))
6. mock.module("stripe", () => ({ default: class {} }))
7. const { setPaymentProvider, resetPaymentProvider } = await import("../provider-factory")
8. const { handlerName } = await import("../commands/handler-name")
9. describe block with beforeEach calling resetPaymentProvider() + setPaymentProvider(mockProvider)
```

**cancel-subscription.test.ts (~3 tests):**
Read source: queries billingCustomers for providerSubscriptionId, calls provider.cancelSubscription.
- "cancels subscription at period end" -- mockDb select returns `[{ providerSubscriptionId: "sub_123", currentPeriodEnd: new Date("2026-05-01") }]`, verify ok with `cancelledAt: "period_end"`, verify mockProvider.cancelSubscription called with `{ providerSubscriptionId: "sub_123", cancelAtPeriodEnd: true }`
- "returns error when no active subscription" -- mockDb select returns `[{}]` (no providerSubscriptionId), verify err "NO_ACTIVE_SUBSCRIPTION"
- "returns error when provider throws" -- mockDb returns valid customer, mock provider.cancelSubscription to reject, verify err

**create-checkout-session.test.ts (~3 tests):**
Read source: queries billingCustomers for providerCustomerId, calls provider.createCheckoutSession.
- "creates checkout session" -- mockDb select returns `[{ providerCustomerId: "cus_123" }]`, verify ok with session data
- "returns error when no billing customer" -- mockDb select returns `[]`, verify err
- "returns error when provider throws" -- mock rejection

**change-subscription.test.ts (~3 tests):**
Read source: queries for active subscription, calls provider.changeSubscription.
- "changes subscription to new price" -- mockDb returns customer with active sub, verify ok
- "returns error when no active subscription" -- mockDb returns customer without sub, verify err
- "returns error when provider throws" -- mock rejection

**create-one-time-payment.test.ts (~3 tests):**
Read source: queries billingCustomers, calls provider.createOneTimePayment.
- "creates one-time payment session" -- mockDb returns customer, verify ok with session/url
- "returns error when no billing customer" -- mockDb returns empty, verify err
- "returns error when provider throws" -- mock rejection

**create-portal-session.test.ts (~3 tests):**
Read source: queries billingCustomers, calls provider.createPortalSession.
- "creates portal session" -- mockDb returns customer, verify ok with url
- "returns error when no billing customer" -- mockDb returns empty, verify err
- "returns error when portal not supported" -- mock provider.createPortalSession to return null, verify behavior

**record-usage.test.ts (~3 tests):**
Read source: likely inserts into usageRecords table via ctx.db.insert.
- "records usage successfully" -- verify ok with recorded flag
- "validates metric and quantity" -- provide invalid input, verify err or validation failure
- Read source carefully -- record-usage may call provider.reportUsage OR just insert into DB. Mock accordingly.
  </action>
  <verify>
    <automated>cd C:/Projetos/baseworks && bun test packages/modules/billing/src/__tests__/cancel-subscription.test.ts packages/modules/billing/src/__tests__/create-checkout-session.test.ts packages/modules/billing/src/__tests__/change-subscription.test.ts packages/modules/billing/src/__tests__/create-one-time-payment.test.ts packages/modules/billing/src/__tests__/create-portal-session.test.ts packages/modules/billing/src/__tests__/record-usage.test.ts --bail</automated>
  </verify>
  <acceptance_criteria>
    - All 6 billing command test files exist with `describe("handlerName"` blocks
    - Each file has at least 2 tests (success + error case)
    - Each file contains `mock.module("@baseworks/config"` and `mock.module("stripe"`
    - Each file imports from `__test-utils__/mock-context` and `__test-utils__/mock-payment-provider`
    - Each file uses `setPaymentProvider` and `resetPaymentProvider` in beforeEach
    - `bun test` on all 6 exits 0
  </acceptance_criteria>
  <done>6 billing command handler test files created with ~3 tests each. All use shared mock utilities and provider injection pattern. All pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create billing query handler tests (2 handlers)</name>
  <files>
    packages/modules/billing/src/__tests__/get-subscription-status.test.ts
    packages/modules/billing/src/__tests__/get-billing-history.test.ts
  </files>
  <read_first>
    packages/modules/billing/src/queries/get-subscription-status.ts
    packages/modules/billing/src/queries/get-billing-history.ts
    packages/modules/billing/src/provider-factory.ts
    packages/modules/__test-utils__/mock-context.ts
    packages/modules/__test-utils__/mock-payment-provider.ts
    packages/modules/__test-utils__/assert-result.ts
  </read_first>
  <action>
Create 2 test files for billing query handlers (per D-06, D-03). Same billing mock pattern as Task 1.

**get-subscription-status.test.ts (~3 tests):**
Read source: queries billingCustomers for subscription data.
- "returns active subscription status when record exists" -- mockDb select returns `[{ providerSubscriptionId: "sub_123", status: "active", priceId: "price_abc", currentPeriodEnd: new Date() }]`, verify ok with subscription data
- "returns inactive/no-subscription when no record found" -- mockDb select returns `[]`, verify ok with inactive defaults or err (read source to determine)
- "returns error when db query throws" -- mock db.select to throw, verify err

**get-billing-history.test.ts (~3 tests):**
Read source: queries billingCustomers, then calls provider.getInvoices.
- "returns invoices when customer exists" -- mockDb returns customer with providerCustomerId, mock provider.getInvoices to return `[{ id: "inv_1", amount: 1000, status: "paid" }]`, verify ok
- "returns empty array when no billing customer" -- mockDb returns `[]`, verify ok with empty invoices (or err -- read source)
- "returns error when provider throws" -- mock getInvoices rejection, verify err
  </action>
  <verify>
    <automated>cd C:/Projetos/baseworks && bun test packages/modules/billing/src/__tests__/get-subscription-status.test.ts packages/modules/billing/src/__tests__/get-billing-history.test.ts --bail</automated>
  </verify>
  <acceptance_criteria>
    - `get-subscription-status.test.ts` contains `describe("getSubscriptionStatus"` with at least 2 tests
    - `get-billing-history.test.ts` contains `describe("getBillingHistory"` with at least 2 tests
    - Both files use billing mock pattern (mock.module for config + stripe)
    - `bun test` on both exits 0
  </acceptance_criteria>
  <done>2 billing query handler test files created with ~3 tests each. All pass. TEST-03 and TEST-04 complete.</done>
</task>

<task type="auto">
  <name>Task 3: Create Stripe adapter conformance tests</name>
  <files>
    packages/modules/billing/src/__tests__/stripe-adapter.test.ts
  </files>
  <read_first>
    packages/modules/billing/src/__tests__/pagarme-adapter.test.ts
    packages/modules/billing/src/adapters/stripe/stripe-adapter.ts
    packages/modules/billing/src/ports/payment-provider.ts
    packages/modules/billing/src/ports/types.ts
  </read_first>
  <action>
Create `stripe-adapter.test.ts` that mirrors the EXACT structure of `pagarme-adapter.test.ts` (per D-05, D-07).

**Step 1:** Read `pagarme-adapter.test.ts` completely. Note every test name, assertion pattern, and setup structure.

**Step 2:** Read `stripe-adapter.ts` completely. Note every method, its Stripe SDK calls, and return value mapping.

**Step 3:** Create `stripe-adapter.test.ts` with:

1. Mock the Stripe SDK at module level via `mock.module("stripe", ...)` returning a `class MockStripe` with mock methods for each Stripe API namespace used by the adapter:
   - `customers = { create: mock(...) }` -- returns `{ id: "cus_stripe_123" }`
   - `subscriptions = { create: mock(...), retrieve: mock(...), update: mock(...) }` -- appropriate return shapes
   - `checkout = { sessions: { create: mock(...) } }` -- returns `{ id: "cs_123", url: "https://checkout.stripe.com/..." }`
   - `billingPortal = { sessions: { create: mock(...) } }` -- returns `{ url: "https://billing.stripe.com/..." }`
   - `invoices = { list: mock(...) }` -- returns `{ data: [{ id: "inv_1", amount_paid: 1000, status: "paid", ... }] }`
   - `subscriptionItems = { createUsageRecord: mock(...) }` -- returns `{ id: "ur_123" }`
   - `webhooks = { constructEvent: mock(...) }` -- returns event object

2. Dynamic import: `const { StripeAdapter } = await import("../adapters/stripe/stripe-adapter")`

3. `beforeEach` creates a fresh adapter: `adapter = new StripeAdapter({ secretKey: "sk_test_stripe_123", webhookSecret: "whsec_test_stripe_456" })`

4. Mirror Pagarme test cases adapted for Stripe:
   - "has name 'stripe'" -- `expect(adapter.name).toBe("stripe")`
   - "createCustomer calls Stripe customers.create" -- call `adapter.createCustomer(...)`, verify mock called, verify return has `providerCustomerId`
   - "createSubscription calls Stripe subscriptions.create" -- verify mock, verify return has `providerSubscriptionId`, `status`, `priceId`, `currentPeriodEnd`
   - "cancelSubscription calls subscriptions.update with cancel_at_period_end" -- Stripe cancels via update (not delete like Pagarme), verify mock called correctly
   - "changeSubscription retrieves then updates subscription" -- verify retrieve + update mocks called
   - "getSubscription returns null on error" -- mock subscriptions.retrieve to reject, verify returns null
   - "createCheckoutSession creates payment-mode session" -- verify checkout.sessions.create mock
   - "createOneTimePayment creates checkout session" -- verify mock, verify return shape
   - "createPortalSession returns portal URL" -- verify billingPortal.sessions.create, verify return has `url`
   - "verifyWebhookSignature uses constructEvent" -- verify webhooks.constructEvent mock called
   - "normalizeEvent delegates to mapStripeEvent" -- verify event normalization output shape
   - "getInvoices maps to ProviderInvoice format" -- verify invoices.list mock, verify return mapping
   - "reportUsage creates usage record" -- verify subscriptionItems.createUsageRecord mock

Total: ~12-13 tests (mirroring Pagarme's 10 + Stripe-specific extras for changeSubscription, createOneTimePayment, reportUsage).
  </action>
  <verify>
    <automated>cd C:/Projetos/baseworks && bun test packages/modules/billing/src/__tests__/stripe-adapter.test.ts --bail</automated>
  </verify>
  <acceptance_criteria>
    - `stripe-adapter.test.ts` contains `describe("StripeAdapter"` with at least 10 tests
    - File contains `mock.module("stripe"` with MockStripe class
    - Test names mirror pagarme-adapter.test.ts structure (has name, createCustomer, createSubscription, cancelSubscription, getSubscription, verifyWebhookSignature, etc.)
    - Each PaymentProvider method has at least 1 test
    - `bun test packages/modules/billing/src/__tests__/stripe-adapter.test.ts` exits 0
  </acceptance_criteria>
  <done>Stripe adapter has 10+ conformance tests mirroring Pagarme test structure. All PaymentProvider interface methods covered. Tests pass. TEST-05 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Not applicable -- unit tests only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | I (Information Disclosure) | Test files | accept | All Stripe test values use `sk_test_*` / `whsec_test_*` prefixes. No real secrets. |
</threat_model>

<verification>
1. `bun test packages/modules/billing/src/__tests__/*.test.ts` -- all billing tests pass (new + existing)
2. Stripe adapter test count >= Pagarme adapter test count
3. No real Stripe API calls made (all mocked)
</verification>

<success_criteria>
- 6 billing command handler test files created
- 2 billing query handler test files created
- 1 Stripe adapter conformance test file with 10+ tests
- All use shared mock utilities
- All tests pass
- TEST-03, TEST-04, TEST-05 complete
</success_criteria>

<output>
After completion, create `.planning/phases/14-unit-tests/14-05-SUMMARY.md`
</output>
