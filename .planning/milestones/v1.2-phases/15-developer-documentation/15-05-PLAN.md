---
phase: 15
plan: 05
type: execute
wave: 4
depends_on: [15-01, 15-03, 15-04]
files_modified:
  - docs/integrations/better-auth.md
  - docs/integrations/billing.md
  - docs/integrations/bullmq.md
  - docs/integrations/email.md
  - scripts/validate-docs.ts
autonomous: true
requirements: [DOCS-06, DOCS-07, DOCS-08, DOCS-09]
must_haves:
  truths:
    - "docs/integrations/better-auth.md explains Baseworks-specific wiring (basePath, drizzleAdapter, magic-link flow via BullMQ + Resend) and links out to better-auth.com for library internals"
    - "docs/integrations/better-auth.md includes a Mermaid sequenceDiagram for the magic-link flow"
    - "docs/integrations/better-auth.md names the basePath doubling gotcha (prefix must NOT be added when mounting auth.handler)"
    - "docs/integrations/billing.md documents the PaymentProvider port, both adapters (Stripe + Pagar.me), and provider-factory.ts as the selection point"
    - "docs/integrations/billing.md includes a Mermaid sequenceDiagram for the webhook flow with verifyWebhookSignature as the FIRST step"
    - "docs/integrations/billing.md includes an 'Add a third payment provider' section mirroring how Pagar.me was added"
    - "docs/integrations/bullmq.md documents createQueue and createWorker defaults, the queue naming convention (module:action), and the inline-processor (no sandboxed workers) constraint"
    - "docs/integrations/bullmq.md includes a Mermaid sequenceDiagram for enqueue → worker → retry and a 'Add a new queue + worker + job' section"
    - "docs/integrations/email.md documents the send-email dispatcher (template map, Resend call, graceful skip when RESEND_API_KEY absent) and lists all existing templates"
    - "docs/integrations/email.md includes a Mermaid sequenceDiagram for the email enqueue → Resend flow and an 'Add a new email template' section"
    - "All 4 integration docs link to their official upstream docs and do NOT re-document the upstream library API surface (per D-08)"
    - "A `scripts/validate-docs.ts` Bun script runs at phase close and (a) fails if any docs/**/*.md file contains the forbidden string `@baseworks/test-utils`, (b) fails on leaked secret shapes (`sk_live_`, `sk_test_[a-zA-Z0-9]{24,}`, `re_[a-zA-Z0-9]{20,}`, `whsec_[a-zA-Z0-9]{24,}`), and (c) counts Mermaid code fences across docs/ — failing if the count is below 8 (D-01 minimum)"
  artifacts:
    - path: "docs/integrations/better-auth.md"
      provides: "DOCS-06 — better-auth setup, magic-link flow, customization, adding OAuth providers"
      min_lines: 100
      contains: "better-auth.com"
    - path: "docs/integrations/billing.md"
      provides: "DOCS-07 — PaymentProvider port, Stripe/Pagar.me adapters, webhook flow, adding a third provider"
      min_lines: 120
      contains: "PaymentProvider"
    - path: "docs/integrations/bullmq.md"
      provides: "DOCS-08 — Queue conventions, worker entrypoint, adding a queue/worker/job"
      min_lines: 90
      contains: "createQueue"
    - path: "docs/integrations/email.md"
      provides: "DOCS-09 — Resend dispatcher, React Email templates, adding a new template"
      min_lines: 90
      contains: "Resend"
    - path: "scripts/validate-docs.ts"
      provides: "Phase-close validator: forbidden imports, secret-shape greps, Mermaid-count floor across docs/"
      min_lines: 40
      contains: "@baseworks/test-utils"
  key_links:
    - from: "docs/integrations/better-auth.md"
      to: "https://www.better-auth.com"
      via: "Upstream Documentation section"
      pattern: "better-auth\\.com"
    - from: "docs/integrations/billing.md"
      to: "packages/modules/billing/src/ports/payment-provider.ts"
      via: "port reference"
      pattern: "payment-provider\\.ts"
    - from: "docs/integrations/billing.md"
      to: "https://stripe.com/docs"
      via: "Upstream Documentation section"
      pattern: "stripe\\.com/docs"
    - from: "docs/integrations/bullmq.md"
      to: "packages/queue/src/index.ts"
      via: "createQueue/createWorker citation"
      pattern: "packages/queue/src/index\\.ts"
    - from: "docs/integrations/email.md"
      to: "packages/modules/billing/src/jobs/send-email.ts"
      via: "dispatcher citation"
      pattern: "send-email\\.ts"
---

<objective>
Author the four third-party integration documents (DOCS-06..09) that explain Baseworks-specific wiring around better-auth, billing (Stripe + Pagar.me), BullMQ, and email (Resend + React Email). Each doc follows the locked integration template (D-07): Overview, Upstream Documentation, Setup (env vars + module wire-up + smoke test), Wiring in Baseworks (with one Mermaid sequenceDiagram), Gotchas, and Extending (with an "Add another X" subsection per D-09).

Purpose: Close DOCS-06, DOCS-07, DOCS-08, DOCS-09. Per D-08, these docs document Baseworks wiring in detail and LINK to upstream docs for library internals — no re-documenting better-auth plugin API, Stripe SDK, BullMQ semantics, or Resend API.

Output: Four new markdown files under `docs/integrations/`, all conforming to Plan 01 contracts and the unified template.
</objective>

<execution_context>
@C:/Projetos/baseworks/.claude/get-shit-done/workflows/execute-plan.md
@C:/Projetos/baseworks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-developer-documentation/15-CONTEXT.md
@.planning/phases/15-developer-documentation/15-RESEARCH.md
@.planning/phases/15-developer-documentation/15-PATTERNS.md
@.planning/phases/15-developer-documentation/15-VALIDATION.md
@docs/README.md
@docs/architecture.md
@docs/configuration.md
@docs/testing.md
@packages/modules/auth/src/auth.ts
@packages/modules/auth/src/routes.ts
@packages/modules/billing/src/ports/payment-provider.ts
@packages/modules/billing/src/provider-factory.ts
@packages/modules/billing/src/routes.ts
@packages/modules/billing/src/adapters/stripe/stripe-adapter.ts
@packages/modules/billing/src/adapters/pagarme/pagarme-adapter.ts
@packages/modules/billing/src/hooks/on-tenant-created.ts
@packages/modules/billing/src/jobs/send-email.ts
@packages/modules/billing/src/templates/welcome.tsx
@packages/modules/billing/src/templates/password-reset.tsx
@packages/modules/billing/src/templates/team-invite.tsx
@packages/modules/billing/src/templates/billing-notification.tsx
@packages/queue/src/index.ts
@packages/queue/src/types.ts
@apps/api/src/worker.ts
@packages/config/src/env.ts
</context>

<interfaces>
<!-- Concrete anchors the four integration docs cite. -->

### Integration doc template (D-07, applies to all 4)

```markdown
# [Integration Name]

## Overview
[One paragraph: what the integration provides, which upstream lib it wraps.]

## Upstream Documentation
- [Official docs link]
- [Relevant SDK reference]

## Setup
### Env vars
### Module wire-up
### Smoke test

## Wiring in Baseworks
[File paths, abstractions used, one Mermaid sequenceDiagram]

## Gotchas
[Pitfalls hit during integration]

## Extending
### Add another [provider | queue | template | OAuth provider]
```

### DOCS-06 — better-auth anchors

- `packages/modules/auth/src/auth.ts::auth` — `betterAuth({ basePath: "/api/auth", ... })` config with `drizzleAdapter`, `emailAndPassword`, conditional `socialProviders` (Google/GitHub), `magicLink`, `organization` plugin, `databaseHooks`. Cite (> 10 lines).
- `packages/modules/auth/src/auth.ts::auth (basePath)` — CRITICAL gotcha: `basePath: "/api/auth"`. Mount via `.mount(auth.handler)` WITHOUT a prefix — prefixing doubles to `/api/auth/api/auth/*`.
- Env vars: `BETTER_AUTH_SECRET` (min 32), `BETTER_AUTH_URL`, optional `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET` / `GITHUB_CLIENT_ID` / `GITHUB_CLIENT_SECRET`.
- Magic-link email flow: `sendMagicLink`/`sendResetPassword` → `getEmailQueue().add("...", ...)` → `email:send` queue → worker → `sendEmail` job → Resend.
- Security invariants (from 15-RESEARCH.md §"Security Domain"): 32+ char secret enforced; DB-backed sessions (not JWT); `trustedOrigins` set from `WEB_URL` + `ADMIN_URL`.
- Upstream link: `https://www.better-auth.com`.

### DOCS-07 — billing anchors

- `packages/modules/billing/src/ports/payment-provider.ts::PaymentProvider` — `PaymentProvider` interface (~14 methods). Cite (> 10 lines).
- `packages/modules/billing/src/provider-factory.ts::getPaymentProvider` — lazy singleton branching on `PAYMENT_PROVIDER` env. Cite by function-name anchor.
- `packages/modules/billing/src/adapters/stripe/stripe-adapter.ts` — Stripe implementation (reference only; cite directory).
- `packages/modules/billing/src/adapters/pagarme/pagarme-adapter.ts` — Pagar.me implementation (template for "add another provider").
- `packages/modules/billing/src/routes.ts:52-110` — webhook route: signature verification → event normalization → idempotency check → enqueue `billing:process-webhook`. Cite.
- Env vars: `PAYMENT_PROVIDER`, `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `PAGARME_SECRET_KEY`, `PAGARME_WEBHOOK_SECRET`.
- Upstream links: `https://stripe.com/docs`, `https://docs.pagar.me`.

### DOCS-08 — BullMQ anchors

- `packages/queue/src/index.ts::createQueue/createWorker` — `createQueue` (removeOnComplete 3 days, removeOnFail 7 days, attempts 3, exponential backoff 1000ms) and `createWorker` (concurrency 5, inline processor). Cite.
- `packages/queue/src/index.ts::createWorker (sandboxed-worker comment)` — gotcha: sandboxed workers are NOT used because they are broken on Bun runtime.
- `apps/api/src/worker.ts:32-77` — worker entrypoint that iterates every module's `def.jobs` and starts a `createWorker` per job. Cite.
- Queue naming convention: `module:action` (e.g., `email:send`, `billing:process-webhook`, `example:process-followup`).
- Upstream link: `https://docs.bullmq.io`.

### DOCS-09 — Email anchors

- `packages/modules/billing/src/jobs/send-email.ts:15-24` — `templates` map routing template name → React Email component. Inline snippet (≤ 10 lines).
- `packages/modules/billing/src/jobs/send-email.ts:26-35` — `subjects` map.
- `packages/modules/billing/src/jobs/send-email.ts` (dispatcher body) — reads job data, looks up template, calls `render()`, calls `resend.emails.send()`. Graceful skip when `RESEND_API_KEY` absent. Cite.
- Templates: `welcome.tsx`, `password-reset.tsx` (reused for magic-link with aliased userName), `team-invite.tsx`, `billing-notification.tsx`.
- Call site example: `packages/modules/auth/src/auth.ts:70-82` (password-reset via `getEmailQueue().add(...)`).
- Env var: `RESEND_API_KEY` (optional; dispatcher skips gracefully).
- Upstream links: `https://resend.com/docs`, `https://react.email`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/integrations/better-auth.md (DOCS-06)</name>
  <files>docs/integrations/better-auth.md</files>
  <read_first>
    - docs/README.md (contracts)
    - packages/modules/auth/src/auth.ts:1-180 (full betterAuth config, conditional social providers, magic-link enqueue)
    - packages/modules/auth/src/routes.ts (.mount pattern)
    - packages/config/src/env.ts (BETTER_AUTH_SECRET, BETTER_AUTH_URL, OAuth client IDs)
    - .planning/phases/15-developer-documentation/15-PATTERNS.md §"docs/integrations/better-auth.md"
    - .planning/phases/15-developer-documentation/15-RESEARCH.md §"Pattern 3" and §"Security Domain" V2-V3 rows
  </read_first>
  <action>
Create `docs/integrations/better-auth.md` following the unified integration template. Target length ~130-190 lines. Tone: strict reference-doc (D-11).

Required sections (headings verbatim):

1. `# better-auth` (H1)
2. `## Overview` (H2). One paragraph: better-auth provides email/password authentication, OAuth providers, magic-link sign-in, and an organization plugin that powers Baseworks' multitenancy. Configuration lives in `packages/modules/auth/src/auth.ts`.
3. `## Upstream Documentation` (H2). Bulleted list:
   - [better-auth official documentation](https://www.better-auth.com)
   - [better-auth Drizzle adapter](https://www.better-auth.com/docs/adapters/drizzle)
   - [better-auth organization plugin](https://www.better-auth.com/docs/plugins/organization)
   - [better-auth magic-link plugin](https://www.better-auth.com/docs/plugins/magic-link)
4. `## Setup` (H2) with three subsections:

   `### Env vars` — 3-column table (`Env var | Required | Purpose`). Rows:
   - `BETTER_AUTH_SECRET` | yes | Session signing; ≥ 32 chars (Zod-enforced).
   - `BETTER_AUTH_URL` | no (default `http://localhost:3000`) | Public base URL; used in magic-link and OAuth callbacks.
   - `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET` | no | Enable Google OAuth.
   - `GITHUB_CLIENT_ID` / `GITHUB_CLIENT_SECRET` | no | Enable GitHub OAuth.

   Close with: "Full env reference: [configuration.md](../configuration.md)."

   `### Module wire-up` — one paragraph citing `packages/modules/auth/src/auth.ts::auth` (the exported instance) and `packages/modules/auth/src/routes.ts` (`.mount(auth.handler)` pattern). State that the auth module is already present in the `modules` array in both `apps/api/src/index.ts:27` and (for shared scenarios) the worker entrypoint.

   `### Smoke test` — fenced bash block:

   ```bash
   curl -X POST http://localhost:3000/api/auth/sign-up/email \
     -H "Content-Type: application/json" \
     -d '{"email":"test@example.com","password":"test-password-123","name":"Test User"}'
   ```

   One-paragraph expectation: a 200 response with a session cookie. A 404 likely means the basePath gotcha (see Gotchas below).

5. `## Wiring in Baseworks` (H2). One paragraph: `betterAuth(...)` is configured in `packages/modules/auth/src/auth.ts::auth`. The config uses `drizzleAdapter(db, { provider: "pg" })` against `packages/db`, conditionally registers `socialProviders` based on env var presence, enables email/password with `minPasswordLength: 8`, and enables the `magicLink` and `organization` plugins. Sessions are DB-backed (not JWT) to support revocation.

   Include ONE Mermaid `sequenceDiagram` for the magic-link flow:

   ```mermaid
   sequenceDiagram
     participant Client
     participant Route as /api/auth/magic-link/request
     participant Auth as better-auth magicLink plugin
     participant Queue as email:send BullMQ queue
     participant Worker as sendEmail worker
     participant Resend

     Client->>Route: POST { email }
     Route->>Auth: sendMagicLink(user, url)
     Auth->>Queue: getEmailQueue().add("magic-link", { to, template, data })
     Queue-->>Auth: enqueued
     Auth-->>Route: 200
     Route-->>Client: 200
     Worker->>Queue: consume job
     Worker->>Resend: resend.emails.send(renderedTemplate)
     Resend-->>Worker: 200
   ```

   Cite `packages/modules/auth/src/auth.ts:70-82` (sendResetPassword call site; same enqueue pattern as magic-link).

6. `## Gotchas` (H2). Three bulleted items:
   - **basePath doubling.** `betterAuth({ basePath: "/api/auth" })` already carries the prefix. Mount via `.mount(auth.handler)` WITHOUT an Elysia prefix. Prefixing produces `/api/auth/api/auth/*` routes — source: `packages/modules/auth/src/auth.ts::auth (JSDoc)`.
   - **OAuth providers silently absent.** `socialProviders` is built conditionally from `env.GOOGLE_CLIENT_ID` / `env.GITHUB_CLIENT_ID` presence (`auth.ts::socialProviders (conditional block)`). If a provider fails to show up in the UI, confirm both the ID and the secret env vars are set.
   - **Graceful email fallback.** When `REDIS_URL` is absent (dev without Docker), `getEmailQueue()` returns `null` and `sendResetPassword` logs the reset URL to stdout instead of sending email (`auth.ts:70-82`). This is intentional for dev; never rely on it in production.

7. `## Extending` (H2) with one subsection.

   `### Add another OAuth provider` — one paragraph: better-auth exposes an extensible `socialProviders` option plus a plugin system. To add a new provider, extend `socialProviders` in `packages/modules/auth/src/auth.ts::socialProviders (conditional block)` following the Google/GitHub pattern (conditional on env var presence, pass `clientId` + `clientSecret`). Provider-specific options (scopes, profile mapping) are documented by better-auth — link to [better-auth social providers docs](https://www.better-auth.com/docs/authentication/social).

8. `## Security` (H2). Bulleted list of invariants (from 15-RESEARCH.md §"Security Domain"):
   - `BETTER_AUTH_SECRET` is validated as `z.string().min(32)` in `packages/config/src/packages/config/src/env.ts::envSchema (BETTER_AUTH_SECRET: z.string().min(32))`.
   - Sessions are DB-backed via the Drizzle adapter — supports revocation.
   - `trustedOrigins` is derived from `WEB_URL` and `ADMIN_URL` env vars; OAuth callbacks only accept redirects to these origins.
   - `minPasswordLength: 8` is enforced by better-auth.
   - Cross-origin requests use CORS configured in `apps/api/src/index.ts`; the auth routes are mounted BEFORE the tenant middleware so unauthenticated sign-in/sign-up is allowed.

9. `## Next steps` (H2). Bulleted list:
   - [Billing integration](./billing.md) — auth emits `tenant.created`; billing listens and provisions a customer.
   - [Email integration](./email.md) — the Resend dispatcher that magic-link and password-reset enqueue into.
   - [Configuration](../configuration.md) — full env reference.

Formatting constraints:
- No re-documented better-auth API surface. Links only.
- No `npm`/`yarn`/`pnpm`.
- No filler words.
- No real-shaped secrets (the smoke-test curl uses `test@example.com` and `test-password-123` — these are placeholders, not real keys; grep rules check for `sk_live_`, `sk_test_[a-zA-Z0-9]{20,}`, etc., not arbitrary passwords).
- Exactly ONE Mermaid sequenceDiagram.
  </action>
  <verify>
    <automated>cd "C:/Projetos/baseworks" &amp;&amp; test -f docs/integrations/better-auth.md &amp;&amp; grep -q "^# better-auth$" docs/integrations/better-auth.md &amp;&amp; grep -q "^## Overview$" docs/integrations/better-auth.md &amp;&amp; grep -q "^## Upstream Documentation$" docs/integrations/better-auth.md &amp;&amp; grep -q "^## Setup$" docs/integrations/better-auth.md &amp;&amp; grep -q "^## Wiring in Baseworks$" docs/integrations/better-auth.md &amp;&amp; grep -q "^## Gotchas$" docs/integrations/better-auth.md &amp;&amp; grep -q "^## Extending$" docs/integrations/better-auth.md &amp;&amp; grep -q "better-auth\.com" docs/integrations/better-auth.md &amp;&amp; grep -q "BETTER_AUTH_SECRET" docs/integrations/better-auth.md &amp;&amp; grep -q "basePath" docs/integrations/better-auth.md &amp;&amp; grep -q "auth\.ts" docs/integrations/better-auth.md &amp;&amp; grep -q "Add another OAuth provider" docs/integrations/better-auth.md &amp;&amp; test "$(grep -c '^```mermaid$' docs/integrations/better-auth.md)" = "1" &amp;&amp; ! grep -nE "sk_live_|sk_test_[a-zA-Z0-9]{20,}|re_[a-zA-Z0-9]{15,}|whsec_[a-zA-Z0-9]{20,}" docs/integrations/better-auth.md &amp;&amp; ! grep -nE "\bnpm install\b|\byarn \b|\bpnpm \b" docs/integrations/better-auth.md &amp;&amp; ! grep -nE "\b(basically|simply|just)\b" docs/integrations/better-auth.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/integrations/better-auth.md` exists
    - All 8 required `##` sections present: `Overview`, `Upstream Documentation`, `Setup`, `Wiring in Baseworks`, `Gotchas`, `Extending`, `Security`, `Next steps`
    - Links to `better-auth.com` at least once (in Upstream Documentation)
    - Names `BETTER_AUTH_SECRET` and documents the min-32 constraint
    - Names the `basePath` gotcha explicitly
    - Cites `auth.ts` at least once
    - Contains `Add another OAuth provider` subsection
    - Exactly 1 ```mermaid fenced block (magic-link sequenceDiagram)
    - Zero real-shaped secrets
    - Zero `npm|yarn|pnpm` and zero filler words
  </acceptance_criteria>
  <done>`docs/integrations/better-auth.md` covers setup/wiring/gotchas/extending with the basePath gotcha, the magic-link sequenceDiagram, and the OAuth-provider extension pattern — linking out to better-auth.com for library internals.</done>
</task>

<task type="auto">
  <name>Task 2: Author docs/integrations/billing.md (DOCS-07)</name>
  <files>docs/integrations/billing.md</files>
  <read_first>
    - docs/README.md (contracts)
    - packages/modules/billing/src/ports/payment-provider.ts (full PaymentProvider interface)
    - packages/modules/billing/src/provider-factory.ts (getPaymentProvider singleton + branch)
    - packages/modules/billing/src/routes.ts:52-110 (webhook handler with verifyWebhookSignature)
    - packages/modules/billing/src/adapters/stripe/stripe-adapter.ts (adapter for reference)
    - packages/modules/billing/src/adapters/pagarme/pagarme-adapter.ts (second-provider template)
    - packages/modules/billing/src/hooks/on-tenant-created.ts (auth→billing event wiring)
    - packages/config/src/env.ts (PAYMENT_PROVIDER + STRIPE_/PAGARME_ secrets)
    - .planning/phases/15-developer-documentation/15-PATTERNS.md §"docs/integrations/billing.md"
    - .planning/phases/15-developer-documentation/15-RESEARCH.md §"Security Domain" (webhook signature invariant)
  </read_first>
  <action>
Create `docs/integrations/billing.md` following the unified integration template. Target length ~160-230 lines. Tone: strict reference-doc (D-11).

Required sections (headings verbatim):

1. `# Billing` (H1)
2. `## Overview` (H2). One paragraph: Baseworks supports Stripe and Pagar.me via a port-and-adapter pattern. The `PaymentProvider` port (14 methods) is the contract; adapters translate it to each SDK. A factory selects the adapter at runtime based on the `PAYMENT_PROVIDER` env var.
3. `## Upstream Documentation` (H2). Bulleted list:
   - [Stripe API reference](https://stripe.com/docs)
   - [Stripe webhooks](https://stripe.com/docs/webhooks)
   - [Pagar.me documentation](https://docs.pagar.me)
   - [Stripe Node SDK](https://github.com/stripe/stripe-node)
4. `## Setup` (H2) with three subsections:

   `### Env vars` — 3-column table (`Env var | Required | Purpose`). Rows:
   - `PAYMENT_PROVIDER` | no (default `stripe`) | Enum `stripe | pagarme`.
   - `STRIPE_SECRET_KEY` | conditional | Required when `PAYMENT_PROVIDER=stripe` (non-test).
   - `STRIPE_WEBHOOK_SECRET` | yes in production | Webhook signature verification.
   - `PAGARME_SECRET_KEY` | conditional | Required when `PAYMENT_PROVIDER=pagarme` (non-test).
   - `PAGARME_WEBHOOK_SECRET` | yes in production (pagarme) | Webhook signature verification.

   Close with: "See [configuration.md](../configuration.md) for full schema and `validatePaymentProviderEnv` enforcement."

   `### Module wire-up` — one paragraph: the billing module is listed in the `modules` array in both `apps/api/src/index.ts:27` and `apps/api/src/worker.ts:23`. Registration happens automatically via `ModuleRegistry.loadAll()`.

   `### Smoke test` — fenced bash block:

   ```bash
   bun api
   # In another shell:
   curl http://localhost:3000/api/billing/subscription/status \
     -b "better-auth.session_token=<paste session cookie from sign-in>"
   ```

   One-paragraph expectation: a `Result` JSON envelope with `success: true` and subscription data for the authenticated tenant.

5. `## Wiring in Baseworks` (H2). Explain the layer structure.

   Subsection `### PaymentProvider port` — one paragraph citing `packages/modules/billing/src/ports/payment-provider.ts::PaymentProvider`. The port declares ~14 methods: customer lifecycle (`createCustomer`, `updateCustomer`), checkout (`createCheckoutSession`, `createOneTimePayment`), subscription lifecycle (`cancelSubscription`, `changeSubscription`), portal (`createCustomerPortalSession`), usage (`recordUsage`), and webhooks (`verifyWebhookSignature`, `normalizeWebhookEvent`).

   Subsection `### Provider factory` — one paragraph citing `packages/modules/billing/src/provider-factory.ts::getPaymentProvider`. Lazy singleton branching on `env.PAYMENT_PROVIDER`. First call instantiates the adapter; subsequent calls return the cached instance.

   Subsection `### Adapters` — one paragraph: `packages/modules/billing/src/adapters/stripe/stripe-adapter.ts` implements the port against `stripe` SDK; `packages/modules/billing/src/adapters/pagarme/pagarme-adapter.ts` implements it against Pagar.me's HTTP API. Each adapter's directory also contains a `*-webhook-mapper.ts` that normalizes provider-specific webhook payloads into the Baseworks canonical event shape.

   Subsection `### Webhook flow` — one Mermaid `sequenceDiagram`:

   ```mermaid
   sequenceDiagram
     participant Stripe
     participant Route as POST /api/billing/webhooks
     participant Provider as PaymentProvider (adapter)
     participant DB as scopedDb / unscoped-db
     participant Queue as billing:process-webhook
     participant Worker as processWebhook job

     Stripe->>Route: POST raw body + signature header
     Route->>Provider: verifyWebhookSignature({ rawBody, signature })
     Provider-->>Route: rawEvent (verified)
     Route->>Provider: normalizeWebhookEvent(rawEvent)
     Provider-->>Route: normalizedEvent
     Route->>DB: check webhook_events for providerEventId (idempotency)
     DB-->>Route: existing | null
     Route->>DB: insert webhook_events row
     Route->>Queue: add("process-webhook", normalizedEvent)
     Queue-->>Route: enqueued
     Route-->>Stripe: 200
     Worker->>Queue: consume job
     Worker->>DB: apply event to billing tables
   ```

   Cite `packages/modules/billing/src/routes.ts:52-110` for the full webhook route implementation.

   Subsection `### Auth → Billing hook` — one paragraph citing `packages/modules/billing/src/hooks/on-tenant-created.ts`. Registered via `registerBillingHooks(registry.getEventBus())` at API startup (`apps/api/src/index.ts:34`). The hook listens for the `tenant.created` event emitted by the auth module and provisions a provider customer.

6. `## Gotchas` (H2). Four bulleted items:
   - **Webhook signature verification must run before body parsing.** The webhook route receives the raw request body and computes the HMAC from it. If Elysia auto-parses JSON before the handler, signature verification fails. The route in `routes.ts` reads the raw body explicitly.
   - **Idempotency is critical.** Providers re-deliver webhooks on retry. The `webhook_events` table (see `packages/db/src/schema/*`) stores `providerEventId` and is checked before processing. Duplicate deliveries are ack'd with 200 and skipped.
   - **No tenant middleware on webhook route.** Webhooks originate from the provider, not from a tenant user. Tenant context is derived from the webhook payload (e.g., `customer.id` → `billingCustomers.tenantId` lookup).
   - **Test-mode validation is lenient.** `validatePaymentProviderEnv` warns instead of throwing when `NODE_ENV=test`, so the test runner can import the billing module without real keys. Production MUST have the active provider's key set.

7. `## Extending` (H2) with one subsection.

   `### Add a third payment provider` — ordered list (mirrors how Pagar.me was added alongside Stripe):

   1. Implement the `PaymentProvider` port in a new adapter directory: `packages/modules/billing/src/adapters/{provider}/{provider}-adapter.ts`. All 14 methods must be implemented — the compiler enforces this via `implements PaymentProvider`.
   2. Implement a webhook mapper in `packages/modules/billing/src/adapters/{provider}/{provider}-webhook-mapper.ts` that normalizes the provider's webhook payload into the canonical Baseworks event shape. Reference: `pagarme-webhook-mapper.ts`.
   3. Add the provider name to the `PAYMENT_PROVIDER` Zod enum in `packages/config/src/env.ts::envSchema (PAYMENT_PROVIDER enum)` and add the corresponding `{PROVIDER}_SECRET_KEY` and `{PROVIDER}_WEBHOOK_SECRET` entries.
   4. Extend `validatePaymentProviderEnv` in `packages/config/src/env.ts::validatePaymentProviderEnv` with a new conditional check — mirror the Stripe/Pagar.me branches.
   5. Register the new adapter in `packages/modules/billing/src/provider-factory.ts::getPaymentProvider` — add a new `case` that returns the adapter instance.
   6. Write a conformance test at `packages/modules/billing/src/__tests__/{provider}-adapter.test.ts`. Reference template: `packages/modules/billing/src/__tests__/pagarme-adapter.test.ts`.

8. `## Security` (H2). Bulleted list:
   - Webhook handlers MUST call `provider.verifyWebhookSignature(...)` as the first operation on the raw request body. Rejection returns 400.
   - Idempotency prevents double-applying events on provider re-delivery.
   - Webhook routes are exempt from `tenantMiddleware` — tenant resolution happens after signature + event normalization.
   - Real API keys and webhook secrets are never committed. All env examples use placeholders (`sk_test_xxx`, `whsec_xxx`, see [configuration.md](../configuration.md)).

9. `## Next steps` (H2). Bulleted list:
   - [better-auth integration](./better-auth.md) — billing hooks into the `tenant.created` event.
   - [BullMQ integration](./bullmq.md) — `billing:process-webhook` is a standard BullMQ queue.
   - [Testing](../testing.md) — adapter conformance test pattern.

Formatting constraints:
- One Mermaid sequenceDiagram.
- No re-documented Stripe SDK or Pagar.me API — link only.
- No `npm|yarn|pnpm`.
- No filler words.
- No real-shaped secrets.
  </action>
  <verify>
    <automated>cd "C:/Projetos/baseworks" &amp;&amp; test -f docs/integrations/billing.md &amp;&amp; grep -q "^# Billing$" docs/integrations/billing.md &amp;&amp; grep -q "^## Overview$" docs/integrations/billing.md &amp;&amp; grep -q "^## Upstream Documentation$" docs/integrations/billing.md &amp;&amp; grep -q "^## Setup$" docs/integrations/billing.md &amp;&amp; grep -q "^## Wiring in Baseworks$" docs/integrations/billing.md &amp;&amp; grep -q "^## Gotchas$" docs/integrations/billing.md &amp;&amp; grep -q "^## Extending$" docs/integrations/billing.md &amp;&amp; grep -q "stripe\.com/docs" docs/integrations/billing.md &amp;&amp; grep -q "docs\.pagar\.me" docs/integrations/billing.md &amp;&amp; grep -q "PaymentProvider" docs/integrations/billing.md &amp;&amp; grep -q "verifyWebhookSignature" docs/integrations/billing.md &amp;&amp; grep -q "provider-factory" docs/integrations/billing.md &amp;&amp; grep -q "payment-provider\.ts" docs/integrations/billing.md &amp;&amp; grep -q "Add a third payment provider" docs/integrations/billing.md &amp;&amp; grep -q "stripe-adapter\.ts" docs/integrations/billing.md &amp;&amp; grep -q "pagarme-adapter\.ts" docs/integrations/billing.md &amp;&amp; test "$(grep -c '^```mermaid$' docs/integrations/billing.md)" = "1" &amp;&amp; ! grep -nE "sk_live_|sk_test_[a-zA-Z0-9]{20,}|re_[a-zA-Z0-9]{15,}|whsec_[a-zA-Z0-9]{20,}" docs/integrations/billing.md &amp;&amp; ! grep -nE "\bnpm install\b|\byarn \b|\bpnpm \b" docs/integrations/billing.md &amp;&amp; ! grep -nE "\b(basically|simply|just)\b" docs/integrations/billing.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/integrations/billing.md` exists
    - All 9 required `##` sections present: `Overview`, `Upstream Documentation`, `Setup`, `Wiring in Baseworks`, `Gotchas`, `Extending`, `Security`, `Next steps` (plus Setup subsections)
    - Links to `stripe.com/docs` and `docs.pagar.me`
    - Names `PaymentProvider` port; cites `payment-provider.ts`, `provider-factory`, `stripe-adapter.ts`, `pagarme-adapter.ts`
    - Names `verifyWebhookSignature` as the first webhook step
    - Contains `Add a third payment provider` subsection with ordered list
    - Exactly 1 ```mermaid block (webhook sequenceDiagram)
    - Zero real-shaped secrets
    - Zero `npm|yarn|pnpm` and zero filler words
  </acceptance_criteria>
  <done>`docs/integrations/billing.md` explains the port + factory + adapters, shows the webhook flow with signature-verify-first, and lays out the 6-step path for adding a third provider.</done>
</task>

<task type="auto">
  <name>Task 3: Author docs/integrations/bullmq.md (DOCS-08)</name>
  <files>docs/integrations/bullmq.md</files>
  <read_first>
    - docs/README.md (contracts)
    - packages/queue/src/index.ts:1-55 (createQueue, createWorker, defaults, sandboxed-workers-broken note)
    - packages/queue/src/types.ts (QueueConfig, WorkerConfig, EmailJobData)
    - packages/queue/src/connection.ts (getRedisConnection)
    - apps/api/src/worker.ts:1-80 (worker entrypoint; the iteration that auto-starts workers)
    - packages/modules/example/src/index.ts (post-Plan-02 — demonstrates the module jobs map)
    - packages/modules/example/src/jobs/process-followup.ts (post-Plan-02 — demonstrates a handler)
    - .planning/phases/15-developer-documentation/15-PATTERNS.md §"docs/integrations/bullmq.md"
  </read_first>
  <action>
Create `docs/integrations/bullmq.md` following the unified integration template. Target length ~110-170 lines. Tone: strict reference-doc (D-11).

Required sections (headings verbatim):

1. `# BullMQ` (H1)
2. `## Overview` (H2). One paragraph: BullMQ is the job queue used by Baseworks for asynchronous work (emails, webhooks, billing follow-ups). Queues and workers are created by helpers in `packages/queue/src/index.ts`; modules declare job handlers in their `ModuleDefinition.jobs` map and the worker process (`apps/api/src/worker.ts`) auto-starts one BullMQ worker per job.
3. `## Upstream Documentation` (H2). Bulleted list:
   - [BullMQ documentation](https://docs.bullmq.io)
   - [BullMQ job options](https://docs.bullmq.io/guide/jobs/job-options)
   - [BullMQ worker concurrency](https://docs.bullmq.io/guide/workers/concurrency)
4. `## Setup` (H2) with three subsections:

   `### Env vars` — 3-column table:
   - `REDIS_URL` | yes (for `worker` or `all` roles) | BullMQ backing Redis; `assertRedisUrl` enforces at startup.
   - `WORKER_HEALTH_PORT` | no (default 3001) | HTTP port for the worker health endpoint.

   `### Module wire-up` — one paragraph: modules declare jobs in their `ModuleDefinition.jobs` map (see [add-a-module.md](../add-a-module.md) §"Step 6"). Each entry is `"module:action": { queue: "module:action", handler }`. The worker process iterates every loaded module's `def.jobs` and calls `createWorker(...)` once per entry — no manual worker registration is needed.

   `### Smoke test` — fenced bash block:

   ```bash
   bun docker:up
   bun worker
   # Then from another shell, trigger a command that enqueues:
   curl -X POST http://localhost:3000/api/example ...
   ```

   One-paragraph expectation: worker logs `Worker started for job` on boot and, after the enqueuing API call, logs `Job started` and `Job completed`.

5. `## Wiring in Baseworks` (H2).

   Subsection `### Queue defaults` — one paragraph citing `packages/queue/src/index.ts::createQueue`. `createQueue(name, redisUrl)` returns a BullMQ `Queue` with defaults: `removeOnComplete.age = 259200` (3 days), `removeOnFail.age = 604800` (7 days), `attempts = 3`, `backoff = { type: "exponential", delay: 1000 }`. Callers may pass their own options when needed, but the defaults are appropriate for most module jobs.

   Subsection `### Worker defaults` — one paragraph citing `packages/queue/src/index.ts::createWorker`. `createWorker(name, processor, redisUrl, opts?)` returns a BullMQ `Worker` with default `concurrency: 5` and an inline processor. The inline processor is a hard constraint on Bun — see Gotchas.

   Subsection `### Queue naming convention` — one paragraph: every queue name follows `module:action`. Current queues in the repo: `email:send` (auth module → emails), `billing:process-webhook` (billing webhook processing), `example:process-followup` (example module demo). Names are used verbatim as the BullMQ queue name AND as the job-map key.

   Subsection `### Worker entrypoint flow` — one Mermaid `sequenceDiagram`:

   ```mermaid
   sequenceDiagram
     participant Cmd as Command handler
     participant Q as BullMQ Queue
     participant Redis
     participant W as BullMQ Worker
     participant H as Job handler

     Cmd->>Q: ctx.enqueue("example:process-followup", payload)
     Q->>Redis: persist job
     W->>Redis: poll / pop
     Redis-->>W: job
     W->>H: handler(job.data)
     alt success
       H-->>W: resolve
       W->>Redis: removeOnComplete after 3 days
     else error (transient)
       H-->>W: throw
       W->>Redis: reschedule with exponential backoff
       Note over W,Redis: up to 3 attempts; then removeOnFail after 7 days
     end
   ```

   Cite `apps/api/src/worker.ts:32-77` for the worker entrypoint loop.

6. `## Gotchas` (H2). Four bulleted items:
   - **Sandboxed workers are broken on Bun.** BullMQ's sandboxed-processor mode (processor as a file path) does not work under the Bun runtime. All processors are declared inline as JavaScript functions in the main worker process (`packages/queue/src/index.ts::createWorker (sandboxed-worker comment)`). Do not pass a processor file path.
   - **Redis connection sharing.** `getRedisConnection(redisUrl)` in `packages/queue/src/connection.ts` memoizes an `ioredis` instance per URL. Do not create ad-hoc `new IORedis(...)` connections alongside it; let BullMQ use the shared connection to avoid leaking sockets.
   - **`ctx.enqueue` is optional.** `HandlerContext.enqueue` is `Promise<void> | undefined`. In test contexts the mock factory provides a no-op; production command handlers invoke `await ctx.enqueue?.(...)` — the optional chaining preserves type-safety if a future context skips the queue wiring.
   - **Worker health check is independent of readiness.** The worker process starts a separate `Bun.serve` on `WORKER_HEALTH_PORT` (default 3001) for liveness probes. A healthy Redis plus at least one registered worker returns `"status": "ok"`; see `apps/api/src/worker.ts:84-125`.

7. `## Extending` (H2) with one subsection.

   `### Add a new queue + worker + job type` — ordered list:

   1. Export a job handler function with signature `(data: unknown) => Promise<void>` from your module. Reference: `packages/modules/example/src/jobs/process-followup.ts` (log-only demo) or `packages/modules/billing/src/jobs/send-email.ts` (real dispatcher).
   2. In your module's `index.ts`, add the job to the `jobs` map:

      ```typescript
      // Module definition — jobs map entry
      jobs: {
        "yourmodule:action": {
          queue: "yourmodule:action",
          handler: yourHandler,
        },
      },
      ```

   3. Ensure your module name is listed in the `modules` array in `apps/api/src/worker.ts:21-24` so the worker loads it. No separate worker-registration call is required — `apps/api/src/worker.ts:32-77` iterates all `def.jobs` automatically.
   4. Enqueue from a command handler: `await ctx.enqueue?.("yourmodule:action", payload)`.
   5. If you need non-default BullMQ options (custom retention, custom attempts), call `createQueue` directly with your options instead of relying on the auto-created queue.

8. `## Security` (H2). Bulleted list:
   - Job payloads cross the app ↔ Redis trust boundary. Validate the payload shape at the top of the handler. Never trust that a job retrieved from Redis has the shape your TypeScript types claim.
   - Workers run with the worker role's env vars. If a job handler needs tenant-scoped DB access, derive `scopedDb(tenantId)` inside the handler from a `tenantId` field in the job payload — the request-time `ctx.db` is not available.
   - Retention defaults (3-day complete, 7-day fail) are designed to aid debugging. Adjust `removeOnComplete` / `removeOnFail` for jobs whose payloads contain sensitive data.

9. `## Dashboard (optional)` (H2). One paragraph: Bull Board / bull-monitor are optional dashboards for queue visibility. They are NOT wired in the current codebase. See [Bull Board docs](https://github.com/felixmosh/bull-board) if you want to add one.

10. `## Next steps` (H2). Bulleted list:
    - [Email integration](./email.md) — the `email:send` queue.
    - [Billing integration](./billing.md) — the `billing:process-webhook` queue.
    - [Add a module](../add-a-module.md) — where module-level jobs are declared.

Formatting constraints:
- One Mermaid sequenceDiagram.
- No re-documented BullMQ API surface beyond the wiring.
- No `npm|yarn|pnpm`.
- No filler words.
  </action>
  <verify>
    <automated>cd "C:/Projetos/baseworks" &amp;&amp; test -f docs/integrations/bullmq.md &amp;&amp; grep -q "^# BullMQ$" docs/integrations/bullmq.md &amp;&amp; grep -q "^## Overview$" docs/integrations/bullmq.md &amp;&amp; grep -q "^## Upstream Documentation$" docs/integrations/bullmq.md &amp;&amp; grep -q "^## Setup$" docs/integrations/bullmq.md &amp;&amp; grep -q "^## Wiring in Baseworks$" docs/integrations/bullmq.md &amp;&amp; grep -q "^## Gotchas$" docs/integrations/bullmq.md &amp;&amp; grep -q "^## Extending$" docs/integrations/bullmq.md &amp;&amp; grep -q "docs\.bullmq\.io" docs/integrations/bullmq.md &amp;&amp; grep -q "createQueue" docs/integrations/bullmq.md &amp;&amp; grep -q "createWorker" docs/integrations/bullmq.md &amp;&amp; grep -q "packages/queue/src/index\.ts" docs/integrations/bullmq.md &amp;&amp; grep -q "apps/api/src/worker\.ts" docs/integrations/bullmq.md &amp;&amp; grep -q "email:send" docs/integrations/bullmq.md &amp;&amp; grep -q "billing:process-webhook" docs/integrations/bullmq.md &amp;&amp; grep -q "sandboxed" docs/integrations/bullmq.md &amp;&amp; grep -q "Add a new queue" docs/integrations/bullmq.md &amp;&amp; test "$(grep -c '^```mermaid$' docs/integrations/bullmq.md)" = "1" &amp;&amp; ! grep -nE "\bnpm install\b|\byarn \b|\bpnpm \b" docs/integrations/bullmq.md &amp;&amp; ! grep -nE "\b(basically|simply|just)\b" docs/integrations/bullmq.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/integrations/bullmq.md` exists
    - All 9 required `##` sections present: `Overview`, `Upstream Documentation`, `Setup`, `Wiring in Baseworks`, `Gotchas`, `Extending`, `Security`, `Dashboard (optional)`, `Next steps`
    - Links to `docs.bullmq.io`
    - Names `createQueue` and `createWorker` and cites `packages/queue/src/index.ts`
    - Cites `apps/api/src/worker.ts`
    - Lists existing queue names `email:send` and `billing:process-webhook`
    - Names the sandboxed-workers gotcha
    - Contains `Add a new queue` subsection
    - Exactly 1 ```mermaid block
    - Zero `npm|yarn|pnpm` and zero filler words
  </acceptance_criteria>
  <done>`docs/integrations/bullmq.md` covers queue/worker defaults, the naming convention, the sandboxed-worker gotcha, and the step-by-step for adding a new queue/worker/job.</done>
</task>

<task type="auto">
  <name>Task 4: Author docs/integrations/email.md (DOCS-09)</name>
  <files>docs/integrations/email.md</files>
  <read_first>
    - docs/README.md (contracts)
    - packages/modules/billing/src/jobs/send-email.ts:1-200 (dispatcher, templates/subjects maps, Resend call, graceful-skip branch, i18n pre-resolution for team-invite)
    - packages/modules/billing/src/templates/welcome.tsx
    - packages/modules/billing/src/templates/password-reset.tsx
    - packages/modules/billing/src/templates/team-invite.tsx
    - packages/modules/billing/src/templates/billing-notification.tsx
    - packages/modules/auth/src/auth.ts:70-82 (password-reset enqueue call site)
    - packages/config/src/env.ts (RESEND_API_KEY)
    - .planning/phases/15-developer-documentation/15-PATTERNS.md §"docs/integrations/email.md"
  </read_first>
  <action>
Create `docs/integrations/email.md` following the unified integration template. Target length ~110-170 lines. Tone: strict reference-doc (D-11).

Required sections (headings verbatim):

1. `# Email` (H1)
2. `## Overview` (H2). One paragraph: Baseworks sends transactional emails via Resend with React Email templates. A single dispatcher job (`packages/modules/billing/src/jobs/send-email.ts`) routes a template name to a React Email component, renders it to HTML, and calls `resend.emails.send(...)`. All callers enqueue into the `email:send` BullMQ queue.
3. `## Upstream Documentation` (H2). Bulleted list:
   - [Resend documentation](https://resend.com/docs)
   - [Resend Node SDK](https://github.com/resend/resend-node)
   - [React Email](https://react.email)
4. `## Setup` (H2) with three subsections:

   `### Env vars` — 3-column table:
   - `RESEND_API_KEY` | no | Resend API key. When absent, the dispatcher logs and returns without throwing (dev/test friendly).
   - `REDIS_URL` | yes (for enqueuing) | Required for the `email:send` queue.

   `### Module wire-up` — one paragraph: the dispatcher lives in the billing module at `packages/modules/billing/src/jobs/send-email.ts` and is registered in the billing `ModuleDefinition.jobs` map. The worker entrypoint auto-starts a BullMQ worker for `email:send`. Callers (auth, billing, any future module) enqueue via `queue.add("template-name", { to, template, data })`.

   `### Smoke test` — fenced bash block:

   ```bash
   bun worker
   # From another shell, trigger a password reset:
   curl -X POST http://localhost:3000/api/auth/forget-password \
     -H "Content-Type: application/json" \
     -d '{"email":"test@example.com"}'
   ```

   One-paragraph expectation: worker logs `Job started` for `email:send` and either sends the email (when `RESEND_API_KEY` is set) or logs a graceful-skip message (when absent).

5. `## Wiring in Baseworks` (H2).

   Subsection `### Dispatcher` — one paragraph citing `packages/modules/billing/src/jobs/send-email.ts`. The handler accepts `{ to, template, data, locale? }`, looks up the React Email component in the `templates` map, calls `render()` to produce HTML, and invokes `resend.emails.send({ from, to, subject, html })`. When `env.RESEND_API_KEY` is absent the handler logs and returns without calling Resend (dev and test paths).

   Subsection `### Template map` — inline snippet (≤ 10 lines) with first-line citation:

   ```typescript
   // From packages/modules/billing/src/jobs/send-email.ts:15-24
   const templates: Record<string, (data: any) => JSX.Element> = {
     "welcome": (data) => WelcomeEmail(data),
     "password-reset": (data) => PasswordResetEmail(data),
     "magic-link": (data) => PasswordResetEmail({ ...data, userName: data.email }),
     "billing-notification": (data) => BillingNotificationEmail(data),
     "team-invite": (data) => TeamInviteEmail(data),
   };
   ```

   One-paragraph note: `magic-link` reuses the `PasswordResetEmail` component with an aliased `userName`. `team-invite` pre-resolves its i18n strings via `@baseworks/i18n` before `render()` is called — see `send-email.ts::resolveTeamInvite`.

   Subsection `### Flow` — one Mermaid `sequenceDiagram`:

   ```mermaid
   sequenceDiagram
     participant Caller as Command handler / auth callback
     participant Q as email:send queue
     participant Redis
     participant W as sendEmail worker
     participant Tmpl as React Email component
     participant Resend

     Caller->>Q: queue.add("welcome", { to, template, data })
     Q->>Redis: persist
     W->>Redis: consume
     W->>Tmpl: templates[template](data)
     Tmpl-->>W: JSX.Element
     W->>W: render(element) → html
     alt RESEND_API_KEY set
       W->>Resend: resend.emails.send({ from, to, subject, html })
       Resend-->>W: id
     else absent
       W->>W: console.log("[EMAIL] skipped — RESEND_API_KEY not set")
     end
   ```

   Cite `packages/modules/billing/src/jobs/send-email.ts` for the full dispatcher.

   Subsection `### Existing templates` — 2-column table:

   | Template | File |
   |----------|------|
   | `welcome` | `packages/modules/billing/src/templates/welcome.tsx` |
   | `password-reset` | `packages/modules/billing/src/templates/password-reset.tsx` |
   | `magic-link` (alias of password-reset) | same file |
   | `billing-notification` | `packages/modules/billing/src/templates/billing-notification.tsx` |
   | `team-invite` | `packages/modules/billing/src/templates/team-invite.tsx` |

   Subsection `### Call-site example` — inline snippet (≤ 10 lines):

   ```typescript
   // From packages/modules/auth/src/auth.ts:70-82 — password reset enqueue
   const queue = getEmailQueue();
   if (queue) {
     await queue.add("password-reset", {
       to: user.email,
       template: "password-reset",
       data: { url, userName: user.name },
     });
   }
   ```

6. `## Gotchas` (H2). Four bulleted items:
   - **Graceful skip when `RESEND_API_KEY` is absent.** The dispatcher returns `void` without error. This is intentional for dev and test; in production, deployment must set `RESEND_API_KEY` or users will never receive emails.
   - **i18n pre-resolution for team-invite.** Unlike other templates that receive raw data and resolve strings at render time, `team-invite` has its i18n strings pre-resolved in the enqueueing context (via `@baseworks/i18n` and request-locale AsyncLocalStorage) and passed as already-translated props. Do not call i18n lookups inside `TeamInviteEmail`.
   - **Template name is both a template lookup key and the BullMQ job name.** `queue.add("welcome", { to, template: "welcome", ... })` passes the same string twice — first as the BullMQ job name (used in metrics), second as the `template` field in the payload (used by the dispatcher to look up the component).
   - **Magic-link reuses the password-reset component.** The templates map aliases `magic-link` to `PasswordResetEmail` with an aliased `userName` field. This is a dev convenience; a dedicated `MagicLinkEmail` component can replace it if visual differentiation is needed.

7. `## Extending` (H2) with one subsection.

   `### Add a new email template` — ordered list:

   1. Create a React Email component in `packages/modules/billing/src/templates/{name}.tsx`. Reference: `welcome.tsx` (simplest) or `team-invite.tsx` (with pre-resolved i18n strings).
   2. Add entries to the `templates` and `subjects` maps in `packages/modules/billing/src/jobs/send-email.ts:15-35`. Both are required — the dispatcher looks up by the same key.
   3. Enqueue from the call site: `await queue.add("{name}", { to, template: "{name}", data: { ... } })`. Reference: `packages/modules/auth/src/auth.ts:70-82`.
   4. Test by enqueuing in a scratch script and running `bun worker`; inspect the log output or a real Resend inbox.

8. `## Security` (H2). Bulleted list:
   - `RESEND_API_KEY` is a secret. All documentation examples use the placeholder `re_xxx`; real keys are configured per-environment in the deployment's env.
   - Template data crosses the app ↔ Redis boundary via the job payload. Validate payload fields before rendering. In particular, do not echo untrusted HTML into an email template — React Email escapes text nodes by default, but raw HTML passed via `dangerouslySetInnerHTML` bypasses this.
   - The `from` address is configured at the Resend account level. Ensure your sending domain is verified in Resend before production.

9. `## Next steps` (H2). Bulleted list:
    - [BullMQ integration](./bullmq.md) — the `email:send` queue uses the standard BullMQ conventions.
    - [better-auth integration](./better-auth.md) — auth emits `password-reset` and `magic-link` enqueues.
    - [Add a module](../add-a-module.md) — new modules can enqueue emails the same way.

Formatting constraints:
- One Mermaid sequenceDiagram.
- No re-documented Resend API or React Email beyond wiring.
- No `npm|yarn|pnpm`.
- No filler words.
- No real-shaped secrets (`re_xxx` placeholder only; acceptance grep enforces `re_[a-zA-Z0-9]{15,}` returns zero).
  </action>
  <verify>
    <automated>cd "C:/Projetos/baseworks" &amp;&amp; test -f docs/integrations/email.md &amp;&amp; grep -q "^# Email$" docs/integrations/email.md &amp;&amp; grep -q "^## Overview$" docs/integrations/email.md &amp;&amp; grep -q "^## Upstream Documentation$" docs/integrations/email.md &amp;&amp; grep -q "^## Setup$" docs/integrations/email.md &amp;&amp; grep -q "^## Wiring in Baseworks$" docs/integrations/email.md &amp;&amp; grep -q "^## Gotchas$" docs/integrations/email.md &amp;&amp; grep -q "^## Extending$" docs/integrations/email.md &amp;&amp; grep -q "resend\.com/docs" docs/integrations/email.md &amp;&amp; grep -q "react\.email" docs/integrations/email.md &amp;&amp; grep -q "send-email\.ts" docs/integrations/email.md &amp;&amp; grep -q "RESEND_API_KEY" docs/integrations/email.md &amp;&amp; grep -q "email:send" docs/integrations/email.md &amp;&amp; grep -q "welcome\.tsx" docs/integrations/email.md &amp;&amp; grep -q "password-reset\.tsx" docs/integrations/email.md &amp;&amp; grep -q "team-invite\.tsx" docs/integrations/email.md &amp;&amp; grep -q "billing-notification\.tsx" docs/integrations/email.md &amp;&amp; grep -q "Add a new email template" docs/integrations/email.md &amp;&amp; test "$(grep -c '^```mermaid$' docs/integrations/email.md)" = "1" &amp;&amp; ! grep -nE "sk_live_|sk_test_[a-zA-Z0-9]{20,}|re_[a-zA-Z0-9]{15,}|whsec_[a-zA-Z0-9]{20,}" docs/integrations/email.md &amp;&amp; ! grep -nE "\bnpm install\b|\byarn \b|\bpnpm \b" docs/integrations/email.md &amp;&amp; ! grep -nE "\b(basically|simply|just)\b" docs/integrations/email.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/integrations/email.md` exists
    - All 8 required `##` sections present: `Overview`, `Upstream Documentation`, `Setup`, `Wiring in Baseworks`, `Gotchas`, `Extending`, `Security`, `Next steps`
    - Links to `resend.com/docs` and `react.email`
    - Cites `send-email.ts` and names `RESEND_API_KEY`, `email:send`
    - Lists all 4 template files (`welcome.tsx`, `password-reset.tsx`, `team-invite.tsx`, `billing-notification.tsx`)
    - Contains `Add a new email template` subsection
    - Exactly 1 ```mermaid block
    - Zero real-shaped secrets
    - Zero `npm|yarn|pnpm` and zero filler words
  </acceptance_criteria>
  <done>`docs/integrations/email.md` documents the dispatcher, template map, the flow with graceful-skip branch, all 4 current templates, and the 4-step path to add a new template.</done>
</task>

<task type="auto">
  <name>Task 5: Phase-close validate-docs script</name>
  <files>scripts/validate-docs.ts</files>
  <read_first>
    - docs/README.md (contracts -- Mermaid syntaxes permitted, forbidden test-utils import)
    - docs/architecture.md (4 Mermaid diagrams -- validator must count ≥ 4 here)
    - docs/integrations/*.md (one Mermaid sequenceDiagram each -- validator must see 4 more for a floor of 8 total)
    - docs/add-a-module.md, docs/testing.md (forbidden-import check -- must NOT contain `@baseworks/test-utils`)
    - .planning/phases/15-developer-documentation/15-CONTEXT.md §D-01 (≥ 8 Mermaid diagrams across the 9 docs)
  </read_first>
  <action>
Create `scripts/validate-docs.ts` -- a single-file Bun script that runs a phase-close sanity check across `docs/**/*.md` AFTER Tasks 1-4 of this plan have landed. It is invoked via `bun run scripts/validate-docs.ts` and exits 0 on success, non-zero on failure.

Script contents (use Bun globals `Bun.glob` and `Bun.file` -- no external dependencies):

```typescript
#!/usr/bin/env bun
/**
 * Phase 15 docs validator.
 *
 * Runs at phase close. Asserts three invariants across the developer docs
 * under `docs/`:
 *   1. No forbidden imports: the string `@baseworks/test-utils` MUST NOT appear
 *      anywhere under docs/ (no such workspace package exists -- see Plan 15-04
 *      revision). All test-utils imports in docs use relative paths.
 *   2. No leaked secrets of common provider shapes: `sk_live_`, `sk_test_{24+}`,
 *      `re_{20+}`, `whsec_{24+}`.
 *   3. Mermaid coverage floor: per D-01, at least 8 Mermaid fenced code blocks
 *      appear across docs/ (4 architecture diagrams + 4 integration sequenceDiagrams).
 *
 * Exit code 0 on success, 1 on any failure. All failures print to stderr.
 */
import { readFile } from "node:fs/promises";

const ROOT = new URL("..", import.meta.url).pathname;
const docsGlob = new Bun.Glob("docs/**/*.md");

const forbiddenImport = /@baseworks\/test-utils/g;
const secretShapes: Array<{ name: string; re: RegExp }> = [
  { name: "Stripe live", re: /sk_live_[A-Za-z0-9]+/g },
  { name: "Stripe test", re: /sk_test_[A-Za-z0-9]{24,}/g },
  { name: "Resend",      re: /re_[A-Za-z0-9]{20,}/g },
  { name: "Webhook",     re: /whsec_[A-Za-z0-9]{24,}/g },
];
const mermaidFence = /^\`\`\`mermaid$/gm;

let failures = 0;
let mermaidTotal = 0;

for await (const relPath of docsGlob.scan({ cwd: ROOT })) {
  const full = `${ROOT}${relPath}`;
  const text = await readFile(full, "utf8");

  // (1) Forbidden import
  const forbidden = text.match(forbiddenImport);
  if (forbidden && forbidden.length > 0) {
    console.error(
      `[validate-docs] FAIL: ${relPath} contains forbidden string "@baseworks/test-utils" (${forbidden.length}x). Use the relative path "../../../__test-utils__/..." instead.`,
    );
    failures++;
  }

  // (2) Leaked secret shapes
  for (const { name, re } of secretShapes) {
    const matches = text.match(re);
    if (matches && matches.length > 0) {
      console.error(
        `[validate-docs] FAIL: ${relPath} contains ${name}-shaped string (${matches.length}x). Use short placeholders (e.g. "sk_test_replace_me", "re_replace_me").`,
      );
      failures++;
    }
  }

  // (3) Count Mermaid fences (accumulate across all docs)
  const fences = text.match(mermaidFence);
  if (fences) mermaidTotal += fences.length;
}

// Mermaid floor: D-01 specifies 4 architecture + 4 integration diagrams = 8 minimum.
if (mermaidTotal < 8) {
  console.error(
    `[validate-docs] FAIL: found ${mermaidTotal} Mermaid fenced blocks across docs/; D-01 requires at least 8 (4 in docs/architecture.md + 1 per integration doc).`,
  );
  failures++;
} else {
  console.log(`[validate-docs] OK: found ${mermaidTotal} Mermaid fenced blocks across docs/ (>= 8 required).`);
}

if (failures > 0) {
  console.error(`[validate-docs] ${failures} failure(s). Exit 1.`);
  process.exit(1);
}
console.log("[validate-docs] PASS");
```

The script uses Bun's built-in `Bun.Glob` (stable since Bun 1.1) so no npm dependency is added. It is a Bun runtime entrypoint (shebang present, though not strictly needed since we invoke via `bun run`).

After writing the file, run it ONCE to confirm it passes with the current docs tree (assumes Tasks 1-4 of this plan have produced all 4 integration docs with their single sequenceDiagram each AND Plan 03 Task 2 produced `docs/architecture.md` with 4 Mermaid diagrams).
  </action>
  <verify>
    <automated>cd "C:/Projetos/baseworks" &amp;&amp; test -f scripts/validate-docs.ts &amp;&amp; bun run scripts/validate-docs.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/validate-docs.ts` exists
    - Script references `@baseworks/test-utils` (as the forbidden pattern) -- `grep -q "@baseworks/test-utils" scripts/validate-docs.ts` matches. This is the ONE file in the repo permitted to contain the forbidden string (because it greps for it).
    - Script references at least one of each secret shape pattern: `sk_live_`, `sk_test_`, `re_`, `whsec_`
    - Script references `mermaid` (fence counter)
    - `bun run scripts/validate-docs.ts` exits 0 (all Tasks 1-4 docs present with the 8+ Mermaid blocks, no forbidden import, no leaked secrets)
    - Failure mode verification: a reviewer can temporarily append `@baseworks/test-utils` to any docs/*.md, re-run the script, and observe a non-zero exit with a clear error message. (Not required automatically -- manual smoke only.)
  </acceptance_criteria>
  <done>`scripts/validate-docs.ts` exists, implements all three validation layers (forbidden import, secret shapes, Mermaid floor), and `bun run scripts/validate-docs.ts` passes against the current docs tree produced by Plans 01, 03, 04, and Tasks 1-4 of this plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → public reader | All 4 integration docs are committed and readable publicly. Integration-related secret shapes (`sk_test_`, `re_`, `whsec_`) must use placeholders only. |
| doc code → reader's production | Integration patterns are commonly copied verbatim into production code. Any omission of signature verification, env validation, or graceful-skip behavior propagates to downstream projects. |
| webhook routes (documented path) → provider | Documentation shapes the reader's expectation for webhook-handler structure. Missing "verify signature first" would propagate to a spoofing vulnerability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-15 | Spoofing | `docs/integrations/billing.md` omits `verifyWebhookSignature` as the first webhook step, leading readers to implement webhooks without signature verification. | mitigate | The webhook sequenceDiagram MANDATES `verifyWebhookSignature` as the first message after "POST raw body + signature header". The `## Security` section lists signature verification as the first invariant. Acceptance grep confirms `verifyWebhookSignature` appears in the file. |
| T-15-16 | Information Disclosure | Integration docs leak real API keys in examples. | mitigate | All 4 docs use placeholder shapes (`sk_test_xxx`, `re_xxx`, `whsec_xxx`). Acceptance grep enforces `sk_live_|sk_test_[a-zA-Z0-9]{20,}|re_[a-zA-Z0-9]{15,}|whsec_[a-zA-Z0-9]{20,}` returns zero matches in each file. The `Security` section in each doc reinforces that real keys are per-deployment. |
| T-15-17 | Tampering | `docs/integrations/better-auth.md` documents an insecure auth config (e.g., `minPasswordLength: 4`, JWT sessions, disabled email verification). | mitigate | All config references mirror `packages/modules/auth/src/auth.ts` verbatim: `minPasswordLength: 8`, database-backed sessions via `drizzleAdapter`, `trustedOrigins` set. No anti-pattern variants shown. The `Security` section enumerates the invariants. |
| T-15-18 | Tampering | `docs/integrations/email.md` shows rendering untrusted HTML via `dangerouslySetInnerHTML` without mitigation guidance. | mitigate | The `Security` section explicitly warns against `dangerouslySetInnerHTML` for untrusted data and states React Email's default text-node escaping. No snippet in the doc uses `dangerouslySetInnerHTML`. |
| T-15-19 | Elevation of Privilege | `docs/integrations/bullmq.md` shows a worker handler that trusts `job.data` without validation. | mitigate | The `## Gotchas` and `## Security` sections in `bullmq.md` explicitly require payload validation at the top of each handler and note that `job.data` crosses the app ↔ Redis trust boundary. The "Add a new queue + worker + job" extension steps cite `process-followup.ts` which validates the shape before use. |
| T-15-20 | Information Disclosure | Integration docs re-document upstream API surfaces, drift, and silently lie. | mitigate | D-08 is enforced: each doc has an `## Upstream Documentation` section with links to the official source; no parameter tables for upstream SDK methods appear in the Baseworks docs. The planner's acceptance greps confirm each upstream link is present. |
</threat_model>

<verification>
Automated (per-task grep chains):
- File existence, required sections, required citations, exactly-one Mermaid block per doc, zero real-shaped secrets, zero forbidden words.

Manual-only (per 15-VALIDATION.md):
- Reviewer pushes branch and opens each of the 4 files in GitHub PR preview. Confirms all 4 Mermaid sequenceDiagrams render correctly.
- Reviewer opens each cited file path (e.g., `packages/modules/billing/src/routes.ts:52-110`) and confirms the range matches the documented content.
- Reviewer configures a throwaway Resend/Stripe test account, runs `bun docker:up`, `bun worker`, and exercises the smoke tests in each doc. Confirms documented responses and logs match reality.
- Reviewer runs the security grep from 15-VALIDATION.md across all 4 new files: `grep -nE "sk_live|sk_test_[a-zA-Z0-9]{20,}|password\\s*=\\s*['\"][^'\"]+['\"]" docs/integrations/*.md` — must return zero matches.
</verification>

<success_criteria>
1. All 4 integration docs exist under `docs/integrations/`.
2. Every doc follows the unified template (Overview → Upstream → Setup → Wiring → Gotchas → Extending → Security → Next steps).
3. Every doc contains exactly 1 Mermaid sequenceDiagram for its characteristic flow (magic-link, webhook, enqueue→worker→retry, email→Resend).
4. Every doc links to its upstream official documentation site in the `## Upstream Documentation` section.
5. Every doc contains an "Add another X" subsection per D-09 (OAuth provider, payment provider, queue/worker/job, email template).
6. DOCS-06, DOCS-07, DOCS-08, DOCS-09 requirement IDs are covered.
7. No real-shaped secret strings appear in any of the 4 files.
8. Billing doc MANDATES `verifyWebhookSignature` as the first webhook step.
9. better-auth doc names the `basePath` doubling gotcha.
10. BullMQ doc names the sandboxed-worker-broken-on-Bun gotcha.
11. Email doc lists all 4 existing templates and documents graceful skip on missing `RESEND_API_KEY`.
12. `scripts/validate-docs.ts` exists and `bun run scripts/validate-docs.ts` exits 0 -- forbidden `@baseworks/test-utils` import absent from all docs/**/*.md, no leaked provider secrets, and Mermaid-fence count across docs/ is ≥ 8 (D-01 floor).
</success_criteria>

<output>
After completion, create `.planning/phases/15-developer-documentation/15-05-SUMMARY.md` summarizing:
- All 4 integration docs delivered under `docs/integrations/`
- Each doc's sequenceDiagram subject (magic-link, webhook, enqueue/worker/retry, email)
- Each doc's "Add another X" extension section
- Security invariants stated in each (signature verification, secret placeholders, payload validation, upstream linking per D-08)
- Phase 15 is complete after this plan merges: all 9 DOCS-* requirements closed across Plans 03, 04, 05.
</output>
