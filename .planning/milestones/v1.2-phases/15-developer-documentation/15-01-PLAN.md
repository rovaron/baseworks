---
phase: 15
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/README.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "docs/ directory contains a README.md that indexes the 9 Phase 15 deliverables by filename and purpose"
    - "docs/README.md declares the tone contract by linking to docs/jsdoc-style-guide.md"
    - "docs/README.md states the code-citation format (path:line for >10 lines, function-name anchors preferred, inline for ≤10 lines)"
    - "docs/README.md states the Mermaid syntax contract (flowchart / sequenceDiagram / stateDiagram-v2 only; never the deprecated `graph` keyword)"
    - "docs/README.md declares the integrations/ subfolder convention for DOCS-06..09"
  artifacts:
    - path: "docs/README.md"
      provides: "Navigation index + tone contract + citation contract + Mermaid contract for Phase 15 docs"
      contains: "getting-started.md, architecture.md, add-a-module.md, configuration.md, testing.md, integrations/better-auth.md, integrations/billing.md, integrations/bullmq.md, integrations/email.md"
  key_links:
    - from: "docs/README.md"
      to: "docs/jsdoc-style-guide.md"
      via: "relative markdown link in a 'Tone' section"
      pattern: "\\]\\(\\.\\/jsdoc-style-guide\\.md"
    - from: "docs/README.md"
      to: "docs/integrations/"
      via: "table row listing integrations/better-auth.md etc."
      pattern: "integrations/better-auth\\.md"
---

<objective>
Lock Phase 15 information architecture before any doc authoring begins. Create `docs/README.md` as the navigation index and the single source of truth for the three cross-cutting contracts that every Phase 15 doc must follow: tone (inherits Phase 13's `docs/jsdoc-style-guide.md`), code-citation format (D-10), and Mermaid diagram syntax (D-02, GitHub-native).

Purpose: Eliminate wayfinding ambiguity for readers and format drift across doc-authoring plans. Plan 03, Plan 04, and Plan 05 all cite back to this index and the contracts it pins.

Output: `docs/README.md` listing all 9 Phase 15 deliverables with a one-line purpose each, plus the three contracts inline.
</objective>

<execution_context>
@C:/Projetos/baseworks/.claude/get-shit-done/workflows/execute-plan.md
@C:/Projetos/baseworks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-developer-documentation/15-CONTEXT.md
@.planning/phases/15-developer-documentation/15-RESEARCH.md
@.planning/phases/15-developer-documentation/15-PATTERNS.md
@docs/jsdoc-style-guide.md
</context>

<interfaces>
<!-- Only existing artifact in docs/ so far -->

From `docs/jsdoc-style-guide.md:1-23` — tone reference that Phase 15 docs inherit (D-11):

```markdown
# JSDoc Style Guide

This guide standardizes JSDoc across the Baseworks codebase. All exported symbols
(functions, types, interfaces, classes, constants) must have a JSDoc block unless
explicitly exempted. The goal is consistent IDE tooltips, self-documenting APIs,
and maintainable inline documentation that stays accurate as the code evolves.

---

## General Rules

- **Technical-precise tone:** Describe what the code does, not what the developer
  should feel. Use domain terminology. Avoid filler words ("basically", "simply",
  "just").
- **One declarative sentence opener:** Every JSDoc block starts with a single
  sentence stating what the export does. Use present tense, active voice.
```

Planned `docs/` tree (per 15-RESEARCH.md §"Recommended Output Structure"):

```
docs/
├── README.md              # THIS PLAN — navigation + contracts
├── getting-started.md     # Plan 03 — DOCS-01
├── architecture.md        # Plan 03 — DOCS-02 (4 Mermaid diagrams)
├── add-a-module.md        # Plan 04 — DOCS-03
├── configuration.md       # Plan 04 — DOCS-04
├── testing.md             # Plan 04 — DOCS-05
├── integrations/          # Plan 05
│   ├── better-auth.md     # DOCS-06
│   ├── billing.md         # DOCS-07
│   ├── bullmq.md          # DOCS-08
│   └── email.md           # DOCS-09
└── jsdoc-style-guide.md   # Pre-existing (Phase 13)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/README.md index with contracts</name>
  <files>docs/README.md</files>
  <read_first>
    - docs/jsdoc-style-guide.md (tone contract source; cite lines 1-23 as the authoritative tone reference)
    - .planning/phases/15-developer-documentation/15-CONTEXT.md (D-01..D-11 decisions; especially D-02, D-07, D-10, D-11)
    - .planning/phases/15-developer-documentation/15-RESEARCH.md §"Recommended Output Structure" (locked tree)
    - .planning/phases/15-developer-documentation/15-PATTERNS.md §"docs/README.md" (doc-index analog notes)
  </read_first>
  <action>
Create `docs/README.md` with the exact structure below. The file is 70-120 lines of markdown; no code beyond fenced inline examples. Tone: technical-precise, present tense, active voice, no filler words ("basically", "simply", "just").

Required sections (in order, headings verbatim):

1. `# Baseworks Developer Documentation` (H1)
2. One-paragraph lead: a single declarative sentence stating that this directory contains the in-repo developer documentation for Baseworks, with a second sentence naming the four deliverable categories (Onboarding, Architecture, Guides, Integrations) — no marketing superlatives.
3. `## Reading Order` (H2): one-paragraph recommendation — read `getting-started.md` first, then `architecture.md`, then the relevant integration or guide for the task at hand. `add-a-module.md` assumes `architecture.md` is read first (per D-06).
4. `## Contents` (H2): a 2-column markdown table with columns `Document | Purpose`. Rows (in this exact order, relative links, single-line purpose strings):
   - `[getting-started.md](./getting-started.md)` | `Prerequisites, install, env setup, run dev server, run tests.`
   - `[architecture.md](./architecture.md)` | `Module system, CQRS flow, request lifecycle, and tenant scoping with Mermaid diagrams.`
   - `[add-a-module.md](./add-a-module.md)` | `Annotated walkthrough of packages/modules/example for creating a new module end to end.`
   - `[configuration.md](./configuration.md)` | `Environment variables, module loading, provider selection, and deployment configuration.`
   - `[testing.md](./testing.md)` | `Test runner scope, HandlerContext mocks, and patterns for testing commands, queries, and adapters.`
   - `[integrations/better-auth.md](./integrations/better-auth.md)` | `better-auth setup, magic-link flow, and adding OAuth providers.`
   - `[integrations/billing.md](./integrations/billing.md)` | `PaymentProvider port, Stripe and Pagar.me adapters, webhook flow, and adding a third provider.`
   - `[integrations/bullmq.md](./integrations/bullmq.md)` | `Queue conventions, worker entrypoint, and adding a new queue or job type.`
   - `[integrations/email.md](./integrations/email.md)` | `Resend dispatcher, React Email templates, and adding a new template.`
   - `[jsdoc-style-guide.md](./jsdoc-style-guide.md)` | `JSDoc conventions for source files (Phase 13 output).`
5. `## Tone` (H2): one paragraph stating that all documents in this directory follow the technical-precise, API-reference tone established in `[jsdoc-style-guide.md](./jsdoc-style-guide.md)` §"General Rules" (lines 12-23). Declarative sentences, present tense, active voice. Forbidden filler words enumerated inline: `basically`, `simply`, `just`. No emojis, no second-person exclamations. `getting-started.md` has discretion for a slightly warmer onboarding voice within these bounds (per D-11 and CONTEXT.md Claude's Discretion note).
6. `## Code Citations` (H2): one paragraph plus a short rules list. State the mixed strategy from D-10:
   - Cite a file with `path:start-end` (e.g., `packages/modules/billing/src/provider-factory.ts:32-75`) when the referenced code is longer than ~10 lines or likely to evolve.
   - Prefer function-name anchors (`provider-factory.ts::getPaymentProvider`) over line ranges when a stable named anchor exists — more resistant to drift.
   - Inline short snippets (≤ 10 lines, stable usage examples) verbatim inside fenced ```typescript blocks. Every inline snippet MUST begin with a first-line comment naming its source: `// From packages/modules/example/src/commands/create-example.ts:22-34`.
7. `## Mermaid Diagrams` (H2): one paragraph plus rules list.
   - GitHub renders ```mermaid fenced blocks natively since Feb 2022; no build tooling is needed.
   - Permitted syntaxes: `flowchart`, `sequenceDiagram`, `stateDiagram-v2`. The deprecated `graph` keyword is forbidden; use `flowchart` instead.
   - Every box label uses a concrete code identifier (`ModuleRegistry`, `CqrsBus`, `scopedDb`, `PaymentProvider`) that matches an actual file or class name so readers can grep (per D-02). Abstract labels such as "Bus" or "Database Layer" are forbidden.
   - Preview diagrams in a GitHub PR before committing (not only in an IDE plugin).
8. `## Scope` (H2): two-sentence paragraph — this directory covers v1.2 deliverables DOCS-01 through DOCS-09. TypeDoc auto-generated API reference, contributing guide, and changelog are deferred to v2 (see `.planning/REQUIREMENTS.md` §"v2 Requirements").

Formatting constraints:
- Single `#` H1 at top; `##` for all sections; `###` only if unavoidable.
- Use `---` horizontal rule between the lead paragraph and `## Reading Order`.
- Relative links use `./` prefix (e.g., `./getting-started.md`, `./integrations/better-auth.md`).
- No `npm`/`yarn`/`pnpm` references; no Prisma/NextAuth/Lucia/Express/tRPC references.
- No emojis.

Do NOT create placeholder files for the other 9 documents in this plan. Plan 03, 04, 05 create them. This plan creates the index only.
  </action>
  <verify>
    <automated>test -f docs/README.md &amp;&amp; grep -q "^# Baseworks Developer Documentation$" docs/README.md &amp;&amp; grep -q "^## Reading Order$" docs/README.md &amp;&amp; grep -q "^## Contents$" docs/README.md &amp;&amp; grep -q "^## Tone$" docs/README.md &amp;&amp; grep -q "^## Code Citations$" docs/README.md &amp;&amp; grep -q "^## Mermaid Diagrams$" docs/README.md &amp;&amp; grep -q "^## Scope$" docs/README.md &amp;&amp; grep -q "\[getting-started\.md\](\./getting-started\.md)" docs/README.md &amp;&amp; grep -q "\[architecture\.md\](\./architecture\.md)" docs/README.md &amp;&amp; grep -q "\[add-a-module\.md\](\./add-a-module\.md)" docs/README.md &amp;&amp; grep -q "\[configuration\.md\](\./configuration\.md)" docs/README.md &amp;&amp; grep -q "\[testing\.md\](\./testing\.md)" docs/README.md &amp;&amp; grep -q "integrations/better-auth\.md" docs/README.md &amp;&amp; grep -q "integrations/billing\.md" docs/README.md &amp;&amp; grep -q "integrations/bullmq\.md" docs/README.md &amp;&amp; grep -q "integrations/email\.md" docs/README.md &amp;&amp; grep -q "jsdoc-style-guide\.md" docs/README.md &amp;&amp; grep -q "flowchart" docs/README.md &amp;&amp; grep -q "sequenceDiagram" docs/README.md &amp;&amp; ! grep -nE "\bnpm\b|\byarn\b|\bpnpm\b" docs/README.md &amp;&amp; ! grep -nE "\b(basically|simply|just)\b" docs/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/README.md` exists and is readable
    - File begins with `# Baseworks Developer Documentation` (H1, exact text)
    - All 8 required `##` section headings present: `Reading Order`, `Contents`, `Tone`, `Code Citations`, `Mermaid Diagrams`, `Scope` (plus the H1)
    - Contents table links to all 10 files: `./getting-started.md`, `./architecture.md`, `./add-a-module.md`, `./configuration.md`, `./testing.md`, `./integrations/better-auth.md`, `./integrations/billing.md`, `./integrations/bullmq.md`, `./integrations/email.md`, `./jsdoc-style-guide.md`
    - `Tone` section links to `./jsdoc-style-guide.md` with text referencing "General Rules" and lines "12-23"
    - `Code Citations` section names the three rules: `path:start-end`, function-name anchors, inline ≤10 lines
    - `Mermaid Diagrams` section names `flowchart`, `sequenceDiagram`, `stateDiagram-v2` and forbids `graph`
    - `grep -nE "\bnpm\b|\byarn\b|\bpnpm\b" docs/README.md` returns zero matches
    - `grep -nE "\b(basically|simply|just)\b" docs/README.md` returns zero matches (filler words)
  </acceptance_criteria>
  <done>`docs/README.md` exists with all 8 required sections, links to all 10 Phase 15 documents (including the existing jsdoc-style-guide.md), encodes the tone / citation / Mermaid contracts inline, and passes the forbidden-word grep checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → public reader | docs/README.md is committed and readable by anyone who clones the repo. Any content leaked here is permanent in git history. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Information Disclosure | docs/README.md contents | mitigate | Index file contains no env var names, no secret placeholders, no URL patterns revealing production infrastructure. Content is limited to relative filenames, rule statements, and references to the existing public `docs/jsdoc-style-guide.md`. Acceptance criteria grep-enforces absence of `npm`/`yarn`/`pnpm` examples but content is inherently secret-free (navigation-only). |
| T-15-02 | Tampering | downstream doc plans ignoring the contracts stated here | mitigate | Plans 03, 04, 05 explicitly list `docs/README.md` in each task's `read_first` field so the authoring executor loads the contracts before authoring. Plan-checker validates consistency. |
</threat_model>

<verification>
Manual-only (per 15-VALIDATION.md Manual-Only table):
- Open `docs/README.md` in a GitHub PR preview; confirm all relative links are syntactically correct (they point to files that will be created in Plans 03-05; clicking them will 404 until those plans land — this is expected and acceptable for an index committed first).
- Reviewer confirms tone of the index itself matches `docs/jsdoc-style-guide.md` §"General Rules".

Automated (the acceptance-criteria grep chain in Task 1 covers):
- File exists.
- All required headings present.
- All 10 document links present.
- No forbidden package-manager names or filler words in the file's own prose.
</verification>

<success_criteria>
1. `docs/README.md` exists and is committed.
2. All 8 required `##` section headings are present verbatim.
3. All 10 document links are present (getting-started, architecture, add-a-module, configuration, testing, 4 integrations, jsdoc-style-guide).
4. Tone / Citation / Mermaid contracts are stated inline with the rules from D-02, D-10, D-11.
5. No forbidden package-manager references (`npm`, `yarn`, `pnpm`) appear in the index's own prose.
6. No filler words (`basically`, `simply`, `just`) appear in the index's own prose.
</success_criteria>

<output>
After completion, create `.planning/phases/15-developer-documentation/15-01-SUMMARY.md` summarizing:
- What was built (the navigation index)
- The three locked contracts (tone, citation format, Mermaid syntax) with one-line recaps each
- Downstream: Plans 03, 04, 05 must read `docs/README.md` before authoring
</output>
